{
  "name": "o-estado-do-pais",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "O Estado do País — observatório de dados de Portugal. Escrito por IA, dirigido por uma pessoa.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run ledger:check && astro build && npm run gate:html",
    "preview": "astro preview",
    "astro": "astro",
    "ledger:check": "node scripts/check-ledger.mjs",
    "gate:html": "node scripts/gate-html.mjs",
    "verify": "npm run ledger:check && npm run gate:html"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "astro": "7.2.1"
  },
  "devDependencies": {
    "js-yaml": "5.2.3",
    "node-html-parser": "9.0.1"
  }
}
