{
  "name": "o-estado-do-pais",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "O Estado do País — observatório de dados de Portugal. Escrito por IA, dirigido por uma pessoa.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run ledger:check && npm run check:cruzamento && npm run check:documentos && astro build && npm run stamp:version && npm run cartoes && npm run gate:html && npm run check:cadeia && npm run check:dados && npm run check:mapa && npm run check:regioes && npm run check:areas && npm run check:voz && npm run check:lingua",
    "preview": "astro preview",
    "astro": "astro",
    "ledger:check": "node scripts/check-ledger.mjs",
    "cartoes": "node scripts/cartoes.mjs",
    "gate:html": "node scripts/gate-html.mjs",
    "check:dados": "node scripts/check-dados.mjs",
    "check:mapa": "node scripts/check-mapa.mjs",
    "check:regioes": "node scripts/check-regioes.mjs",
    "check:areas": "node scripts/check-areas.mjs",
    "check:cruzamento": "node scripts/check-cruzamento.mjs",
    "check:documentos": "node scripts/check-documentos.mjs",
    "check:cadeia": "node scripts/check-cadeia.mjs",
    "check:voz": "node scripts/check-voz.mjs",
    "check:lingua": "node scripts/check-lingua.mjs",
    "stamp:version": "node scripts/stamp-version.mjs",
    "verify:deploy": "node scripts/verify-deploy.mjs",
    "typecheck": "tsc -p tsconfig.check.json",
    "verify": "npm run ledger:check && npm run check:cruzamento && npm run check:documentos && npm run gate:html && npm run check:cadeia && npm run check:dados && npm run check:mapa && npm run check:regioes && npm run check:areas && npm run check:voz && npm run check:lingua"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "@resvg/resvg-js": "2.6.2",
    "astro": "7.2.1"
  },
  "devDependencies": {
    "js-yaml": "5.2.3",
    "node-html-parser": "9.0.1",
    "playwright": "^1.60.0",
    "typescript": "^7.0.2"
  }
}
