# PLANO — what remains, phase by phase, and the next session

*Written 2026-08-16 (early), by Claude Fable 5 in seat, at the director's request, after the day of 2026-08-15. This is the roadmap; `DECISIONS.md` records what actually gets built, and §4 there is the defect-and-deferral register that must be clean before a block starts. Costs are rough, in subagent tokens as agents report them. Model routing: Opus builds, Sonnet does mechanical passes and blind fetches, Codex reviews across families, Fable designs and reviews in seat.*

## Where we are (2026-08-20)

**2026-08-20.** Block T is merged and live since the night of 2026-08-18 on the director's word (`main` fast-forwarded to `28709e1`, then `17bd69c` for the design-bundle generator; `verify:deploy` ✓ on 18.08 and again on 20.08: on air = `origin/main` = `main`). The paragraphs below that describe it as built and not merged are the state of 18.08 before the merge, kept as history. What is the director's after the merge is listed under «The next session», item 3, as put to him on 20.08 with a recommendation beside each.

Live when this paragraph was written (`main` was then `c75f4a1`, `verify:deploy` ✓; `main` is `fc8f032` today): everything of Block V. Orthography AO90 PT-PT enforced (wordlist 254 pairs, reversible tool, gate check, remainder 0, no em dashes); `/sobre` (the decided text, gate-compared); `/metodo` as ten rules with mechanism and live proof (`prova.mjs`, 26 keys recomputed by the gate, `/prova.json`, the mechanism drawn); `/correcoes` (register moved, policy once, entries as doors); `/agenda` from the engine's records (5 items, 16 calendar events, four states, full change log); masthead = time signal + agenda line; footer navigation only; the door to Sobre on every page including hosted documents; `Afecta` on DECISIONS entries with the tether check and a git audit in the monthly sweep; housing pre-registered in the engine (unfrozen); source calendar and watches in the monthly agent; word cuts (frame words 31 852 → 24 858); `ABOUT.md` cut to the idea. Plus what was live before: the Évora município page and five reading pages, the index of all 308 concelhos, a correction door on every page, every value sealed; 322 pages; 132 ledger rows, 12 with provenance debt. Verified: build green, engine gate PASS, Sonnet blind re-fetch of the calendar 8/8, Codex five passes (8/8 canaries caught). Record: `DECISIONS.md` §1.38 to §1.42.

**Phase 2 is built on the branch `identidade-v2` and merged into `main` on 2026-08-18.** Direction S (the synthesis of A, B and C: `design/DECISAO.md`) is applied to the real pages, and `IDENTIDADE.md` v2 is the rule (v1 at `e340fa6`). What it delivered: warm neutral tokens with every used pair measured in both themes by `scripts/medir-contraste.mjs` (all text pairs pass AA, before and after); the seal writing «fonte» / «source» with the study label moved into a declared attribute the gate compares; two labelled readings in the masthead; the front page grid closed to an explicit 4/2/1 with the threshold as a mono line and no per-cell bars; the row page reordered as a receipt with its own seal on the head value; the attribution sentence that stops where it does not know; the município mandates drawn as a band with a legend of doors; the Método proof line made legible; the agenda's state board on top with counts by `data-prova` opening the section each one counts, items as short cards, and the source calendar drawn on a time axis; and 390px measured clean on every route, both editions (front page 438 → 390, agenda 574 → 390). Four gate checks were added as extensions of existing ones, each proven on a planted defect, plus the `verify:deploy` fix that made it compare the branch it names. Record: `DECISIONS.md` §1.43.

**Merged and live 2026-08-18** on the director's word: Codex read the built pages twice, context-starved, with five planted defects each time and caught 10 of 10 (its real findings closed in two fix rounds, `DECISIONS.md` §1.44); the branch went to a protected Vercel preview; `main` fast-forwarded to it and pushed; `verify:deploy` and the design bundle plus DesignSync run right after the merge (the seat records the outcome in the vault, §Ninth block). **Block T is next.**

**Given on 2026-08-18, in conversation (`DECISIONS.md` §1.45):** the `/metodo` cut is «keep all ten rules, change two words» («número» → «medição» in the short reading and in rule 5, both editions, because the site's own `data-prova` counts carry doors and not seals, IDENTIDADE §10); the three watches are decided and stay under way, with the decision on the record; the housing question is read and its pre-registration is sealed (`core_sha256` `a52898c8638e`), which is the first act of the study.

**Block T was built on the branch `confianca` (from `main` = `fc8f032`) and was NOT merged when this paragraph was written; it was merged and went live the same night (see the 2026-08-20 paragraph above).** It awaited the cross-family read, the preview and the director's word, in that order, and got all three. Four stages, all Opus except the two blind reads (Sonnet): T1 put `document.page` and `verifications[]` in the format, the validator, the exporter, the gate and the page, and filled the second from the engine's record of the 2026-08-15 blind re-read and from the weekly panel run, never by hand; T2 brought the printed line itself across as a file (`document.crop`, 22 of the 23 PDF rows, produced by `core/pdfproof.py` on exact-match only, checked at arrival by the digest of its own bytes); T3 closed the "computed over a hosted data file" origin for the four CAOP rows and left the three PRR ones open on a licence question that is the director's, put the human page of a series before the request in 42 of the 57 API rows (proven by machine, not by reading), and fixed the engine's fixed-width excerpt cut in 169 excerpts; T3c re-ran the PRR extraction against today's snapshot, which moved three of the five sums with a typed `atualizacao` each, under a new exporter check (V16) that refuses a crossed row whose value moves with nothing recording it; T4 made the ledger downloadable as a dataset with the licence left to the director, answered the `lastmod` question with a measurement instead of a promise, taught the gate to compare the *string* of a value rather than its digits and to read the constitution's quotations of governed texts, and rewrote the one governed sentence that had become false. Against `BRIEF-confianca.md` §6.8: test 1 passes in 22 rows and test 3 in 53. The ruler, `fc8f032` against the branch: markers 502 → 358, «[a verificar]» 602 → 458, provenance debt 12 → 8, indexable rows 120 → 124, frame phrases 73 · 2 471 → 77 · 2 367, pages 307 → 307. 93 planted defects across the block, each closed and each reverted. Record: `DECISIONS.md` §1.47, with a closing section that lists what stays open by item and by phase.

**One thing in Block T is a proposal, not a decision, and the director sees it at preview:** the limit of the Método's rule 6 said the field that would hold a re-reading does not exist in the format, and it does since T1, with the count beside it reading 53. A false sentence must not ship, so T4 rewrote it in both editions and stamped the tether. It is the only governed-text change in the block, and it is overrulable before the merge.

Still the director's, on the live site: correct the English of Sobre and Método if he wants, and confirm or overrule the orthography default. New with Block T: **the licence of the ledger dataset** (recommended CC BY 4.0; until it is decided the files build and no page links them) and **the licence of the PRR snapshot**, which is a legal question and goes with `legal/counsel-brief.md`.

## The rules that govern every block

1. A block does not start while the previous block's found defects are neither fixed nor deferred in writing (`DECISIONS.md` §4).
2. No new gate unless real content broke a rule no gate covers; extend existing checks first. Every check is proven on a planted defect before it counts.
3. Everything public goes through: build green, cross-family review (Codex, canaried where the artifact is prose), preview, the director's word, merge, `verify:deploy`.
4. State is rendered, never written. The idea is written and changes by decision.
5. The About states the idea and stops. No em dashes. Show, don't declare.

---

## Phase 1 (remaining) — voice, closed in the next session

| Item | Owner | Done when |
|---|---|---|
| Orthography decision | applied by default (Acordo), overrulable at preview | `IDENTIDADE.md` §9; reversal is a rerun plus a hand pass on the one-way forms |
| Método cut | director, at preview | the built `/metodo` is on the preview; rules 1, 6, 9, 10 have the weakest proofs |
| Footer line | decided: navigation only | built (Block V, on preview) |
| Time signal | built: «Painel europeu reconferido a …» + «Agenda: N em curso · N a seguir» | on preview |

## Block V — the site's own voice and self-description (BUILT, MERGED AND LIVE 2026-08-16)

Goal: `/sobre`, `/metodo`, `/correcoes`, `/agenda` built; the self-description made unable to go stale; the orthography rule enforced; the deferred word cuts done now that the voice exists.

Deliverables:
1. **Orthography**: the rule in `IDENTIDADE.md` v2; one conversion pass over all public strings, house prose (`derivation`, notes shown, reading-page sentences, page texts), the two mixed identifiers in the ledger's kind names (`actualizacao` → the chosen form, with the migration recorded); verbatim fields untouched; a check in `gate-html.mjs` over rendered text with verbatim elements excluded, wordlist of the differing forms, baseline recorded, planted defect proven.
2. **`/sobre` · `/en/about`**: the decided two sentences and the door to Método; English mirror; footer authorship line removed everywhere (gate updated, decision recorded); the vault's counsel-brief note kept open on Art. 50 disclosure at publication level.
3. **`/metodo` · `/en/method`**: the ten points as rules with live proof; the gates emit a machine-readable summary at build time (`dist/prova.json` or similar: rows, pages, sealed values, corrections by kind, last panel check, last blind re-read when the field exists); the page renders each rule with its mechanism and last proof; the mechanism drawn as an instrument (fontes → motor → livro-razão → construção → página → leitor, with agenda, releitura, correcções feeding back) carrying today's numbers on each node; the old Método content folded in at a third of its length; `/metodo#correcoes` register moves to `/correcoes`.
4. **`/correcoes` · `/en/corrections`**: the register (correções and actualizações listed; revisões de proveniência as counts with links to rows), the policy stated once, the door.
5. **`/agenda` · `/en/agenda`**: four states (em curso · a seguir · concluído · retirado); each item: title, the question, porquê with its criterion (institutional framework linked to ledger rows / source calendar / reader request or correction), proposed-by-AI date, decided-by-director date, entered, last change, change log with reasons; seeded with what is real: housing (next study; framework flags), the DGAL 2025 file watch, the Évora 2026 accounts watch, the vanished 2024 page; the agenda is the public face of the engine's pre-registration (`core/prereg.py`, first records written); nothing leaves silently (the change log is gate-checked like corrections).
6. **Source calendar** (engine, `indicators/calendar.json`): built from the sources' own release calendars where published (INE and Eurostat certainly; DGAL, IEFP, ERSAR to verify), `watch_next_events.py` wired to the monthly agent and its three events entered; the calendar feeds the agenda's "a seguir" and, later, the freshness lines.
7. **No quiet course change**: `DECISIONS.md` entries carry `afecta: sobre|metodo|agenda|nenhum`; the check refuses a flagged entry unless the text changed in the block or the entry says why not; proven on a planted entry.
8. **The deferred word cuts**: the corrections policy off the 264 row pages (link to `/correcoes`); the no-rankings pledge to Sobre/Método and the Évora page's «Quem responde pelo quê» only; the Évora apparatus reduced to one home per caveat; the frame-phrase count and meta-commentary count recorded as the baseline `gate:identidade` will hold.
9. **Masthead**: «Edição de …» removed; the weekly panel date line; the agenda line when `/agenda` ships (same block, so both).
10. **`ABOUT.md`** cut to the idea plus pointers to the live pages; artifact republished.

Acceptance: build green with the new checks; Codex review of the diff and a canaried read of the four new pages; preview; director's word; merge; `verify:deploy`; the two-minute test re-scored (expect (a) pass, (b) partial: index exists, measures pending, (c) partial, (d) pass).

Cost: roughly 700k to 1M (Opus build in two or three agents; Codex two reviews; Sonnet for the wordlist and string pass).

## Phase 2 — design explorations (BUILT, REVIEWED TWICE, MERGED AND LIVE 2026-08-18)

Goal: the visual direction chosen against the trust brief's acceptance tests, then `IDENTIDADE.md` v2. **Done, in three steps:** three directions built in parallel (`design/direcao-a`, `-b`, `-c`, each with five pages, mobile, rationale and its own check); a cross-family critique of all three against the nine acceptance tests (`design/CRITICA-codex.md`); and the decision, direction S, which is the synthesis and not one of them (`design/DECISAO.md`, 2026-08-16). `IDENTIDADE.md` v2 was written from that decision and is the rule; the construction on the real pages is `DECISIONS.md` §1.43.

What is left of this phase and did not happen in the block: the design bundle regenerated from the built site and DesignSync run against it. Both come after the merge, and the seat does them.

1. Claude Design trial: import the current tokens and CSS; prototype the row page (the trust page), the front page, the município page, Sobre/Método, the agenda; three directions (refinement / instrument-forward / editorial-generous); live HTML the director reacts to. `[verify]` how well the import handles the Astro/CSS-token codebase; a small trial first.
2. `IDENTIDADE.md` v2: the visual rules that survive, plus the voice rules (state the idea and stop; no dashes; one home per caveat; where time is shown; the orthography), and the seal/legend conventions now written down.
3. DesignSync to keep components aligned once the direction is chosen.

Cost: mostly the director's reading time; orchestration 200k to 300k; Claude Design usage on the Max plan `[verify]`.

## Block T · the trust page (the row page becomes the receipt) (BUILT on `confianca` 2026-08-18; MERGED AND LIVE the same night, `28709e1`)

Goal: any number → the printed line in one click; every row shows when it was last read and last independently re-read; the register-sum rows leave the marker. Spec: `BRIEF-bloco-T.md`. Record: `DECISIONS.md` §1.47 (T1, T2, T3, T3c, T4 and the block's closing section).

1. **Format extensions with fail-tests: done, and there were six not three.** `document.page` (23 rows), `document.crop` (22), `verifications[]` (53 rows, 53 entries, 0 divergent), plus `document.url` (42), `document.hosted` (3) and `document.computed_over` (5). The exporter carries all six and refuses each on its own grounds; the gate compares each on the page. Nothing is written by hand: `verifications[]` has exactly two doors, the exporter and `indicators/refresh.py`, and the validator refuses the field on a row with no address.
2. **Receipts crossing as assets: done.** `publisher/recortes/manifest.json` is the record of what crossed; the files land in `public/recortes/` and the site re-checks the digest of their bytes. 22 of the 23 PDF rows show the crop under «Onde no documento» with «Abrir na página N». The 23rd was **refused** by `core/pdfproof.py` because its engine excerpt pins no file, no page and no quoted line, and the refusal is published rather than worked around. No base64 in HTML; no threshold lowered.
3. **`verifications[]` populated: done.** 21 Évora rows from the 2026-08-15 blind re-read (mapped by `mapa.evora.json`, which carries no values: the comparison happens in the exporter) and the 32 baseline rows from the panel run of 2026-08-18. The row page shows the last two, newest first, with who re-read, what they found and the door to repeat the reading. A divergence is published, not refused.
4. **The hosted-file origin: half done, and the half that is open is not ours.** The licence of each source was read on the source's own page by an agent that did not write the block. CAOP: CC BY 4.0, verified twice, so the counted extract is hosted with the digest of the zip it came from, `check:dados` re-counts it at every build, and the marker left those four rows. PRR: the dataset says «Licença não especificada» beside a platform term that says CC BY 4.0 «except where specified otherwise», which is a legal question and the director's; nothing is redistributed, the three sums keep `[a verificar]`, and `document.computed_over` records which files the sum was made over, with each snapshot date and digest. The re-extraction ran (T3c): the snapshot moved from 2026-08-03 to 2026-08-17, three of the five sums moved, each with a typed `atualizacao`, under the new V16.
5. **API rows: done where it could be proven.** The human page comes first in 42 of 57 rows. Eurostat is proven by machine and not by reading, because the data browser answers 200 with an empty title to any code, real or invented: the code exists if the dissemination API answers 200 to the row's own request, and the title comes from the official table of contents kept in the engine with its digest. The 8 INE rows have no human page because `www.ine.pt` did not answer this machine at all; the 2021 and 2025 local-election pages have none because the applications carry no territory in the address, and that is a proven refusal, not debt. `document.kind` was already the field since 2026-08-15 and the URL heuristic was already gone.
6. **Ledger downloadable: done, and unlinked on purpose.** `/livro-razao.csv` (RFC 4180, no comment preamble), `/livro-razao.json` and 132 per-row JSON files, all built from `ledger/claims/` at every build, with the format's fields minus `note`. The licence is one constant at `null`: with it, no page links the files and the ledger index states that the licence awaits the director; with it set, both editions offer the two files and every row page offers its own. Both states were built and measured.
7. **`lastmod`: answered "none", with the measurement.** Vercel clones at `--depth=10`, is not configurable and has no remote to unshallow; of the 244 versioned input files the last ten commits touch 66, so 178 have no date at all in that build. And an honest per-page model gives all 264 pages the *same* date, because every page depends on the strings, the stylesheet and the masthead. A committed map would be written state, wrong at the next commit. The three measurements are in `astro.config.mjs`, next to the field that is not written.
8. **The excerpt extractor: done.** The rule now lives once, in `core/excerpts.py`: a boundary is whitespace that does not have a digit on both sides, because Portuguese writes thousands with a space. 169 excerpts moved and no value moved; the three rows this item named are among the six that reach the site.

Two gate extensions came with T4, both proven on planted defects and neither a new gate: a `data-claim` is compared as a **string** and not as a run of digits (the minus sign and the decimal comma count; a unit symbol inside the element fails), and the decisions tether now reads `IDENTIDADE.md` and requires every quotation of a governed text to exist there word for word.

Cost estimate was 800k to 1.2M.

## Phase 4 — the two gate exceptions

1. `gate:identidade`: seal beside every value (SVG via declared legend), the marker only in its class, palette (no colour outside the tokens), allowlist size against baseline, frame-phrase and meta-commentary counts against baseline, orthography (moved from gate-html if built there), no em dashes; each proven on planted defects.
2. The prose gate: the engine's `assertions` discipline ported to the site: quantifiers («sempre», «a maior parte», «cerca de», «muito») declared and backed by a row or refused; causal connectives flagged for review; proven on today's four cases.

Cost: 400k to 600k.

## Content — the studies and the fan-out (interleaved with the above: one study per product block)

1. **Housing** (first pre-registered study, first domain page): question fixed and sealed before collection (`core/prereg.py`); the institutional flags as its criterion (MIP threshold, Semester, IMF, OECD, F4GC) each a ledger row; blind re-derivation of headline figures; cross-family review; its agenda item public first. **State on 2026-08-20:** sealed 18.08; H1 framework survey done and audited (63 rows, 32 tier A); H2 blind re-derivation filed (14 figures reproduced; the OECD Better Life score and rank not reproducible from what the OECD prints); sq-3 closed against the site's published dataset (three Eurostat rows, nothing from the OECD side); `SURVEY.md` and `SQ3.md` under the engine's commit gate; a context-starved Codex read of the survey and ledger run with five plants. Next stage is the accounting (sq-5: definition by definition, covered populations, cost concepts, common period, the OECD sample of 38 against 41, whether the WISE data file is the OECD's publication of the score), with the writer never verifying their own claims; sq-7 (INE, Banco de Portugal, IHRU) when INE answers, serially with a gap. No reading page until the accounting is reviewed.
2. **Water**: the director's own question (is the investment going where the loss is); the desalination side from primary sources; closes the water rows in debt; needs the APA/SNIRH session listed in the vault.
3. **The municipal fan-out**: the framework survey for municipal indicators (DGAL's legal debt limit and index, PMP and execution if DGAL publishes them for all `[verify]`, INE population and purchasing power, IEFP unemployment, SCIE enterprises); the aggregator extraction under the engine's gates; 308 pages generated with the empty state where a measure is missing; the stewardship spine per municipality as the governance data allows (the "who governed" open dataset does not exist; building it is the long project, gated on the counsel brief's cluster B). **Proposed on 2026-08-20, pending the director's word:** the coverage map's register (`indicators/coverage/register-candidates.json`, 138 candidate rows, 110 blind-checked) is the framework survey's input and is not re-surveyed; the first study after housing is municipal debt against the article 52 ceiling for the 308 câmaras, run together with this fan-out's finance spine (DGAL's two published columns, no constructed ratio; decision dates, not fiscal-year stocks; wait for the 2025 file and the current article 52); one study per product block.
4. **Domain pages** in the order verified material exists: public money and economy, housing, water, energy; then education and health; then justice, science, culture. Each begins with a framework survey (which institution assesses it, with which few indicators and thresholds), never with our own picks.
5. **Series**: the engine's observations store crossing to the site so evolution can be shown (the ledger holds point values).
6. **The trust ratchet for the weekly refresh**: proposed diff + report reviewed → deploy; after N clean approvals, value-only refreshes that pass all canaries auto-publish.

## Standing items

- Legal: the counsel brief (`legal/counsel-brief.md`) needs the director's §7 checklist before it goes to Portuguese media/IP counsel; cluster B (party affiliation as special-category data) gates the stewardship spine at scale; Art. 50 disclosure at publication level recorded.
- Engine housekeeping: `content/05 Fund Access Register` never committed; `core.reconcile` NBSP regex (a gate helper, under the moratorium); the seven studies' registration debt stays as debt by design.
- The director's own reads: the pt-PT edition of 04 (the recorded missing human step); the Método cut at preview.
- **PRR re-extraction cadence: once a month, with the monthly sweep, not daily** (decided 2026-08-20, `DECISIONS.md` §1.49). The publisher replaces the resource every day at a new dated address, so every re-reading forces a `proveniencia` step on `source_url` in each of the five sums; two re-extractions in three days put six new entries in these rows' histories, and a row's history is for a reader who wants to know whether what they read has changed. The accepted cost, written down: between re-extractions the page publishes a value that is no longer the sum of the served snapshot, with its date in view.
- Review point: end of September 2026, against the two-minute test and this plan.

## The next session, in order

1. Read the pickup files (vault Experiment note and Open → `DECISIONS.md` §1.43 to §1.47 and §4 → engine `NEXT.md` → this plan). §4 must be clean or written before a block starts, and after Block T it is: every Fase 3 row is closed or rewritten with why not.
2. ~~**Finish Block T.**~~ **Done 2026-08-18:** the cross-family read (Codex, context-starved, five plants, four caught, the em dash recorded as the one that passed a human read and is refused by the orthography check), the fix stage T5, the preview, the director's word, the merge (`28709e1`), `verify:deploy` ✓, the design bundle (13 cards) and DesignSync, the vault. **Block T is live.**
3. **The director's answers, put to him on 2026-08-20 with a recommendation beside each. (a) and (d) are DECIDED; (b), (c) and (e) are still pending:** ~~(a) the licence of the ledger dataset (one constant, `src/data/licenca.mjs`; recommended CC BY 4.0 with the scope note that quoted excerpts stay their publishers')~~ **(a) DECIDED 2026-08-20 (`DECISIONS.md` §1.48), built on the branch `decisoes-2026-08-20`:** CC BY 4.0, attribution «O Estado do País, oestadodopaís.pt», one constant changed and nothing else; both editions now offer the CSV and the JSON, all 264 row pages open the door to their own, and the scope note beside the licence says the licence covers the structure, the house values, the derivations and the descriptions, while transcribed excerpts stay under their publishers' terms; (b) the licence of the PRR snapshot (recommended: not hosted; add the question to cluster A of `legal/counsel-brief.md`, which does not carry it, and ask the publisher in writing; dados.gov.pt still says `notspecified` on 20.08 and the resource rotates daily, 20260819 that day); ~~(c) the two republications, 07/08's 338 excerpts and study 04's snapshot~~ **(c) DECIDED 2026-08-20 and BUILT on the branch `republicacao-2026-08-20` (`DECISIONS.md` §1.49):** taken as one block with the re-extraction inside it, so that the page and the rows say the same snapshot. The seven PRR rows crossed on the 2026-08-19 snapshot with five typed `atualizacao` entries; five document editions were republished from the engine, and the three of 07 and 08 stopped being claude.ai artifacts; the two derived rows' arithmetic is now said in words, with no digits. **(b) was decided with it**, and the answer is that the PRR file is neither hosted nor asked for: each counted file gets a door to a dated Internet Archive capture whose SHA-1 matches the bytes the engine read, and the marker stays on the three sums. Two questions go back to the director with the block: whether a third-party capture whose digest matches may retire the marker, and whether `derivation` joins the closed list of provenance fields; ~~(d) the first sentence of the Método's rule 6, read by Codex as an absolute the page denies, recommended minimal edit~~ **(d) DECIDED 2026-08-20 (`DECISIONS.md` §1.48), built on the same branch:** the recommended edit was taken, in both editions and with no other word of the rule changed: «Os números publicados são relidos na fonte por um caminho diferente e por quem não os escreveu, linha a linha, e cada linha diz se já o foi, quando e com que resultado.» / «Published figures are read again at the source by a different route and by someone who did not write them, row by row, and each row says whether it has been yet, when and with what result.» It is the block's only governed-text change, and it is the director's decision, not a proposal; (e) the coverage map's tiering, splits, promotions, first study (municipal debt against the article 52 ceiling, with the municipal-finance fan-out) and the two build items (serial INE client, vintage stamp). Still open from before: his word on the orthography (if «anterior»: rerun `scripts/ortografia.mjs --aplicar --sentido=anterior`, hand-review the listed one-way forms, flip `IDENTIDADE.md` §9, rebuild), and the English of Sobre and Método. The rewritten limit of rule 6 went live with the merge and stands unless he overrules it.
4. **The housing study is under way in the engine** and is the first pre-registered one: the question was sealed on 2026-08-18 before any collection, and the technical record, including an audit of the survey and a blind re-derivation of the headline figures, is in `content/10 Housing/Technical Source/VERIFICATION.md` in the engine. On 2026-08-20 sub-question sq-3 (what this site's livro-razão already publishes) was closed against the site's published dataset, `SQ3.md` and `SURVEY.md` were registered under the engine's commit gate, and a context-starved Codex read of the survey and the ledger was run with five plants (its record is in the engine's `verificacoes/`). sq-5 (the accounting) and sq-7 (INE, Banco de Portugal, IHRU) are open; INE's API still did not answer this machine on 20.08. No figure from it belongs here until it has crossed under the engine's gates and been reviewed; this plan does not carry one.
5. A second session was mapping coverage in the engine at the same time (`indicators/coverage/`, research only, nothing applied to the site). Its own record is the engine's `NEXT.md`, written by that session; it is not described here.
6. ~~**Two republication decisions**~~ **Done 2026-08-20** (item 3 (c) above, `DECISIONS.md` §1.49). What they were, kept as history: the 338 excerpts of the 07 and 08 study documents that fell out of step when the extractor was fixed, and the copy of study 04 that this site serves, which is one snapshot behind the ledger row. Each means republishing bytes the site pins by digest in `studies-src/manifest.yml`.
7. Standing from Block V's record (`DECISIONS.md` §4): the eight sources with no published calendar (re-check ERSAR); the reworded-correction duplication on re-export (engine V10's counterpart); calendar history; the two mechanisms for site counts (house rows vs `data-prova`); dead string keys; the five pre-Acordo forms left in the unpublished `note` field; the engine's failing health checks (`servers.pt_public_money` 8 of 19, `core.health_check` 1 of 5) seen by the sweep.
8. Then Phase 4, which Block T did not open and did shrink: the digits-only comparison and the unchecked constitution quotations are closed, and what is left of that phase is the `gate:identidade` and the prose gate.
