--- ADENDA 7 (from the seat, 29.08.2026; the director's words, in substance) ---
The director is considering renaming the site's title to the single word «estado», lowercase (in Portuguese the lowercase word is the condition, the capital is the institution; the site is about the condition), with a small line under it carrying the descriptor: «observatório de Portugal» (the existing identity sentence) or, as alternatives to compare, «observatório do estado do país» and «observatório do estado de Portugal». And he wants a proper typography for that title: letters designed for it, not a font as is.

Explore, and show, three constructions of the lowercase wordmark «estado», each with the live «e» mark as its initial or at least in the same family:
1. **Geometric, from the mark's own circle**: «e», «s», «t», «a», «d», «o» built on the same circle and stroke (14 % of the x-height circle's diameter, straight-cut terminals); the «s» is the known hard case (twelve constructions failed in the house grid before): try at least three «s» constructions (two arcs of the same circle; a spine of a smaller circle; an «s» drawn as a reversed «e» stroke) and say which reads as «s» at header size and at 60 px.
2. **Humanist sans**: the same six letters with modulated stroke and open apertures, drawn (Spectral's skeleton may guide the proportions; no glyph outline in the sign), the «e» adapted so the mark and the word are visibly one family even if the mark keeps its geometric form.
3. **The house serif as the control**: «estado» set in Spectral (the header's current face) with the live «e» mark as the initial, exactly as the header composes it today but with the word in lowercase, so the director sees what changes with no new drawing.

For each construction, render: (a) the header at the site's real sizes (the `.wordmark` clamp at 320, 390, 768 and 1280 px wide) with the small line under it in the header's secondary style, for each of the three descriptor lines, light and dark (the word and the line only, in the masthead's layout, no other page content needed); (b) the word alone at 512 px on paper and on ink (as a possible large icon); (c) the phone home screen mock (the icon stays the live «e» on ink; the label under it reads «estado»). Compose one sheet per construction (`FOLHA-ESTADO-1.png`, `-2`, `-3`) and one comparison sheet with the three headers side by side at 390 and 1280 (`FOLHA-ESTADO.png`). NOTAS.md gets a section «A palavra «estado», em minúsculas»: for each construction, what you saw (the «s» especially, the rhythm of the six letters, whether the mark and the word read as one family), the measurements (x-height, stroke, the header's height before and after, which must stay equal or the difference stated), the descriptor line's legibility at 320, and your order of preference with reasons; no praise words. Also write there, as a plain footprint for a later rename block, where the formal name lives in the code (`SITE_NAME` in `site.config.mjs`, the `<title>` suffix, the manifests, the identity sentence, the inventory rows), counted, without changing any of it.

Rules: everything under `design/marca/`; no change to `src/`, `public/` or the site's behaviour; the exploration is for the director to decide the rename, not the rename itself. Drawn letters, not typeset (except construction 3, the control). Judge by looking at your renders with the Read tool. No dashes in Portuguese prose (commas, colons, or the middle dot). Commits in the worktree with explicit paths only (never `git add -A`), each green on `npm run typecheck`, with the two trailers `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01BbaH3XteKcsmmN9VD6SGwU`; do not merge or push; never fetch from the network.
--- end ---
