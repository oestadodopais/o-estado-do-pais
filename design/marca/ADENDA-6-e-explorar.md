--- ADENDA 6 (from the seat, 28.08.2026; the director's words, in substance) ---
The director saw the refined «e» and does not like any of them yet: «not quite there»; the colours look off and are not pleasant; black and white might be better than this; maybe other options too. Two concrete ideas to explore, not as rules: the horizontal bar could stop before joining the circle, about two thirds of the way to it, floating inside the bowl; and the cut in the circle should be smaller (the missing fraction smaller than now). He wants an exploration that shows possibilities and says, for each, why it would be better or worse.

Build a grid of variants of the minimal «e» (start from `18n`, 14 %, and keep one stroke weight per variant):
1. **The bar's length**: joined to the ring (as now); stopping at three quarters; at two thirds; at one half of the way from the left inner wall to the right inner wall; and the same lengths starting from the ring on the left (attached at one end, free at the other). Measure at 60 and 16 px whether the bar still reads as part of the letter or as a dash inside a ring, and whether the «e» still reads as «e».
2. **The cut**: smaller apertures than today's 48 degrees: 36, 28, 20 degrees, and a hairline gap; say at which aperture the «e» becomes an «o with a bar» at 16 px (measure the aperture chord in pixels).
3. **Colour**: black and white first: ink `#17191b` on paper `#f6f7f4`, paper on ink, and a pure white on pure black pair; then, only as comparison, one muted pair (the house cobalt `#1f4e8c` on paper, and the grey `#585d5b` on paper). No amber, no ochre in this round unless you want one cell to show why the director found it off (say what you see: the amber on ink reads as what?).
4. **Composition**: two cross tables the director can read in a glance: `FOLHA-E2.png` (rows: bar lengths × columns: cuts, at 180 px, in ink on paper) and `FOLHA-E2-cores.png` (the two or three best geometries × the colour pairs, at 180, 60, 16). Then `ECRA-E2.png`: the four best variants on the phone home screen (light and dark screens) among the same eight reference icons.
5. NOTAS.md, section «O «e», explorado»: for each axis, what changes as it moves, with numbers (minimum run, aperture chord, ink percentage, contrast ratio for each colour pair), and one line per variant on why it is better or worse; a short recommendation, with the reason, of at most two variants. No praise words; describe what is seen.
--- end ---

Rules: drawn geometry, judge by looking at your renders with the Read tool; no dashes in Portuguese prose (commas, colons, or the middle dot); commits in the worktree with explicit paths only (never `git add -A`), each green on `npm run typecheck`, with the two trailers `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01BbaH3XteKcsmmN9VD6SGwU`; do not merge or push; never fetch from the network.
