# ISSUES · redesenho v3, fase 1 (lista corrente; os fechados ficam)

| # | aberto em | o quê | onde | estado |
|---|---|---|---|---|
| I1 | etapa 0 | As cópias das maquetas no repositório (`71b4c65`) estavam atrasadas face ao canvas publicado: postura do mapa (selo em vez de respirar), cauda em secções em vez de portas, sem a prancha «Cartão de partilha» | `design/especime-v3/maquetas/` | fechado na etapa 0: refrescadas do canvas, as antigas guardadas com sufixo `.pre-2026-08-20-tarde` |
| I2 | etapa 0 | A anotação `nota-v3` do canvas ainda diz «mapa em postura de SELO» no parágrafo pós-crítica; o código da prancha respira | canvas | aberto; é texto do canvas, e o canvas não se publica sem a palavra da direção. **Passagem de higiene, 29.08.2026:** a anotação continua nas duas cópias do canvas que o repositório guarda, `design/especime-v3/maquetas/canvas-export.html` e `canvas.json` (medido; controlo positivo: 17 dos 32 ficheiros da pasta levam a palavra «mapa»), e passou a estar errada por uma segunda razão além da que a linha regista: a postura que ela descreve deixou de existir com a Emenda 20, que troca os 308 pontos pelas 29 unidades. Medido na construção de hoje: `dist/index.html` tem **0** `circle.mun` e **29** `a.uni-porta`. Continua a resolver-se do mesmo modo, com a palavra da direção sobre o canvas |
| I3 | etapa 0 | O sinal de tempo do cabeçalho: corpo §3 da constituição e a prancha a cobalto; Emenda 1 diz cor só para limiares publicados | `Masthead.astro` | resolvido por precedência (tinta), assinalado à direção (plano §3 i) |
| I4 | etapa 0 | O quadrado tracejado: a regra 5 do Método fixa-o como «falta um campo»; a fila de estados da maqueta usava-o como «por ler» | estado e cobertura | à espera da decisão (b); recomendação no plano |
| I5 | etapa 0 | As pranchas carregam Spectral/Bitter de `fonts.googleapis.com` | maquetas | não entra; etapa 1b aloja os tipos |
| I6 | etapa 0 | `figuras.mjs` declara limiares sem lado; a posição de investimento é um chão | `src/data/figuras.mjs` | etapa 2a: campo `lado` declarado |
| I7 | etapa 0 | Não há chaves da prova para as contagens de estado do painel | `src/lib/prova.mjs` | etapa 2a: três chaves com estragos plantados |
| I8 | etapa 0 | As pranchas não têm paleta escura; o sítio serve um tema de três estados | `tokens.css` | à espera da decisão (f); proposta medida no plano |
| I9 | etapa 0 | `assertKeyParity()` compara caminhos de chaves, não valores: PT copiado para EN passa | `strings.mjs` | a régua da invariância imprime os iguais (etapa 2); `CHAVES-EN.md` para a revisão de voz |
| I10 | etapa 0 | O Método não tem hoje frase de cor nem de letra; a da prancha («A letra») reclama a linhagem Gini 1938, que a Emenda 5 proíbe | `metodo.mjs` | rascunhos no plano §12, à espera do sim |
| I11 | etapa 1 | As páginas de documento de estudo (`/estudos/<id>/documento/`) escrevem a sua própria folha, com as pilhas de sistema da v2 em literal (`Iowan Old Style…`, `ui-monospace…`), e não leem ficha nenhuma de `tokens.css`: depois da etapa 1 são a única superfície do sítio fora da letra da constituição | `src/lib/documentos.mjs` | **FECHADO** na ronda pós-fusão (22.08.2026, construtor B1), e não como a recomendação previa: a faixa não passa a nomear `--f-prosa` e `--f-instr`, passa a **ler** a folha da casa na construção. Um parser em `estiloDaFaixa()` extrai de `src/styles/tokens.css` a paleta clara do `:root` (`--paper`, `--ink`, `--g1`, `--g3`, com `--muted` a fechar-se sobre `--g1`), as pilhas `--f-prosa` e `--f-versal` e as duas fichas `@font-face` das letras que a faixa de facto usa, e de `src/styles/site.css` a composição de `.wordmark`, `.masthead-compact .wordmark` e `.eyebrow`. Nenhum literal de cor, de tipo ou de corpo fica escrito no ficheiro; as fichas vão para dentro da faixa com o prefixo `--oedp-`, porque **os quinze documentos alojados declaram `--ink` no seu próprio CSS e nove deles declaram também `--paper`**, e um `var(--paper)` na faixa apanharia o do documento (medido: em «Évora, orçamentado, pago, devido» o `--paper` do documento é `#FAFBF9` em claro e `#12181B` em escuro). Três plantas provadas e revertidas provam que o parser morre em vez de adivinhar: `--paper` renomeado, a ficha `@font-face` do Spectral SC retirada, e `.masthead-compact .wordmark` sem `font-size`. `Iowan Old Style` e `ui-monospace` passam a zero em `src/lib/documentos.mjs`. Detalhe em `notas/pos-fusao.md` §B1 |
| I12 | etapa 1 | Quatro literais `var(--yellow)` sobrevivem em atributos `fill` de gabarito, num componente e em duas vistas que a etapa 1 está proibida de abrir (`InstrumentoConvergencia.astro:187`, `AgendaView.astro:673`, `MunicipioView.astro:275` e `:388`). O token já não existe; as seis regras nomeadas de `site.css` remapeiam-nos, e o `fill` calculado foi conferido em Chromium sem cabeça nas três rotas | `src/components/InstrumentoConvergencia.astro`, `src/views/{Agenda,Municipio}View.astro` | aberto; o critério de saída do brief («o grep imprime nada») cumpre-se em `site.css` e não nos três ficheiros. Saem quando as etapas 2 a 4 reescreverem esses desenhos, e o bloco de remapeamento sai com eles. **As duas de `MunicipioView.astro` saíram na subetapa 3d (21.08.2026)**: os dois desenhos foram reescritos na gramática da Emenda 4 (referência a tinta à altura toda, barra = distância, traço fino = valor) e nenhuma cor fica escrita no gabarito. Medido: `grep -rn "var(--yellow)" src/` já não dá nenhum atributo `fill` em `MunicipioView.astro` (as duas ocorrências que ficam nesse ficheiro e em `InstrumentoConvergencia.astro` são comentários), e resta **uma real**, `AgendaView.astro:673`, que é da etapa 4. As duas regras de remapeamento `.mun-distancia-svg rect` e `.mun-serie-svg rect` ficam **sem cliente** e são do construtor A. **FECHADO na subetapa 4c (21.08.2026)**: o último literal real, `AgendaView.astro`, passa a `var(--g2)`, e a regra `.agenda-eixo-svg rect` de `site.css`, que o remapeava, sai no mesmo commit. A página não muda de aspecto — a folha já ganhava ao atributo, e a captura do eixo antes e depois é a mesma —, o que muda é o gabarito deixar de nomear um token morto. Medido: `grep -rn "var(--yellow)" src/` dá **quatro** ocorrências e as quatro são comentários; nenhuma é um atributo `fill` |
| I13 | etapa 1 | O alvo de 44×44 do selo não cabe onde os selos se empilham: medidos 3,1px, 18,2px e 22,4px entre selos consecutivos na primeira página e na página do município, e duas áreas de 44px a 18px de distância sobrepõem-se, com a de baixo a apanhar o clique da de cima. Sete lugares ficam com a área do tamanho da unidade, para que nenhuma porta abra a linha do vizinho | `src/styles/site.css` (`.src-chip::after`) | aberto; medido 6/27 selos com alvo ≥44×44 na primeira página, 8/91 na do município, 1/1 na página de linha, e **0 pares sobrepostos** nas três. A saída é dar altura à fila: etapa 2 (plano §13) e etapa 3. **A parte do instrumento n.º 1 fecha na ronda pós-fusão (22.08.2026, construtor A2)**, e a lista de excepção passa de sete selectores a quatro. Cada um dos sete foi conferido contra as **322 páginas construídas** com `node-html-parser`, e com `a.src-chip` como controlo positivo (1034 selos em 288 páginas): `.figura` acerta em **zero** elementos (a classe não existe em nenhuma página; no código só sobrevive `.figura-nome`), `.prov-vals` tem 4 elementos e **zero** selos, `.linha-verificacoes` tem 264 elementos e **zero** selos. Os três saem. `.glance` estreita-se a `[data-instrumento='mandatos']`: o relance da primeira página passa a levar a área inteira, e o da página do município fica com a unidade porque ali os dois selos do «de → até» quebram de linha a 390 e duas áreas de 44px a 33,8px sobrepõem-se por 10,2 (essa fila é da etapa 3). Medido depois, em Chromium sem cabeça, nas duas edições, a 1280/1024/390 e nos seis estados de região do instrumento: **zero selos abaixo de 44×44 fora da frase, zero aninhados, zero pares sobrepostos** (36 medições), e varrida a rampa de 320 a 1440 de 16 em 16, **852 medições com zero pares**. Entram oito células na matriz de aceitação (107 → 115), com controlo positivo em três delas. Fica **uma excepção medida, e é a única**: o selo da FRASE da leitura breve (`.brief-text`). Duas das seis frases levam dois selos dentro da mesma frase, a entrelinha é 27,65px, e com 44px de área os dois sobrepõem-se por 16,4 na vertical nas bandas 434 a 512 (pt · Península de Setúbal), 534 a 632 (pt · Alentejo), 416 a 534 (en) e 478 a 616 (en · Alentejo). A 1280, 1024 e 390 não se tocam, e por isso uma medição de uma largura só não fecha isto. **A saída medida é levar a entrelinha da frase de 27,65px para 44px, que é mudar a composição da leitura: decisão da direção, não do construtor.** A outra metade do I13 continua a ser a página do município (etapa 3), onde ficam 6 pares sobrepostos a 390, todos entre portas de banda de mandato e nenhum entre selos. **Passagem de higiene, 29.08.2026: a metade da página do município fecha; a da frase continua, e mudou de superfície.** A do município fecha pelo item B10 da §1.66, que estendeu a técnica dos alvos às rotas interiores: medida com a sonda da própria casa (`SONDA_ALVOS` de `tests/inicio/correcoes-a.mjs`, corrida à letra sobre a construção de hoje), a página de Évora dá **0 pares sobrepostos** a 390 nas duas edições, onde tinha 6. A da frase **não** fecha: a excepção continua escrita em `src/styles/site.css:847`, com a mesma razão ao lado, e a leitura breve saiu da primeira página com o instrumento (§1.66 A3) e vive hoje nas páginas de região (Emenda 21, §1.75). Medido: `.brief-text` rende em **20 páginas** (as 18 de região e as 2 do Método) e duas levam dois selos dentro da mesma frase nas duas edições, que são as duas de sempre, Alentejo e Península de Setúbal; a entrelinha continua **27,65px** e a área de cada selo mede **52,5×19 px** em português e **61×19** em inglês, isto é, o tamanho da unidade e não os 44. Varrida a rampa de 320 a 1440 de 16 em 16, 71 larguras em cada uma de quatro rotas, dá **0 pares sobrepostos**, e dá zero precisamente porque a área é 19 e não 44: o defeito não saiu, está contido pela excepção. Falta o que já faltava, e é decisão da direção: levar a entrelinha da frase de 27,65px a 44px |
| I14 | etapa 1 | Sete selos de `.prov-vals` (a fila de proveniência do instrumento da convergência, na primeira página) não se deixam medir por `elementFromPoint` depois de `scrollIntoView`: o ponto fica fora da janela. Conferido que o mesmo acontece no commit `7650aed`, antes de a etapa 1d tocar no selo, e por isso **não é uma regressão desta etapa** | primeira página, instrumento n.º 1 | **FECHADO por remoção** na ronda pós-fusão (22.08.2026, construtor A2). A fila que os tinha saiu da primeira página em `1cac621`, com a camada de aparelho do instrumento (Emenda 15): `grep -c prov-vals dist/index.html` dá **0**. Não há sete selos por medir porque não há sete selos. Conferido em todas as 322 páginas construídas, com `node-html-parser`: `.prov-vals` sobrevive em **4 elementos, em duas páginas** (`/municipios` e a sua edição inglesa) e leva **zero** `a.src-chip` lá dentro. Medidas as duas filas dessa página, a 1280, 1024 e 390 nas duas edições: a primeira (`prov-vals verbatim`) não tem alvo nenhum, a segunda tem um só, que é a porta de descarga do CSV (`a.ligacao-dados`, 206,1×16,5 em pt e 185,3×16,5 em en), e **zero pares sobrepostos** nas seis medições. Não é o mesmo defeito (não há selo abaixo de 44 nem par sobreposto), e por isso fica como está. A regra `.prov-vals a.src-chip::after` sai da lista de excepção do I13 no mesmo commit: uma excepção sem elemento não protege nada |
| I15 | etapa 1 | 41 regras de `site.css` continuam a pôr Bitter em caixa alta fora de um instrumento, contra a Emenda 5 e a `IDENTIDADE.md` §1 v3. A etapa 1 corrigiu as três que são mobília partilhada (`.nav`, `.rodape-nav`, `.masthead-furniture`); as outras pertencem a famílias de página das etapas 2 a 4 | `src/styles/site.css` | aberto; a lista das 44 regras reproduz-se com o varrimento de blocos descrito na nota da etapa 1. **A parte da família do livro-razão fechou na subetapa 3a (21.08.2026)**: os cinco rótulos da página de linha (`.linha-bloco-titulo`, `.linha-campo-k`, `.aparelho-titulo`, `.aparelho-ficha dt`, `.linha-verificacoes dt`) passam a versaletes de Spectral SC em `src/styles/linha.css`, que entra depois de `site.css` no documento. Medido no motor: `Bitter em caixa alta fora de instrumento: 0` na página de linha, nas duas edições (célula «3a · a letra» de `tests/linha/recibo.mjs`). As regras de `site.css` ficam onde estão, porque o ficheiro é do construtor A; o que mudou foi o que a página de linha computa. **Passagem de higiene, 29.08.2026: continua aberta, e agora está contada por página e não por regra.** Medido em Chromium sem cabeça sobre a construção de hoje, com a técnica da própria casa (estilo computado com `text-transform: uppercase` e `font-family` com Bitter, a célula «3a · a letra» de `tests/linha/recibo.mjs`), fora de `.instr`, `[data-instrumento]` e `.rule-svg`: `/agenda` **223**, `/estudos` **69**, `/metodo` **48**, `/municipios/evora` **26**, e uma ocorrência em cada uma de `/`, `/en/`, `/regioes`, `/livro-razao` e `/areas`. A página de linha `/livro-razao/pib-pc-portugal-2024` lê **0**, que é a metade que a 3a fechou e continua fechada. O que resolve o resto é o que a linha já dizia: as famílias de página das etapas 2 a 4, e a agenda é hoje a maior delas |
| I16 | etapa 1 | `README.md` (linha 374) e `scripts/design-bundle.mjs` (linha 577 e a tabela seguinte) descrevem a identidade v2: «Iowan Old Style só em marcas, Avenir Next só em prosa, SF Mono…», «tudo pilhas de sistema, a página não faz nenhum pedido de rede», «o amarelo `#E8A80C` nunca é texto». Nenhum dos dois é da etapa 1 e nenhum entra no `npm run build` | `README.md`, `scripts/design-bundle.mjs` | **FECHADO** na ronda pós-fusão (22.08.2026, ramo `pos-fusao-v3` cortado de `8440781`), no commit desta ficha; o resumo do commit vai no relatório da ronda, porque um commit não pode conter o seu próprio `sha` (é a forma que I52 e I56 já usam). O gerador foi reescrito de raiz: a disciplina da v2 fica inteira (nada de novo, nenhum literal de cor, os números lidos das folhas, nenhum pedido para fora, a conferência a sair a 1) e o conteúdo passa a ser a v3, com 20 cartões, os oito WOFF2 e as três licenças escritos ao lado deles e cada citação procurada em `IDENTIDADE.md` ou em `direcao.md` no momento da corrida. O `README.md` §Identidade foi reescrito da §1, §2 e §5 e das Emendas 1, 5 e 12, e mais três frases v2 caíram fora dela: `tokens.css (tema de três estados)` na §Estrutura, «pilhas de sistema»/«nenhum pedido de rede» e a camada Fundo dos CSV descarregáveis. `Iowan`, `Avenir`, `SF Mono`, `#E8A80C` e «tema de três estados» passam a zero, com o `grep` antes e depois na nota. Um desvio ao brief, medido e escrito: o tecto de tamanho de um cartão sobe de 250 KiB para 512 KiB, porque `dist/index.html` pesa 240,9 KiB só de HTML e as folhas que ela usa pesam 159,8 KiB, e nenhum retrato da primeira página cabe em 250 KiB. Cinco plantas provadas e revertidas. Detalhe em `notas/pos-fusao.md` §A1 |
| I17 | etapa 2 | **Um alvo de 24×24 CSS px por ponto do mapa não é alcançável.** Medido sobre os 308 centróides da CAOP: o vizinho mais próximo está a 1,41 unidades de campo, e a mediana da meia-distância é 6,84. Com o mapa a 281px, o lado do alvo vai de 6,1px (mínimo) a 24,0px (o tecto), com mediana 6,4px. Dar 24px aos pontos densos seria dar-lhes o alvo do vizinho, e uma área sobreposta é uma porta que abre o concelho errado | `src/components/inicio/MapaRespira.astro` | aberto por desenho, e não por resolver: a pesquisa é o caminho principal para escolher um concelho, como a Emenda 3 já decide para o telemóvel. A distribuição inteira é medida na 2f. **SEM OBJETO desde a Emenda 20** (o bloco do mapa por distritos, 27.08.2026, DECISIONS §1.72), conferido a 29.08.2026: não há alvo por ponto porque não há ponto que seja alvo. Medido na construção de hoje: a primeira página tem **0** `circle.mun` e **29** `a.uni-porta` (a célula A6 de `tests/inicio/correcoes-a.mjs` diz o mesmo, «0 leituras e 0 pontos na primeira página»); e o cartão localizador, que é o único sítio onde os 308 pontos sobrevivem, rende-os em `/municipios/evora` e em `/municipios/beja` com **308 pontos e 0 dentro de uma âncora**, cada um a medir 2,55×2,55 px. Um ponto que não é porta não tem alvo para dimensionar. A pergunta que a linha levantava passou para os alvos das 29 unidades, que são a I82 e as células M1 e M2 de `tests/inicio/mapa-distritos.mjs` |
| I18 | etapa 2 | O rótulo de âmbito de um concelho sem página diz «<nome> · município · <distrito>» com o distrito tal como a Carta o escreve («Beja», «Ilha do Faial»), e o de Évora diz «distrito de Évora», que é a cadeia que `municipios.mjs` publica. É uma incoerência de forma entre um concelho e os outros 307 | `Cabeca.astro`, `municipios.mjs` | **fechado na subetapa 2g, com a regra da prancha: `distrito de <d>` quando o campo da Carta é um distrito, o nome de ilha nu quando começa por «Ilha»**. A regra decide-se na construção (`src/lib/inicio.mjs`, `eIlha()`) e cada ponto da CAOP leva `data-ilha`; o prefixo é um par de cadeias validadas (`inicio.cabeca.distritoDe`) que já está escrito na página nas duas edições, e o script só lhe troca o `hidden` — as duas ranhuras continuam ranhuras e nenhum texto é composto no cliente. Vale também no que o mapa lê em voz alta. Medido: Beja «distrito de Beja», Horta «Ilha do Faial», Lagoa (São Miguel) «Ilha de São Miguel», Évora sem prefixo próprio porque traz a etiqueta de `municipios.mjs`; 30 dos 308 são de ilha (`grep -o 'data-ilha="sim"' dist/index.html \| wc -l` → 30) |
| I20 | etapa 2g | **Transbordo horizontal de 10px em Évora na leitura breve, a 320 e a 390.** O rótulo da referência da régua do índice de dívida («limite legal») está centrado no extremo direito da sua escala, porque um tecto legal É o extremo da sua própria escala, e sai para fora da janela. Medido nas duas construções, seis estados × quatro larguras: 23 das 24 combinações dão zero, e esta dá 10 em ambas (`scrollWidth` 400 para `clientWidth` 390, culpado `span.regua-ref-rotulo`) | `src/components/inicio/Regua.astro`, `src/styles/inicio.css` | **fechado na subetapa 2h**: quando a referência calha numa ponta (`pr >= 82` ou `pr <= 18`, os mesmos limites que já decidiam qual das pontas da escala se escreve), o rótulo deixa de se centrar no traço e encosta-se à ponta — `data-ponta` na construção, `right: 0` / `left: 0` e `transform: none` na folha. Nenhuma palavra mudou. **O defeito tinha seis instâncias e não uma**: além de «limite legal» em Évora, os cinco «UE-27 = 100» das regiões estão no mesmo 100% e transbordavam 15px a 320 e a 390 num estado (`?ambito=regiao:<slug>&densidade=leitura`) que a lista dos seis estados varridos não continha. Medido depois: seis estados × quatro larguras, **24 de 24 a zero**, e 0 rótulos fora da caixa da régua. A varredura passou de guião de rascunho a célula da matriz |
| I21 | etapa 2g | **«Toque num ponto para escolher o concelho.» rende-se no telemóvel**, sempre que o âmbito é Município, e a 390 nenhum ponto é alvo: a Emenda 3 diz que o selo inteiro é o alvo e a folha põe `pointer-events: none` em todos os `.mun-alvo` abaixo de 640. É uma dica que descreve o que a página não faz nessa largura | `MapaRespira.astro`, `inicio.css` | **fechado na subetapa 2h**, com a regra das outras duas: entra `hidden` do servidor (que cobre o caso sem script), o script acende-a no âmbito Município, e a folha volta a apagá-la abaixo de 640 e na postura de localizador. Nenhuma palavra mudou. Medido na matriz: visível a 1280 no âmbito Município; não visível a 390, não visível a 1280 no âmbito País, não visível sem script em nenhuma das duas larguras |
| I19 | etapa 2 | As frases de moldura subiram de 77 para 94 distintas. As 17 novas são, uma a uma, as duplicações autorizadas R2 e R6 (as notas das oito medidas de Évora e a frase de abertura do concelho), cada uma agora em duas páginas. Nenhuma frase da v2 saiu, porque as da primeira página apareciam numa página só e a régua só conta as que aparecem em mais do que uma | régua `medir-defeitos.mjs` | aberto; a descida que o plano §4 espera é da fase da voz, e a etapa 3 pode desfazer as duplicações se a direcção quiser. **Passagem de higiene, 29.08.2026: a fase da voz correu e o número subiu, e a razão está medida.** `node scripts/medir-defeitos.mjs` sobre a construção de hoje dá **113 frases de moldura distintas, em 54 143 ocorrências**, das quais 6 590 são a porta de correcções (sem ela, 47 553). Eram 77 e passaram a 94 quando esta linha se abriu. A subida não desmente a fase da voz: a mesma régua conta blocos que aparecem em mais do que uma página, e o sítio passou de 344 para **6 590 páginas medidas** com os 308 concelhos, as 58 páginas de distrito, as 18 de região e as 18 de área, que repetem a mesma mobília. Enquanto a medida contar ocorrência e não intenção, este número cresce com a cobertura, e a leitura que o plano §4 espera pede uma régua por família de página |
| I22 | etapa 1f | A palavra «provisório» não entra dentro de um `<svg>`: um `<span>` não é filho legítimo de um `<text>`, e o `<tspan>` que o substituiria teria de ser posicionado pelo instrumento que o desenha. São três usos, `as="text"` e `as="tspan"`, em `InstrumentoConvergencia.astro`, que é da etapa 2 | `src/components/InstrumentoConvergencia.astro` | **fechado na etapa 2k (21.08.2026)**, e não como o I22 previa: a palavra continua a NÃO entrar dentro do `<svg>` — a leitura do `<span>` dentro do `<text>` mantém-se verdadeira e nenhum `<tspan>` foi posicionado à mão. O achado 13 da segunda leitura cruzada trouxe a pergunta de volta e a direção decidiu o lugar: **a entrada de legenda de selos daquele valor**, que é onde o selo do valor desenhado já vive pela convenção do §1.34. A banda da região já o fazia sem ninguém reparar (a entrada da sua legenda é um `<Claim … chip>`, fora do desenho); o Instrumento n.º 1, cuja entrada é só o selo, recebeu-a na 2k, lida de `source_flag` e nunca de uma lista. Medido: 1 → **2 entradas de legenda com a palavra por linha**, nas seis linhas e nas duas edições; 18 → 24 ocorrências por edição |
| I23 | etapa 1f | O inventário do plano §3 dá a bandeira `p` como sendo de uma linha, `pib-pc-portugal-2024` («the `82 p` claim»). São **seis**: as seis do PIB per capita de 2024, Portugal e as cinco regiões. Há ainda duas linhas com `source_flag: "a)"`, que não levam a palavra e são o controlo negativo | `design/especime-v3/PLANO-redesenho-v3.md` §3 | aberto; o plano é do lugar de direção. A implementação lê a bandeira da linha e não do inventário, por isso não herdou o erro. **Passagem de higiene, 29.08.2026: continua aberta, e o número já não é seis.** O plano continua a dizê-lo: `PLANO-redesenho-v3.md` linha 59 escreve «`pib-pc-portugal-2024` (the `82 p` claim)» como se fosse uma linha só. Medido no livro-razão de hoje: **10** ficheiros de `ledger/claims/` levam `source_flag: "p"`, contra as seis de então (controlo positivo: 12 ficheiros levam `source_flag` de todo, e 2 levam `"a)"`, que são o controlo negativo que a linha já nomeava). A implementação continua a ler a bandeira da linha, e por isso o erro continua a ser só do plano |

| I24 | etapa 2i | **`Claim.astro` assume português quando a língua não chega.** `const { lang = 'pt' } = Astro.props` faz com que uma chamada sem `lang` renda a palavra «provisório» na edição inglesa em vez de «provisional». Foi assim que sete valores da primeira página saíram em português na edição inglesa (achado 7 da leitura cruzada), e a marca não estava em nenhum dos dois ficheiros que o brief mandava ler: estava na ausência de uma propriedade. Os sete lugares foram corrigidos na 2i (`Peca.astro` e `InstrumentoConvergencia.astro`), mas a armadilha fica de pé para as etapas 3 e 4, que têm dezenas de chamadas a fazer | `src/components/Claim.astro` | **fechado na etapa 2j (21.08.2026)**: `lang` deixou de ter defeito e a construção atira quando falta, com a mensagem a nomear o `id` da linha, como as do portão. **Oito chamadas não a passavam**, e a construção apanhou-as uma a uma — `BandaDaRegiao.astro`, `InstrumentoConvergencia.astro` ×2, `EstudoView.astro`, `MunicipioView.astro` ×4. **Duas delas nenhum `grep` deste repositório tinha encontrado**, porque a linha continha `lang === 'pt'` noutro sítio e qualquer varredura por «sem lang» as excluía: só a construção as viu. Cinco das oito estão em `EstudoView.astro` e `MunicipioView.astro`, que são das etapas 3 e 4 e não são desta ronda; a alteração é `lang={lang}` e mais nada, está declarada na nota da 2j e não muda um byte rendido em português. Medido antes: 12 «provisional» e 7 «provisório» na edição inglesa; depois da 2i: 18 e 0; depois da 2j: **18 e 0**, sem mudança, que é o esperado — a armadilha fechou-se, os sete lugares já estavam corrigidos |
| I25 | etapa 2i | **As pastilhas das regiões levam `aria-pressed` sem papel de botão.** O script põe `aria-pressed` em `[data-regiao]`, que são `<a href>` e não são promovidas a `role="button"` (só os comandos de âmbito e de densidade o são). `aria-pressed` num `<a>` sem papel de botão não é ARIA válido, e um leitor de ecrã pode ignorá-lo — a pastilha escolhida deixa de se anunciar como escolhida. As da pesquisa (`[data-escolher]`) são `<button>` e estão certas | `public/js/inicio.js`, `src/views/HomeView.astro` | **fechado na etapa 2j (21.08.2026)**, e foram as duas linhas que a entrada previa: as pastilhas passam a `role="button"` no mesmo sítio onde os segmentos de âmbito e de densidade já o recebem, e ganham a tecla de espaço com o `activaComEspaco()` que já existia. O `href` fica, como nos outros comandos: com script é a forma do estado, sem script é a ligação que abre a mesma leitura. Medido na matriz (célula «2i·5»): `com role="button": âmbito 6/6, densidade 2/2`, e as cinco pastilhas entram no mesmo laço |
| I26 | etapa 2j | **`IDENTIDADE.md` §2 descreve a paleta escura como era antes da Emenda 12.** Diz «A paleta escura é regra provisória… A direção decidiu-a a 20.08.2026 com duas condições… a primeira pré-visualização é o seu primeiro teste real». A pré-visualização aconteceu a 21.08 e a direção decidiu: as duas condições continuam de pé, mas o escuro deixou de vir da preferência do sistema e passa a vir de um controlo do cabeçalho, com a escolha guardada no aparelho do leitor. O parágrafo não está errado sobre a paleta; está incompleto sobre como se chega a ela | `IDENTIDADE.md` §2 | **fechado no commit 3-0 (21.08.2026)**, com a alteração que a entrada pedia e nem uma palavra a mais: o parágrafo da §2 ganha uma frase que nomeia a Emenda 12, diz que as duas condições continuam de pé, que o escuro passa a ser um pedido do leitor guardado no aparelho dele, e que o bloco que consultava a preferência do sistema saiu de `src/styles/tokens.css`. Conferido antes de escrever, e não pelo nome do ficheiro: `grep -c "prefers-color-scheme" src/styles/tokens.css` → **0** (saída 1), com controlo positivo `grep -c "data-theme"` → **3**. Nenhuma citação entre «…» entra na frase, e por isso a amarra das decisões não ganha nada para conferir |
| I27 | etapa 2j | **`Provenance.astro` tem a mesma armadilha que o I24 acabou de fechar em `Claim.astro`**: `const { id, lang = 'pt' } = Astro.props`. Hoje não morde, porque quem o chama sem língua é o próprio `Claim.astro`, que já a exige — mas é a mesma classe de defeito, no componente do lado, e as etapas 3 e 4 vão escrever dezenas de chamadas | `src/components/Provenance.astro` | **fechado no commit 3-0 (21.08.2026)**: `lang` deixou de ter defeito e a construção atira quando falta, com a mensagem a nomear o `id` da linha, como em `Claim.astro`. **Nenhuma chamada precisou de correção**: as doze chamadas a `<Provenance>` do repositório já passavam `lang={lang}`, e a construção passou verde à primeira. Provado que a guarda dispara, plantando a falta numa chamada de `LivroView.astro`: «`<Provenance id="abandono-escolar-precoce-2025"> sem "lang". A língua é obrigatória: sem ela, o selo escreveria a palavra «fonte» e o texto oculto em português na edição inglesa (ISSUES I27). Passe lang={lang} na chamada.`», construção a fechar em `/en/ledger`; revertido, `git diff --stat` do ficheiro vazio |
| I28 | etapa 2j | **`inicio.mapa.naoDizK` («O que o mapa não diz») não é rendida por ninguém.** Veio com a relocação R3 do instrumento n.º 2 da v2 e nunca chegou a ter um lugar na ficha nova; conferido por `grep` sobre `src/components` e `src/views` antes desta ronda, com as três outras `naoDizK` de outras famílias a provarem que o comando encontra o que existe. Não é um defeito rendido: é peso morto no registo das cadeias | `src/i18n/strings.mjs` | aberto, e deixado de propósito: retirá-la muda a contagem «13 chaves relocadas» de R3, que é registo, e isso é uma decisão do lugar de direção e não uma limpeza. **SEM OBJETO desde `1cac621`** (a subetapa da mobília, 21.08.2026), conferido a 29.08.2026: a chave já não está no registo das cadeias, e por isso não há peso morto para decidir retirar. Medido: `naoDiz` dá **0** em `src/i18n/strings.mjs` (controlo positivo no mesmo ficheiro: «mapa» dá 22) e **0** em `src/`, `public/`, `scripts/` e `tests/`. A cadeia «O que o mapa não diz» está no pai de `1cac621` e sai nesse commit, com a gémea inglesa «What the map does not say»; nenhum commit posterior a repôs. A contagem de R3 em `RELOCACOES.md` não foi tocada por esta passagem, e continua a ser do lugar de direção |
| I29 | etapa 2j | **O brief da 2j manda aplicar a regra dos pontos também a `src/components/InstrumentoMapa.astro`, «que a página de concelho usa», e esse ficheiro não existe.** Saiu na subetapa 2b com `public/js/mapa.js`, e a nota da etapa 2 §2 regista que nenhuma outra vista o usava: `MunicipioView.astro` nomeia `InstrumentoConvergencia` num comentário e não importa nenhum dos dois. O mapa da página de concelho, quando a etapa 3 o desenhar, herda a regra de `MapaRespira.astro`, que é o único mapa do sítio | *(ficheiro inexistente)* | fechado por inexistência, e escrito para não voltar como pergunta. Conferido: `git log --diff-filter=D -- src/components/InstrumentoMapa.astro public/js/mapa.js` dá **`0c1ef02`**, que é o commit da 2b; e `grep -rn "InstrumentoMapa\|js/mapa.js" src/ public/ tests/ scripts/` dá **uma linha só**, um comentário em `site.css:1115` que nomeia o ficheiro antigo ao lado do bloco de remapeamento do amarelo. O controlo positivo é o mesmo `grep` a procurar `InstrumentoConvergencia`, que encontra as chamadas reais |
| I30 | etapa 2k | **A legenda de selos do Instrumento n.º 1 mostra UM selo, e o desenho pode ter seis valores.** «Ver todas» põe as seis regiões na régua e `desenhaLeitura()` continua a mostrar só a entrada da região lida: seis cópias desenhadas, um selo, e — desde a 2k — uma palavra «provisório». Não é regressão da 2k: a palavra herdou exactamente a regra do selo, que é anterior. É um buraco da convenção do §1.34 **em estado de execução**, e o portão não o vê porque varre HTML construído. Medido em Chromium sem cabeça: por defeito `desenhados:1 selos:1 palavras:1`; depois de «todas», `desenhados:6 selos:1 palavras:1` | `src/components/InstrumentoConvergencia.astro`, `public/js/convergencia.js` | aberto; a saída barata é mostrar as entradas de legenda de TODAS as regiões activas em vez de só a lida, o que não compõe cadeia nenhuma — são as mesmas seis entradas que o servidor já escreveu, a trocar de `hidden`. Não entra na 2k porque muda o que a legenda mostra, e isso é forma: é da direção. **FECHADO na passagem de higiene de 29.08.2026** (`a96482b`, Emenda 21, DECISIONS §1.75), e não pela saída que a linha propunha: o estado de execução que o defeito descrevia deixou de ser alcançável, porque a régua deixou de se desenhar no cliente. Medido na construção de hoje: `desenhaLeitura` e «todas» dão **0** em `public/js/convergencia.js` (147 linhas), a primeira página não cita o ficheiro e `/regioes` cita-o; e a régua completa, construída no servidor, dá **10 marcas, 10 selos e 10 leituras**, isto é, uma entrada de legenda por cópia desenhada. A lista das regiões é ao mesmo tempo as portas e a legenda de selos (§1.75), e por isso não há uma leitura desenhada sem o seu selo |
| I31 | etapa 2k | **`public/js/tema.js` passou a ter duas partes e o nome só diz uma.** A segunda é o `aria-expanded` das duas divulgações por irmão, que tinha de correr em todas as páginas com cabeçalho, e este é o único ficheiro adiado que todas carregam. O nome é da cadeira (brief da 2j nomeia-o); renomeá-lo é `Base.astro` mais o rasto de medição de duas rondas | `public/js/tema.js`, `src/layouts/Base.astro` | aberto; o cabeçalho do ficheiro declara as duas partes por extenso, para que o nome não seja a única coisa que se lê. A cadeira decide se vale um nome novo |

| I32 | etapa 2l | **Faltam ao painel do Procedimento pelo menos duas linhas, e a identidade do 13.º indicador principal está por confirmar.** A Emenda 16 di-lo por extenso: o painel da Comissão tem 13 indicadores principais no AMR de 2026, as 13 linhas do livro-razão cobrem 11 deles — porque o fluxo de crédito e a dívida do setor privado estão divididos em famílias e empresas —, e faltam pelo menos o **desemprego de longa duração** e o **desemprego jovem**. O livro-razão tem `desemprego-de-longa-duracao-2025`, e a sua nota não declara limiar do Procedimento; a linha do desemprego jovem não existe. A conta das 13 sai de `grep -l "Limiar do Procedimento" ledger/claims/*.yml` (13 ficheiros, de 132) | **`ResearchHub/indicators/`** | aberto, e é **pedido ao motor**: a escolha dos indicadores, o limiar de cada um e a conferência contra a lista da Comissão fazem-se onde elas são registadas (`convergence.md`), e não neste ramo. Este ramo não escreveu um único número por causa disto: a manchete diz o que o sítio pode provar — quantos limiares o país ultrapassa e quantos cumpre, das linhas que o sítio tem — e **não** afirma um total do Procedimento. **Verificação do lugar de direção, 22.08.2026:** a página do painel da Comissão (`economy-finance.ec.europa.eu/…/scoreboard_en`), lida no HTML, enumera **13** indicadores principais, com a dívida e o fluxo de crédito do setor privado já divididos em famílias e SNF, e **sem** o desemprego de longa duração nem o desemprego jovem; as 13 linhas do livro-razão correspondem uma a uma, com os mesmos limiares (a lista está em `PEDIDOS-AO-MOTOR-2026-08-22.md` C1). O painel já é **13 de 13**; a frase da Emenda 16 e este pedido assentam numa premissa que a página não sustenta. Suspenso à palavra do diretor; nada no ar afirma um total. **FECHADA pelo diretor a 22.08.2026, por recomendação do lugar de direção:** a Emenda 16 ganhou a nota de correção datada (cofre e `direcao.md`), o painel é 13 de 13, o pedido C1 ao motor foi retirado; nenhuma linha entra por causa disto |
| I33 | etapa 2l | **A contagem da CAOP por parcelas e a citação transcrita saíram da primeira página e `/municipios` ainda não as tem.** A Emenda 17 manda a contagem por parcelas (278 · 19 · 11) para `/municipios`; a Emenda 15 manda a camada «Método, ressalvas e proveniência» sair da página do leitor, e com ela a citação `data-verbatim="caop-fonte"` que vivia por baixo do mapa. A citação continua em `/municipios` (`MunicipiosView.astro:127`), mas as **três linhas por parcela não** — `grep -n "municipios-continente-caop" src/views/MunicipiosView.astro` não dá nada, com controlo positivo no mesmo ficheiro. Com elas saíram as regras `.compo*` de `site.css`, que nenhuma página construída usava depois desta ronda (`grep -rl 'class="compo' dist/` → 0) | `src/views/MunicipiosView.astro` | **fechado no commit 3-0 (21.08.2026)**: a contagem por parcelas entra em `MunicipiosView.astro` como uma linha por baixo da cabeça da lista, quatro pares rótulo + `<Claim … chip>` («Continente 278 ■ fonte · Açores 19 ■ fonte · Madeira 11 ■ fonte · Total 308 ■ fonte»), com as suas regras numa folha nova, `src/styles/municipio.css`, importada pelas vistas dos concelhos e por mais lado nenhum. Relocação **R9**, escrita antes do movimento. O rótulo «Contagem verificada nos ficheiros» não viaja: é autorreferência (Emenda 15). Medido: 1 rendição por parcela por edição |
| I34 | etapa 2l | **A porta do CSV dos concelhos não pode sair da primeira página enquanto `scripts/check-dados.mjs` for o que é.** A Emenda 15 tira a camada de aparelho do mapa e a sua porta do CSV ia com ela; `check-dados.mjs` exige, com mensagem própria, que **as duas edições da primeira página liguem os dois ficheiros** («Os dados por trás de cada instrumento são descarregáveis nas duas edições, ou não são»). A porta fica na primeira página, como ligação e sem a frase que a explicava, e o mesmo vale para a do CSV da convergência | `scripts/check-dados.mjs`, `src/components/inicio/MapaRespira.astro`, `src/components/InstrumentoConvergencia.astro` | aberto na 2l, **fechado na etapa 2m**: a conferência mudou de objecto no commit 3-0 (`PORTA_DOS_DADOS`, rota declarada por ficheiro) e a porta saiu de `MapaRespira.astro` no commit do mapa. `astro build` e `check:dados` verdes, sem uma linha de conferência alterada; a régua da invariância imprime a linha a sair de `/` e de `/en/`. Relocação R10 fechada |
| I35 | etapa 2l | **A frase que explica a conta de uma medida calculada continua na página de concelho.** «Calculado sobre duas colunas do mesmo ficheiro do regulador. A aritmética está na linha.» é a `nota` de `evora-execucao-da-receita-2025` em `src/data/municipios.mjs`. A Emenda 15 nomeia esta classe («qualquer legenda "calculado" que explique em vez de nomear; o rótulo do selo já leva "calculado ·"»), e a primeira página deixou de a render — a condição lê-se da linha, com `eDerivada()`, e não de uma lista escrita à mão. Em `/municipios/evora` ela continua | `src/data/municipios.mjs`, `src/views/MunicipioView.astro` | **fechado no commit 3-0 (21.08.2026)**, e a entrada nomeava a linha errada: a cadeia «Calculado sobre duas colunas do mesmo ficheiro do regulador. A aritmética está na linha.» é a `nota` de **`evora-indice-de-divida-2024`** e não de `evora-execucao-da-receita-2025` (`sed -n '130,145p' src/data/municipios.mjs`), que é também o que a decisão 5 da direção diz («a sentence explaining that the índice de dívida is computed from two DGAL columns»). Fechada como a primeira página a fechou, e não retirando a cadeia do ficheiro de dados: `MunicipioView.astro` deixa de render a `nota` quando `eDerivada()` é verdadeira sobre a linha daquela medida. Das oito medidas do relance de Évora **uma** é derivada, e por isso a página passou de 8 para 7 `.figura-frase`; medido nas duas edições, com controlo positivo (a nota da DGAL, que não é derivada, continua lá) |
| I36 | etapa 2l | **A primeira página deixou de render `painel_total` e `painel_com_limiar`, e o portão continua a recontá-las.** A manchete da Emenda 16 leva `painel_fora_do_limiar` e `painel_dentro_do_limiar`; o rótulo do âmbito, que levava `painel_total`, deixou de contar porque com dois painéis na página um número ao lado de «painel europeu» diria o do primeiro como se fosse o dos dois. `painel_social_total` entrou e também não se rende. Não é um defeito — `painel_com_limiar` já era assim antes desta ronda, e o portão reconta as quatro na mesma —, mas são agora **três** chaves da prova que nenhuma página mostra | `src/lib/prova.mjs` | aberto; é registo e não construção. Se a direção quiser as contagens à vista, o sítio onde elas cabem é a lista do Painel Social e o rótulo do âmbito, e isso é forma. **Passagem de higiene, 29.08.2026: continua aberta, e são quarenta e não três.** Medido na construção de hoje, lendo as chaves do próprio módulo e procurando `data-prova="<chave>"` nas 6 606 páginas construídas: `prova('pt')` declara **85** chaves, que é o número que o portão imprime; **45** rendem-se nalguma página e **40 não se rendem em nenhuma**. As três da linha continuam lá (`painel_total`, `painel_com_limiar`, `painel_social_total`), e a maior parte das outras não é dívida: **29** são as `mapa_concelhos_<unidade>`, que a Emenda 20 criou para o portão recontar a lista da Carta e que nenhuma página tinha de mostrar; as restantes são `regioes_total`, `concelhos_linhas_completas`, `endereco_correcoes` e as cinco `registos_*`. O que a linha regista mantém-se: o portão exige saber CONTAR cada chave, não que alguma página a renda (§1.66 A3, §1.70) |
| I37 | etapa 3, commit 3-0 | **Um byte NUL literal em `src/lib/ledger.mjs` faz o `grep` tratar o ficheiro como binário** (`DECISIONS.md` §4, item «20.08 Y»). Medido, e não pelo nome: `grep -n "eDerivada" src/lib/ledger.mjs` não imprime nada e `grep -an "eDerivada"` imprime duas linhas (374 e 408); uma varredura de bytes de controlo dá **um** byte, o `\x00` no deslocamento 79478, linha 1737, `const chave = [v.date, v.path, v.by, v.result].join('\u0000');`, onde o NUL está escrito como carácter e não como escape | `src/lib/ledger.mjs` | aberto, e **não corrigido de propósito**: o plano §8 e o brief da etapa 3 §2 autorizam a correção de uma linha **só se a etapa editar aquele ficheiro por outra razão**, e o commit 3-0 não editou. A correção é trocar o carácter por `'\u0000'`. Fica para a etapa que abrir o ficheiro. **Passagem de higiene, 29.08.2026: continua aberta, e a condição que a linha pôs cumpriu-se sem a correção ser feita.** O ficheiro foi aberto por outra razão desde então: o bloco dos vazios escreveu-lhe a lista fechada de marcas publicáveis (DECISIONS §1.77). O byte ficou. Medido, e não pelo nome: um varrimento de bytes dá **um** NUL, hoje no deslocamento **85 259** e não nos 79 478 de então, porque o ficheiro cresceu; a junção da chave continua a levar o carácter escrito e não o escape; e `grep -c "eDerivada"` continua a sair a 1 sem imprimir nada, contra `grep -ac "const chave"` a dar 1, que é o controlo de que o ficheiro se lê com `-a` e é binário sem ele. Fica para a etapa seguinte que o abrir, e desta vez com a nota de que a regra já falhou uma vez. **FECHADA a 29.08.2026, no commit `fbe2375` do ramo `pequenas-6-2026-08-29`.** A condição que a linha punha cumpriu-se: a passagem do rótulo da fonte abriu o ficheiro por outra razão, e o byte saiu com ela. O carácter foi trocado pela sua sequência de escape, que é a mesma cadeia para o motor de JavaScript, e o resto do ficheiro ficou igual byte a byte: desfeita só a troca, os 88 754 bytes de antes voltam exactos (sha256 `099f3e90…`, contra `27349eb6…` depois dos 88 759). Medido outra vez antes de tocar: um NUL, no deslocamento 85 259, linha 1861, e nenhum outro byte de controlo. Depois: `grep -c "eDerivada"` passa de 1 sem imprimir nada (o ficheiro era binário) a **2** com as duas linhas. E a régua fica: `scripts/check-ledger.mjs` varre os bytes de `src/lib/ledger.mjs` e dos dois textos governados que já lia, e fecha a construção com o deslocamento e a linha de qualquer byte abaixo de 0x20 que não seja tabulação, mudança de linha ou retorno, mais o 0x7F. Três plantas correm antes da contagem, e o byte foi reposto no ficheiro real para se ver o vermelho: saída 1 com o deslocamento certo, e 0 reposta a correção |
| I38 | etapa 3, commit 3-0 | **A soma dos concelhos rende duas vezes em `/municipios`.** A frase da contagem publica «São 308 ■ fonte concelhos…» e a linha das parcelas, por baixo, fecha com «Total 308 ■ fonte». As duas são a mesma linha do livro-razão, `municipios-portugal-caop-2025`, a três linhas de distância. A linha das parcelas é o texto exacto da decisão 4 da direção («Continente 278 · Açores 19 · Madeira 11 · 308») e por isso fica como está | `src/views/MunicipiosView.astro` | **fechado na subetapa 3c (21.08.2026)**: a frase da contagem foi recomposta com as duas chaves da prova («1 de 308 concelhos · tem página»), e o `<Claim>` da soma saiu dela. A soma rende **uma vez** na página, na linha das parcelas, com o seu selo; medido em Chromium: `document.querySelectorAll('[data-claim="municipios-portugal-caop-2025"]').length` → **1** |
| I39 | etapa 3, subetapa 3a | **A marca de água do recibo transbordava a página, e a caixa que a corta não a apanhava.** A primeira versão rodava a CAIXA da marca (`inset: 0`, do tamanho do bloco): os cantos de uma caixa rodada saem para fora dela, e o `overflow: hidden` da própria caixa não os apanha, porque a transformação se aplica depois do corte. Medido na linha com marcador: **23px** de transbordo a 320, **22px** a 390 e **2px** a 768, nas duas edições, contra **0** na construção de referência `df76a5f` — regressão desta subetapa, apanhada pela célula do transbordo | `src/styles/linha.css` | **fechado na mesma subetapa**: a caixa fica quieta e corta, e o que roda é o texto, num `::before` cujo conteúdo vem de `data-marca` (a cadeia `POR_VERIFICAR`, escrita pela vista e não pela folha). Medido depois: **30 de 30** combinações (3 linhas × 2 edições × 5 larguras) a zero |
| I40 | etapa 3, subetapa 3b | **A linha-espécime do índice transbordava a 320.** Os dois campos novos (fonte e data de leitura) vão num `inline-flex`, e um `inline-flex` cujo filho não parta tem a largura da cadeia mais longa: «Secretaria-Geral do Ministério da Administração Interna» dava **13px** de transbordo a 320 na edição portuguesa e **21px** na inglesa, contra **0** na construção de referência `df76a5f`. Regressão desta subetapa, apanhada pela célula do transbordo do índice | `src/styles/linha.css` | **fechado na mesma subetapa**: o grupo passa a `flex-wrap: wrap` e o valor a `overflow-wrap: anywhere` com `min-width: 0`. O rótulo e o valor continuam no mesmo grupo; o que muda é que o grupo pode partir em duas linhas. Medido depois: **10 de 10** combinações (2 edições × 5 larguras) a zero |
| I41 | etapa 3, subetapa 3b | **A medida 8 de `medir-defeitos.mjs` diz que exclui blocos com `data-prova` e o código não o faz.** O comentário da medida lista `data-prova` entre as origens declaradas; a constante `ORIGEM_DECLARADA`, que é partilhada com a medida 3, **não o inclui**. Na primeira página não mordeu, porque os blocos com `data-prova` têm texto fora das âncoras e foram classificados como conteúdo; no índice do livro-razão a linha das contagens («132 afirmações · 19 calculadas») é contada pela mesma razão | `scripts/medir-defeitos.mjs` | aberto, e **não corrigido de propósito**: acrescentar `[data-prova]` a `ORIGEM_DECLARADA` mudaria também a medida 3 («frases de moldura»), que é uma linha de base comparada entre construções desde a etapa 0. A escolha é da cadeira: ou o comentário passa a dizer o que o código faz, ou a constante separa-se em duas |
| I42 | etapa 3, subetapa 3c | **O brief da etapa 3 manda ligar `/?ambito=municipio` e o esquema de estado da etapa 2 não aceita esse valor.** `resolveAmbito()` em `public/js/inicio.js` só reconhece `municipio:<slug>` da lista fechada da CAOP; `municipio` sozinho cai, em silêncio e por desenho, no âmbito País. A vista de escolha abre-se por um comando (`data-modo="municipio"`), que **não muda o âmbito** e por isso não tem endereço; no telemóvel, o destino sem script desse comando já é `/municipios` | `public/js/inicio.js`, `src/lib/inicio.mjs`, `src/views/MunicipiosView.astro` | aberto na 3c, **fechado na etapa 2m**: `ambito=municipio` passa a ser um valor do esquema — a vista de escolha —, em `resolveAmbito()`, `modoDe()`, `chaveDoBloco()` e na condição de `data-so-pais` (que passou a `eDoPais()`, escrito uma vez). O endereço normaliza para `/?ambito=municipio`, a recarga repõe-no, a ligação de idioma leva-o, e o valor inválido continua a cair no defeito (`municipios`, `municipio:` e `municipio:evora;alert(1)` medidos na matriz). **Fica um pedido**: `MunicipiosView.astro` é da etapa 3 e a sua porta continua a ser `/#mapa`, que é verdadeira mas menos do que podia ser; e a linha do esquema da `PLANO-redesenho-v3.md` §13 é da cadeira — a frase exacta está na nota da 2m |
| I43 | etapa 3, subetapa 3c | **A régua das frases da casa passou a excluir `[data-cobertura]`, e a contagem da primeira página mexeu com isso.** O índice dos concelhos entrava com **307 blocos** do feitio «Abrantes sem página ainda»; a exclusão é a certa (é vocabulário fechado, que a medida 7 já conta, e a Emenda 15 chama-o conteúdo por definição), mas move também a primeira página: `conteúdo` desce de **41 para 33** e as distintas de 33 para 32, porque as oito peças vazias e a linha de cobertura deixaram de contar. **A autorreferência continua em 0**, que é o alvo | `scripts/medir-defeitos.mjs` | aberto como registo e não como defeito: a medição é a mesma régua, e o «antes» comparável é o desta subetapa em diante. A exclusão fica **fora** de `ORIGEM_DECLARADA` de propósito, para não mexer na medida 3, que é linha de base desde a etapa 0 |
| I44 | etapa 3, subetapa 3d | **A conta que decide o estado e a régua da peça de Évora está escrita duas vezes.** `HomeView.astro` e `MunicipioView.astro` fazem, cada um, o mesmo mapeamento dos oito registos de `municipios.mjs` para as propriedades de `Peca.astro`: ler a linha, `eDerivada()` para a nota, e — só para `evora-indice-de-divida-2024` — `estadoDaRegua()` contra `indice-de-divida-limite-legal` com `colore: true`. As duas cópias podem divergir no dia em que uma mudar | `src/views/HomeView.astro`, `src/views/MunicipioView.astro` | aberto na 3d, **fechado na etapa 2m**: `pecasDoConcelho()` em `src/lib/inicio.mjs`, chamada por `HomeView.astro` e por `MunicipioView.astro`. A conta generalizou-se ao passar: a cópia da primeira página comparava o `claim` com a cadeia `'evora-indice-de-divida-2024'` escrita à mão, e a função lê a medida que o concelho declara em `distancia.indice`, com o tecto que ele declara em `distancia.tecto` |
| I45 | etapa 3, subetapa 3d | **O cartão localizador não entra na página do concelho, porque `MapaRespira.astro` não tem por onde o receber.** O componente fixa `data-postura="inteiro"` no gabarito (é `public/js/inicio.js` que a troca) e o anel do concelho escolhido é uma classe que o mesmo script aplica. Rendido como está, o mapa desta página seria 308 pontos iguais, na postura errada e **sem anel**: um localizador que não localiza | `src/components/inicio/MapaRespira.astro` | aberto na 3d, **fechado na etapa 2m**: `MapaRespira.astro` ganha `postura` (defeito `'inteiro'`) e `escolhido` (o slug da Carta, que põe a classe do anel no servidor), e `MunicipioView.astro` chama-o uma vez no aparelho. Medido em Chromium: postura `localizador`, 1 anel em 308, cartão 300×254 a 1280 e 354×137 a 390, transbordo 0 nas duas. Três coisas saíram com a chamada, e vão ditas: a legenda dos 308 e a leitura em voz alta passaram a construir-se só na postura inteira (fora dela eram bytes escondidos, e a legenda seria uma segunda rendição da mesma linha na mesma página); e as regras de colocação em grelha do telemóvel ficaram presas a `[data-inicio]`, porque fora da grelha da primeira página empurravam o cartão para debaixo do título do aparelho. Relocação R12 |
| I46 | etapa 3, subetapa 3d | **A legenda do desenho da distância deixou de ser verdadeira com a Emenda 4, e foi reescrita.** Dizia «A barra é a dívida total que o regulador publica para o concelho; o fio é o limite legal do mesmo ano.», o que era verdade enquanto a barra enchia do zero até ao valor. A Emenda 4 fixa uma só gramática (a barra é a **distância** à referência), e com o desenho novo a frase passava a descrever uma coisa que a página não desenha | `src/i18n/strings.mjs` (`municipio.distanciaLegenda`) | **reescrita na 3d, e assinalada em vez de decidida**: uma legenda falsa não se publica, e manter o desenho antigo era manter a gramática que a emenda substitui. Mudaram as duas primeiras orações; a terceira, que explica o índice, é a que já estava, palavra por palavra. A redação final é do lugar de direção |
| I47 | etapa 2m | **A lede baixa a primeira letra de um nome declarado, e há um nome que isso estragaria.** A lede do painel é construída dos nomes de `figuras.mjs` e a única transformação que a construção lhes faz é baixar a inicial, para os pôr no meio da frase («Dívida pública» → «dívida pública»). Nenhum dos treze nomes do Procedimento começa por nome próprio, nas duas línguas, e por isso a regra é hoje correcta; uma medida futura que comece por «Eurostat», «OCDE» ou um topónimo sairia com a inicial minúscula e ninguém a apanharia | `src/lib/inicio.mjs` (`nomeEmFrase`) | aberto, **e escrito antes de acontecer**: a saída, se acontecer, é um campo declarado por entrada (como `lado` e `simbolo` já são) e não uma regra que adivinhe. Não o escrevi hoje porque um campo que nenhuma entrada usa é código morto a dizer que ainda serve |
| I48 | etapa 2m | **A lede desaparece se nenhuma medida estiver fora do limiar, e a forma desse estado não está decidida.** `ledeDoPainel()` devolve `null` quando a lista é vazia, e a cabeça não rende lede nenhuma. A alternativa — «Fora do limiar: .» — não é uma frase, e escrever uma («Nenhuma medida fora do limiar.») seria copy nova, que é do lugar de direção. O portão também não diz nada nesse dia: sem `data-prova-lista` na página não há lista para comparar | `src/lib/inicio.mjs`, `src/components/inicio/Cabeca.astro` | aberto, **e é uma decisão de direção e não um defeito**: hoje o país ultrapassa quatro limiares e o estado é hipotético. Se a direção escrever a frase do estado vazio, ela entra em `strings.mjs` com a sua EN e a condição passa a escolher entre duas cadeias validadas |
| I49 | etapa 2m | **Dez frases da primeira página nomeiam um estado em prosa e não se reconferem.** O brief §2 manda procurá-las no inventário; procuradas, e a lista sai por extenso na nota. Duas são de `figuras.mjs` e afirmam um estado contra um limiar publicado, que o portão já calcula ao lado delas («Está acima do limiar do painel europeu, e a descer.», da dívida pública; «O limiar foi ultrapassado em 2024 e o excesso quase duplicou no ano seguinte.», dos preços da habitação). Duas são das frases do Painel Social e afirmam uma posição face a uma MÉDIA que o livro-razão não guarda como linha, e por isso nenhuma conta as pode reconferir. Seis são as frases das cinco regiões e de Portugal, em `regioes.mjs` (cada uma rendida duas vezes: a manchete do seu âmbito e a leitura breve do Instrumento n.º 1), e o mesmo se aplica | `src/data/figuras.mjs`, `src/data/regioes.mjs` | aberto, **e não improvisado**. Reescrevê-las é redação, que é do lugar de direção, e a relocação R1 manda a frase inteira viajar sem mudar uma palavra. Para as duas do limiar há uma saída mecânica que não toca em copy nenhuma — um campo declarado por entrada («esta frase afirma o estado X») e uma conferência no portão que o compare com `estadoDaMedida()` —, e **não a escrevi porque o brief autoriza uma extensão do portão e não duas**. Para as cinco da média não há saída sem uma linha de referência no livro-razão. **Passagem de higiene, 29.08.2026: continua aberta, e na primeira página são hoje duas e não dez.** As duas de `figuras.mjs` continuam a render-se em `/`, uma vez cada, medidas por cadeia exacta no texto da página construída. As seis das regiões deixaram de se render ali: a primeira página tem **0** blocos `[data-regiao]` desde a Emenda 21 (§1.75), e as frases foram com elas para `/regioes` e para as páginas de cada região, onde `regioes.mjs` declara hoje **10** frases e a régua desenha **10** leituras. A saída mecânica que a linha propõe para as duas do limiar continua por escrever, e vale agora para as dez das regiões pela mesma razão; as do Painel Social continuam sem linha de referência que as reconfira |
| I50 | etapa 2m | **O cartão localizador publica «308 ■ fonte» sem substantivo.** A Emenda 15 tirou do cartão a linha de cobertura, e o que ficou foi o `<Claim chip>` da contagem da Carta, nu. Na primeira página já era assim desde a 2l; com o cartão a entrar na página do concelho (I45), a mesma forma passa a ver-se em duas superfícies | `src/components/inicio/MapaRespira.astro` | aberto como observação e não como defeito: o portão está satisfeito (o valor tem selo) e a emenda proíbe a legenda que lá estava, não obriga a nenhuma. Se a direção quiser um substantivo, a cadeia `inicio.mapa.linha` (« concelhos · CAOP ») já existe e serve |
| I51 | etapa 2m | **A fila de escolha de REGIÃO continua sem endereço.** `?ambito=municipio` passou a ser um estado do esquema (I42); o comando «Região» continua a abrir a sua fila de pastilhas sem mudar o âmbito, e por isso essa vista não se partilha nem se recarrega | `public/js/inicio.js`, `PLANO-redesenho-v3.md` §13 | aberto de propósito: o brief da 2m pede `municipio` e só esse, porque é o que `/municipios` precisa de ligar. A simetria custa uma linha em `resolveAmbito()` e outra em `eDoPais()`, e é decisão da cadeira. **FECHADO por remoção a 26.08.2026** (Emenda 19, commit desta ficha): a fila de pastilhas das cinco regiões saiu da primeira página com o comando «Região» no item A2 de 25.08, e o que restava do defeito era uma vista sem endereço que já não existe. O ESTADO ficou e continua a resolver: `?ambito=regiao:<slug>` é endereço partilhável (Emenda 7), rende o bloco de cabeça e o painel daquela região, e a matriz mede-o (célula «2i·1», e `?ambito=regiao:portugal` a cair no defeito). A posição volta ao comando com a página das regiões, e nesse dia a pergunta é outra: não «esta vista não tem endereço», mas «que endereço tem a página das regiões». **A resposta, a 27.08.2026** (Emenda 21, DECISIONS §1.75): `/regioes` e `/en/regions` para o índice com a régua completa, `/regioes/<slug>` e `/en/regions/<slug>` para cada região com linhas. O comando «Região» volta ao âmbito nas duas larguras como LIGAÇÃO para o índice, e não como estado; os estados `?ambito=regiao:<slug>` saem do esquema, e um endereço antigo reencaminha para a página da região, ou para o índice quando a região não existe |
| I52 | etapa 4, commit 4-0 | **A página de Évora entra no inventário com três blocos de autorreferência por edição, e a decisão de 21.08 não os nomeia.** A decisão da direção mandou sair a abertura, as contagens por extenso do Relance e os dois parágrafos de «Quem responde pelo quê», e disse que a rota passa a ler **0**. Saíram esses, mais a sub-linha da Leitura breve, a nota dos trabalhos e a segunda frase da descrição do `<head>`, que são a mesma classe e o mesmo gesto. Sobram três, medidos: «Nenhuma decisão deste mandato atravessou para o livro-razão com valor próprio. Um campo em branco seria diferente disto: o que falta é a linha, não a decisão.» (nota de mandato); «As decisões desta página vão atribuídas a quem as tomou… nada do que foi lido fornece o contrafactual que recortaria a parte de um executivo neles.» (entrada de `metodo`, «Um partido é dono das suas decisões, não de uma curva»); «Não existe contrafactual para nenhum índice. Nada do que foi lido permite separar a parte de um executivo neles.» (entrada de `naoSabe`) | `src/data/municipios.mjs` | **FECHADO** pela direção a 21.08.2026, tarde, e feito no commit a seguir à 4b. A nota de mandato passa às duas palavras da casa, «sem linha ainda» / «no row yet» (o parágrafo sai); a entrada de `metodo` sai inteira, por ser a nota de como a página foi feita; a entrada de `naoSabe` **fica**, e é reclassificada como conteúdo no inventário com a razão **«limite dos dados»** — não fala do cuidado da casa, diz o que as fontes lidas não permitem estabelecer. As duas rotas passam a ler **0**. A regra que decidiu isto está escrita uma vez, na `direcao.md` por baixo da Emenda 15 e na cabeça do `INVENTARIO-FRASES.md` |
| I53 | etapa 4, subetapa 4d | **A edição inglesa não tem página de erro.** `src/pages/` tem `404.astro` e mais nada: um leitor da edição inglesa que caia num endereço inexistente recebe a página portuguesa, com o título «Não existe nada neste endereço.» e as três portas em português. A troca de edição no cabeçalho dessa página aponta para `/en/404`, que não existe e devolve o mesmo ficheiro. Encontrado a fotografar a rota na 4d, e não é um defeito de tipo nem de tokens: é uma rota que falta | `src/pages/404.astro` | aberto, e revisto na etapa 5 sem mudar. Fazer a página inglesa é uma rota nova e uma decisão de conteúdo (o texto existe, `erro404.*` já tem as duas edições em `strings.mjs`). E é mais do que um ficheiro: a Vercel serve UM `404.html` para tudo o que não existe, por isso um 404 por edição é uma regra de encaminhamento em `vercel.json` (fora do âmbito de todas as etapas até aqui) mais a rota declarada em `routes.mjs`. Fica pedido à cadeira. **FECHADA a 22.08.2026 (DECISIONS §1.62, decisão 1), construída pelo lugar de direção:** `src/pages/en/404.astro` rende a mesma vista em inglês (`dist/en/404/index.html`, «Page not found · O Estado do País»); `vercel.json` ganha `routes`: `{ "handle": "filesystem" }` e `{ "src": "/en/(.*)", "status": 404, "dest": "/en/404/index.html" }`. Duas consequências apanhadas pelos portões e corrigidas na raiz: o filtro do sitemap passa a excluir qualquer `…/404` (o `@astrojs/sitemap` só exclui o da raiz; o `/en/404` entrou uma vez e saiu), e `cartoes.mjs` infere a edição do prefixo para as páginas fora da tabela de rotas (o `/en/404` caía no cartão português; o cartão inglês da primeira página cobre agora 22 rotas). A prova da regra é na pré-visualização n.º 3, com `curl`: `/en/nope` 404 com corpo inglês, `/nope` 404 português, `/en/ledger` 200, os 308 dos anfitriões e os seis cabeçalhos. **REABERTA no mesmo dia, pela prova na pré-visualização n.º 3:** com o bloco `routes` presente, a Vercel deixou de aplicar o bloco `headers` (lido no Chrome do diretor depois da entrada: `/en/nope` 404 inglês, `/nope` 404 português, `/en/ledger` 200, mas `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy` ausentes e só o HSTS da própria Vercel presente; controlo: a pré-visualização anterior de `redesenho-v3`, sem `routes`, traz os cinco), e pelo mesmo mecanismo os três `redirects` de anfitrião ficam em risco, que uma pré-visualização não testa. O bloco `routes` saiu (`vercel.json` igual ao de `main`); ficam a página `/en/404`, alcançável pela troca de edição da página de erro portuguesa, o filtro do sitemap e a edição inferida nos cartões. Três formas para a direção: deixar assim (recomendação), converter o `vercel.json` inteiro a `routes` (cabeçalhos prováveis na pré-visualização, redireccionamentos só em produção), ou `rewrites` com 200. **SEGUNDA FORMA, escolhida pela direção a 22.08 («go») e construída no mesmo dia:** o `vercel.json` passa a `$schema` e `routes`, e mais nada. Um bloco ignorado em silêncio é pior do que um bloco que não existe, por isso nenhum valor fica onde a Vercel deixou de olhar: os cinco cabeçalhos, o `noindex` do alias e os três 308 de anfitrião são agora rotas, as duas primeiras com `continue: true`, e todos os valores foram carregados do ficheiro anterior por comparação automática e não por transcrição (cinco cabeçalhos, um anfitrião de alias, três anfitriões de redireccionamento, `$schema`: todos iguais). Duas rotas de **sonda**, presas ao anfitrião da pré-visualização deste ramo, provam na pré-visualização o que só produção exercitaria (`has` de anfitrião selecciona; `status` mais `Location` redirecciona; um cabeçalho condicionado com `continue` é aplicado) e saem num commit próprio antes da fusão. `scripts/verify-deploy.mjs` deixa de perguntar só pelo commit no ar: passa a exigir os cinco cabeçalhos com o valor exacto em duas famílias de página, o `noindex` presente no alias e ausente no canónico, os três 308 com o `Location` exacto, a página de erro da edição certa em cada endereço inexistente, e as duas páginas de erro no sítio. Nenhuma pergunta segue um redireccionamento. Corrida contra produção a 22.08: **25 conferências passam, 3 falham**, e as três são o 404 inglês (`/en/nao-existe` em `pt-PT` e não em `en`; `/en/404` a 404 e não a 200), que é exactamente o que este ramo vai corrigir. Corrida contra a pré-visualização protegida, como negativo conhecido: 21 falham nos 302 da entrada da Vercel. **Falta a prova na pré-visualização, ao lugar de direção: lido no Chrome do diretor a 22.08, pré-visualização de `c5515d2`: os cinco cabeçalhos da casa presentes em `/`, `/en/ledger`, uma página de linha, um documento alojado, `/municipios/evora` e nas duas páginas de erro (`X-Frame-Options: SAMEORIGIN`, o valor da casa); `X-Sonda: host` presente (o cabeçalho condicionado pelo anfitrião, com `continue`, aplica-se); `/sonda-308` redirecciona para `/` (`has` de anfitrião + `status` + `Location` funcionam); `/en/nope` e `/en/nope/deeper` 404 `en`; `/nope` 404 `pt-PT`; `/404` 404; `/en/404` 200; `version.json` `c5515d2`. As duas sondas saíram no commit seguinte e a pré-visualização foi relida sem elas**. **FECHADA na segunda forma a 22.08.2026** (`c5515d2` + `a3b9d1d`): `vercel.json` inteiro em `routes`, provado na pré-visualização com e sem as sondas; em produção, `verify:deploy` afirma os cinco cabeçalhos, o `noindex` do alias, os três 308 e as duas páginas de erro depois de cada deploy |
| I54 | etapa 4, subetapa 4e | **A casa passou a ter dois sinais para a mesma dobra.** `.deep` (a camada de fundo da página de concelho e da página de um trabalho) desenha um triângulo de tinta que roda ao abrir, e é anterior a esta etapa; as dobras que a 4b e a 4c criaram no Método e na Agenda desenham `+` fechado e `−` aberto. São dois glifos para o mesmo gesto, e a Emenda 10 fecha exactamente essa porta («um glifo, um significado») | `src/styles/site.css` (`.deep`), `src/styles/leitura.css` (`.regra-dobra`, `.agenda-dobra`), `src/styles/inicio.css` (`.peca-seta`, `.conv-porta-seta`) | **FECHADO** no commit 5-0. Eram **três** sinais e não dois: além do triângulo de bordos e do `+`/`−`, a peça da primeira página desenhava `▸`/`▴` e a porta da convergência `▸`/`▾`, os dois glifos do tipo. Os quatro passam a ser a declaração do `.deep`, copiada à letra (bordo esquerdo de 5px, bordos de 4px transparentes, rotação de 90° ao abrir). Só folha. Medido com Chromium sobre `dist/`, estilo computado de cada pseudo-elemento: as **46** dobras das quatro rotas do brief dão a mesma forma. Fica uma variação de cor de repouso (tinta na mobília e na peça, `--g1` no Método e na Agenda), que é decisão de ênfase da 4b e não um segundo glifo; se a direção a quiser igual, é uma ficha (nota da etapa 5, §1.2) |
| I55 | etapa 5, commit 5-0 | **A régua da ortografia não vê uma palavra dentro de um nome com hífenes.** O brief da etapa 5 mandava renomear a classe `mun-tecto-rot` «para que `ortografia.mjs` saia 0»; medido, a régua nunca a assinalou. Os valores de atributo SÃO varridos (`regioesDeAtributos()`, chamada em `scripts/ortografia.mjs:540`), mas o comparador não reconhece `tecto` dentro de `mun-tecto-rot`: `procura()` sobre `"mun-tecto-rot"` dá 0 achados e sobre `"tecto"` dá 1. O que fechava a régua era outra coisa (a comparação de cadeias em `Peca.astro:216`), e essa foi feita | `scripts/ortografia.mjs`, `src/views/MunicipioView.astro:372`, `src/styles/site.css:2717` | aberto como observação. Não é ortografia publicada: é um nome de classe, que o leitor não lê. Renomeá-lo é uma vista (lista do «nunca» deste brief) e a folha do construtor A. Se a cadeira quiser que a régua veja nomes com hífenes, é uma mudança ao comparador com um custo conhecido: passaria a assinalar identificadores de código em todo o lado |
| I56 | etapa 5, commit 5-0 | **A amarra da constituição não lê o `FECHO` do Método, e por isso as duas frases governadas mais recentes não podem ser citadas.** `cadeiasDoTexto()` (`scripts/check-ledger.mjs:437`) lê `[REGRAS, ABERTURA, LEITURA_BREVE]` de `src/data/metodo.mjs` e não lê `FECHO`, que é onde vivem «A cor» e «A letra», as duas frases que a direção aprovou a 21.08. Ao escrever a citação que o brief da etapa 5 pede, o `ledger:check` recusou-a («essa frase não existe em src/data/metodo.mjs») com a frase a estar lá — `grep -c` dá 1 | `scripts/check-ledger.mjs`, `IDENTIDADE.md` | **FECHADO** pelo construtor F, no commit desta ficha. `cadeiasDoTexto()` passa a andar `[REGRAS, ABERTURA, LEITURA_BREVE, FECHO]`: o `FECHO` entra inteiro e nas duas línguas pelo mesmo `anda()` recursivo que já percorria os outros três, e por isso quem acrescentar uma entrada à entrada de fecho não tem de voltar ao ficheiro. É a **mesma** conferência a alcançar mais texto, e não um portão novo (moratória de 2026-08-15): nenhuma marca nova, nenhuma mensagem nova. Com ela a ler o fecho, a citação que a etapa 5 tinha revertido entrou na `IDENTIDADE.md` §2, entre «…» e com a marca (`metodo`), **carácter a carácter** como está no módulo (244 caracteres, comparação achatada contra `FECHO.entradas[a-cor].texto.pt[0]` → `idêntica: true`). Duas plantas fecharam a amarra e foram revertidas, as duas com a mensagem que já existia («e essa frase não existe em src/data/metodo.mjs»): uma palavra trocada dentro da citação («âmbar» → «ocre») e uma frase que não existe no Método. As citações conferidas passam de 1 para 2, de 43 entre «…». Comandos e saídas na nota da etapa 5, §6 · I56 |
| I57 | etapa 5 | **Há uma segunda cópia dos tipos no repositório.** `tipos-cartao/` tem cinco TTF (1,2 MB) derivados dos mesmos bytes que `public/tipos/` serve. Duas medições obrigam a isso: o `fontdb` do rasterizador recusa WOFF2 («malformed font»), e o Bitter servido é variável com defeito no Thin, eixo que o rasterizador não move | `tipos-cartao/`, `scripts/cartoes.mjs` | aberto como dívida declarada, não como defeito. Custa 1,2 MB ao repositório e zero ao leitor (está fora de `public/`, e não é servido). Os resumos, os comandos de derivação e a conferência das seis propriedades estão na nota da etapa 5, §3. Desaparece no dia em que o rasterizador leia WOFF2 e mova eixos, ou no dia em que a casa compre a Sebenta e a Parnaso, que são estáticas |
| I58 | etapa 5 | **O portão dos cartões lê o registo, não os píxeis.** Não há OCR: se alguém desenhar no PNG uma cadeia diferente da que regista, a conferência não vê. As quatro plantas do brief foram todas apanhadas porque todas passam pelo modelo, que é a origem única do desenho E do registo | `scripts/gate-html.mjs`, `scripts/cartoes.mjs` | aberto e escrito, não fechado. O que reduz o risco a um gesto deliberado é a arquitectura: o rasterizador não tem uma única cadeia legível pelo leitor, e a cópia registada sai do mesmo `modelo` que a função de desenho escreve. Fechar isto a sério pede leitura de píxeis, que é outra dependência e outra conversa |
| I59 | etapa 5 | **Os 532 PNG pesam 19,1 MB** (41 KB de média), em RGBA sem quantização, para um desenho de seis cores. `dist/cartoes` mede 22 MB com os registos, de um `dist/` de 37 MB | `scripts/cartoes.mjs`, `scripts/png-paleta.mjs` | **FECHADO na ronda pós-fusão (22.08.2026, construtor A3), e sem dependência nenhuma**: a paleta indexada não precisava de uma biblioteca, precisava de trinta linhas de `node:zlib`. O rasterizador expõe os RGBA crus (`render().pixels`, conferido em `node_modules/@resvg/resvg-js/index.d.ts` e à corrida), e `scripts/png-paleta.mjs` escreve deles um PNG de tipo de cor 3, profundidade 8, com o `PLTE` do próprio cartão, filtro 0 em todas as linhas e sem `tRNS`. Medido nesta construção: os **532 de 532** cabem em paleta, entre **57 e 118 cores** (quatro cartões acima de 100), zero píxeis translúcidos, **zero recusas**. Não é quantização e não é aproximação: os píxeis do ficheiro são os do rasterizador, e prova-se com `node scripts/provar-cartoes-paleta.mjs`, que descodifica cada PNG escrito e o compara byte a byte com o RGBA da mesma passagem (**532 de 532 iguais**). Os números: **20 064 037 → 8 874 864 bytes** (menos 55,8%; média de 37 714 para 16 682; a primeira página de 62 554 para 27 034), `dist/cartoes` de **22 800 KB para 11 896 KB**, `dist/` de **37 852 KB para 26 948 KB**, e o passo `npm run cartoes` de **5,91 s para 5,39 s** (três corridas de cada, na mesma máquina): mais pequeno E mais rápido, porque o nível 7 do deflate custa menos do que o `asPng()` que substitui. O nível está escolhido por uma tabela de seis medições escrita ao lado dele. O portão não mudou uma linha: lê o `IHDR` nos bytes 16 a 24, que são os mesmos no tipo de cor 3. **Não fecha o I58**, e a distinção está na nota: esta prova compara píxeis com píxeis do mesmo desenho, não lê o que lá está escrito. Detalhe, plantas e a tabela do deflate em `notas/pos-fusao.md` §A3 |

| I60 | etapa 6 | Um documento alojado traz a ilha `rcpt-data` duas vezes (606 KB cada, id duplicado) nos bytes originais do motor; os bytes fixados não se tocam deste lado | `studies-src/` | **fechado 24.08.2026** (`675f828`): republicado dos bytes do motor, origem `researchhub`, uma ilha; o motor tinha provado a 23.08 que o duplicado era do empacotador do artefacto (I60 na nota do motor) |
| I61 | pós-fusão, fecho de A | **O marcador do Instrumento n.º 1 (nome da região, valor, ponto) não pintava, no sítio no ar desde a fusão de 21.08.** `convergencia.js` desenha o `rect.mk-hit` (a área de toque) em último lugar dentro do grupo do marcador, e a regra de remapeio `.rule-svg .mk rect { fill: var(--paper) }` (etapa 1c, `7650aed`), escrita para a chapa por baixo do rótulo, apanha-o e ganha a `.rule-svg .mk-hit { fill: transparent }` por especificidade (0,2,1 contra 0,2,0): a área pintava-se de papel por cima do nome, do valor e do ponto. Observado pelo construtor A2 em Chromium sem cabeça («não pinta»), reproduzido pelo lugar de direção em Chrome com janela sobre a construção local e conferido no CSS servido em produção (`Base.KO2lSj4k.css` traz as duas regras por esta ordem) | `src/styles/site.css` | **fechado** no commit de fecho da etapa A: a regra `.rule-svg .mk rect.mk-hit { fill: transparent }` depois do remapeio. Prova em Chromium sem cabeça, `/` e `/en` a 1280: `fill` calculado do `.mk-hit` `rgb(246, 247, 244)` → `rgba(0, 0, 0, 0)`; píxeis escuros na caixa do valor «82» (22×22 com margem, 900 píxeis) **0 → 116** |
| I62 | pós-fusão, leitura do Codex | **Três frases da casa em `/municipios/evora` escapam à régua do inventário por construção.** «Como esta linha do tempo é feita» (um `<summary>`), «Cada valor tem linha no livro-razão. …» (um `p.deep-v` dentro da dobra do instrumento dos mandatos) e «Escolher uma em silêncio seria esconder que a diferença existe.» (um `span[data-nonledger="data-de-referencia"]`) existem na página construída nas duas edições; a rota lê 0 de autorreferência porque a medida 8 exclui blocos com origem declarada e texto de comandos. Achado da leitura cruzada de 22.08 (`critica/2026-08-22-codex-leitura-pos-fusao-A-B1.md`, alta 2) | `src/views/MunicipioView.astro`, `src/data/municipios.mjs`, `scripts/medir-defeitos.mjs` | aberto, e são duas coisas: a régua (se um `<summary>` e um `data-nonledger` devem entrar no inventário, é desenho da régua) e o corte (editorial, da direção, fase da voz). Nada mexido nesta ronda. **Passagem de higiene, 29.08.2026: o corte está feito; a régua continua por decidir.** O corte foi o item G6 da grelha da voz (DECISIONS §1.69), que tirou da página do concelho as doze ressalvas com as duas secções que as guardavam e a dobra «Como esta linha do tempo é feita». Medido no texto de `dist/municipios/evora/index.html`, contado em Node porque o `grep` desta máquina não conta acentos de forma fiável: as três frases dão **0, 0 e 0** (controlo positivo no mesmo texto: «Évora» dá 104). A metade da régua fica como estava, e a linha continua aberta por ela: um `<summary>` continua a ser deixado cair como rótulo de comando e `[data-nonledger]` continua dentro de `ORIGEM_DECLARADA` em `scripts/medir-defeitos.mjs`, e a página ainda tem **8** `<summary>` e **45** `[data-nonledger="data-de-referencia"]` que a medida 8 não vê. Se a régua deve ver estes dois feitios é desenho da régua, e é da cadeira |
| I63 | pós-fusão, leitura do Codex | **Uma conjunção portuguesa dentro de `reason_en`.** `ledger/claims/evora-prr-vencido-aprovado-2026.yml:89` traz «…listagem-de-entidades-prr-20260817.xlsx e listagem-de-projetos-prr-20260817.xlsx…», e rende em `/en/corrections` e em `/en/ledger/evora-prr-vencido-aprovado-2026`; a entrada seguinte da mesma linha (linha 102) escreve «and». A linha é escrita pelo exportador do motor (V16) e o sítio não a emenda à mão | `ledger/claims/evora-prr-vencido-aprovado-2026.yml` | **fechado 24.08.2026** (`9e2f11d` + motor `b2b4bfe`): a mesma edição de um carácter dos dois lados de uma vez, como a V17 exige; o `--write` deu 0 alteradas |
| I64 | parte 3, P1 | **Um estrago no arquivo mata o `check:documentos` com um rasto de pilha em vez da sua lista de erros.** `src/lib/documentos.mjs:130` **atira** quando encontra em `studies-src/` uma edição que `src/data/studies.mjs` não declara, e o portão chama `todosOsDocumentos()` na linha 207, antes de acumular um único erro. Medido a 24.08 ao plantar o estrago do D4 na primeira forma (retirar a edição inglesa do 04 do arquivo): exit 1, com a frase certa, mas por `Error:` e não pela linha `✗` da lista. O resultado é o certo (a construção pára, e diz o que está errado) e por isso não é bloqueante; o que se perde é a forma da casa, e o facto de os outros erros da mesma corrida ficarem por dizer | `src/lib/documentos.mjs`, `scripts/check-documentos.mjs` | aberto; é anterior a esta etapa e fora do âmbito da P1. A planta do D4 mudou para o `registos/manifest.json`, que é onde o D4 a pode ver e é a forma mais fiel do estrago. **Passagem de higiene, 29.08.2026: continua aberta, reproduzida, e as duas linhas que a linha cita mudaram de número.** Plantado o estrago na forma que a linha descreve, um `studies-src/alentejo-algarve/pt.html` numa edição que `src/data/studies.mjs` não declara (`alentejo-algarve` declara `en` e mais nada), `npm run check:documentos` morre com `Error:` e rasto de pilha, e não com a linha `✗` da lista: a mensagem certa vem de `src/lib/documentos.mjs:131` (era 130) chamado de `scripts/check-documentos.mjs:218` (era 207). Controlo: a mesma conferência sem a planta sai a 0. A planta foi criada e apagada nesta passagem, e a árvore ficou limpa. O resultado continua a ser o certo e por isso continua a não ser bloqueante; o que se perde é a forma da casa e os outros erros da mesma corrida |
| I65 | parte 3, P2 | **Seis das oito páginas de leitura acabam num troço sem título, porque a passagem de voz cortou o título e deixou o corpo da secção.** Medido a 24.08 sobre os registos fixados: no 04 pt e en, no 07 pt e en, no 08 pt e no 09 pt, a última operação da voz corta o título «Método, e onde o verificar» / «Método, e como verificar este documento» e deixa por baixo dele o que resta da secção — no 04 pt, a frase «Tudo foi recolhido em direto para este documento.» e a tabela das oito fontes, com o último título da página três blocos acima (bloco 97 de 101). A página de leitura rende exatamente isso, porque rende o registo; a edição arquivada, ao lado, ainda tem o título. Não é um defeito do renderizador nem do portão: o L1 conta os blocos que o registo tem e bate | `registos/*.record.json` (fixados), `ResearchHub/publisher/voz.py` | **fechada a 27.08.2026** (motor, S3 aplicada em `530e380`): as caudas sem título levam «Fontes» (04, 07, 08) e «Fontes e lacunas» (09) |
| I66 | parte 3, P2 | **O selo cai entre a figura e o resto do texto da unidade, quando a unidade continua depois da figura.** Na tabela de cabeçalho do 04 pt, a célula do registo diz «51,95 %» e a figura cobre «51,95»: a página rende «51,95 ■ fonte %», porque o contrato da nona origem manda o selo **colado** à figura e o resto do texto é do documento. A convenção da casa noutras páginas é a oposta — `Claim.astro` põe o símbolo da unidade a seguir ao valor e o selo depois dele | `src/lib/registo-html.mjs`, `DECISIONS.md` §2.2 item 9 | aberto como observação e **não corrigido de propósito**: pôr o selo depois do « %» seria a página a mover um carácter do documento para outro sítio, e a página de leitura é uma transcrição. Medido: acontece em 4 das 12 figuras com selo do 04 pt. Se a direção preferir a convenção do `Claim`, é uma mudança ao contrato (o selo passaria para o fim da unidade) e ao L6, e deixa de ser possível dizer «este selo é deste algarismo» quando a unidade tem dois. **Recontada na P4 (24.08) nas oito edições, com as duas definições separadas porque dão números muito diferentes:** dos 196 selos do âmbito, **130 não fecham a sua unidade** (depois deles ainda vem texto do documento, que é o universo) e **32 caem entre o algarismo e o símbolo da sua unidade**, que é a forma descrita acima. A repartição da segunda: 12 por edição do par 07, 3 por edição do par 04, 2 no 06 pt, e zero no 03 pt, no 08 pt e no 09 pt. A contagem de 4 no 04 pt acima é da definição da P2; a estrita da P4 dá 3 (`#12.2.1`, `#42` e `#89`, os três com « %» a seguir ao selo), e a diferença é de definição e não de página. O par 07 é onde a forma mais aparece, e o 08 pt e o 09 pt não a têm de todo |
| I67 | parte 3, correções 1 | **As linhas que o motor exclui da travessia não se resolvem deste lado, e por isso o mesmo âmbito conta-se de duas maneiras.** O plano da parte 3 escreveu «2 396 figuras sem linha do livro-razão deste sítio» e deste lado da fronteira são **2 405**: as 9 de diferença são figuras cujas linhas o manifesto de travessia do MOTOR declara `excluded` com razão escrita, e esta casa não as sabe nomear: o `ledger/cruzamentos/evora.json` só traz `rows` (medido: 70 entradas, nenhuma lista de excluídas) e as entradas `excluded` do motor nomeiam padrões de linha em prosa («xw-* (14 linhas)»), que não se resolvem em identificadores. Custou quatro sítios de prosa a corrigir nesta ronda | motor: `publisher/`, o manifesto de travessia; sítio: `ledger/cruzamentos/` | **fechada a 27.08.2026** (motor `057244b`): os nove grupos nomeados por identificador, 1 604 linhas, exportação igual byte a byte; a diferença 2 405 / 2 396 explicada por cinco identificadores homónimos |
| I68 | parte 3, correções 1 | **A porta que vai a seguir a uma ligação do documento dá altura às linhas das tabelas.** A porta é uma caixa de 24px (o mínimo da 2.5.8) e não uma área posicionada de 44px, porque as portas de uma mesma ligação ficam encostadas e duas áreas de 44px a 13px uma da outra sobrepõem-se, que é a lição que a `site.css` escreveu ao lado da área do selo. Nas células da tabela do bloco 62 do 04, cuja entrelinha é 19,6px, a linha cresce para caber a caixa: medido em Chromium sem cabeça, a tabela passa de 594,1 para 697,1px a 1280 e de 1 298,3 para 1 331,2px a 390. Na prosa o custo é zero (a caixa cabe na entrelinha de 30,4px), e não há um único par de áreas sobrepostas nas duas edições do 04, a 1280 e a 390 | `src/styles/texto.css` | aberto como observação, para a sessão de UX. A alternativa medida é a da `.brief-text` na `site.css`: dentro das tabelas a porta fica com o tamanho da unidade (cerca de 14px de lado) e a linha não cresce, trocando o alvo pelo compasso. Dar altura à fila é a saída que a folha da casa prescreve, e é a que ficou |
| I69 | parte 3, leitura do diretor (25.08) | **Os documentos transcritos ainda se explicam a si próprios, e o diretor quer uma segunda passagem de voz.** Lida a página de leitura do 04 pt no ar, o diretor apontou dois exemplos e a classe: o título «Ler esta parte primeiro» é o documento a dizer ao leitor como o ler; a abertura «Uma leitura transversal de um município português» devia dizer qualquer coisa como «Uma leitura transversal do município de Évora» (a sugestão do diretor, 25.08; a gémea inglesa segue), e nunca «um município português», porque o sítio é sobre Portugal e não precisa de o repetir; **Nota do lugar de direção:** a abertura é também a descrição da página do estudo (`verbatimDaAbertura`, transcrita e conferida contra a edição arquivada) e o «cross-vertical» inglês de que ela foi traduzida é jargão do motor (as «verticais» são as suas linhas de estudo); a primeira ronda de voz só mexeu nos registos, e mudar só o registo deixaria a página de leitura com uma abertura e a página do estudo e a edição arquivada com outra. Para a abertura, a correção limpa é no `.md` do motor, de onde a edição HTML, o registo e a descrição do estudo mudam juntos (republicação pela §1.49 e re-travessia); e há «explicações a toda a hora» desta família, as que a passagem de voz existia para tirar e só em parte tirou (instruções de leitura, descrições genéricas do objeto, o documento a explicar-se). Não é do sítio: o texto é o registo fixado do motor | motor: os registos e os ficheiros de operações (`publisher/voz.py`) | **fechada a 27.08.2026** (motor `530e380`, sítio `7b326a9`): a segunda passagem de voz aplicada às doze edições e atravessada; a §1.71 |
| I70 | Emenda 19 (26.08.2026) | **Nas zonas densas do mapa a ligação num ponto não chega, e o alvo é o ponto.** Medido no desenho construído, a 1280, na coluna da cabeça: o ponto mede **7,35 × 7,35 px** e **44 dos 308** têm um vizinho a menos de um diâmetro. São Lisboa, Oeiras, Amadora, Odivelas, Aveiro, Ílhavo, Alcobaça e Nazaré, entre outros; o par mais próximo da Carta (Lajes das Flores e Santa Cruz das Flores, a 2,816 unidades de campo) fica a **2,30 px** um do outro. Um alvo de 7,35 px está abaixo dos 44 da casa, e dois pontos a menos de um diâmetro põem uma porta por cima do vizinho: aumentar o ponto contraria a Emenda 10 (os 308 são iguais) e aumentar a área invisível é a porta que abre o concelho do lado, que foi o que as 308 áreas de toque faziam antes de saírem. A vista de escolha e a lente eram a resposta anterior, e saíram com a Emenda 19b porque escolhiam um concelho dentro da primeira página, que é o que a emenda proíbe | `src/components/inicio/MapaRespira.astro`, `src/styles/inicio.css` | **FECHADO na Emenda 20 (27.08.2026), e não como o I70 previa: o alvo deixa de ser o ponto porque deixa de haver pontos.** O mapa da primeira página passa a ser as 29 unidades da Carta como áreas, nas duas larguras, cada uma a porta da sua página, e a página de uma unidade mostra os seus concelhos como áreas e como lista. Medido na construção (`tests/inicio/mapa-distritos.mjs`, 22 células verdes, quatro estragos plantados): a 1280 o mapa mede 490 × 646,1 px e **19 das 29** chegam aos 44 px; a 390 mede 354 × 466,8 px e chegam as mesmas 19, a menor **Viana do Castelo com 44,5 px**. As dez que não chegam são as nove ilhas dos Açores e Porto Santo, e a Emenda 20c dá-lhes a resposta: os nomes das ilhas de cada moldura ficam por baixo dela como ligações, uma por linha, com 44 px de altura cada. Com o distrito aberto, os concelhos passam a áreas e ficam dois abaixo de 44 px em Aveiro e nenhum em Lisboa nem em São Miguel; a lista da página é a porta dos que não chegam. A pesquisa continua onde está, e deixa de ser o único caminho para as zonas densas. Fechado em `3f2292b`, `1fc9406`, `f42ccb8` e `c40961f`; DECISIONS §1.72 |
| I71 | leitura cruzada do Codex (26.08.2026) | **O campo de pesquisa da primeira página não faz nada sem script.** Abaixo de 640 a folha mostra o bloco da pesquisa sempre (bloco A de 25.08, item A4), e o campo `<input type="search">` não tem formulário, nome nem destino: sem JavaScript, escrever não leva a lado nenhum; o comando «Concelho» continua a ser a ligação a `/municipios`, que funciona. Vem do bloco A, não do bloco do mapa; a leitura do Codex de 26.08 apontou-o (§5 do relatório). | `src/components/inicio/Pesquisa.astro` | **aberto**: no bloco das páginas dos 308 (decisão 5B), em que `/municipios` passa a ter os 308, a pesquisa pode ganhar um `<form>` com destino; até lá, sem script, a ligação ao índice é o caminho. **Passagem de higiene, 29.08.2026: continua aberta, e a condição que a linha pôs já se cumpriu.** O bloco das páginas dos 308 correu (DECISIONS §1.68) e o `<form>` não foi escrito: medido em `src/components/inicio/Pesquisa.astro`, **0** ocorrências de `<form` e **0** de `action=`, com controlo positivo de 1 `input` no mesmo ficheiro. Sem script, escrever no campo continua a não levar a lado nenhum, e o que funciona continua a ser o comando «Concelho» como ligação a `/municipios`, que hoje lista os 308. O que a resolve é uma linha de gabarito, e o destino já existe |
| I72 | bloco dos 308 concelhos (26.08.2026) | **Os 30 concelhos das ilhas não têm desemprego registado.** O IEFP publica por concelho só o continente (278); as fontes regionais (DRQPE nos Açores, IEM na Madeira) são PDF e não foram lidas. As peças dizem «sem linha ainda». | `content/12 Concelhos` no motor; `src/data/concelhos.gerado.json` | **fechada a 28.08.2026** (motor `87ac763`, sítio `97b6ca2`): as 30 linhas da DRQPE e do IEM, dezembro de 2025, somas iguais aos totais dos ficheiros; a definição dos Açores muda sobre as medidas ativas e fica escrita na linha |
| I73 | bloco dos 308 concelhos (26.08.2026) | **As fontes dos concelhos têm cadência e o sítio ainda não tem ciclo de releitura.** O INE atualiza a população em junho e as empresas em dezembro, o poder de compra é bienal, a DGAL publica a dívida em dezembro e o PMP em abril, o IEFP todos os meses; a primeira edição é uma leitura fixa de agosto de 2026. | `content/12 Concelhos` no motor (`source/`, `FETCH.json`); `publisher/export_site_rows.py` | **fechada a 28.08.2026** (motor `4a8239d`): `publisher/releitura_concelhos.py` e `RELEITURA.md`; primeira corrida sem alarmes, dois períodos novos em medidas de fim de ano |
| I74 | bloco «A grelha da voz» (26.08.2026) | **O inventário tem 58 linhas declaradas que já não se rendem em página nenhuma, e nada o impede.** O ficheiro escreve, desde o bloco dos 308, que «uma frase corrigida sai desta lista», porque repô-la passaria em silêncio; mas nenhuma régua confere isso, e a limpeza é à mão. Contadas na construção final: 58 das 453 declarações não correspondem a nenhum bloco do `dist/`. Uma parte é deliberada e tem de ficar (o ficheiro guarda duas leituras de uma contagem lado a lado, porque uma contagem volta a ser o que era no dia em que o livro-razão encolher); outra parte são variantes velhas de frases que os itens B6, E7, E10 e E11 corrigiram e que ninguém retirou. Este bloco tirou 25 no G1 e no G5, 34 no G6 das páginas de trabalho, 18 no G6 da página do concelho e 2 na reescrita da nota dos pelouros, e não mexeu no resto | `design/especime-v3/INVENTARIO-FRASES.md` | **fechado** no bloco «A grelha, segunda passagem» (`0240be7`): cada linha declara `viva` ou `retirada`, e `check:voz` fecha a construção nos dois sentidos (uma `viva` que não se rende, uma `retirada` que voltou). Das 57 desta construção, 39 passam a sentinelas com o bloco que tirou a frase e 18 saem, porque levam uma contagem por dentro ou deixaram de ser frases da casa: uma frase com um número que se move volta com outro número, e a sentinela nunca morderia. DECISIONS §1.73 |
| I75 | bloco «A grelha da voz» (26.08.2026) | **`factor-sustentabilidade-2026` perdeu a única página que a citava.** O valor do fator de sustentabilidade só se rendia na ressalva «A comparação junta duas figuras do relatório», na página do trabalho das pensões, e essa ressalva era proveniência e derivação, que vivem no recibo. A linha continua no livro-razão, com a sua página, o seu selo e a sua entrada no índice; o que ela perdeu foi a página que a citava, e o portão passa a dizê-lo (os avisos passam de 19 para 20) | `src/data/leituras.mjs`, `ledger/claims/factor-sustentabilidade-2026.yml` | **fechado a 27.08.2026**: uma linha com um número é conteúdo, e retirar um parágrafo não pode retirar um valor publicado. O fator volta à nota das medidas daquele trabalho, como UMA frase com o facto por sujeito e com o seu selo, nas duas edições. A conta foi conferida antes: a regra do relatório é F = FS (1 − 0,005 M), e com FS = 0,8237 e M = 12 dá uma redução de 22,57 %, contra os 22,6 % que a linha publica. Os avisos do portão voltam de 20 a 19 |
| I76 | bloco «A grelha da voz» (26.08.2026) | **O nome de uma medida do trabalho 05 ainda diz «sediadas».** «€ de valor acrescentado bruto das empresas sediadas no concelho», no relance de `/estudos/evora-economia-investidores-portas-abertas-2026`, e a inglesa «headquartered in the municipality». O item E7 do bloco dos 308 mediu que o INE não diz «sede», diz «Localização geográfica», e corrigiu as superfícies da página do concelho; o nome desta medida não estava na busca dele. A ressalva do mesmo trabalho, reescrita neste bloco, já não o diz | `src/data/leituras.mjs` | **fechada a 27.08.2026** (`7b326a9`): «empresas do concelho» / «enterprises in the municipality» |
| I77 | bloco «A grelha da voz» (26.08.2026) | **A incerteza sobre o nome legal do presidente interino de 2013 saiu da página com o G6, e a página continua a escrever o nome.** A entrada dizia que duas fontes oficiais dão nomes completos diferentes para o mesmo homem e que «Manuel Melgão» é a forma que o próprio município usa. A regra do G6 mantém uma ressalva quando ela muda a leitura de um NÚMERO, e um nome não é um número: por isso saiu. É a única das doze entradas retiradas da página do concelho cuja saída tira uma incerteza viva sobre uma coisa que a página mostra | `src/data/municipios.mjs` (`quem` do mandato de 2009–2013) | **fechado a 27.08.2026**: o nome leva o marcador `[a verificar]` ao lado, nas duas edições, com a porta da página do marcador. A razão vive lá e na linha, e não num parágrafo. `quemPorVerificar` declara-o e `scripts/gate-html.mjs` fecha a construção se o marcador não se render ou se abrir a porta da outra edição |
| I78 | bloco «A grelha da voz» (26.08.2026) | **A única menção ao auditor independente saiu da superfície pública.** A ressalva «Duas vozes de fora, não uma», na página do concelho, era a única frase do sítio que dizia que sobre as contas de um município existe uma opinião assinada por um auditor independente. Saiu com a secção do método, porque é proveniência. A opinião do auditor continua a ser a fonte de linhas do livro-razão, e o recibo de cada uma nomeia-a | `src/data/municipios.mjs` | **aberto, para o diretor**: decidir se a existência de uma voz de fora sobre as contas é conteúdo de uma página de concelho, ou se vive só nos recibos. **Passagem de higiene, 29.08.2026: a premissa da linha está meia certa, e a medição corrige-a.** A ressalva nomeada saiu mesmo: «Duas vozes de fora» dá **0** em `src/data/municipios.mjs`. Mas a página construída não ficou muda sobre o auditor: «auditor» rende **uma vez** em `/municipios/evora`, na nota do mandato, «O auditor emitiu por isso uma declaração de impossibilidade de certificação legal das contas.», que é uma das nove ressalvas que o G6 deixou de pé por mudarem a leitura de um número (§1.69). O que saiu da superfície pública não foi toda a menção ao auditor: foi a frase que dizia que sobre as contas de um município existe, por regra, uma opinião assinada por alguém de fora. A decisão que fica para o diretor é essa, e mais estreita do que a linha a escreveu |
| I79 | bloco da voz do livro-razão (27.08.2026) | **A régua da voz não lê os `title` nem os `aria-label`.** Lê os blocos de texto e a descrição do `<head>`; uma dica antiga replantada («itens da agenda atravessados do motor») não fica vermelha em lado nenhum, e as dicas são texto do leitor. | `scripts/voz.mjs`, `scripts/medir-defeitos.mjs` | **fechado** no bloco «A grelha, segunda passagem» (`0240be7`): as dicas e os rótulos das treze rotas inventariadas entram pelas duas medidas, 50 cadeias distintas, e a planta ficou vermelha pelo nome e pelo marcador «atravess» (a gémea inglesa por «crossed into»). DECISIONS §1.73 |
| I80 | bloco do mapa por distritos (27.08.2026) | **O manifesto do mapa guarda contagens por unidade e não a pertença.** A junção reproduz-se com os 29 ficheiros e a lista da Carta (o `check:mapa` fá-lo), não só do manifesto. | `publisher/mapa_distritos.py` (motor), `mapa/manifest.json` | **fechada a 28.08.2026** (motor `d98066c`, sítio `97b6ca2`): `concelhos_slugs` por unidade no manifesto, o resto igual byte a byte |
| I81 | bloco do mapa por distritos (27.08.2026) | **Viana do Castelo chega aos 44 px a 390 por 0,5 px.** Abaixo de uma janela de 386 px deixa de ser alvo e a Emenda 20c não se cumpre para ele. | `src/styles/mapa.css`, `src/components/inicio/MapaRespira.astro` | **fechado** no bloco «A grelha, segunda passagem» (`9d8f3e7`), e a resposta medida foi as duas coisas e não uma: a tela toma a janela abaixo de 640 (320, 360, 390 e 430 medidos), e à largura da janela numa janela de 320 Viana do Castelo mede 40,2 px de caixa e continua a não ser alvo, pelo que a rede da Emenda 20c passou de ser por moldura a ser por parcela. DECISIONS §1.73 |
| I82 | medição M3 do mapa (27.08.2026) | **«Caixa ≥ 44 px» sobrestima o alvo de uma forma côncava.** O centro da caixa da Ilha da Madeira cai fora do polígono; o clique acerta no ponto representativo. | `tests/inicio/mapa-distritos.mjs` | **fechado** no bloco «A grelha, segunda passagem» (`9d8f3e7`): rasterização a 2 px com `isPointInFill` e o quadrado máximo por programação dinâmica. A medida muda a resposta e não só na Madeira (caixa 257,5 px, inscrito 12): pela caixa chegavam 19 das 29 a 1280 e 19 a 390, pela área inscrita chegam 5 e 1. DECISIONS §1.73 |
| I83 | bloco «A grelha, segunda passagem» (27.08.2026) | **O rótulo de acessibilidade da porta de uma figura nomeia a chave interna da linha do motor.** Numa página de leitura, `<a href="#linha-tc-year-1-2008" aria-label="linha do motor: tc-year-1-2008">`: o que um leitor de ecrã ouve é o identificador de um artefacto do motor, que é o que a medida 6 da régua chama «um localizador que nomeia um artefacto interno». São 34 cadeias distintas nas duas edições, e foi a varredura das dicas (I79) que as expôs; a régua normaliza-as para «linha do motor: <linha>» para que o inventário não cresça com os documentos, o que arruma a contagem e não o rótulo. | `src/components/`, as páginas de leitura | **fechado 28.08.2026** (`a5d15b6`): o rótulo passa a «a linha desta figura» e «this figure’s row», e o identificador fica só no `href`. A normalização da régua que punha `<linha>` no lugar da chave saiu com as cadeias, porque estas 34 eram as únicas dicas do sítio que a exerciam. As duas linhas do inventário ficam `retirada` com o motivo escrito e entram duas de navegação, uma por edição. Medido em `dist/`: zero `aria-label` com «linha do motor» ou «engine row», e as 21 ocorrências de cada edição intactas, com a chave no `href` |
| I84 | leitura cruzada do Codex e X2 da grelha 2 (27.08.2026) | **A ordem por pontos de código sobrevive em dois sítios:** a ordem dos caminhos dentro do `svg` do mapa (a do artefacto do motor: a ordem de tabulação segue-a) e os cabeçalhos dos grupos por distrito em `/municipios`. A lista por baixo do mapa já ordena com a colação portuguesa. | `publisher/mapa_distritos.py` (motor), `src/views/MunicipiosView.astro` | **fechado 28.08.2026** (`9ddef92`, motor `087fbcc`): o `--write` do motor trouxe seis dos 31 ficheiros, `pais.json`, `aveiro`, `lisboa`, `porto`, `santarem` e o manifesto, cada um com o mesmo número de bytes que tinha, porque só a ordem mudou. `check:mapa` ganha a R7, que compara a ordem dos caminhos de cada `svg` e a das unidades do manifesto com a do `Intl.Collator('pt')`: foi vista vermelha sobre os artefactos de `main` com seis queixas antes da travessia e verde depois, e leva três estragos plantados, um por sítio que lê. `/municipios` ordena os grupos com a mesma colação, e um caso plantado fora de ordem foi visto vermelho nas duas edições. **Segunda volta no mesmo ramo** (`df4a2a6`, leitura do Codex, 28.08.2026): a primeira corrigiu os cabeçalhos e deixou os 308 nomes por baixo deles na ordem do código oficial de cada concelho, em quatro dos 29 grupos e nas duas edições (Aveiro, Lisboa, Porto e Santarém; os outros 25 estavam em ordem por acaso). `MunicipiosView.astro` ordena também as entradas de cada grupo, pelo nome que a página imprime, e a R7 passa a medir as listas CONSTRUÍDAS e não só os ficheiros: os cabeçalhos e os concelhos de cada grupo de `/municipios` nas duas edições, e a lista de cada uma das 58 páginas de distrito. Vista vermelha com oito queixas antes e verde depois; os estragos plantados passam de onze a catorze. |
| I85 | bloco das regiões (27.08.2026) | **A régua tem quatro patamares, e isso chega para seis leituras mas não por definição.** O empacotador dos rótulos põe no ÚLTIMO patamar o que já não cabe em nenhum: com o Norte, o Centro e os Açores na régua, dois rótulos da mesma vizinhança da escala podem voltar a cruzar-se. Hoje as seis leituras cabem, e a célula M1b da régua mede-o a zero. | `src/data/regioes.mjs` (`GEOMETRIA.patamares`), `src/components/InstrumentoConvergencia.astro` | **fechada a 28.08.2026 por medição** (`63aba22`): com dez leituras os quatro patamares ficam ocupados (1, 1, 3, 5 marcas) e nenhum rótulo se cruza; a régua imprime a ocupação dos patamares; a receita do quinto fica escrita ao lado da constante. |
| I86 | bloco das regiões (27.08.2026) | **`tests/inicio/correcoes-a.mjs` rebenta em A5 e A6, e não é do bloco das regiões.** A régua procura `[data-readout]`, `.mun-porta` e `circle.mun` na primeira página; os três saíram com os 308 pontos quando o mapa passou a ser por distritos (Emenda 20a), e nenhuma das duas células foi refeita nesse bloco. `page.evaluate` atira `Cannot read properties of null` e a régua pára antes das células seguintes. | `tests/inicio/correcoes-a.mjs` (A5, A6) | **fechada a 28.08.2026** (`c4dcc1b`): não eram duas células mas seis (A2, A4, A5, A6, A8, A10, a 390): movidas para as 29 áreas onde o seu objeto vive, ou retiradas com a razão; `correcoes-a` 32 de 32. |
| I87 | bloco das ilhas (28.08.2026) | **A definição do desemprego registado dos Açores muda sobre as medidas ativas.** A DRQPE concorda com o IEFP em cada critério que enuncia e não diz se exclui quem está em medidas ativas (num mês, quase tantos como os desempregados); a exclusão é inferência da disposição do ficheiro, escrita na `note` de cada linha açoriana. | `content/12 Concelhos/ledger.json` (motor) | **aberto**: pedir a confirmação por escrito à DRQPE; até lá, nenhuma página compara os Açores com o continente. |
| I88 | leitura do Codex, bloco dos vazios (28.08.2026) | **«O índice de dívida da Direção-Geral desceu de …» atribui à DGAL um índice que a casa calcula** sobre as duas colunas que a DGAL publica (a peça di-lo). Nas duas edições, `strings.mjs` `tempoSerieA`. | `src/i18n/strings.mjs` | **fechado 28.08.2026** (`68d1409`): «O índice de dívida, calculado sobre os dados da Direção-Geral, desceu de » e «The debt index, computed on the directorate-general’s data, fell from ». Medido em `dist/`: a antiga a zero nas duas edições, a nova numa página por edição, que é onde a série se rende. O inventário não muda, porque o bloco que a leva tem `data-claim` e a régua deixa cair um bloco com origem declarada. Não há frase de subida a tratar, e a razão está na I89. |
| I89 | passagem de correções pequenas 3 (28.08.2026) | **O verbo da frase da série está fixo em «desceu» e «fell from».** `municipio.tempoSerieA` traz o verbo dentro da cadeia, e `tempoSerieB` a `tempoSerieE` são só os pedaços entre os valores (« em », « para », « em », «.»): o sítio não escolhe entre subida e descida. Hoje a frase rende-se numa página por edição, Évora, e ali a série desce (242,6% em 2014 para 105,5% em 2025), pelo que o que está escrito é verdadeiro; um concelho cuja série suba escreveria uma falsidade sem que régua nenhuma a apanhasse. Encontrado a fechar a I88. | `src/i18n/strings.mjs`, `src/views/MunicipioView.astro` | **fechada a 29.08.2026** (`4a9f80b`): o verbo sai dos dois valores, com `tests/municipio/serie.mjs`; as três formas não se classificam no inventário porque o bloco leva `data-claim` e a régua o deixa cair inteiro |
| I90 | leitura do Codex sobre a passagem de correções pequenas 3 (28.08.2026) | **A edição inglesa imprime a vírgula decimal, e é a regra da casa e não um esquecimento.** A leitura assinalou «242,6%» na frase inglesa de Évora. Medida a edição inglesa inteira: 2 348 valores com vírgula decimal e **zero** com ponto, em 3 283 páginas com valores (zero lido com positivo conhecido). Não há formatador por língua em `src/` nem em `scripts/`: `Claim.astro` imprime o valor tal como o livro-razão o guarda, e `direcao.md` fixa «vírgula decimal» nas Regras de algarismos, sem distinguir edições. | `design/especime-v3/direcao.md`, `src/components/Claim.astro` | **fechado sem alteração 28.08.2026**: não há desvio a corrigir. Mudar para ponto decimal em inglês seria uma regra nova de composição sobre 2 348 valores, e é decisão da direção sobre a `direcao.md`, não uma correção |
| I91 | releitura do Codex, áreas de governo (29.08.2026) | **Nas páginas inglesas, a referência legal do selo das áreas («Decreto-Lei n.º 87-A/2025 · Artigo 15.º, n.º 1») não leva `lang="pt-PT"`**, e os títulos portugueses de documentos das fontes também não, em todo o livro-razão. | `src/views/` das áreas; `src/components/ItemDoLivro.astro` | **metade fechada 29.08.2026** (passagem de correções pequenas 4): a referência legal do selo passa a levar `lang="pt-PT"` nas duas edições, pela mesma razão e na mesma forma do rótulo de cada matéria. A A5 de `check:areas` passa a conferi-lo, com um estrago plantado (a marca tirada da página inglesa das Finanças) visto vermelho antes do verde. Medido em `dist/`: 18 referências, nove por edição, todas com a marca. **a outra metade fechada 29.08.2026** (`3335d4b`, passagem de correções pequenas 5, decisão do diretor de 29.08 sobre a recomendação do lugar de direção): o título de um documento é um NOME, fica em português e diz em que língua está, nas duas edições. `src/i18n/lingua-dos-titulos.mjs` declara a língua dos 66 títulos distintos do livro-razão, **33 portugueses e 33 ingleses** — marcar todos `pt-PT` mandava ler «Real GDP per capita» com fonética portuguesa, que é o mesmo defeito ao contrário, e adivinhar a língua por acentos era inventá-la. Com os títulos fecham o localizador (é português de ponta a ponta, e é onde viviam 928 das ocorrências do nome da lei), o nome da lei na frase do limite de dívida das 616 páginas de concelho, e os dois títulos de estudo que `linguaDoTitulo()` deixava passar por a mesma cadeia ser título das duas edições. Medido em `dist/en` com o mesmo instrumento nas duas construções: **4 518 títulos portugueses sem marca antes, zero depois**; leis, 1 237 sem marca antes e 316 depois, todas dentro de texto transcrito e impressas em cada construção; títulos de estudo, 11 antes e zero depois. A régua nova é `scripts/check-lingua.mjs`, passo do `build` e do `verify`, com seis estragos plantados vistos vermelhos antes de qualquer zero valer |
| I92 | leitura do Codex, pequenas 4 (29.08.2026) | **As unidades das linhas do livro-razão rendem em português nas páginas inglesas** («% do PIB», «Pessoas»), tal como a linha as guarda; as medidas dos concelhos têm unidade traduzida na definição da medida, as outras não. | `src/components/ItemDoLivro.astro`, o livro-razão inteiro | **fechado 29.08.2026** (`d5ef2b3`, passagem de correções pequenas 5): a unidade é um RÓTULO e não uma citação, e diz-se na língua da página. `src/i18n/unidades.mjs` traduz 34 das 36 unidades distintas do livro-razão, cada entrada com a origem do inglês escrita ao lado — o que a casa já escreve para a mesma coisa (`concelhos.mjs`, `strings.mjs`, `leituras.mjs`), o inglês do próprio organismo no excerto da linha, ou o dicionário. «avisos» e «factor» ficam em português com `lang="pt-PT"` e a razão declarada: para a primeira não há inglês do organismo (fonte e documento «[a verificar]») e escolher entre «call» e «notice» era a casa a decidir o que a fonte quis dizer; a segunda é o nome que o relatório citado dá ao número. O portão de HTML passa a esperar a unidade da edição (`CAMPOS_DA_LINHA_POR_LINGUA`), como já fazia com a `derivation`, e continua a conferi-la carácter a carácter. Medido em `dist/en`: 5 329 unidades rendidas, **5 320 traduzidas e 9 em português, todas com marca**. Fica de fora, escrito e não esquecido: os 580 cartões de partilha continuam a levar a unidade do livro-razão na edição inglesa, porque a manchete de um cartão é medida em pixels e reconstruí-los é trabalho de outro bloco |
| I93 | leitura do lugar de direção ao empacotar a passagem de correções pequenas 5 (29.08.2026) | **«Em `dist/en/areas/financas/index.html` um elemento leva o atributo duas vezes, `lang="pt-PT" lang="pt-PT"`».** | `dist/en/areas/financas/index.html`; `scripts/check-lingua.mjs` | **fechado sem alteração 29.08.2026** (`dbc08a7`): **é um falso positivo da procura por cadeia.** O elemento é `<a class="lang" href="/areas/financas" hreflang="pt-PT" lang="pt-PT">Português</a>`, o comutador de língua do cabeçalho, e leva quatro atributos distintos: `hreflang` é a língua da PÁGINA LIGADA e `lang` é a língua do TEXTO da ligação («Português»). Os dois são precisos, e o par certo contém a cadeia procurada — «hreflang="pt-PT" lang="pt-PT"» termina em «lang="pt-PT" lang="pt-PT"». Medido com um tokenizador de atributos sobre `dist/` inteiro, e não com uma procura por cadeia: **6 606 ficheiros, zero elementos com um atributo repetido**, de qualquer nome, antes e depois. O elemento também não é desta passagem: o comutador de língua vive no cabeçalho, que este ramo não toca. **A régua fica na mesma**: L8 de `check-lingua.mjs` fecha a construção com qualquer atributo escrito duas vezes no mesmo elemento, com um estrago plantado visto vermelho numa cópia onde o par certo ficou intacto ao lado dele. O tokenizador é próprio porque `parse()` normaliza os atributos num mapa e apaga a repetição em silêncio, e porque o conteúdo de `<script>` tem de sair primeiro: sem isso, `for(var i=0;i<b.length-1;i++)` parece uma etiqueta com «var» duas vezes, e a primeira medição acusou dez elementos que não existem |
| I94 | leitura do Codex à passagem de correções pequenas 5 (29.08.2026, 3 plantas em 3) | **O texto oculto do selo de proveniência repete o título português do trabalho nas páginas inglesas, sem a marca da língua** («Avaliação Económica Regional de Portugal 2026», «Évora — Quinze Anos, Cinco Mandatos», «Água Não Faturada» e mais quatro). E duas entradas do dicionário das unidades não seguem o inglês da própria fonte. | `src/components/Provenance.astro`, `src/i18n/unidades.mjs`, `scripts/check-lingua.mjs` | **fechado 29.08.2026** (`d3ce282`): o nome do trabalho passa a levar `lang="pt-PT"` dentro do `.vh`, e só o nome — «calculated» e o separador são prosa da casa na língua da página. O texto rendido não muda um carácter, e o portão continua a compará-lo com `seloDaLinha()`. Medido em `dist/en`: **382 ocorrências sem marca antes, 382 com marca depois**, com a varredura corrida primeiro contra a construção anterior, que a viu vermelha. A L6 da régua passa a varrer o oculto e não só o visível. No dicionário: «euros per inhabitant» passa a «euro per capita» (é o que o Eurostat imprime) e «% of the OECD and non-OECD EU total» recupera «countries»; e nove entradas ganham a origem escrita do seu inglês — as seis que a leitura nomeou e mais três que a auditoria das 34 encontrou. **A frase de atribuição das páginas de linha já estava marcada** e não precisou de nada: a leitura olhou para uma construção anterior ao commit `3335d4b` |
| I95 | pequenas 5 (29.08.2026) | **315 nomes de leis portuguesas sem marca de língua dentro de texto transcrito** na edição inglesa (314 numa `derivation_en` do motor, 1 numa nota da agenda): marcar um fragmento obrigava a partir uma cadeia que a casa copia carácter a carácter; a régua `check-lingua` conta-os em cada construção. | motor (`derivation_en`), `src/data/agenda.json` | **aberto**: um campo de língua por fragmento no motor, ou uma derivação inglesa que não cite a lei pelo nome português. |
| I96 | pequenas 5 (29.08.2026) | **Os 580 cartões de partilha da edição inglesa levam a unidade em português**: a manchete do cartão é medida em píxeis e refazê-los é um bloco. | `scripts/cartoes.mjs` | **aberto**: bloco próprio, com a régua dos cartões. |
| I97 | pequenas 5 (29.08.2026) | **Os nomes dos organismos (`source`, 17 valores, 930 linhas só na DGAL) e 12 das 62 edições de documentos rendem em português sem marca na edição inglesa.** | `src/components/ItemDoLivro.astro`, o livro-razão inteiro | **fechada a 29.08.2026** (`442ce8f`): a mesma regra dos títulos, e sem esperar pela I95, que é do motor e de outra espécie de cadeia. Dezasseis organismos com língua declarada (15 pt, 1 en, o Eurostat) e 61 edições (16 pt, 45 sem língua nenhuma, 0 en); nenhum traduzido, nem os que têm nome inglês oficial. **A contagem da coluna «o quê» estava por baixo: eram 16 edições portuguesas e não 12, e 61 distintas além do marcador e não 62.** Medido em `dist/en`: organismos sem marca 6 911 → 0 (ficam 164 do Eurostat, que não a levam), edições portuguesas sem marca 3 148 → 0; e em `dist/pt`, os 164 do Eurostat passam a levar `lang="en"`, de 0. A régua ganha L2c, L2d, L4d e L4e. **A leitura cruzada do mesmo dia achou-as meias, e a correcção está em `609abd1`**: L4d e L4e só exigiam a marca que falta e nunca recusavam a que está a mais, pelo que `lang="pt-PT"` no Eurostat numa página inglesa, `lang="en"` num organismo português numa página portuguesa e qualquer marca num código de série passavam as três; e as duas passavam em vazio, porque a única guarda era a global das páginas lidas. Passam a comparar a língua EFECTIVA do elemento contra a que a tabela manda, e a exigir um mínimo positivo tirado da própria tabela; L2c e L2d passam a recusar um valor fora de «pt», «en» e, só nas edições, `null`. Onze estragos plantados ao todo, vistos vermelhos, incluindo os dois «fr» na própria tabela, que é importada e não tem porta de ambiente (reposição conferida pelo sha256 e pelo diff). O inventário não mudou: 701 frases, 30 123 ocorrências, 582 linhas, antes e depois |
| I98 | pequenas 6 (29.08.2026) | **`scripts/design-bundle.mjs` está vermelho desde `58cc881`** por um seletor de `inicio.css` (`.movel-selo`) que o feixe do desenho não reconhece; não está na cadeia da construção. | `scripts/design-bundle.mjs`, `src/styles/inicio.css` | **fechada a 29.08.2026** (`83fec32`), e não era o que a linha dizia. O seletor não é de `inicio.css`: não existe em folha nenhuma, em `src/` nenhum, nem em `dist/index.html` (controlo positivo: `mapa-linha` dá 6 na mesma folha). Saiu com a forma do telemóvel da Emenda 18, que a Emenda 20c substituiu. **E era a primeira de dez**, porque o feixe morre à primeira: a peça sem limiar, o valor provisório e o antetítulo em `span` mudaram de página; o marcador passou de `span` a âncora; a banda das regiões não rende em página nenhuma desde a Emenda 21 (0 em 6 606 ficheiros `.html`, com `class="peca` a dar 636 como controlo); o mapa da primeira página deixou de ter pontos com a Emenda 20a (0 `circle.mun`, 29 `path.uni`); e `inicio.css` passou a importar `mapa.css`, o que reprovava nove dos vinte cartões por pedido para fora. Corrigidos os dez: a folha importada entra no lugar da linha que a importava, a rota de uma peça procura-se numa lista de candidatas em vez de se escrever, as duas ausências afirmadas passam a ser medidas, e o cartão do mapa retrata o mapa que existe. Doze estragos plantados em `dist/`, vistos vermelhos, com as páginas repostas e o sha256 conferido. Entra no `npm run verify`: a corrida inteira mede 0,93 a 1,02 s com a cache quente e 1,82 s com ela fria, contra um tecto de trinta. **Reaberta e fechada outra vez na leitura cruzada do mesmo dia** (`deefd7b`): o feixe afirmava mais do que media, e as duas afirmações foram corrigidas medindo, não escrevendo menos. A ausência da banda e do selo do telemóvel passa a ser varrida nos 6 606 ficheiros `.html` de `dist/` e não em três rotas; e as contagens do mapa, que eram todas relacionais (uma unidade, uma âncora e um nome passavam as três), passam a ser conferidas contra o artefacto do motor (29 unidades, 14 concelhos no distrito, 308 no localizador) e por âncora, porque âncoras iguais a caminhos não prova que cada caminho tem a sua. **Fica por decidir, e é da direção:** o feixe não tem cartão nenhum para as três famílias de página que nasceram depois dele (`area.css`, `distrito.css`, `regiao.css`) nem para `texto.css`, e `FOLHAS` não as conhece |
| I99 | leitura do Codex, pequenas 6 (29.08.2026) | **As linhas da população dos concelhos citam o indicador 0012918 do INE no documento, na edição, no localizador e no `source_url`, e o extrato alojado (`path`) é do indicador 0012917**: dois indicadores da população residente, e a linha não diz de qual saiu o valor. | motor, estudo 12 (o leitor do INE), `ledger/claims/*-populacao-2025.yml` | **aberto, respondida a 01.09.2026** pelo inventário das fontes (linha P1, lote 2): os indicadores 0012918 e 0012917 são a mesma tabela (a mesma operação, os mesmos períodos, a mesma nomenclatura territorial), com a idade agrupada de duas maneiras (19 grupos quinquenais no 0012918, 7 por ciclos de vida no 0012917), e o total é o mesmo (Portugal 11 424 031; Évora 58 567, lidos a 01.09); a linha da casa não está errada no valor, está indeterminada na proveniência. Fecha quando o motor disser qual leu e alinhar os quatro campos. |
| I100 | bloco «Os nomes ao lado do mapa» (29.08.2026) | **A célula M4 de `tests/inicio/areas.mjs` está vermelha desde `fc1d013`**, e não é um defeito das páginas: a régua das áreas conta os blocos por classificar com a sua própria varredura, que não conhece a marca `data-nome` acrescentada naquele commit. Medido hoje: 18 blocos por classificar nas duas edições, e são os nove nomes das áreas duas vezes; `npm run check:voz` diz «nada por classificar» na mesma construção, porque `medir-defeitos.mjs` foi ensinado e a régua das áreas não. | `tests/inicio/areas.mjs` | **fechada a 29.08.2026** (`0754e18`): a marca tinha duas metades e a régua não sabia nenhuma. Com `[data-nome]` na lista de origens os 18 descem para 9; com a substituição na descrição do `<head>` (o texto de cada elemento marcado trocado por `<lugar>` ou `<nome>`, como a régua da casa faz) descem para 0, e a régua fica 22 de 22 com 103 blocos por edição. A lista das marcas passa a ser LIDA de `scripts/medir-defeitos.mjs` pelo nome das cinco constantes, em vez de copiada, e a corrida pára com o nome da que não encontrar. **Não chama `medir-defeitos.mjs` como a M7 da régua do mapa**, e a razão é o estrago plantado: os desta régua são uma transformação do HTML entre o ficheiro e o navegador, nunca em disco, e a outra régua lê o `dist/` do disco — a célula ficaria com o número certo e sem controlo positivo. Estrago novo: `data-nome` retirado das páginas de área devolve exactamente os 18. **E o corredor dos estragos aceitava deteção parcial** (leitura cruzada; corrigido em `f4bb0a1`): um estrago contava como apanhado se qualquer célula tocada ficasse vermelha, e este, que estraga as duas edições, passava com a inglesa por detetar. Cada estrago passa a declarar as células que tem de pôr vermelhas, por edição, e o corredor exige que todas as declaradas fiquem vermelhas, que cada alvo case com alguma célula corrida, e que as não declaradas fiquem verdes |
| I101 | bloco «Os nomes ao lado do mapa» (29.08.2026) | **A construção não sabe medir o alvo pela medida da casa.** O alvo de uma área mede-se pelo QUADRADO INSCRITO desde a I82, e `src/lib/mapa.mjs` só pode medir a CAIXA (a fronteira do mapa proíbe calcular geometria, e o artefacto do motor dá a caixa e o ponto representativo). As duas discordam: pela caixa o continente deixa de precisar da rede de nomes a partir de uma tela de 351 px; pelo quadrado inscrito, 18 dos 18 distritos ficam abaixo de 44 px numa janela de 360, 17 numa de 390 e 15 numa de 430 (medido a 29.08 sobre `a76f829`). **Decidido a 29.08 pelo lugar de direção:** a rede não se esconde. Abaixo de 1024 mostra-se sempre, em linha; a partir de 1024 mostra-se a lista da coluna esquerda; nenhuma largura mostra as duas, e a célula L9 de `tests/inicio/lista.mjs` mede-o às sete larguras. O `data-alvo-abaixo-de` que o construtor tinha posto na marcação **saiu**, com o `larguraMinimaPx` que o alimentava: ninguém o lia, e bytes que nenhum leitor usa não ficam na página (Emenda 19a). | `src/lib/mapa.mjs`, `mapa/` (motor) | **aberto na forma que resta**: se um dia se quiser esconder a rede, a construção precisa da medida do quadrado inscrito, e ela tem de vir do motor no artefacto e não de um cálculo deste lado da fronteira. |
| I102 | pequenas 7 (29.08.2026) | **`src/components/inicio/BandaDaRegiao.astro` não é chamado por vista nenhuma e não rende em página nenhuma.** A banda da região era a régua da convergência em largura inteira na primeira página; a Emenda 21 mudou a leitura breve de sítio (§1.75) e o componente ficou. Medido a 29.08 sobre a construção de hoje: `class="banda` rende em **0** das 6 606 páginas construídas (controlo positivo: `class="peca` rende em 636), e nenhum ficheiro de `src/` o importa (controlo positivo: `InstrumentoConvergencia` tem 2 importações). O cartão do feixe do desenho que o retratava passou a dizer que ele saiu, com a corrida a parar se voltar. | `src/components/inicio/BandaDaRegiao.astro` | **aberto**: sai, ou volta a ser chamado. Achado fora do brief das correções pequenas 7, e por isso não corrigido aqui |
| I103 | pequenas 7 (29.08.2026) | **As três cadeias `inicio.movel.*` de `src/i18n/strings.mjs` não são lidas por código nenhum e não rendem em página nenhuma, e a razão escrita ao lado delas envelheceu.** «Abrir um concelho», «Ver uma região» e «Abrir a escolha de concelho» ficaram quando a forma do telemóvel da Emenda 18 saiu, com o comentário a dizer que «a forma do telemóvel que elas nomeiam volta com o mapa por distritos». O mapa por distritos chegou com a Emenda 20c e elas não voltaram. Medido: 0 ocorrências das três nas 6 606 páginas, nas duas edições (controlo positivo: «Livro-razão» rende em 3 297), e 0 leituras de `inicio.movel` em `src/`. | `src/i18n/strings.mjs` | **aberto**: saem com a razão, ou a razão passa a dizer o que as segura. Achado fora do brief das correções pequenas 7, e por isso não corrigido aqui |
| I104 | bloco da cabeça e leituras cruzadas (01.09.2026) | **A régua dos 44 px tem um buraco entre 641 e 1023.** `.seg { min-height: 44px }` e o dos `.chipb` vivem dentro de `@media (max-width: 640px)`: a 768 há 11 alvos abaixo de 44 px no corpo da primeira página (as seis posições do comando a 33,6 px, cinco resultados da busca a 34,9) e mais os da mobília; anteriores ao bloco da cabeça, iguais antes e depois. | `src/styles/inicio.css`, `src/styles/site.css`, `tests/inicio/lista.mjs` | **aberto**: bloco pequeno da mobília, com a régua a cobrir as sete larguras |
| I105 | bloco da cabeça (01.09.2026) | **Quatro alvos de texto abaixo de 32 px a 1024 e a 1280**: «O livro-razão →» (14 px), «a página inteira →» (19,2), o endereço de correio (19) e «O registo de correções →» (24); anteriores ao bloco. | `src/views/HomeView.astro`, `src/styles/inicio.css` | **aberto**: no mesmo bloco da I104 |
| I106 | leitura do Codex à cabeça (01.09.2026) | **A célula da `matriz` sobre a língua dos títulos citados está vermelha (86 de 87)**: 11 títulos de trabalhos repetem a língua da página em `/en/areas/*` e `/en/ledger/*` («Água Não Faturada» com `lang=pt-PT` dentro de página inglesa era o esperado, e não está). Exceção aprovada na §1.91(6): a matriz corre fora dos portões da construção. | `tests/inicio/matriz.mjs`, `src/components/ItemDoLivro.astro` | **aberto**: junta-se à família I91 a I97 (a língua na edição inglesa) num bloco só |
| I107 | piloto do corredor (01.09.2026) | **Cinco linhas de `dados.gov.pt` vigiam a página de apresentação, que traz telemetria por pedido**, e não o ficheiro de dados; o resumo dos bytes nunca é estável e a conferência não deteta nada. | `ledger/claims/` (as cinco linhas do PRR), motor `indicators/corredor.py` | **aberto**: o `source_url` passa ao ficheiro de dados num bloco pequeno, com a releitura |
| I108 | piloto do corredor (01.09.2026) | **A rota `linha` não está em `ROTAS_DO_INVENTARIO`**: «Publicado pela fonte a» e «Sem resposta desde» (o recibo das três datas) são texto do leitor que o inventário da voz não conhece; inventariar a rota é classificar todas as suas frases. | `scripts/medir-defeitos.mjs`, `design/especime-v3/INVENTARIO-FRASES.md` | **aberto**: bloco próprio |
| I109 | piloto do corredor (01.09.2026) | **As datas de difusão que o inventário registou à mão (19 distintas, em 48 entradas de 18 linhas) não estão em `indicators/calendar.json`**, e nem o INE nem o Eurostat servem datas no HTML: `--so-devidas` pede hoje 79 de 79. | motor `indicators/calendar.json`, `design/observatorio/INVENTARIO-DAS-FONTES.md` | **aberto**: bloco do lugar de direção (o território `indicators/*.json` é reservado) |
| I110 | piloto do corredor (01.09.2026) | **`snippets.json` de três verticais é gitignorado como «derived cache» e uma worktree nova não o tem**: `publisher.export_site_rows_test` fecha com uma mensagem sobre o sítio para um defeito que é da worktree; custou uma construção a descobrir. | motor `.gitignore`, `publisher/export_site_rows_test.py` | **aberto**: a nota no `.gitignore` (correr `make_snippets.py` primeiro) ou os três deixam de ser ignorados |
| I111 | leitura do lugar de direção (01.09.2026) | **`scripts/gate-html.mjs` importa `temLeitura` (desde 26.08) e declara `TAGS_SVG` (desde 16.08) sem os usar**: código morto anterior ao rótulo, não uma regra desligada; confirmado por `git blame`. | `scripts/gate-html.mjs` | **aberto**: commit de limpeza com as réguas a provar que nada mudou |
