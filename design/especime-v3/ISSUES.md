# ISSUES · redesenho v3, fase 1 (lista corrente; os fechados ficam)

| # | aberto em | o quê | onde | estado |
|---|---|---|---|---|
| I1 | etapa 0 | As cópias das maquetas no repositório (`71b4c65`) estavam atrasadas face ao canvas publicado: postura do mapa (selo em vez de respirar), cauda em secções em vez de portas, sem a prancha «Cartão de partilha» | `design/especime-v3/maquetas/` | fechado na etapa 0: refrescadas do canvas, as antigas guardadas com sufixo `.pre-2026-08-20-tarde` |
| I2 | etapa 0 | A anotação `nota-v3` do canvas ainda diz «mapa em postura de SELO» no parágrafo pós-crítica; o código da prancha respira | canvas | aberto; é texto do canvas, e o canvas não se publica sem a palavra da direção |
| I3 | etapa 0 | O sinal de tempo do cabeçalho: corpo §3 da constituição e a prancha a cobalto; Emenda 1 diz cor só para limiares publicados | `Masthead.astro` | resolvido por precedência (tinta), assinalado à direção (plano §3 i) |
| I4 | etapa 0 | O quadrado tracejado: a regra 5 do Método fixa-o como «falta um campo»; a fila de estados da maqueta usava-o como «por ler» | estado e cobertura | à espera da decisão (b); recomendação no plano |
| I5 | etapa 0 | As pranchas carregam Spectral/Bitter de `fonts.googleapis.com` | maquetas | não entra; etapa 1b aloja os tipos |
| I6 | etapa 0 | `figuras.mjs` declara limiares sem lado; a posição de investimento é um chão | `src/data/figuras.mjs` | etapa 2a: campo `lado` declarado |
| I7 | etapa 0 | Não há chaves da prova para as contagens de estado do painel | `src/lib/prova.mjs` | etapa 2a: três chaves com estragos plantados |
| I8 | etapa 0 | As pranchas não têm paleta escura; o sítio serve um tema de três estados | `tokens.css` | à espera da decisão (f); proposta medida no plano |
| I9 | etapa 0 | `assertKeyParity()` compara caminhos de chaves, não valores: PT copiado para EN passa | `strings.mjs` | a régua da invariância imprime os iguais (etapa 2); `CHAVES-EN.md` para a revisão de voz |
| I10 | etapa 0 | O Método não tem hoje frase de cor nem de letra; a da prancha («A letra») reclama a linhagem Gini 1938, que a Emenda 5 proíbe | `metodo.mjs` | rascunhos no plano §12, à espera do sim |
| I11 | etapa 1 | As páginas de documento de estudo (`/estudos/<id>/documento/`) escrevem a sua própria folha, com as pilhas de sistema da v2 em literal (`Iowan Old Style…`, `ui-monospace…`), e não leem ficha nenhuma de `tokens.css`: depois da etapa 1 são a única superfície do sítio fora da letra da constituição | `src/lib/documentos.mjs` | aberto; o ficheiro não é da etapa 1. Recomendação: a etapa que tocar nos documentos de estudo (4) passa-o a `--f-prosa` e `--f-instr` |
| I12 | etapa 1 | Quatro literais `var(--yellow)` sobrevivem em atributos `fill` de gabarito, num componente e em duas vistas que a etapa 1 está proibida de abrir (`InstrumentoConvergencia.astro:187`, `AgendaView.astro:673`, `MunicipioView.astro:275` e `:388`). O token já não existe; as seis regras nomeadas de `site.css` remapeiam-nos, e o `fill` calculado foi conferido em Chromium sem cabeça nas três rotas | `src/components/InstrumentoConvergencia.astro`, `src/views/{Agenda,Municipio}View.astro` | aberto; o critério de saída do brief («o grep imprime nada») cumpre-se em `site.css` e não nos três ficheiros. Saem quando as etapas 2 a 4 reescreverem esses desenhos, e o bloco de remapeamento sai com eles |
| I13 | etapa 1 | O alvo de 44×44 do selo não cabe onde os selos se empilham: medidos 3,1px, 18,2px e 22,4px entre selos consecutivos na primeira página e na página do município, e duas áreas de 44px a 18px de distância sobrepõem-se, com a de baixo a apanhar o clique da de cima. Sete lugares ficam com a área do tamanho da unidade, para que nenhuma porta abra a linha do vizinho | `src/styles/site.css` (`.src-chip::after`) | aberto; medido 6/27 selos com alvo ≥44×44 na primeira página, 8/91 na do município, 1/1 na página de linha, e **0 pares sobrepostos** nas três. A saída é dar altura à fila: etapa 2 (plano §13) e etapa 3 |
| I14 | etapa 1 | Sete selos de `.prov-vals` (a fila de proveniência do instrumento da convergência, na primeira página) não se deixam medir por `elementFromPoint` depois de `scrollIntoView`: o ponto fica fora da janela. Conferido que o mesmo acontece no commit `7650aed`, antes de a etapa 1d tocar no selo, e por isso **não é uma regressão desta etapa** | primeira página, instrumento n.º 1 | aberto; a etapa 2 reescreve este instrumento e a matriz de aceitação do plano §13 volta a medi-los |
| I15 | etapa 1 | 41 regras de `site.css` continuam a pôr Bitter em caixa alta fora de um instrumento, contra a Emenda 5 e a `IDENTIDADE.md` §1 v3. A etapa 1 corrigiu as três que são mobília partilhada (`.nav`, `.rodape-nav`, `.masthead-furniture`); as outras pertencem a famílias de página das etapas 2 a 4 | `src/styles/site.css` | aberto; a lista das 44 regras reproduz-se com o varrimento de blocos descrito na nota da etapa 1. **A parte da família do livro-razão fechou na subetapa 3a (21.08.2026)**: os cinco rótulos da página de linha (`.linha-bloco-titulo`, `.linha-campo-k`, `.aparelho-titulo`, `.aparelho-ficha dt`, `.linha-verificacoes dt`) passam a versaletes de Spectral SC em `src/styles/linha.css`, que entra depois de `site.css` no documento. Medido no motor: `Bitter em caixa alta fora de instrumento: 0` na página de linha, nas duas edições (célula «3a · a letra» de `tests/linha/recibo.mjs`). As regras de `site.css` ficam onde estão, porque o ficheiro é do construtor A; o que mudou foi o que a página de linha computa |
| I16 | etapa 1 | `README.md` (linha 374) e `scripts/design-bundle.mjs` (linha 577 e a tabela seguinte) descrevem a identidade v2: «Iowan Old Style só em marcas, Avenir Next só em prosa, SF Mono…», «tudo pilhas de sistema, a página não faz nenhum pedido de rede», «o amarelo `#E8A80C` nunca é texto». Nenhum dos dois é da etapa 1 e nenhum entra no `npm run build` | `README.md`, `scripts/design-bundle.mjs` | aberto; ficam errados a partir do commit `a21cc2a`. Recomendação: atualizar no fim da fase, junto com o pacote de desenho |
| I17 | etapa 2 | **Um alvo de 24×24 CSS px por ponto do mapa não é alcançável.** Medido sobre os 308 centróides da CAOP: o vizinho mais próximo está a 1,41 unidades de campo, e a mediana da meia-distância é 6,84. Com o mapa a 281px, o lado do alvo vai de 6,1px (mínimo) a 24,0px (o tecto), com mediana 6,4px. Dar 24px aos pontos densos seria dar-lhes o alvo do vizinho, e uma área sobreposta é uma porta que abre o concelho errado | `src/components/inicio/MapaRespira.astro` | aberto por desenho, e não por resolver: a pesquisa é o caminho principal para escolher um concelho, como a Emenda 3 já decide para o telemóvel. A distribuição inteira é medida na 2f |
| I18 | etapa 2 | O rótulo de âmbito de um concelho sem página diz «<nome> · município · <distrito>» com o distrito tal como a Carta o escreve («Beja», «Ilha do Faial»), e o de Évora diz «distrito de Évora», que é a cadeia que `municipios.mjs` publica. É uma incoerência de forma entre um concelho e os outros 307 | `Cabeca.astro`, `municipios.mjs` | **fechado na subetapa 2g, com a regra da prancha: `distrito de <d>` quando o campo da Carta é um distrito, o nome de ilha nu quando começa por «Ilha»**. A regra decide-se na construção (`src/lib/inicio.mjs`, `eIlha()`) e cada ponto da CAOP leva `data-ilha`; o prefixo é um par de cadeias validadas (`inicio.cabeca.distritoDe`) que já está escrito na página nas duas edições, e o script só lhe troca o `hidden` — as duas ranhuras continuam ranhuras e nenhum texto é composto no cliente. Vale também no que o mapa lê em voz alta. Medido: Beja «distrito de Beja», Horta «Ilha do Faial», Lagoa (São Miguel) «Ilha de São Miguel», Évora sem prefixo próprio porque traz a etiqueta de `municipios.mjs`; 30 dos 308 são de ilha (`grep -o 'data-ilha="sim"' dist/index.html \| wc -l` → 30) |
| I20 | etapa 2g | **Transbordo horizontal de 10px em Évora na leitura breve, a 320 e a 390.** O rótulo da referência da régua do índice de dívida («limite legal») está centrado no extremo direito da sua escala, porque um tecto legal É o extremo da sua própria escala, e sai para fora da janela. Medido nas duas construções, seis estados × quatro larguras: 23 das 24 combinações dão zero, e esta dá 10 em ambas (`scrollWidth` 400 para `clientWidth` 390, culpado `span.regua-ref-rotulo`) | `src/components/inicio/Regua.astro`, `src/styles/inicio.css` | **fechado na subetapa 2h**: quando a referência calha numa ponta (`pr >= 82` ou `pr <= 18`, os mesmos limites que já decidiam qual das pontas da escala se escreve), o rótulo deixa de se centrar no traço e encosta-se à ponta — `data-ponta` na construção, `right: 0` / `left: 0` e `transform: none` na folha. Nenhuma palavra mudou. **O defeito tinha seis instâncias e não uma**: além de «limite legal» em Évora, os cinco «UE-27 = 100» das regiões estão no mesmo 100% e transbordavam 15px a 320 e a 390 num estado (`?ambito=regiao:<slug>&densidade=leitura`) que a lista dos seis estados varridos não continha. Medido depois: seis estados × quatro larguras, **24 de 24 a zero**, e 0 rótulos fora da caixa da régua. A varredura passou de guião de rascunho a célula da matriz |
| I21 | etapa 2g | **«Toque num ponto para escolher o concelho.» rende-se no telemóvel**, sempre que o âmbito é Município, e a 390 nenhum ponto é alvo: a Emenda 3 diz que o selo inteiro é o alvo e a folha põe `pointer-events: none` em todos os `.mun-alvo` abaixo de 640. É uma dica que descreve o que a página não faz nessa largura | `MapaRespira.astro`, `inicio.css` | **fechado na subetapa 2h**, com a regra das outras duas: entra `hidden` do servidor (que cobre o caso sem script), o script acende-a no âmbito Município, e a folha volta a apagá-la abaixo de 640 e na postura de localizador. Nenhuma palavra mudou. Medido na matriz: visível a 1280 no âmbito Município; não visível a 390, não visível a 1280 no âmbito País, não visível sem script em nenhuma das duas larguras |
| I19 | etapa 2 | As frases de moldura subiram de 77 para 94 distintas. As 17 novas são, uma a uma, as duplicações autorizadas R2 e R6 (as notas das oito medidas de Évora e a frase de abertura do concelho), cada uma agora em duas páginas. Nenhuma frase da v2 saiu, porque as da primeira página apareciam numa página só e a régua só conta as que aparecem em mais do que uma | régua `medir-defeitos.mjs` | aberto; a descida que o plano §4 espera é da fase da voz, e a etapa 3 pode desfazer as duplicações se a direcção quiser |
| I22 | etapa 1f | A palavra «provisório» não entra dentro de um `<svg>`: um `<span>` não é filho legítimo de um `<text>`, e o `<tspan>` que o substituiria teria de ser posicionado pelo instrumento que o desenha. São três usos, `as="text"` e `as="tspan"`, em `InstrumentoConvergencia.astro`, que é da etapa 2 | `src/components/InstrumentoConvergencia.astro` | **fechado na etapa 2k (21.08.2026)**, e não como o I22 previa: a palavra continua a NÃO entrar dentro do `<svg>` — a leitura do `<span>` dentro do `<text>` mantém-se verdadeira e nenhum `<tspan>` foi posicionado à mão. O achado 13 da segunda leitura cruzada trouxe a pergunta de volta e a direção decidiu o lugar: **a entrada de legenda de selos daquele valor**, que é onde o selo do valor desenhado já vive pela convenção do §1.34. A banda da região já o fazia sem ninguém reparar (a entrada da sua legenda é um `<Claim … chip>`, fora do desenho); o Instrumento n.º 1, cuja entrada é só o selo, recebeu-a na 2k, lida de `source_flag` e nunca de uma lista. Medido: 1 → **2 entradas de legenda com a palavra por linha**, nas seis linhas e nas duas edições; 18 → 24 ocorrências por edição |
| I23 | etapa 1f | O inventário do plano §3 dá a bandeira `p` como sendo de uma linha, `pib-pc-portugal-2024` («the `82 p` claim»). São **seis**: as seis do PIB per capita de 2024, Portugal e as cinco regiões. Há ainda duas linhas com `source_flag: "a)"`, que não levam a palavra e são o controlo negativo | `design/especime-v3/PLANO-redesenho-v3.md` §3 | aberto; o plano é do lugar de direção. A implementação lê a bandeira da linha e não do inventário, por isso não herdou o erro |

| I24 | etapa 2i | **`Claim.astro` assume português quando a língua não chega.** `const { lang = 'pt' } = Astro.props` faz com que uma chamada sem `lang` renda a palavra «provisório» na edição inglesa em vez de «provisional». Foi assim que sete valores da primeira página saíram em português na edição inglesa (achado 7 da leitura cruzada), e a marca não estava em nenhum dos dois ficheiros que o brief mandava ler: estava na ausência de uma propriedade. Os sete lugares foram corrigidos na 2i (`Peca.astro` e `InstrumentoConvergencia.astro`), mas a armadilha fica de pé para as etapas 3 e 4, que têm dezenas de chamadas a fazer | `src/components/Claim.astro` | **fechado na etapa 2j (21.08.2026)**: `lang` deixou de ter defeito e a construção atira quando falta, com a mensagem a nomear o `id` da linha, como as do portão. **Oito chamadas não a passavam**, e a construção apanhou-as uma a uma — `BandaDaRegiao.astro`, `InstrumentoConvergencia.astro` ×2, `EstudoView.astro`, `MunicipioView.astro` ×4. **Duas delas nenhum `grep` deste repositório tinha encontrado**, porque a linha continha `lang === 'pt'` noutro sítio e qualquer varredura por «sem lang» as excluía: só a construção as viu. Cinco das oito estão em `EstudoView.astro` e `MunicipioView.astro`, que são das etapas 3 e 4 e não são desta ronda; a alteração é `lang={lang}` e mais nada, está declarada na nota da 2j e não muda um byte rendido em português. Medido antes: 12 «provisional» e 7 «provisório» na edição inglesa; depois da 2i: 18 e 0; depois da 2j: **18 e 0**, sem mudança, que é o esperado — a armadilha fechou-se, os sete lugares já estavam corrigidos |
| I25 | etapa 2i | **As pastilhas das regiões levam `aria-pressed` sem papel de botão.** O script põe `aria-pressed` em `[data-regiao]`, que são `<a href>` e não são promovidas a `role="button"` (só os comandos de âmbito e de densidade o são). `aria-pressed` num `<a>` sem papel de botão não é ARIA válido, e um leitor de ecrã pode ignorá-lo — a pastilha escolhida deixa de se anunciar como escolhida. As da pesquisa (`[data-escolher]`) são `<button>` e estão certas | `public/js/inicio.js`, `src/views/HomeView.astro` | **fechado na etapa 2j (21.08.2026)**, e foram as duas linhas que a entrada previa: as pastilhas passam a `role="button"` no mesmo sítio onde os segmentos de âmbito e de densidade já o recebem, e ganham a tecla de espaço com o `activaComEspaco()` que já existia. O `href` fica, como nos outros comandos: com script é a forma do estado, sem script é a ligação que abre a mesma leitura. Medido na matriz (célula «2i·5»): `com role="button": âmbito 6/6, densidade 2/2`, e as cinco pastilhas entram no mesmo laço |
| I26 | etapa 2j | **`IDENTIDADE.md` §2 descreve a paleta escura como era antes da Emenda 12.** Diz «A paleta escura é regra provisória… A direção decidiu-a a 20.08.2026 com duas condições… a primeira pré-visualização é o seu primeiro teste real». A pré-visualização aconteceu a 21.08 e a direção decidiu: as duas condições continuam de pé, mas o escuro deixou de vir da preferência do sistema e passa a vir de um controlo do cabeçalho, com a escolha guardada no aparelho do leitor. O parágrafo não está errado sobre a paleta; está incompleto sobre como se chega a ela | `IDENTIDADE.md` §2 | **fechado no commit 3-0 (21.08.2026)**, com a alteração que a entrada pedia e nem uma palavra a mais: o parágrafo da §2 ganha uma frase que nomeia a Emenda 12, diz que as duas condições continuam de pé, que o escuro passa a ser um pedido do leitor guardado no aparelho dele, e que o bloco que consultava a preferência do sistema saiu de `src/styles/tokens.css`. Conferido antes de escrever, e não pelo nome do ficheiro: `grep -c "prefers-color-scheme" src/styles/tokens.css` → **0** (saída 1), com controlo positivo `grep -c "data-theme"` → **3**. Nenhuma citação entre «…» entra na frase, e por isso a amarra das decisões não ganha nada para conferir |
| I27 | etapa 2j | **`Provenance.astro` tem a mesma armadilha que o I24 acabou de fechar em `Claim.astro`**: `const { id, lang = 'pt' } = Astro.props`. Hoje não morde, porque quem o chama sem língua é o próprio `Claim.astro`, que já a exige — mas é a mesma classe de defeito, no componente do lado, e as etapas 3 e 4 vão escrever dezenas de chamadas | `src/components/Provenance.astro` | **fechado no commit 3-0 (21.08.2026)**: `lang` deixou de ter defeito e a construção atira quando falta, com a mensagem a nomear o `id` da linha, como em `Claim.astro`. **Nenhuma chamada precisou de correção**: as doze chamadas a `<Provenance>` do repositório já passavam `lang={lang}`, e a construção passou verde à primeira. Provado que a guarda dispara, plantando a falta numa chamada de `LivroView.astro`: «`<Provenance id="abandono-escolar-precoce-2025"> sem "lang". A língua é obrigatória: sem ela, o selo escreveria a palavra «fonte» e o texto oculto em português na edição inglesa (ISSUES I27). Passe lang={lang} na chamada.`», construção a fechar em `/en/ledger`; revertido, `git diff --stat` do ficheiro vazio |
| I28 | etapa 2j | **`inicio.mapa.naoDizK` («O que o mapa não diz») não é rendida por ninguém.** Veio com a relocação R3 do instrumento n.º 2 da v2 e nunca chegou a ter um lugar na ficha nova; conferido por `grep` sobre `src/components` e `src/views` antes desta ronda, com as três outras `naoDizK` de outras famílias a provarem que o comando encontra o que existe. Não é um defeito rendido: é peso morto no registo das cadeias | `src/i18n/strings.mjs` | aberto, e deixado de propósito: retirá-la muda a contagem «13 chaves relocadas» de R3, que é registo, e isso é uma decisão do lugar de direção e não uma limpeza |
| I29 | etapa 2j | **O brief da 2j manda aplicar a regra dos pontos também a `src/components/InstrumentoMapa.astro`, «que a página de concelho usa», e esse ficheiro não existe.** Saiu na subetapa 2b com `public/js/mapa.js`, e a nota da etapa 2 §2 regista que nenhuma outra vista o usava: `MunicipioView.astro` nomeia `InstrumentoConvergencia` num comentário e não importa nenhum dos dois. O mapa da página de concelho, quando a etapa 3 o desenhar, herda a regra de `MapaRespira.astro`, que é o único mapa do sítio | *(ficheiro inexistente)* | fechado por inexistência, e escrito para não voltar como pergunta. Conferido: `git log --diff-filter=D -- src/components/InstrumentoMapa.astro public/js/mapa.js` dá **`0c1ef02`**, que é o commit da 2b; e `grep -rn "InstrumentoMapa\|js/mapa.js" src/ public/ tests/ scripts/` dá **uma linha só**, um comentário em `site.css:1115` que nomeia o ficheiro antigo ao lado do bloco de remapeamento do amarelo. O controlo positivo é o mesmo `grep` a procurar `InstrumentoConvergencia`, que encontra as chamadas reais |
| I30 | etapa 2k | **A legenda de selos do Instrumento n.º 1 mostra UM selo, e o desenho pode ter seis valores.** «Ver todas» põe as seis regiões na régua e `desenhaLeitura()` continua a mostrar só a entrada da região lida: seis cópias desenhadas, um selo, e — desde a 2k — uma palavra «provisório». Não é regressão da 2k: a palavra herdou exactamente a regra do selo, que é anterior. É um buraco da convenção do §1.34 **em estado de execução**, e o portão não o vê porque varre HTML construído. Medido em Chromium sem cabeça: por defeito `desenhados:1 selos:1 palavras:1`; depois de «todas», `desenhados:6 selos:1 palavras:1` | `src/components/InstrumentoConvergencia.astro`, `public/js/convergencia.js` | aberto; a saída barata é mostrar as entradas de legenda de TODAS as regiões activas em vez de só a lida, o que não compõe cadeia nenhuma — são as mesmas seis entradas que o servidor já escreveu, a trocar de `hidden`. Não entra na 2k porque muda o que a legenda mostra, e isso é forma: é da direção |
| I31 | etapa 2k | **`public/js/tema.js` passou a ter duas partes e o nome só diz uma.** A segunda é o `aria-expanded` das duas divulgações por irmão, que tinha de correr em todas as páginas com cabeçalho, e este é o único ficheiro adiado que todas carregam. O nome é da cadeira (brief da 2j nomeia-o); renomeá-lo é `Base.astro` mais o rasto de medição de duas rondas | `public/js/tema.js`, `src/layouts/Base.astro` | aberto; o cabeçalho do ficheiro declara as duas partes por extenso, para que o nome não seja a única coisa que se lê. A cadeira decide se vale um nome novo |

| I32 | etapa 2l | **Faltam ao painel do Procedimento pelo menos duas linhas, e a identidade do 13.º indicador principal está por confirmar.** A Emenda 16 di-lo por extenso: o painel da Comissão tem 13 indicadores principais no AMR de 2026, as 13 linhas do livro-razão cobrem 11 deles — porque o fluxo de crédito e a dívida do setor privado estão divididos em famílias e empresas —, e faltam pelo menos o **desemprego de longa duração** e o **desemprego jovem**. O livro-razão tem `desemprego-de-longa-duracao-2025`, e a sua nota não declara limiar do Procedimento; a linha do desemprego jovem não existe. A conta das 13 sai de `grep -l "Limiar do Procedimento" ledger/claims/*.yml` (13 ficheiros, de 132) | **`ResearchHub/indicators/`** | aberto, e é **pedido ao motor**: a escolha dos indicadores, o limiar de cada um e a conferência contra a lista da Comissão fazem-se onde elas são registadas (`convergence.md`), e não neste ramo. Este ramo não escreveu um único número por causa disto: a manchete diz o que o sítio pode provar — quantos limiares o país ultrapassa e quantos cumpre, das linhas que o sítio tem — e **não** afirma um total do Procedimento |
| I33 | etapa 2l | **A contagem da CAOP por parcelas e a citação transcrita saíram da primeira página e `/municipios` ainda não as tem.** A Emenda 17 manda a contagem por parcelas (278 · 19 · 11) para `/municipios`; a Emenda 15 manda a camada «Método, ressalvas e proveniência» sair da página do leitor, e com ela a citação `data-verbatim="caop-fonte"` que vivia por baixo do mapa. A citação continua em `/municipios` (`MunicipiosView.astro:127`), mas as **três linhas por parcela não** — `grep -n "municipios-continente-caop" src/views/MunicipiosView.astro` não dá nada, com controlo positivo no mesmo ficheiro. Com elas saíram as regras `.compo*` de `site.css`, que nenhuma página construída usava depois desta ronda (`grep -rl 'class="compo' dist/` → 0) | `src/views/MunicipiosView.astro` | **fechado no commit 3-0 (21.08.2026)**: a contagem por parcelas entra em `MunicipiosView.astro` como uma linha por baixo da cabeça da lista, quatro pares rótulo + `<Claim … chip>` («Continente 278 ■ fonte · Açores 19 ■ fonte · Madeira 11 ■ fonte · Total 308 ■ fonte»), com as suas regras numa folha nova, `src/styles/municipio.css`, importada pelas vistas dos concelhos e por mais lado nenhum. Relocação **R9**, escrita antes do movimento. O rótulo «Contagem verificada nos ficheiros» não viaja: é autorreferência (Emenda 15). Medido: 1 rendição por parcela por edição |
| I34 | etapa 2l | **A porta do CSV dos concelhos não pode sair da primeira página enquanto `scripts/check-dados.mjs` for o que é.** A Emenda 15 tira a camada de aparelho do mapa e a sua porta do CSV ia com ela; `check-dados.mjs` exige, com mensagem própria, que **as duas edições da primeira página liguem os dois ficheiros** («Os dados por trás de cada instrumento são descarregáveis nas duas edições, ou não são»). A porta fica na primeira página, como ligação e sem a frase que a explicava, e o mesmo vale para a do CSV da convergência | `scripts/check-dados.mjs`, `src/components/inicio/MapaRespira.astro`, `src/components/InstrumentoConvergencia.astro` | **fechado no commit 3-0 (21.08.2026)**, e a conferência mudou em vez de ser contornada: `scripts/check-dados.mjs` deixa de exigir os dois ficheiros **na primeira página** e passa a exigir cada um **na rota que o declara**, numa tabela lida antes de correr (`PORTA_DOS_DADOS`: a convergência na primeira página, os 308 em `/municipios`); um ficheiro sem rota declarada fecha a construção em vez de deixar de ser conferido. Planta: a porta rendida só na edição portuguesa fecha a construção com «a página "/en/municipalities" (edição "en") não liga para "/dados/municipios-308.csv". Os dados por trás de cada instrumento são descarregáveis nas duas edições, ou não são.»; revertida. Medido que a porta da primeira página já pode sair (retirada à mão de `MapaRespira.astro`: construção e `check:dados` verdes), e **a retirada fica como pedido ao construtor B**: o ficheiro é dele |
| I35 | etapa 2l | **A frase que explica a conta de uma medida calculada continua na página de concelho.** «Calculado sobre duas colunas do mesmo ficheiro do regulador. A aritmética está na linha.» é a `nota` de `evora-execucao-da-receita-2025` em `src/data/municipios.mjs`. A Emenda 15 nomeia esta classe («qualquer legenda "calculado" que explique em vez de nomear; o rótulo do selo já leva "calculado ·"»), e a primeira página deixou de a render — a condição lê-se da linha, com `eDerivada()`, e não de uma lista escrita à mão. Em `/municipios/evora` ela continua | `src/data/municipios.mjs`, `src/views/MunicipioView.astro` | **fechado no commit 3-0 (21.08.2026)**, e a entrada nomeava a linha errada: a cadeia «Calculado sobre duas colunas do mesmo ficheiro do regulador. A aritmética está na linha.» é a `nota` de **`evora-indice-de-divida-2024`** e não de `evora-execucao-da-receita-2025` (`sed -n '130,145p' src/data/municipios.mjs`), que é também o que a decisão 5 da direção diz («a sentence explaining that the índice de dívida is computed from two DGAL columns»). Fechada como a primeira página a fechou, e não retirando a cadeia do ficheiro de dados: `MunicipioView.astro` deixa de render a `nota` quando `eDerivada()` é verdadeira sobre a linha daquela medida. Das oito medidas do relance de Évora **uma** é derivada, e por isso a página passou de 8 para 7 `.figura-frase`; medido nas duas edições, com controlo positivo (a nota da DGAL, que não é derivada, continua lá) |
| I36 | etapa 2l | **A primeira página deixou de render `painel_total` e `painel_com_limiar`, e o portão continua a recontá-las.** A manchete da Emenda 16 leva `painel_fora_do_limiar` e `painel_dentro_do_limiar`; o rótulo do âmbito, que levava `painel_total`, deixou de contar porque com dois painéis na página um número ao lado de «painel europeu» diria o do primeiro como se fosse o dos dois. `painel_social_total` entrou e também não se rende. Não é um defeito — `painel_com_limiar` já era assim antes desta ronda, e o portão reconta as quatro na mesma —, mas são agora **três** chaves da prova que nenhuma página mostra | `src/lib/prova.mjs` | aberto; é registo e não construção. Se a direção quiser as contagens à vista, o sítio onde elas cabem é a lista do Painel Social e o rótulo do âmbito, e isso é forma |
| I37 | etapa 3, commit 3-0 | **Um byte NUL literal em `src/lib/ledger.mjs` faz o `grep` tratar o ficheiro como binário** (`DECISIONS.md` §4, item «20.08 Y»). Medido, e não pelo nome: `grep -n "eDerivada" src/lib/ledger.mjs` não imprime nada e `grep -an "eDerivada"` imprime duas linhas (374 e 408); uma varredura de bytes de controlo dá **um** byte, o `\x00` no deslocamento 79478, linha 1737, `const chave = [v.date, v.path, v.by, v.result].join('\u0000');`, onde o NUL está escrito como carácter e não como escape | `src/lib/ledger.mjs` | aberto, e **não corrigido de propósito**: o plano §8 e o brief da etapa 3 §2 autorizam a correção de uma linha **só se a etapa editar aquele ficheiro por outra razão**, e o commit 3-0 não editou. A correção é trocar o carácter por `'\u0000'`. Fica para a etapa que abrir o ficheiro |
| I38 | etapa 3, commit 3-0 | **A soma dos concelhos rende duas vezes em `/municipios`.** A frase da contagem publica «São 308 ■ fonte concelhos…» e a linha das parcelas, por baixo, fecha com «Total 308 ■ fonte». As duas são a mesma linha do livro-razão, `municipios-portugal-caop-2025`, a três linhas de distância. A linha das parcelas é o texto exacto da decisão 4 da direção («Continente 278 · Açores 19 · Madeira 11 · 308») e por isso fica como está | `src/views/MunicipiosView.astro` | aberto, e com saída marcada: a **3c** compõe a frase da contagem com as duas chaves da prova que o brief lhe pede (`municipios_com_pagina`, `municipios_total`), e a soma com selo fica só na linha das parcelas |
| I39 | etapa 3, subetapa 3a | **A marca de água do recibo transbordava a página, e a caixa que a corta não a apanhava.** A primeira versão rodava a CAIXA da marca (`inset: 0`, do tamanho do bloco): os cantos de uma caixa rodada saem para fora dela, e o `overflow: hidden` da própria caixa não os apanha, porque a transformação se aplica depois do corte. Medido na linha com marcador: **23px** de transbordo a 320, **22px** a 390 e **2px** a 768, nas duas edições, contra **0** na construção de referência `df76a5f` — regressão desta subetapa, apanhada pela célula do transbordo | `src/styles/linha.css` | **fechado na mesma subetapa**: a caixa fica quieta e corta, e o que roda é o texto, num `::before` cujo conteúdo vem de `data-marca` (a cadeia `POR_VERIFICAR`, escrita pela vista e não pela folha). Medido depois: **30 de 30** combinações (3 linhas × 2 edições × 5 larguras) a zero |
