# BRIEF · Correções pequenas, sexta passagem (o nome de cada linha, o subconjunto dos tipos, I37)

*Escrito a 29.08.2026 pelo lugar de direção (Claude Fable 5) para o construtor (Claude Opus). Ramo `pequenas-6-2026-08-29` saído de `main` `58cc881`. Sem travessões na prosa.*

## 0 · As três

1. **O nome de cada linha, vindo do rótulo da fonte.** O motor (ResearchHub, `master` `1869d48`, `RELATORIO-P4.md`) deu a 1 553 das 2 536 linhas que atravessam um campo `name` (o rótulo sob o qual a fonte publica o número, copiado carácter a carácter do ficheiro alojado) e `name_source` (onde no ficheiro foi lido); as outras 983 não têm rótulo impresso e ficam sem o campo. O sítio recusa hoje qualquer chave fora da lista fechada `CAMPOS` (`src/lib/ledger.mjs`, linha 87) e publica só `CAMPOS_PUBLICADOS` (linha 123). O trabalho: `CAMPOS` e `CAMPOS_PUBLICADOS` aprendem as duas chaves (opcionais; `name` uma cadeia sem quebras, `name_source` um localizador no mesmo formato dos outros); o conjunto de dados descarregável ganha as duas colunas e `scripts/check-dados.mjs` passa a exigi-las; a validação confere que `name`, quando existe, é uma cadeia e não um número nem uma marca; depois corres, a partir de `~/Instruments/ResearchHub`, `python3 publisher/export_site_rows.py --manifest publisher/manifest.concelhos.json --write`, e o mesmo para `manifest.evora.json` e `manifest.regioes.json`, que escrevem as linhas em `ledger/claims/` deste repositório (esperado: 1 537 + 12 + 4 linhas alteradas, só pelas duas chaves; nenhuma nova; conta e prova), sem tocar em mais nada no motor. Na superfície: `ItemDoLivro.astro` e os índices do livro-razão mostram `name` como o nome da medida quando existe (na língua da fonte, com `lang` quando não é a da página, como os títulos), e o identificador passa a atributo e ao recibo; onde não existe, tudo fica como está (o título do documento). As páginas de concelho, que já têm nome pela definição da medida, não mudam. O inventário classifica o que entrar (os nomes são origem, não prosa da casa: confirma pela régua); a régua `check:cruzamento` confere `name` contra o registo do cruzamento como confere os outros campos.
2. **O subconjunto dos tipos.** O estudo tipográfico mediu que os oito ficheiros de `public/tipos` pesam 694,8 KiB sem subconjunto e 405,3 KiB cortados ao latim com as features todas (`fontTools.subset --layout-features='*'`, `brotli`). Faz o corte sobre os ficheiros de origem (os TTF ou os WOFF2 atuais, o que o repositório tiver; diz de onde vieram), mantém o `OFL.txt` ao lado, e prova que nenhum glifo usado no sítio se perdeu: lê todos os caracteres distintos de `dist/` (as duas edições, os cartões incluídos) e confere que cada um tem glifo no ficheiro cortado da família que o rende (uma célula de teste, com um caso plantado: um carácter fora do subconjunto visto vermelho). Regista os bytes antes e depois por ficheiro e o total.
3. **I37 · o byte nulo.** A linha I37 de `ISSUES.md` nomeia um ficheiro com um byte NUL (a passagem de higiene mediu-o no deslocamento 85 259). Abre o ficheiro, tira o byte, prova que nada mais mudou (o resto igual byte a byte), e acrescenta à régua que já lê esse ficheiro a recusa de bytes de controlo, vista vermelha num caso plantado. Fecha I37 com o commit.

## 1 · O que é «feito»

* As três verificáveis: as linhas com `name` contadas em `ledger/claims/` iguais às do motor; a superfície a mostrar o nome onde existe, medido numa página de área e numa de linha nas duas edições; os bytes dos tipos antes e depois; o byte nulo fora e a régua a recusá-lo.
* `npm run build`, `verify`, `typecheck` verdes; o inventário atualizado se algo entrar; uma linha `| pequenas-6 | n | por ler | … |` em `critica/REVISOES-DO-INVENTARIO.md`; ISSUES: I37 fechada.
* Commits com caminhos explícitos e os dois trailers (`Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`, `Claude-Session: https://claude.ai/code/session_01BbaH3XteKcsmmN9VD6SGwU`); `DECISIONS.md` é do lugar de direção; relatório em `design/especime-v3/medicoes/pequenas-6-construtor.md` com as contagens e o custo.
