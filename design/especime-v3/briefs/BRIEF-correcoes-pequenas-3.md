# BRIEF · Correções pequenas, terceira passagem (I83, I84, I88)

*Escrito a 28.08.2026 pelo lugar de direção (Claude Fable 5) para o construtor (Claude Opus). Corre depois da fusão do bloco dos vazios, num ramo `pequenas-3-2026-08-28` saído de `main`. Sem travessões na prosa.*

## 0 · As três

1. **I83 · o rótulo de acessibilidade da porta de uma figura.** Numa página de leitura, `<a href="#linha-tc-year-1-2008" aria-label="linha do motor: tc-year-1-2008">`: um leitor de ecrã ouve o identificador interno do motor. O rótulo passa a nomear o que a porta abre para um leitor: «a linha desta figura» / «this figure's row» (uma frase da casa, classificada no inventário como navegação), e o identificador fica só no `href`. São 34 cadeias distintas nas duas edições; a régua da voz normaliza-as hoje para «linha do motor: <linha>», e essa normalização sai quando as cadeias saírem.
2. **I84 · a ordem por pontos de código, o lado do sítio.** Os cabeçalhos dos grupos por distrito em `/municipios` (`src/views/MunicipiosView.astro`) ordenam-se com a colação portuguesa (`Intl.Collator('pt')`), como a lista por baixo do mapa já faz. O lado do motor já está escrito (`087fbcc` no motor: as 29 unidades e os concelhos de cada uma na colação portuguesa; só a ordem muda, a geometria é igual byte a byte, e o gerador confere a ordem contra o `Intl.Collator('pt')` em node antes de escrever): corres, a partir de `~/Instruments/ResearchHub`, `python3 publisher/mapa_distritos.py --write`, que escreve `mapa/pais.json`, os 29 `mapa/distritos/<slug>.json` e `mapa/manifest.json` neste repositório (esperado: só `pais.json`, `aveiro`, `lisboa`, `porto`, `santarem` e o manifesto mudam, com os mesmos bytes por ficheiro). `check:mapa` ganha a regra R7, que compara a ordem dos caminhos de cada `svg` e a das unidades do manifesto com a ordem do `Intl.Collator('pt')` sobre os nomes; prova-a vermelha na construção de `main` antes do `--write` (a de hoje ainda tem a ordem por pontos de código) e verde depois.
3. **I88 · a frase de Évora.** `strings.mjs`, `tempoSerieA`: «O índice de dívida da Direção-Geral desceu de » passa a «O índice de dívida, calculado sobre os dados da Direção-Geral, desceu de », e a inglesa «The directorate-general’s debt index fell from » a «The debt index, computed on the directorate-general’s data, fell from »; confirma que a frase de subida (`tempoSerieB` ou o que exista) tem o mesmo tratamento; o inventário reclassifica as cadeias que mudam.

## 1 · O que é «feito»

* As três verificáveis na construção: nenhuma `aria-label` com «linha do motor» em `dist/`; os cabeçalhos de `/municipios` na ordem do `Intl.Collator('pt')` (teste com um caso plantado fora de ordem visto vermelho); a frase de Évora nova nas duas edições e a antiga a zero em `dist/`.
* `npm run build`, `verify`, `typecheck` verdes; `check:voz` verde com o inventário atualizado (as cadeias que saem `retirada`, as que entram classificadas, bloco `pequenas-3`); uma linha `| pequenas-3 | n | por ler | … |` em `critica/REVISOES-DO-INVENTARIO.md`.
* Commits com caminhos explícitos e os dois trailers (`Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`, `Claude-Session: https://claude.ai/code/session_01BbaH3XteKcsmmN9VD6SGwU`); `DECISIONS.md` é do lugar de direção. Relatório em `design/especime-v3/medicoes/pequenas-3-construtor.md` com as contagens antes e depois e o custo em símbolos.
