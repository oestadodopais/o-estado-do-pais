# BRIEF · pós-fusão · I53, segunda forma: o `vercel.json` inteiro em `routes`, com sonda na pré-visualização e a conferência permanente em `verify:deploy`

*For the builder (Claude Opus), 22.08.2026, branch `pos-fusao-v3` at HEAD `12cb589` or later. Managing seat: Claude Fable 5. Read first: `vercel.json` (as it is: `headers` with the alias `noindex` and five security headers; `redirects` with three host 308s), `src/pages/en/404.astro` and its comment (what was built and why the `routes` block was removed), `design/especime-v3/ISSUES.md` I53 (the measurement of 22.08: with a `routes` block present, Vercel stopped applying the `headers` block), `DECISIONS.md` §1.62 and its addendum, `scripts/verify-deploy.mjs` whole (the launch gate: reads `version.json` from the live site and compares with `origin/main`), `README.md` §Deploy, `design/especime-v3/notas/pos-fusao.md` §I53. The director's word (22.08, «go») on the seat's plan: one routing system, proven on the preview with a probe, and the production checks made permanent. No em dashes in anything you write.*

## 0 · The facts that bind this

- Vercel's `vercel.json` reference (read 22.08) gives the route object: `src` (PCRE), `dest`, `headers`, `status`, `continue`, `has` (array of `{type, key, value}`; `type: "host"` for the host), `methods`; `{ "handle": "filesystem" }` is deprecated but supported and is the form of Vercel's own KB article for a custom 404 (`{ "src": "/(.*)", "status": 404, "dest": "/other-404.html" }` after the filesystem handle). Routes are processed in order.
- Measured 22.08 on preview n.º 3: **with a `routes` block present, the `headers` block is ignored** (the four house headers vanished; the old `redesenho-v3` preview without `routes` keeps them). Assume the same for `redirects`. Therefore everything moves into `routes`, and nothing stays in `headers`/`redirects`/`rewrites`.
- The preview host is `o-estado-do-pais-git-pos-fusao-v3-nunos-projects-b945a519.vercel.app` (protected; the seat reads it in the director's Chrome by same-origin `fetch`). Host-conditioned rules for the production hosts cannot be exercised there; the probe below proves the mechanism on the preview host.

## 1 · What to build

**`vercel.json`** becomes `$schema` + `routes` only, in this order, each with a one-line `//`-free comment impossible in JSON, so the explanation lives in `README.md` §Deploy instead:

1. the five security headers on every response: `{ "src": "/(.*)", "headers": { "X-Content-Type-Options": "nosniff", "Referrer-Policy": "strict-origin-when-cross-origin", "X-Frame-Options": "SAMEORIGIN", "Strict-Transport-Security": "max-age=15552000", "Permissions-Policy": "camera=(), microphone=(), geolocation=()" }, "continue": true }` (values copied from the current file, not retyped from memory: diff them);
2. the alias `noindex`: `{ "src": "/(.*)", "has": [{ "type": "host", "value": "o-estado-do-pais.vercel.app" }], "headers": { "X-Robots-Tag": "noindex" }, "continue": true }`;
3. the three host redirects, one route each, `status: 308`, `headers: { "Location": "https://xn--oestadodopas-2fb.pt/$1" }`, `has` host as in the current `redirects` (copy the three host values byte for byte);
4. **the probe, temporary**: `{ "src": "/sonda-308", "has": [{ "type": "host", "value": "o-estado-do-pais-git-pos-fusao-v3-nunos-projects-b945a519.vercel.app" }], "status": 308, "headers": { "Location": "/" } }` and `{ "src": "/(.*)", "has": [{ "type": "host", "value": "o-estado-do-pais-git-pos-fusao-v3-nunos-projects-b945a519.vercel.app" }], "headers": { "X-Sonda": "host" }, "continue": true }`: they prove on the preview that `has: host` selects, that `status: 308` + `Location` redirects, and that a host-conditioned header with `continue` is applied. They are removed in a later commit before merge (say so in the file's README paragraph and in the note);
5. `{ "handle": "filesystem" }`;
6. the English 404: `{ "src": "/en/(.*)", "status": 404, "dest": "/en/404/index.html" }`.

The Portuguese `404.html` at the root keeps Vercel's default behaviour for everything else (verify in the KB text already quoted in ISSUES I53: «Emit a `404.html` file to your Output Directory and it will be served as the 404 page when a route does not match any other static file»). `src/pages/en/404.astro`'s comment is rewritten to describe the new state.

**`scripts/verify-deploy.mjs`** gains, after its existing checks and without weakening them, the **production invariants** (the site's canonical host `xn--oestadodopas-2fb.pt`, read from `site.config.mjs` as the script already does): (a) the five headers present with the exact values on `/` and on `/en/ledger`; (b) `X-Robots-Tag: noindex` on `https://o-estado-do-pais.vercel.app/` and **absent** on the canonical host; (c) the three 308s with the exact `Location` for `https://oestadodopais.pt/x`, `https://www.oestadodopais.pt/x`, `https://www.xn--oestadodopas-2fb.pt/x` (follow no redirects; read status and `Location`); (d) `/en/nao-existe` answers 404 with `<html lang="en"` in the body and `/nao-existe` answers 404 with `lang="pt-PT"`; (e) `/en/404` and `/404` exist. Each check prints observed against expected; the run exits 1 on any miss. Prove the checker can fail: run it with `--anfitriao <preview alias>` (add that option if the script has none) and show it failing on the 302s, and run it against current production and show exactly which checks pass today (all but the English 404, which is the point) and which fail (the English 404, expected until the merge). Keep the script network-only and outside `npm run build`, as it is.

**`README.md` §Deploy** rewritten for the new file: why `routes` only (the 22.08 measurement), the order and what each entry does, the probe and its removal, and that `verify:deploy` now asserts headers, redirects and the two 404s after every deploy.

## 2 · Local proof before the push

`python3 -c "import json; json.load(open('vercel.json'))"`; `npm run build` green (the five gates; `/en/404` is built and out of the sitemap, the cards cover it: already true at HEAD); `npm run typecheck` 0; `node scripts/ortografia.mjs --verificar` 0. No value, no string, no page moves. Commit once with the trailers (`Co-Authored-By: Claude Opus <noreply@anthropic.com>`, `Claude-Session: <your session URL or [verify]>`), Portuguese message in the house's voice. **Do not push**: the seat pushes, reads the preview in the director's Chrome (headers on five page families, the two 404s, `/sonda-308` → 308 `Location: /`, `X-Sonda` present), and only then you (or the seat) remove the two probe routes in a second commit.

## 3 · Records and exit

`ISSUES.md` I53 (the second form, with the probe results filled in by the seat), `notas/pos-fusao.md` §I53 (append), `DECISIONS.md` §1.62 addendum (two lines: the second form, the director's «go» of 22.08). Report in at most 30 lines: the new `vercel.json` verbatim, the `verify:deploy` additions and their two runs (preview as the known-negative, production as today), the commit, model and tokens. Rough scale: 120k to 200k. Stop and ask if a current header or host value cannot be carried byte for byte, or if `verify:deploy` would need to follow a redirect to decide anything.
