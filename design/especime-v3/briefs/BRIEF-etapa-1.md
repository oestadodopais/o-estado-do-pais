# BRIEF · etapa 1 · foundations: the two constitution entries, self-hosted type, tokens v3, shared chrome

*For the stage-1 builder (Opus). Written by the managing seat (Fable), 20.08.2026. You own, for the whole phase, the shared files listed in §2; later stages ask through the seat before touching them. Read order: `../PLANO-redesenho-v3.md` §0 to §5 (stage 1) and §12; `../direcao.md` §2, §3, §4 and the Emendas 1, 5, 7; `IDENTIDADE.md` §1, §2, §5, §8, §9, §10; `DECISIONS.md` §1.49 (the head, for the entry format) and the tether comment in `scripts/check-ledger.mjs` (lines 85 to 140); `src/styles/tokens.css`, `src/styles/site.css`, `src/layouts/Base.astro`, `src/components/{Masthead,SiteFooter,Claim,Provenance,Frase,PortaDeCorreccoes}.astro`; `scripts/medir-contraste.mjs`; `vercel.json`. Then the boards `../maquetas/V3Completo.dc.html` (its `<style>` block is the type and token reference; its markup is stage 2's, not yours) and `../maquetas/Linha.dc.html` for the receipt's type.*

## 1. Rules that bind every line you write

- **Nothing outward.** Commit locally on the branch `redesenho-v3`, one commit per substage, in the house style (Portuguese subject line with the area prefix, a body that says what and why, and the two trailers `Co-Authored-By: Claude Opus <noreply@anthropic.com>` and `Claude-Session: <this session's URL, given below>`). No push, no deploy, no change to `vercel.json`.
- **No governed byte moves.** `src/data/metodo.mjs` and `src/data/sobre.mjs` are not yours. `IDENTIDADE.md` is yours in §1 and §2 only, and every sentence it quotes from the Método between «…» followed by (`metodo`) stays exactly as it is (the tether reads it; §5 has the one quotation). Run `npm run ledger:check` after every edit to `DECISIONS.md` or `IDENTIDADE.md`.
- **No new gate.** If you need a check, it is an extension of `gate:html`, `ledger:check` or `check:dados`, proven on a planted defect that you revert, and it is in your note. If you think you need more than that, stop and write it in the note; do not build around it.
- **No invented content.** Every number you write in a document (a contrast ratio, a width, a digest) comes from a measurement you ran and whose command is in the note. A fact you did not verify is written `[verify]`.
- **English with the Portuguese**, same commit, considered, not copied. `assertKeyParity()` throws at every build if the keys diverge. Append every new key to `../CHAVES-EN.md` under «Etapa 1».
- **The stage note** `../notas/stage-1.md` is written at the checkpoint after substage 1c, before you audit anything, and completed at the end. If your session is cut, the note is what survives.
- **Read the scripts, not their names.** When this brief says what a check does, confirm it in the file before relying on it.

## 2. The files you own (the shared set, for the whole phase)

`src/styles/tokens.css`, `src/styles/site.css`, `src/layouts/Base.astro`, `src/components/Masthead.astro`, `src/components/SiteFooter.astro`, `src/components/Claim.astro`, `src/components/Provenance.astro`, `src/components/Frase.astro`, `src/components/PortaDeCorreccoes.astro`, `public/tipos/**`, `scripts/medir-contraste.mjs` (the `PARES` array), `IDENTIDADE.md` §1 and §2, `DECISIONS.md` (one new entry, §1.50), `design/especime-v3/TIPOS.md`, `design/especime-v3/notas/stage-1.md`, `design/especime-v3/CHAVES-EN.md` (your section), `design/especime-v3/ISSUES.md` (append only). `src/i18n/strings.mjs`: only keys you need for the chrome. Nothing under `src/views/` and no other component: if a page breaks visually because a token it used is gone, you fix it in `site.css` by remapping the rule (§5), not by editing the view.

## 3. Substage 1a · the two constitution entries, first (commit 1)

Write `DECISIONS.md` **§1.50**, after §1.49, in the grammar of §1.48 and §1.49 (Portuguese, AO90, no em dashes except inside quotations). First line under the title: `**Afecta:** nenhum`, with the same parenthetical reasoning §1.49 uses (`IDENTIDADE.md` is the constitution, not a governed text in the tether's sense). The entry records, with the why: (i) the redesign v3 phase 1 opens on the branch from base `77e82eb`, plan at `design/especime-v3/PLANO-redesenho-v3.md`; (ii) `IDENTIDADE.md` §1 changes: the three system stacks and «Sem tipos de rede» become «Só tipos alojados aqui» (self-hosted only, no third-party font hosts; the families enter by recorded decision and live in the repository with their licence); the three roles become: Spectral for prose and the wordmark, Bitter for values, labels, axes and the transcribed, Spectral SC for editorial eyebrows and section labels; Bitter in capitals only inside instruments; tabular lining figures in instruments; the rule «a monospaced face marks a ledger value» becomes «Bitter marks a ledger value, through `<Claim>`»; (iii) `IDENTIDADE.md` §2 changes: the two accents become the state pair, colour only where the source publishes a formal threshold (Emenda 1): amber `#E0A21A` as the marker with an ink contour (amber on paper measures 2,09:1, which is why the contour exists) and ochre `#7A5300` as the state word = fora do limiar; cobalt `#1F4E8C` = dentro do limiar; ink and grey for everything else; `--yellow` retires as a token and as a meaning; `--oxblood` retires and the correction register says a correction by form (old value struck through in grey, new value in ink, the date), with the provenance form of its values pending the director's decision (c) of the plan; the time signal in the masthead is ink, by Emenda 1 over the constitution's body §3, flagged to the director. Update §8's sentence that lists unchecked stylesheet rules («`--yellow` nunca como `color`») to name the new tokens. Do not touch §5's quotation. Do not write the Método sentences (they are the director's to approve, plan §12). Run `npm run ledger:check`: green means the tether accepted the entry.

## 4. Substage 1b · the type, self-hosted (commit 2)

1. `vercel.json` was read in stage 0: no Content-Security-Policy header, no font policy; same-origin fonts load. Confirm by reading it yourself and say so in the note.
2. Fetch upstream bytes. Preferred upstream: the families' own repositories (`[verify]` at fetch time: Spectral and Spectral SC by Production Type, Bitter by Huerta Tipográfica / Sol Matas, all under the SIL Open Font License 1.1). If the upstream repository does not carry built binaries, the `google/fonts` repository paths `ofl/spectral/`, `ofl/spectralsc/`, `ofl/bitter/` are acceptable as upstream **only** with the repository commit SHA recorded (`git ls-remote` or the GitHub API) and the exact path of each file. Record, in `design/especime-v3/TIPOS.md`: family, upstream URL, commit, file path, SHA-256 of the upstream bytes, licence file and its SHA-256, designer and licence as stated in the file's name table (`fontTools` can read it), the conversion tool and version.
3. Ship the minimal style set the constitution needs and the boards use: Spectral Regular, Italic, Medium, SemiBold (and Bold only if a rule uses it); Spectral SC Regular and SemiBold; Bitter as the variable font (roman; italic only if a rule uses it). Convert **losslessly** to WOFF2 with `fontTools` (`font.flavor = 'woff2'; font.save(...)`; fonttools 4.61.1 and brotli are installed on this machine), **no subsetting**, no glyph removal, no renaming (the OFL's reserved names). Put the files in `public/tipos/<familia>/` with `OFL.txt` beside each family, copied from upstream. Verify after conversion that the glyph count and the `name` table of the WOFF2 equal the upstream TTF (fontTools), and write the numbers in `TIPOS.md`.
4. `@font-face` rules at the top of `tokens.css` (local paths, `font-display: swap`, `font-weight` ranges for the variable Bitter, `font-style` per file), and real fallback stacks: Spectral → Georgia, 'Times New Roman', serif; Bitter → Rockwell, 'Roboto Slab', Georgia, serif; Spectral SC → Spectral with `font-variant-caps: small-caps` as the last resort. Preload only the two faces the first paint needs (Spectral Regular, Bitter variable) in `Base.astro`.
5. Measure Bitter's `tnum` on the built page: render two strings of equal glyph count with different digits («1111111» and «0000000» is the classic) in Bitter with and without `font-variant-numeric: tabular-nums`, in headless Chromium, and record the four widths. Measure Spectral's `onum` the same way (the design session found it inert in the Google build; say what you find). Playwright: `npm i -D playwright` (devDependency, used only by scripts outside the build; the Chromium binaries may already be in `~/Library/Caches/ms-playwright`, otherwise `npx playwright install chromium`). Put the measuring script in `scripts/medir-tipos.mjs` (a ruler, not a gate; outside `npm run build`).
6. After the build, `grep -r "fonts.googleapis.com\|fonts.gstatic.com" dist/` must print nothing, and the note says it ran and printed nothing (the command, not the adjective).

## 5. Substage 1c · tokens v3 and the re-skin of the shared stylesheet (commit 3; the checkpoint note is written after this one)

1. `tokens.css`, light block: `--paper: #F6F7F4; --ink: #17191B; --g1: #585D5B; --g2: #7F8681; --g3: #D9DDD8; --amber: #E0A21A; --ochre: #7A5300; --cobalt: #1F4E8C;` plus the derived tokens the sheet needs (`--rule` = `--g3`, `--rule-strong` = `--g2`, `--muted` = `--g1`, `--axis` = `--g2`, `--focus` = `--ink`, `--onamber` = `--ink`). There is no `--paper-2` and no `--paper-3` in v3: the boards separate the apparatus with hairlines and grey frames, not with tinted panels (Emenda 1), so every rule that used them is remapped to `--paper` plus a `--g3` frame or a hairline; a second paper would be an invented value with no line in the constitution. Keep the three-state theme mechanism exactly as it is (the `@media` block and the `[data-theme='dark']` block must stay equal, the contrast ruler checks that). The dark block carries the **proposal** of the plan §3 (f): paper `#15171A`, ink `#ECEEEA`, `--g1 #B9BEBA`, `--g2 #8E948F`, `--g3 #3A3F3C`, amber `#E0A21A`, the ochre word in dark = the amber (ochre fails on dark paper), cobalt word `#7FA6DC`, cobalt marker `#1F4E8C` with ink contour; under a comment that says in one line that this block is a proposal awaiting the director's word (plan §3 f) and will change by his decision. Remove `--yellow`, `--oxblood`, `--onyellow`, `--dotcol`, `--shadow` if no rule uses them after your pass; remove the three `--f-*` system stacks and declare `--f-prosa`, `--f-instr`, `--f-versal`.
2. `site.css`: the re-skin of the shared rules, and the remap of every rule that used a retired token: every `--yellow` use (the lit município, the composition bars, the distance bar, the chips' read state, the calendar windows) → `--ink` or `--g2` per the rule «instruments are drawn in ink and grey; colour only where a published threshold is compared»; every `--oxblood` use (the correction register, the correction labels) → `--ink`, with no form change yet (stage 4 and decision c). After the pass, `grep -n "yellow\|oxblood\|--f-sans\|--f-serif\|--f-mono" src/styles/site.css src/components src/views src/layouts` prints nothing; the note carries the command and «0 lines». Base typography: prose in Spectral at `--t-leitura` 17,5px, line-height 1,58 (the v2 scale stays); values and furniture in Bitter with `font-variant-numeric: tabular-nums lining-nums`; prose figures `oldstyle-nums` only if your `onum` measurement shows Spectral has them; eyebrows and section labels in Spectral SC (lowercase text with `.05em` tracking as the boards do, weight 600). Frames of pieces and cards in `--g3` (Emenda 1: grey frames; state lives in marker, word and ruler). Focus ring `2px solid var(--focus)` unchanged in contract.
3. `scripts/medir-contraste.mjs`: rewrite `PARES` for the new tokens, every pair the sheet uses, with `onde` naming the rule; add the pairs amber/paper (interface), ochre/paper (text), cobalt/paper (text), amber/cobalt (interface, the state pair's own distinction), ink/amber (the contour), cobalt-word/paper in dark, and the frames (`--g3`/paper, decoration). Run it; the light palette must show 0 text failures; in dark, 0 text failures with the proposal; interface pairs below 3:1 are listed with why (a decorative frame is not an interface object). Paste the two tables into the note.
4. Build. Every page must still pass the gate (no markup changed yet). Run `node scripts/medir-defeitos.mjs` and save both rulers' `--json` to `design/especime-v3/medicoes/2026-08-20-etapa-1c-<sha>-{defeitos,contraste}.json`.

## 6. Substage 1d · the shared chrome (commit 4)

1. `Masthead.astro`: the neutral head of the constitution §4 under Emenda 1: wordmark in Spectral (the `h1`/`p` rule stays), the method line under it, the two labelled readings (time signal and agenda) **in ink**, no colour, the words already there; the navigation as a list rendered from an array of route keys so that it takes seven or eight items (decision a pending: ship seven, leave the eighth as a one-line change you point to in the note), in **Spectral SC** (editorial small caps, lowercase text with `.05em` tracking, weight 600), not the board's Bitter capitals: Emenda 5 keeps Bitter capitals inside instruments only, and the navigation is not an instrument (boards never win); measured at 320, 390 and 1280 with eight items so the decision cannot break the layout; the language link keeps `hreflang` and `lang`, and on the home it must carry the page's query string through (stage 2 will depend on it: read `Astro.url.search` and append it to `outra` only on the `home` route key). Compact variant on interior pages: the wordmark shrinks to one line (the 250px head ends).
2. `SiteFooter.astro`: navigation only (the decision of 15.08); confirm nothing else renders; Spectral SC as the masthead's navigation.
3. The seal. Read `Claim.astro` and `Provenance.astro` and the gate's `auditaSelo()` and `seloDaLinha()` (`scripts/gate-html.mjs` lines 1373 to 1520) before touching anything: the class `.src-chip`, the `data-selo-etiqueta` attribute, the visible text «fonte» / «source» and the marker, and the `href` to the row are all compared by the gate; keep them. What changes is form: the square (■ filled for complete provenance, □ dashed for incomplete, drawn in CSS or inline SVG as today) plus the word, the whole unit the link, sitting **at line height in prose** (no chip background, no border box; a hairline underline on the word is fine), Bitter small caps or capitals at 12px; a tap target of at least 44×44 CSS px through a positioned pseudo-element hit area that does not change the line box; and the seal **never nested** inside another link or button (a rule for stage 2's tiles; write it in the component's comment and in `IDENTIDADE.md` §5 if you are already editing §5, otherwise in the note for the seat). Verify in headless Chromium on one row page and on the home that the seal's bounding box of the clickable area is ≥ 44px in both axes; record the numbers.
4. `PortaDeCorreccoes.astro` and `Frase.astro`: type only.
5. Build; gate green; rulers; screenshots at 1280 and 390, both editions, both themes, of `/`, `/livro-razao/divida-publica-2025`, `/metodo`, `/municipios/evora` (the structure is still v2 on these pages; what you are showing is the type, the tokens and the chrome), saved under `design/especime-v3/capturas/etapa-1/` (PNG, named route-width-edition-theme).

## 7. Exit criteria (deliverables, not tokens)

- §1.50 in `DECISIONS.md`, `IDENTIDADE.md` §1 and §2 rewritten, `ledger:check` green.
- `public/tipos/` with the WOFF2 files, `OFL.txt` per family, `TIPOS.md` with upstream, commit, digests, glyph counts before and after conversion, tool and version.
- `tokens.css` v3 light, dark as the labelled proposal, three-state mechanism intact; `site.css` with 0 retired tokens and 0 system stacks; `PARES` rewritten; the two contrast tables.
- `tnum` and `onum` measurements with the four widths each.
- Masthead, footer, seal per §6; the 44px measurement; the eight-item measurement.
- `npm run build` green; `dist/` free of third-party font hosts; both rulers saved; screenshots saved.
- `notas/stage-1.md` with: what shipped, the commands you ran and their outputs (not adjectives), the deltas of both rulers against `medicoes/2026-08-20-base-77e82eb-*.json`, the EN keys added, ISSUES appended, which model ran which part (you are Opus; say so), and your token count as reported to you.

Rough scale: 250k to 350k tokens. If you pass 400k, stop at the next commit boundary, write the note, and say so.

Session URL for the trailer: https://claude.ai/code/session_01EY7AceRq6jcySfcNyXcBTb
