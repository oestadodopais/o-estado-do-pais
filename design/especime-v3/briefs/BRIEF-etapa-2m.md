# BRIEF · etapa 2m · the director's third reading of the home: the map that fills and grows, the lede that stays true

*For a fresh Opus builder (B8), to run right after stage 3 lands (it must not overlap with builder C, who owns `src/styles/inicio.css` for commit 3-0). Written 21.08.2026, afternoon, from the director's notes on the preview of `6039db2`. Read first: `BRIEF-etapa-2.md` §1 and §1b, `BRIEF-etapa-2l.md`, `../notas/stage-2.md` (2j to 2l) and `../notas/stage-3.md` (what 3-0 changed in the home), `../direcao.md` Emendas 10 to 17, `../INVENTARIO-FRASES.md`, `scripts/gate-html.mjs` (`contasDoPortao()` and the `data-prova` block), `src/lib/prova.mjs`, `src/lib/estado.mjs`, `src/data/figuras.mjs`, the home's components, `public/js/inicio.js`, `tests/inicio/matriz.mjs`. You own stage 2's files; the shared chrome is not yours.*

## 1 · The map fills its column and grows to choose

- **País (resting):** the map fills the right column of the head (the column is 1fr beside the 582px text mancha at ≥1024): scale the field to the column's width (keep the aspect; islands as insets at the same scale), points sized to the field; the line «308 concelhos · CAOP 2025 ■ fonte» (and nothing else) sits to the **right of the island insets**, in the space the insets leave, not under the map. Measure at 1024, 1180, 1280, 1440: the map's box, the line's position, overflow 0.
- **Município (choosing):** the map **expands to the full content width** (text column collapses above it; the search stays above the map), with the field scaled so that the nearest neighbours on the coast separate (measure the minimum centre-to-centre distance in CSS px at 1280 before and after; target ≥ 16px, report what you reach), the point targets enlarged accordingly, the islands' insets proportionally; scroll-wheel and pinch zoom inside the map box as a second path (transform on the SVG group, clamped 1× to 4×, the readout and keyboard model unchanged; double-tap resets); `Escape` or «fechar» returns to the column map. Mobile unchanged (stamp-door, search, proximity list). No magnifier lens.
- Everything prevalidated: no new text except the «fechar» control label (new key pair, PT «fechar» / EN «close»; if one exists, reuse it).

## 2 · The lede stays true at every build

The headline's counts are proof keys (`painel_fora_do_limiar`, `painel_dentro_do_limiar`) and recount themselves. The lede «Fora do limiar: dívida pública, …, em 2025.» is written, and becomes false the day a fifth indicator crosses. Make it **generated**: the list is the names (`figuras.mjs` `nome`, both languages) of the PDM pieces whose state is `fora`, in panel order, joined by the edition's list grammar («, » and « e » / « and »), followed by «, em <ano>» where the year is the rows' `reference_date` when all `fora` rows share it, and omitted when they differ (the pieces carry their own years). No digit is written by the template except the year, which renders as `{ ref }` as elsewhere. Extend `contasDoPortao()`'s `data-prova` block (not a new gate): the lede's list element carries `data-prova-lista="painel_fora_do_limiar"` and the gate counts its items (split on the separators it knows) and compares with the key; **plants**: a name removed from the list (count 3 vs 4), a name added (5 vs 4), each closing the build, each reverted. Do the same for any other sentence on the home that names a state or a count in prose (search the inventory for them; list what you found). EN and PT in the same commit; the generated sentence's pieces are prevalidated strings (names and grammar words), never runtime-formatted.

## 2b · Stage 3's requests to the home's owner (from `../notas/stage-3.md` §7), yours in this round

- `MapaRespira.astro`: remove the CSV door (the link now lives on `/municipios`; `check-dados.mjs` declares the route per file since 3-0: read it, and make sure the home edition still satisfies it or update the declaration in the same commit with a plant); add the `postura` and `escolhido` props the município page needs for its locator card (the card in dots, the chosen concelho as a ring), and wire `MunicipioView.astro`'s call site minimally (that file is stage 3's: the one line that passes the props, say which).
- Share the Évora piece mapper: one function in `src/lib/inicio.mjs` used by `HomeView.astro` and `MunicipioView.astro` (I44).
- `?ambito=municipio` (mode, no concelho chosen) becomes a valid state of the schema (the choosing view), so `/municipios` can link to it; update `inicio.js`, the matrix cell for invalid values, and the plan's §13 line through the seat (write the exact sentence in the note).
- Stage 3's captures (its 3e, not taken): `/livro-razao`, `/livro-razao/divida-publica-2025`, `/livro-razao/evora-indice-de-divida-2024`, `/livro-razao/avisos-pt2030-abertos`, `/municipios`, `/municipios/evora` at 1280 and 390, both editions, light, into `../capturas/etapa-3/`.

## 3 · Exit

Two commits («início, mapa: …», «início, lede: …»), build/verify green, the plants' messages recorded, the matrix re-run whole (`--json` to `medicoes/`), captures of País and the choosing view at 1024/1280/1440 and 390, the note appended to `notas/stage-2.md` under «2m», `CHAVES-EN.md` and `ISSUES.md` updated. Verification per the seat's policy of 21.08: only the matrix cells your changes touch during the round, the whole matrix once at the end; captures only of the states you changed plus stage 3's list. Rough scale: 250k to 350k tokens. Reply in at most 30 lines. Trailers as in the earlier briefs.
