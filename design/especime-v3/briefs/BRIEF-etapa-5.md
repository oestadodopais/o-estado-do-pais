# BRIEF · etapa 5 · share cards, and the phase's loose ends before the whole-branch read

*For the stage-5 builder (Opus, builder E), 21.08.2026. Read first: `../PLANO-redesenho-v3.md` §5 «Stage 5» (the director's contract, binding), §3 (decisions), `../direcao.md` Emendas 1 to 17 (Emenda 11: no method line on the card; Emenda 15: the card says nothing about the site; Emenda 10: the seal square is the only glyph), `../notas/stage-2.md` (2l: the panel and the proof keys), `../notas/stage-4.md` (§9 requests), `scripts/gate-html.mjs` (the `<head>` block that composes a row page's title/description/og from the row; the `data-claim` string rule; `contasDoPortao()`), `scripts/check-dados.mjs`, `src/lib/prova.mjs`, `src/lib/livro.mjs` (`tituloDaLinha`, `descricaoDaLinha`), `src/components/Claim.astro` (the value's formatting path), `src/layouts/Base.astro` (the og: tags), `public/tipos/` (the self-hosted fonts), `../maquetas/CartaoPartilha.dc.html` (the reference board, minus the method line).*

## 0 · Opening commit (5-0): stage 4's loose ends
- `src/components/inicio/Peca.astro`: the class `mun-tecto-rot` becomes `mun-teto-rot` (and its CSS), so `node scripts/ortografia.mjs --verificar` exits 0; quote the run.
- One fold glyph (I54): the constitution's glyphs are the square (evidence) and the dot (places); disclosure controls use one mark site-wide: the small triangle the v2 `.deep` fold already uses; the `+`/`−` of stage 4 becomes that triangle (CSS only); measure that every `<summary>` on `/`, `/metodo`, `/agenda`, `/livro-razao/divida-publica-2025` shows the same mark.
- I53 (no EN 404 route): leave as is, note why (Vercel serves one `404.html`; a per-edition 404 is a routing decision for the director).
- `IDENTIDADE.md` §2 quotes «A cor» with the (`metodo`) mark, word for word, so the tether guards it (stage 4's request); `ledger:check` green.

## 1 · The cards (the director's contract, plan §5, verbatim conditions)
One card per page and edition, 1200×630 (og) and 1200×600 (Twitter/X), rendered at build time **from the rows and the proof through the same component path the page uses**: the headline template and its proof-key counts, the value with unit · period through `Claim`'s own formatting function, the state words from the decided vocabulary; never a second formatter, never a hand-written string, never `og:` metadata typed by hand. Design per the board: paper `#F6F7F4`, ink, the wordmark small, the page's headline sentence in Spectral, for the home the state strip of squares (amber with ink contour / cobalt / outline) and for a measure page the value with unit · period, the seal square ■ as the constant footer mark; **no method line** (Emenda 11), no map dots, no colour outside the state pair, no sentence about the site. Scope for phase 1: the home (both editions) and the 264 row pages (both editions); the other routes get the home's card of their edition (say so in the record). Rasterisation: `@resvg/resvg-js` with the self-hosted WOFF2/TTF (a build dependency; verify it installs on Vercel's Node image by reading its docs offline-capable notes, or mark `[verify]` and make the step skip-safe with a hard failure in `gate:html` if a card is missing). Build step `scripts/cartoes.mjs` between `astro build` and `gate:html` in `package.json`'s `build`. Each card writes a record `dist/cartoes/<rota>.<lang>.<w>x<h>.json`: route, edition, dimensions, output digest, exact visible copy, each value with its row id or proof key, unit, period. `gate:html` extension (not a new gate): for every page, `og:image` and `twitter:image` name the card of its own route and edition; for every record, every value is re-derived from `ledger/claims/` and the proof and compared **as a string by the `data-claim` rule**; the visible copy contains no digit that is not one of those values; dimensions match. **Four plants**, each closing the build, each reverted, each recorded with the gate's message: a wrong number on a card, a value stale against its row, a card for the wrong route or language, a wrong dimension. Cards are build artefacts (`dist/` only; never committed). The state is written in words on the card as well as shown in the strip.

## 2 · Exit
Commits per substage (5-0, the renderer, the records and gate extension, the plants and measurements); build, verify, ledger:check green; one PT and one EN card of the home and of `divida-publica-2025` saved as PNG under `../capturas/etapa-5/` for the director; the record of one card quoted in the note; `notas/stage-5.md`, `CHAVES-EN.md` (the card's strings, if any are new), `ISSUES.md`. Rough scale: 300k to 450k tokens. Trailers as in the earlier briefs. Reply in at most 40 lines.
