# BRIEF · etapa 2l · the second reading of preview n.º 1: the page stops explaining itself, the panel becomes the whole MIP scoreboard, the map goes right

*For a fresh Opus builder (B7), 21.08.2026, morning. The director read the updated preview and decided; the constitution gained **Emendas 15 to 17** (vault is the source; verbatim text below; mirror it into `../direcao.md` in your first commit, including the correction inside Emenda 16). One builder for the shared chrome and the home again, by the seat's decision (nobody else is writing). Read first: `BRIEF-etapa-2.md` §1 and §1b (rules bind you word for word), `BRIEF-etapa-2j.md`, `../notas/stage-2.md` (2j, 2k), `../RELOCACOES.md`, `../CHAVES-EN.md`, `../ISSUES.md`, `IDENTIDADE.md` §5, §6, §7, §10, `scripts/gate-html.mjs` (`contasDoPortao()`, `auditaSelo()`, `seloDaLinha()`, the `data-prova` block), `scripts/medir-defeitos.mjs`, `src/lib/prova.mjs`, `src/lib/estado.mjs`, `src/data/figuras.mjs`, `src/i18n/strings.mjs`, the home's components (`src/views/HomeView.astro`, `src/components/inicio/*`, `InstrumentoConvergencia.astro`), `src/components/{Claim,Provenance,Masthead,PortaDeCorreccoes}.astro`, `public/js/*.js`, `tests/inicio/matriz.mjs`, and, read-only, `/Users/nunosantos/Instruments/ResearchHub/indicators/convergence.md` (the engine's framework comparison: which rows are MIP headline and which are Social Scoreboard; you do not edit anything in that repository). Rules as always: nothing outward (no push); no governed byte (`metodo.mjs`, `sobre.mjs`); no new gate; prevalidated strings only at runtime; PT and EN together; no invented numbers; build, verify and `ledger:check` green at every commit; conflicts written, not improvised around.*

## The Emendas, verbatim (vault, 2026-08-21, second reading)

> 15. **A página do leitor não se explica (§5 «Voz» emendado; revoga a legenda de neutralidade da Emenda 3 e as legendas de aparelho nas páginas de leitura):** uma página do leitor leva conteúdo (a medida, o valor, a unidade, o período, o nome da fonte) e navegação. Não leva nenhuma frase sobre o método, a verificação, a honestidade, a cobertura ou as intenções do próprio sítio. Uma legenda nomeia o que a coisa é («Concelhos, CAOP 2025»), nunca o que fizemos («contagem verificada») nem o que não afirmamos («não marcam qualidade nem importância»). O selo é toda a prova que a página mostra; o Método, o Sobre e o recibo da página de linha são onde o método vive. A ausência diz-se em duas palavras («sem linha»), não num parágrafo. O texto oculto do selo, para leitores de ecrã, encurta para «fonte · <estudo>». **Medida:** o inventário de todas as frases da casa na superfície pública, classificadas em conteúdo, navegação e autorreferência; a terceira classe vai a zero fora do Método, do Sobre e do recibo, e a régua imprime a contagem para que não volte. É o alvo das «frases de moldura ≤ 12» do §5, posto em prática.
> 16. **O painel da primeira página é o painel inteiro, não a sua metade má (§4 «A página inicial lidera» emendado):** o livro-razão guarda 13 linhas com limiar publicado do Procedimento dos Desequilíbrios Macroeconómicos (o painel da Comissão tem 13 indicadores principais no AMR de 2026, lido em economy-finance.ec.europa.eu a 21.08.2026; as 13 linhas cobrem 11 deles, porque o fluxo de crédito e a dívida do setor privado estão divididos em famílias e empresas, e faltam pelo menos o desemprego de longa duração e o desemprego jovem: pedido ao motor, não tarefa deste ramo); Portugal ultrapassa 4 e está dentro em 9. A primeira página mostrava só os 4. Passa a mostrar as 13, cada uma com o seu estado (fora · dentro), e a manchete leva as duas contagens sem afirmar um total do Procedimento: «Portugal ultrapassa 4 limiares do Procedimento dos Desequilíbrios Macroeconómicos e cumpre 9.» com a lede «Fora do limiar: dívida pública, posição de investimento internacional, custo unitário do trabalho e preços da habitação, em 2025.» (EN: «Portugal breaches 4 thresholds of the Macroeconomic Imbalance Procedure and meets 9.» / «Outside the threshold: government debt, net international investment position, unit labour cost and house prices, in 2025.»), as contagens por chaves da prova, «para já, para ver como funciona» (diretor; a primeira redação do lugar de direção dizia «4 dos 13» e «13 dos 14», e estava errada: corrigida na mesma madrugada contra a página da Comissão). O **Painel Social Europeu** entra como lista compacta por baixo (nome, valor, unidade, fonte, selo; sem cor, porque não tem limiares), com os indicadores que o livro-razão guarda e cujo registo nomeia esse painel; o resto do livro-razão fica atrás de uma porta. Nada escolhido pela casa: os dois painéis como as instituições os publicam, completos até onde o livro-razão vai.
> 17. **O mapa na primeira página (§4 emendado):** o texto à esquerda e o mapa à direita, como a prancha A2b, acima de 1024; por baixo do mapa uma só linha, «308 concelhos · CAOP 2025 ■ fonte»; a contagem por parcelas (278 · 19 · 11) vive em `/municipios`. A porta das correções reduz-se a «Encontrou um erro? correcoes@oestadodopais.pt · O registo de correções →»; a frase da política vive em `/correcoes`.

## Commits, in order

**2l-1 · records.** `../direcao.md`: the three Emendas appended verbatim after Emenda 14. `DECISIONS.md` §1.53 (`**Afecta:** nenhum`): the director's second reading, the three Emendas, the correction of the seat's «13 dos 14», what this round implements, the engine ask (long-term and youth unemployment rows; the 13th headline indicator's identity to be confirmed against the Commission's list in the engine, not here). `ledger:check` green.

**2l-2 · the panel is the whole MIP scoreboard, and the Social Scoreboard list.** `figuras.mjs` becomes two declared lists with the framework named per entry: `FIGURAS_PDM` = the 13 rows whose `note` states «Limiar do Procedimento relativo aos Desequilíbrios Macroeconómicos» (find them with `grep -l "Limiar do Procedimento" ledger/claims/*.yml`; the list is written by hand from that grep, each entry with `claim`, `nome` PT/EN, `medida` PT/EN (the unit line, from the row's `unit`, figures only as `{ ref }` or `{ nl, motivo }` as today), `limiar` with `nl`, `sinal`, `lado` declared from the threshold in the note: `saldo-da-balanca-corrente-2025` has a two-sided band −4/+6 and `taxa-de-cambio-efectiva-real-2025` ±3: extend `limiar` to carry `inferior` and `superior` bounds and `estadoDaMedida()` to treat a band (within both bounds = dentro), with a plant for each direction; `taxa-de-actividade-2025`'s threshold is −0,2 pp (the note says «-0.»: read the row's note and the Commission's page cited in Emenda 16 before writing the number; if the note truncates it, the threshold renders from the note as written and ISSUES gets the engine ask to fix the note; never a value the row does not carry), no `frase` unless the row's existing prose supplies one (the four current sentences stay by R1; the nine new pieces carry no sentence: name, value, unit, state, seal); `FIGURAS_SOCIAL` = the rows the engine's `convergence.md` marks in its «Social SB» column among the `quadro-institucional` rows (read the table; list each row with the line of the document that places it; a row the document does not place is not in the list; the four current social pieces keep their sentences by R1, the others carry none). The proof keys: `painel_total`, `painel_com_limiar`, `painel_fora_do_limiar` recount over `FIGURAS_PDM`; add `painel_dentro_do_limiar` (recounted by the gate; plant: flip one state) and `painel_social_total`; the doors unchanged. The panel renders the 13 PDM pieces in the grid (4 columns at 1280, so 3 rows + 1, the last row's remaining cells as an explicit state per `IDENTIDADE.md` §7: either a 4-wide grid that closes with the Social list's head, or a 13-cell grid whose last row is left-aligned with no empty frames since there are no frames; say which), each with marker, state word («fora do limiar» / «dentro do limiar», the decided vocabulary; the threshold line «limiar 60% · acima» stays as today's relocated line where it exists and is written for the nine new ones from the row's threshold with the same `limiar-do-quadro` motive), value, unit line, name, the sentence where one exists, the seal at the foot; Leitura breve adds the ruler with the one grammar for every piece (13 thresholds, 13 rulers; the band thresholds drawn as two references). Below the panel, the **Social Scoreboard list**: a heading «Painel Social Europeu» (content: the scoreboard's name), one row per measure: name, value (`<Claim>`), unit · period, source name, the seal; no colour, no state word, no sentence except the four relocated ones in Leitura breve if you keep them (your call, documented). Below it one door «O livro-razão →» (`/livro-razao`) for everything else. The headline and lede exactly as Emenda 16 gives them, the counts as `data-prova` figures (`painel_fora_do_limiar`, `painel_dentro_do_limiar`). Relocation register: the 9 + N new pieces are new surfaces for existing rows (no sentence moves): record them as R7 (PDM rows from `/livro-razao/<id>` to `/`, 1 → 1 + 1 each) and R8 (Social rows likewise); the four v2 sentences stay under R1.

**2l-3 · the page stops explaining itself (Emenda 15), and the map goes right (Emenda 17).** Remove from the home, both editions, every self-referential sentence and caption; the known list, to which you add what you find: the coverage line «1 de 308 concelhos · tem página»; the neutrality sentence; «Contagem verificada nos ficheiros» and the per-parcel table (the counts stay on `/municipios`: check `MunicipiosView` already renders them; if not, leave them there as a request for stage 3, not on the home); the «Método, ressalvas e proveniência» fold of the map (its CSV door moves to `/municipios`: request for stage 3 if not there; the CAOP citation lives on the CAOP row's page already); the hover and keyboard hints (keep them as `aria-describedby` text for the map's accessible description, not as visible captions); «Painel europeu reconferido a …» under the panel (the masthead carries it); «o recibo completo está na linha»; «Sem referência publicada: não há barra a desenhar.» (the piece shows «sem limiar» and no ruler); the empty-municipality explanatory sentence (the eight empty pieces with «sem linha ainda» speak); the doors' label «As páginas · o resto vive a uma porta» (the three doors need no caption); the explanatory sentence of the proximity gesture (the buttons speak; keep an `aria-label`); the band's «As regiões não se desenham em pontos de concelho…»; any «calculado» or «distância calculada» caption that explains rather than names (the seal's label carries «calculado ·» already). The correction door (`PortaDeCorreccoes.astro`, shared, yours this round) becomes «Encontrou um erro? correcoes@oestadodopais.pt · O registo de correções →» in both editions; the gate requires the address visible and exactly one door per page: read that block of `gate-html.mjs` first and keep the `data-porta-correccoes` contract. The seal's hidden text (`Provenance.astro`/`Claim.astro` and `seloDaLinha()` in the gate): the key `prov.verLinha` «Linha do livro-razão» becomes the form «fonte · <estudo>»: change the string and the gate's composition in the same commit (the gate builds the expected text from the same `t(lang)` keys, so it is one change in `strings.mjs` plus the template order if needed; plant: a seal whose hidden text keeps the old form must close the build). Then the layout: above 1024 the head is two columns, text left (`582px` mancha as the board) and the map right with its one line «308 concelhos · CAOP 2025 ■ fonte» (the count is `<Claim id="municipios-portugal-caop-2025">`, the seal its door) and the search below the map in Município mode; no ficha, no fold; below 1024 the map sits under the text as now. The **inventory**: `scripts/medir-defeitos.mjs` gains «frases da casa»: every text block of the home (both editions) that is not a `data-claim`, `data-prova`, `data-verbatim`, `data-nonledger`, a navigation link, a measure name or a unit line is listed with the route and classified by a declared list in `design/especime-v3/INVENTARIO-FRASES.md` (content / navegação / autorreferência); the ruler prints the count per class per route and the list of unclassified blocks; on the home the autorreferência count must read 0 in both editions; write the inventory file with every sentence you kept and why it is content or navigation.

**2l-4 · measure, capture, record.** Matrix updated (13 pieces; the band thresholds; the Social list's seals ≥ 44px and never nested; the head's two columns at 1024/1180/1280 with no overflow; the inventory count 0; the new proof keys) and run whole; both rulers; invariance against a worktree build of `b79672f`; captures at 1280 and 390, both editions, light (and dark through the control) for País Relance and Leitura, Alentejo, Évora, Beja, the choosing view; `RELOCACOES.md` (R7, R8, the retired sentences listed), `CHAVES-EN.md` («Etapa 2l»: the headline and lede keys, the state words for the nine new pieces if any key is new, the Social list heading, the correction door's new string, `prov.verLinha`'s new form), `ISSUES.md` (the engine asks; what you leave), `notas/stage-2.md` «2l».

## Exit

Four local commits (Portuguese subjects; trailers `Co-Authored-By: Claude Opus <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01EY7AceRq6jcySfcNyXcBTb`); build, verify, `ledger:check` green; the plants recorded with the gate's messages (band thresholds both directions; `painel_dentro_do_limiar`; the old seal form); the inventory at 0 autorreferência on the home; matrix whole; captures; the note. Rough scale: 400k to 600k tokens; stop at the next commit boundary past 750k with the note written. Reply in at most 50 lines.
