# BRIEF · etapa 2i · the Codex read's real findings on the home, before preview n.º 1

*For a fresh Opus builder (B4), 20.08.2026, night. The seat ran a context-starved Codex read of the home (`../critica/2026-08-20-codex-leitura-da-primeira-pagina.md`: the report, the plants, the seat's triage) and verified each finding against the real build. What follows are the ones that are real and yours. Read first: `BRIEF-etapa-2.md` §1 and §1b (the rules bind you word for word), `BRIEF-etapa-2g.md`, `../notas/stage-2.md` (whole), the triage section of the Codex record, `../RELOCACOES.md`, `../CHAVES-EN.md`, then the files: `src/views/HomeView.astro`, `src/components/inicio/*.astro`, `src/components/InstrumentoConvergencia.astro`, `src/components/InstrumentoMapa.astro`, `src/lib/inicio.mjs`, `src/data/regioes.mjs`, `public/js/inicio.js`, `public/js/convergencia.js`, `src/styles/inicio.css`, `tests/inicio/matriz.mjs`. You own exactly the files stage 2 owned (BRIEF-etapa-2 §2). `Claim.astro` and the other shared files are builder A's: if the fix for item 2 is there, write it as a request in the note and fix the call sites instead.*

## The corrections

1. **Portugal is not a region** (Codex 5; the seat's brief caused it). The closed Região list is the five regions of the plan §13 schema: `grande-lisboa`, `peninsula-de-setubal`, `algarve`, `madeira`, `alentejo`. Remove the `regiao:portugal` state: its head block, its panel, its chip, its slot in `inicio.mjs` and `inicio.js`; `?ambito=regiao:portugal` falls to the default like any unknown value. Portugal stays in the band as the reference marker and in the País instrument, as the board draws it. Update the plan's schema is not yours; the seat does it.
2. **Seven «provisório» in the English edition** (Codex 7). `grep -o 'claim-provisorio">[^<]*' dist/en/index.html | sort | uniq -c` gives 12 «provisional» and 7 «provisório». Find the seven call sites (the regional panels and the convergence instrument, by the report) where the value is rendered without the edition's `lang` reaching the word, and pass it; if `Claim.astro` itself defaults to Portuguese when `lang` is missing, that is builder A's file: request a hard failure there (a missing `lang` should throw at build, not default) in the note, and fix every call site yourself. Exit: 0 «provisório» in `dist/en/`, 19 «provisional» (or the count you measure), and the same count of the PT word in `dist/index.html`.
3. **The map** (Codex 8). Emenda 3: all 308 points equal, the only distinction ■/□ coverage. (a) Same size for every point, Évora included; coverage is the fill, nothing else. (b) The chosen concelho is an interaction state, not coverage: mark it with a thicker ink ring or a second outline, never a fill, so an unbuilt chosen concelho cannot look covered; the matrix gets a cell that proves the chosen point's fill equals the paper in every scope where it has no page. (c) The neutrality sentence («O ponto aceso marca cobertura editorial…») must stay beside the map in every posture, including the locator card: put it in the card (short, `--g1`), the same string, counted as the same relocation. (d) Below 640px no point is activatable by any input: the keyboard handler must not select a point there (keep the arrow-key readout if you wish, never Enter/Space selection); the stamp as a whole stays the one target. Measure each in the matrix.
4. **The three-density remnant** (Codex 9): the comment «CAMADA 3 — FUNDO» and the class `deep` in the convergence instrument's fold. The fold stays (it is the apparatus, «Método, ressalvas e proveniência», not a density): rename the class to what it is (`aparelho` or the name the map's fold uses) and delete the comment. `grep -rn "FUNDO\|Fundo\|fundo" src/components/InstrumentoConvergencia.astro src/components/inicio src/views/HomeView.astro public/js/inicio.js public/js/convergencia.js src/styles/inicio.css` must return nothing that names a density; quote the command and its output.
5. **Accessible names and Space** (Codex 16). Every `svg.regua-svg` carries `aria-hidden="true"` (the state and the values are always in words beside it; the ruler is a drawing of them), or an `aria-label` that is one of the prevalidated state strings; choose one and apply it to all eleven. The scope and density controls that the script upgrades to `role="button"` must activate on Space as well as Enter (`keydown` on ` ` with `preventDefault`), and the matrix gets a cell for it.
6. **The script after `</html>`** (Codex 18): `dist/index.html` ends with `</body></html><script src="/js/inicio.js" defer></script>`. Move the script tag inside the document (into the layout's slot that `convergencia.js` uses, or the head with `defer`); `tail -c 120 dist/index.html` must end with `</html>`; quote it.
7. **The register** (Codex 6). Bring `RELOCACOES.md` to the truth of the build, with the counts you measure: R3 (the CAOP source text now appears in the ficha's compact line and in the fold: count both), R4 (each regional sentence appears in the Região headline, in the regional panel piece and in the País instrument: count them, and either keep the piece's sentence as an authorised duplication written down, or remove the duplicate from the piece if the headline already says it, your call as form, documented), and list `distancia-alentejo-ue27-2000` and `pib-pc-alentejo-2000` under R4 if the instrument renders them (it did in v2). Nothing else in the register changes.

## Not yours, by the triage (do not touch)

Codex 4 (house counts and thresholds without seals: `IDENTIDADE.md` §10 and the declared motive are the rule), 10 (regional distances have rows; decision (e) forbids unsourced distances), 11 (`convergencia.js` composes prevalidated island strings; the island is gate-checked), 13, 14, 15 (I13/I14 open), 12 and 17 (package artefacts).

## Exit

One commit (Portuguese subject «início, leitura cruzada: …», the trailers `Co-Authored-By: Claude Opus <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01EY7AceRq6jcySfcNyXcBTb`); `npm run build` and `npm run verify` green; the whole matrix re-run (`node tests/inicio/matriz.mjs --json design/especime-v3/medicoes/2026-08-20-etapa-2i-matriz.json`) with the new cells; both rulers and the invariance advisor against the previous build, deltas in the note; the captures of the states you changed retaken; `notas/stage-2.md` appended under «2i · a leitura cruzada»; `RELOCACOES.md` corrected; `ISSUES.md` appended for anything you find and leave. Local only; no push. Rough scale: 150k to 250k tokens. Reply in at most 40 lines with the SHA, each item's measurement, the matrix total, the deltas, the token spend.
