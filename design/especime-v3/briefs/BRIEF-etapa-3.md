# BRIEF · etapa 3 · the ledger family and the município page

*For the stage-3 builder (Opus, builder C). Written by the managing seat (Fable), 20.08.2026, to run after preview n.º 1 of the home. Read order: `../PLANO-redesenho-v3.md` (§2, §3 with the director's decisions, §4, §5 stage 3, §6, §8, §10, §11); `../direcao.md` §1, §3, §4 («O recibo é o boletim», «Selo», «Instrumentos a tinta», «Telemóvel»), §5 and the Emendas 1 to 5 and 9; `IDENTIDADE.md` §3 (disposition B), §5, §6, §7, §10, §11 («A página de linha é um recibo, e esta é a ordem»); `../notas/stage-1.md` and `../notas/stage-2.md` (what the shared chrome, the seal, the ruler `Regua.astro` and the map `MapaRespira.astro` now are; they are yours to consume, not to fork); `../RELOCACOES.md`; `../CHAVES-EN.md`; `scripts/gate-html.mjs` in full, with special attention to the row-page blocks (the head composed from the row, origin 6 `data-linha-*`, the crop, the hosted file, the human page, `computed_over`, the verifications, the archived copy, the JSON door) and `seloDaLinha()`; `scripts/check-dados.mjs`; `src/lib/ledger.mjs`, `src/lib/livro.mjs`, `src/lib/documentos.mjs`, `src/lib/dados.mjs`, `src/lib/conjunto.mjs`; the views and components you own (§2); `DECISIONS.md` §1.47 (T1 to T4), §1.48, §1.49 (what the row page gained and why) and §4; then the boards `../maquetas/Linha.dc.html`, `LinhaMovel.dc.html`, `Municipio.dc.html`, `MunicipioMovel.dc.html` (v1 interiors, to be implemented **under the Emendas**; their known conflicts are listed in §4 below). Boards never win.*

## 1. Rules

The rules of `BRIEF-etapa-1.md` §1 and `BRIEF-etapa-2.md` §1 apply word for word. In particular for this stage: **every door the row page has today is preserved** (state note §2 item 1; plan §7): «Esta linha em JSON», the archived-copy doors inside «Calculado sobre» rendered only when the digest matches, the crop with «Abrir na página N», the hosted CAOP files with licence and attribution, `document.computed_over`, `verifications[]` as the two-row table with the marker when the re-reading is missing, the corrections history, the dataset door; the `<head>` stays composed from the row (the gate compares title, description, `og:title`, `og:description`). **Every sentence of the município page is preserved word for word** (its prose is the voice phase's: `DECISIONS.md` §4 items Q and T). The ledger index's legend texts and the dataset block's scope sentence are preserved word for word (§4 item AB is voice-phase). If a board shows a door the repo has and the board does not, the repo wins; if a board shows copy the repo does not have, it is new copy under «Texto novo» or it does not ship.

## 2. The files you own

`src/views/LivroView.astro`, `src/views/LinhaView.astro`, `src/views/MunicipiosView.astro`, `src/views/MunicipioView.astro`, `src/components/{CampoDaLinha,HistoricoDaLinha,ValorDaProva}.astro`, `src/styles/linha.css` and `src/styles/municipio.css` (new, imported by their views only), `src/lib/ledger.mjs` **only** for the one-line NUL-byte fix of `DECISIONS.md` §4 («Um byte NUL no `src/lib/ledger.mjs`») and only if you edit that file for another reason, `src/i18n/strings.mjs` (families `linha.*`, `livro.*`, `municipios.*`, `municipio.*`), `scripts/gate-html.mjs` only for an extension you prove on a plant, `design/especime-v3/notas/stage-3.md`, `../RELOCACOES.md`, `../CHAVES-EN.md`, `../ISSUES.md`, `../medicoes/`, `../capturas/etapa-3/`. The shared files are builder A's; the home's components (`src/components/inicio/*`, `MapaRespira.astro`, `Regua.astro`, `InstrumentoConvergencia.astro`, `InstrumentoMapa.astro`) are builder B's: consume them through their props; a change you need goes in the note under «Pedidos» and the seat routes it.

## 3. Substages

- **3a · the row page as the receipt** (`LinhaView.astro`, commit 1). Disposition B (corpo 68ch, aparelho 300px). The order of `IDENTIDADE.md` §11, fixed: the value with its own seal (an anchor to the page itself, §5.3), unit and id; the attribution sentence; the human page of the series when proven; the proof block (the crop with «Abrir na página N»; in a counted row the hosted file with licence and attribution; the returned field or the excerpt in `--f-instr`, the transcribed voice; the exact request or address); the verifications as the two-row table «Lido a» / «Reconferido a» with the marker when missing and the door to repeat the reading; the corrections and updates of the row. The apparatus column: the provenance in fields, the provenance state, the access to the data (the row's JSON, the dataset), this row elsewhere, the correction door (`portaNoRodape={false}` as today). The boletim vocabulary of the constitution: thin rules (`--g3`), the same labels in the same positions, Bitter for the transcribed and the identifiers, Spectral for the house prose. **The watermark** for a missing field: the marker's own words «[a verificar]» (the string `POR_VERIFICAR` of `ledger.mjs`, not a new string) set diagonally in `--g2` behind the field block, `aria-hidden`, with the marker itself still rendered in text where it is today (the watermark is form, the marker is the word; one uncertainty language, plan deviation 4); never «POR CONFIRMAR». The provisional word of decision (d) comes from `Claim.astro` (builder A); do not duplicate it. Mobile 390: one thing per line, value · title · seal in one visible group (`IDENTIDADE.md` §11 «Móvel»), the crop image within its box.
- **3b · the ledger index** (`LivroView.astro`, commit 2). Specimen rows: one row per claim, the same fields in the same order (id, value with seal, unit, source, read date, state), the two seal states drawn side by side in the legend (texts word for word), the dataset block with CC BY 4.0, the attribution and the scope sentence (preserved), the CSV and JSON doors, the `data-prova` counters that exist (`afirmacoes`, `divida`, `derivadas`, …) with their doors. No colour: the ledger has no thresholds.
- **3c · `/municipios`** (`MunicipiosView.astro`, commit 3). The 308 names with the unified coverage vocabulary of stage 2 (`cobertura.temPagina` / `cobertura.semPaginaAinda`, `data-cobertura` on each), Évora's door, the CAOP source block, and the door to the home's map state (`/?ambito=municipio` and `/en?ambito=municipio`; stage 2's schema). The count sentence keeps its two `data-prova` figures.
- **3d · `/municipios/evora`** (`MunicipioView.astro`, commit 4). Disposition B. The eight measures through stage 2's `Peca.astro` if its props fit the município's `relance` records (they were written for it: R2), else a local rendering with the same grammar; the one ruler (`Regua.astro`) for the índice de dívida against the sealed `indice-de-divida-limite-legal` (cobalt, `dentro`), the poder de compra against the base 100 of its index as a word in ink (an average, Emenda 1); the debt-against-ceiling bars in ink with the ceiling as the full-height reference; the mandates band in `--g2`/`--g3` with the party label as record and the legend of doors; the series drawn in ink (the `var(--yellow)` literals at lines 275 and 388 of the view, ISSUES I12, replaced by the ink grammar); the locator card (`MapaRespira.astro`, `localizador`) with the CAOP seal in the apparatus; every sentence preserved; the apparatus («o que esta página não sabe», the doors) preserved. Mobile 390 as §11.
- **3e · closing** (commit 5): rulers before and after (`medir-defeitos.mjs` including the coverage measure, `medir-contraste.mjs`), the invariance advisor against the stage-2 build (these routes differ by your enumerated set; every other route identical), screenshots at 1280 and 390, both editions, both themes, of `/livro-razao`, `/livro-razao/divida-publica-2025`, `/livro-razao/evora-indice-de-divida-2024` (a derived row), `/livro-razao/avisos-pt2030-abertos` (a row with the marker, to see the watermark), `/municipios`, `/municipios/evora`; the note.

## 4. Known conflicts between the v1 boards and the rules (correct on sight)

Old ruler geometry (fill-from-minimum bars) → the one grammar (ink reference at full height, bar = distance, tick = value, no bar without a published reference); cobalt on the poder de compra (an average) → word in ink; any three-density remnant → the receipt is the seal's destination, not a mode; seals nested inside larger tap targets → never; the boards' Parnaso Mono → Bitter; the boards' Google Fonts link → nothing; «Publicação: 2026-08-12»-style dates → never from `updated`; the Linha board has none of the 20.08 doors → the repo wins; the Município board's mandates band in colour → grey with the label as record.

## 5. Exit criteria

Build green; every row-page door and check of T1 to T4 and 20.08 still green (the gate proves most of it; list the ones it does not and how you checked them); the invariance summary; the coverage measure still 1 per state per edition; screenshots; EN keys listed; `RELOCACOES.md` and `CHAVES-EN.md` updated; the note complete with commands, outputs, deltas, ISSUES, requests, model (Opus) and tokens. Rough scale: 350k to 500k tokens; stop at the next commit boundary past 650k with the note written.

Session URL for the trailer: https://claude.ai/code/session_01EY7AceRq6jcySfcNyXcBTb
