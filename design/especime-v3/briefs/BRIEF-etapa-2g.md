# BRIEF · etapa 2g · the seat's review of the home, seven corrections

*For a fresh Opus builder (builder B2), 20.08.2026, late evening. Stage 2 (commits `b80200f` to `2d34617`) built the home; the seat verified it green from the tree (build, five gates, no shared file touched, the third plant re-run by the seat) and reviewed the 64 captures. What follows is what the captures show and the 55-cell matrix could not see. All seven are form, all inside stage 2's ownership. Read first: `BRIEF-etapa-2.md` whole (its rules §1 and §1b bind you word for word: nothing outward, no shared file, no governed byte, no new gate, prevalidated strings only, PT and EN together, no invented numbers, relocation before movement), `../notas/stage-2.md` whole (what was built, the decisions taken, the requests), `../RELOCACOES.md`, `../CHAVES-EN.md`, then the files you inherit: `src/views/HomeView.astro`, `src/components/inicio/*.astro`, `src/components/InstrumentoConvergencia.astro`, `src/components/InstrumentoMapa.astro`, `public/js/inicio.js`, `src/lib/inicio.mjs`, `src/styles/inicio.css`, `tests/inicio/matriz.mjs`, `scripts/medir-invariancia.mjs`, and the two live boards `../maquetas/V3Completo.dc.html` (its `renderVals()` is the behavioural reference) and `../maquetas/V3Movel.dc.html`. You own exactly the files stage 2 owned (BRIEF-etapa-2 §2) and nothing else.*

## The seven corrections

1. **Scope bleed (the one real defect).** The line «Painel europeu reconferido a 2026-08-18.» under the panel renders in every scope: it is visible under Évora's eight measures and under Beja's empty state in the captures. It is the European panel's time signal and belongs to País scope only; in Região, Município and the empty state it must not render. Then check every other País-only string the same way (the lede, the headline template, the doors' section label) and list in the note what you found and what you changed.
2. **The head's ficha is too tall and drives the head's height.** The full CAOP citation (Publicação, Distribuição, the zip names, the two URLs, «Acedido a…»), the CSV door and the two hints («Passe o cursor…», «Teclado: …») moved into the head beside the map. The live board's ficha is compact. Keep in the ficha: the coverage line («1 de 308 concelhos · tem página», proof figures with doors), the neutrality sentence, the counts by parcel with their seals, and one compact source line built from the CAOP row's existing fields through the existing `Provenance` form (the seal to `municipios-portugal-caop-2025` with «fonte»; no new copy). Move the citation, the CSV door and the hints into a `<details>` «Método, ressalvas e proveniência» (the existing key of the v2 instrument, `home.instr2.deepTitulo` or its relocated name) below the map. Relocation R3 already covers the texts (same route, same scope): update R3's note in `RELOCACOES.md`. The hover hint renders only where the map is a pointer picker (desktop, full map), never at mobile widths; the keyboard hint likewise.
3. **Leitura breve width.** An open piece keeps the tile's single column, so the rulers are about 110px wide. The board's rule is `grid-column: span 2` for the open piece; closed tiles keep one column; with all eight open the grid is 2 × 4. Implement with the `[open]` attribute (`.peca[open] { grid-column: span 2 }` or your equivalent), measure overflow at 768 and 1280 again, and retake the `pais-leitura-*` and `evora-leitura-*` captures.
4. **Mobile convergence rule.** Stage 2 kept the full instrument inline at 390 because its anchor is right below; but the director's point 10a says the convergence rule is a door on mobile, and at 390 the instrument's ruler labels overlap («120 130»). Under 640px render it as a `<details>` whose `<summary>` is the one-line door **in words only** (the instrument's title and the open label; no value and no seal inside the summary, because a seal inside a summary is a nested target) with the instrument inside when open; desktop unchanged. Inside, the instrument scrolls in its own box if it must (`IDENTIDADE.md` §11 «Móvel»); no overlapping labels at 320 or 390 (measure the label boxes).
5. **Choosing view, empty query.** The result chips show the first eight names of the CAOP list (Arcos de Valdevez, Caminha, …). The board's logic with an empty query is: Évora («tem página») only, plus the currently chosen concelho if there is one; other results appear only once the reader types. Apply that, in `inicio.js` and in the server-rendered default.
6. **Évora at Leitura breve.** Emenda 3: the map contracts to the locator card when a concelho is chosen and the reading deepens; the build renders a medium map beside the card. Put the small map (`MapaRespira` in its `localizador` size) inside the card and drop the separate map in that state; at Relance the full map stays, as the board has it.
7. **The district label (ISSUES I18).** One rule for all 308, the board's own: `distrito de <d>` when the CAOP field is a district, the bare island name when it starts with «Ilha». Évora's label then reads exactly as it does now and Beja's reads «distrito de Beja». The two slots stay slots; «distrito de» / «district of» is a prevalidated string pair in `strings.mjs`. Close I18 in `ISSUES.md` with the rule.

## Exit

One commit (substage 2g, Portuguese subject «início, revisão: …», the two trailers as in the earlier briefs with `Co-Authored-By: Claude Opus <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01EY7AceRq6jcySfcNyXcBTb`); `npm run build` green; the matrix cells your changes touch re-run (state changes, overflow at four widths, the no-JS render, the seal measurements on the open pieces, the summary of item 4 is not a nested target) and the whole matrix re-run once at the end; the two rulers and the invariance advisor re-run with the deltas; the captures retaken for every state you changed (same names, same folder); `notas/stage-2.md` appended under «2g · a revisão do lugar de direção» with what each of the seven changed, the commands and their outputs, the token spend; `RELOCACOES.md` R3 note updated; `ISSUES.md` I18 closed. Rough scale: 120k to 200k tokens.
