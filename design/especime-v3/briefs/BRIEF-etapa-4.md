# BRIEF · etapa 4 · the reading family: Correções first, then Método with the two governed lines, Agenda, Estudos, Sobre, the marker page, 404

*For the stage-4 builder (Opus, builder D). Written by the managing seat (Fable), 20.08.2026, to run after stage 3. Read order: `../PLANO-redesenho-v3.md` (§2, §3 with the director's decisions c, g, h, §5 stage 4, §6, §8, §12); `../direcao.md` §4, §5 and the Emendas; `IDENTIDADE.md` §3 (disposition A), §5, §6, §7, §8 (how the constitution quotes a governed text), §9, §10, §11 («A agenda»); `scripts/check-ledger.mjs` lines 85 to 260 (the tether: `Afecta:`, `Texto:` digest, `Sem alteração:`, the constitution's quotations) and `scripts/gate-html.mjs` (the Sobre compared character by character; origin 5 `data-correcao-*`; origin 8 `data-agenda`; the `data-prova` recount; `data-exemplo` refused); `src/data/metodo.mjs`, `src/data/sobre.mjs`, `src/data/correcoes.mjs`, `src/data/studies.mjs` (its header on dates), `src/data/agenda.json`, `src/data/calendario.json`; the views and components you own; `DECISIONS.md` §1.38 to §1.42 (how Block V built these pages) and §1.48; `../notas/stage-1.md`, `stage-2.md`, `stage-3.md`; then the boards `../maquetas/Metodo.dc.html`, `Agenda.dc.html`, `Correcoes.dc.html`, `Estudo.dc.html` (v1, under the Emendas). Boards never win.*

## 1. Rules

The rules of the earlier briefs apply word for word. For this stage: **the governed texts move only as §3 below says**, each in the same commit as its `DECISIONS.md` entry with `**Afecta:** metodo` and `**Texto:** metodo <digest>` (compute the digest of the file as it will be at that commit: `resumoDoTexto()` in `check-ledger.mjs`, 12 hex of the SHA-256 of the file with `\r\n` normalised), and `npm run ledger:check` green at that commit. The Sobre does not change (the gate compares it to `sobre.mjs`). The agenda and the calendar records do not change (they crossed from the engine). The hosted documents are not touched (`check:documentos` proves their bytes).

## 2. The files you own

`src/views/{MetodoView,AgendaView,CorrecoesView,SobreView,EstudosView,EstudoView,MarcadorView,NaoEncontradoView}.astro`, `src/components/{InstrumentoMecanismo,RegistoCorrecoes,CaixaDeCorrecoes}.astro`, `src/styles/leitura.css` (new, imported by these views), `src/data/metodo.mjs` (only as §3), `DECISIONS.md` (the entries of §3 and one for decision c), `IDENTIDADE.md` §5 (one sentence for decision c: in the register the door is the row's seal), `scripts/gate-html.mjs` (the `data-correcao` extension of §3, proven on a plant), `src/i18n/strings.mjs` (families `metodo.*`, `agenda.*`, `correcoes.*`, `estudos.*`, `estudo.*`), `design/especime-v3/notas/stage-4.md`, `../RELOCACOES.md`, `../CHAVES-EN.md`, `../ISSUES.md`, `../medicoes/`, `../capturas/etapa-4/`.

## 2b. What changed since this brief was written (21.08), and your opening commit 4-0

Binding since: Emendas 10 to 17 in `../direcao.md` (read them; Emenda 15, the reader's page does not explain itself, governs every page you rebuild; the inventory measure in `scripts/medir-defeitos.mjs` and `../INVENTARIO-FRASES.md` must read 0 autorreferência on `/agenda`, `/correcoes`, `/estudos`, `/estudos/<slug>` in both editions; `/metodo` and `/sobre` are the method's home and are exempt, as is the row page). Read `../notas/stage-2.md` (2j to 2m), `../notas/stage-3.md`, `../critica/2026-08-21-codex-quarta-leitura-interiores.md` (triage). **Commit 4-0, before 4a**, the director's decisions of 21.08 afternoon and the read's two items (files outside your list named here are yours for this commit only):
1. Ledger index (`src/views/LivroView.astro`, `strings.mjs` `livro.*`): the seal legend becomes state names, PT «■ proveniência completa · □ um campo por confirmar», EN «■ provenance complete · □ one field unconfirmed»; the old two sentences retire; the inventory reads 0 on `/livro-razao` and `/en/ledger`. `DECISIONS.md` §4 item AB closes with the reference to this decision.
2. Évora (`src/views/MunicipioView.astro`, `src/data/municipios.mjs`): the opening («Esta página mede o município de Évora… Não interpreta: …») and the written counts («Oito medidas. Seis vêm de organismos…») go, both editions; «Quem responde pelo quê» stays only as the section's name over the mandates band; the relocation register R6 is updated (the home's Évora lede came from that opening: it goes from the home too, the scope headline stays); the inventory adds `/municipios/evora` and reads 0; `DECISIONS.md` §4 items Q and T close with the reference.
3. Évora's locator (`src/components/inicio/MapaRespira.astro` `localizador` posture, or the call site): `aria-describedby` points at an `id` that exists on that page, and the keyboard instructions render only where the home's script is loaded (the município page loads `tema.js` only).
4. The convergence instrument on the home (`src/components/InstrumentoConvergencia.astro`): `RELOCACOES.md` says its apparatus (data door, meaning, caveat, provenance) left the home and the build still renders the selection sentence, the index's definition and the CSV door of `/dados/convergencia.csv`; make the register true: keep the definition sentence as content (it names what the index is), move the CSV door to `/livro-razao` beside the dataset block (`check-dados.mjs` route declaration updated in the same commit, plant, revert), and register both; the selection sentence is the instrument's instruction and stays as navigation (classify it in the inventory).

## 3. Substages, in this order (the order is part of the decision)

- **4a · Correções, the form first** (`CorrecoesView.astro`, `RegistoCorrecoes.astro`, commit 1). The director's decision (c): each entry keeps the field-by-field comparison against the row's `corrections[]` (origin 5, `data-correcao-*`, untouched) **and gains the row's seal as the door**: a `.src-chip` to the row (`seloDaLinha` form, «fonte» with the row's label in the attribute) beside the pair, so that a reader can open the row where the history lives. Extend the gate's `data-correcao` block: an entry whose element has no `.src-chip` to its own row closes the build; plant a register entry without the door and a register entry whose door points to another row, show both refusals, revert, record. `IDENTIDADE.md` §5 gains one sentence saying that in the correction register the door is the row's seal and not a value seal (no quotation of a governed text). Then **the form**: the old value struck through in `--g1` (`<s>` with an accessible prefix «valor anterior» / «previous value» for screen readers), the new value in ink beside it, the date, the kind as today's labels; no oxblood, no colour. This form must render on every entry of the register before 4b starts, because the «A cor» sentence asserts it (decision g).
- **4b · Método with the two governed lines** (`MetodoView.astro`, `metodo.mjs`, commit 2). Disposition A under the v3 type; an in-page sumário at the top (the ten rule titles as anchors, plus the closing entry); the rules as sections that fold («abre no sítio»: `<details>` with the rule's title in the `<summary>`, the first open by default, the rest closed; the mechanism, proof and limit inside; `data-prova` counters keep their doors; no JavaScript needed); the mechanism instrument in ink; the short reading and the ten rules word for word. **The closing entry** after the ten rules, titled by two labelled lines, both editions, in `metodo.mjs` in a form the view renders after the rules (follow the module's existing shape; add the two items as data, not as view strings):
  - **A cor** (PT): «A cor aparece só onde a fonte publica um limiar: âmbar quando o valor está fora dele, cobalto quando está dentro. Tudo o resto é tinta e cinzento, e uma correção diz-se pela forma, com o valor antigo riscado e o novo ao lado, nunca por uma cor.» (EN): «Colour appears only where the source publishes a threshold: amber when the value is outside it, cobalt when it is within. Everything else is ink and grey, and a correction is said by form, with the old value struck through and the new one beside it, never by a colour.» Ships only because 4a shipped the form; say so in the entry.
  - **A letra** (PT): «Os tipos deste sítio são Spectral, com a sua família de versaletes Spectral SC, e Bitter, de licença aberta (SIL Open Font License), alojados aqui e servidos por este sítio, sem anfitriões de terceiros.» (EN): «This site's typefaces are Spectral, with its small-caps family Spectral SC, and Bitter, under an open licence (SIL Open Font License), hosted here and served by this site, with no third-party hosts.» The director's verbatim text; not one character changes. True on the branch since `7643251` (self-hosting live; `public/tipos/*/OFL.txt` present).
  The `DECISIONS.md` entry for this commit: `**Afecta:** metodo`, `**Texto:** metodo <new digest>`, the two sentences quoted, the director's decisions g and h cited with the date, the condition of g stated as met by 4a. Run `npm run ledger:check` and paste its tether line in the note. `IDENTIDADE.md` §2 may then quote «A cor» with the (`metodo`) mark if you want the tether to guard it; if you do, quote it word for word.
- **4c · Agenda** (`AgendaView.astro`, commit 3): disposition A, sumário and folds, the state board as drawn state (§7) with the `agenda_*` counters and their doors, items as short cards, the source calendar on its time axis in ink (the windows as `--g2` marks, `data-nonledger="escala-de-instrumento"` on the axis marks as today; the `var(--yellow)` literal at line 673 replaced, ISSUES I12), the change log; records untouched.
- **4d · Sobre, the marker page, 404** (commit 4): type and tokens only; the Sobre's `data-sobre` block unchanged and compared by the gate.
- **4e · Estudos index and reading pages** (`EstudosView.astro`, `EstudoView.astro`, commit 5): the index lists each work with `edition.date ?? [a verificar]` read from the archive record and never from `updated`, Git, the manifest or the filesystem (plant a null-date edition in a scratch copy and show the marker renders; record); the `.placeholder` state box becomes words without the specimen visual (`DECISIONS.md` §4 item M: the words stay, the striped box goes); the reading page's layers (Relance: the measure that makes the study worth reading; Leitura breve: the sentence; the document as the door, «Ler o documento») with the seals as they are; the hosted document route untouched.
- **4f · closing** (commit 6): rulers, the invariance advisor against the stage-3 build, screenshots at 1280 and 390 in both editions and themes of `/metodo`, `/agenda`, `/correcoes`, `/sobre`, `/estudos`, `/estudos/agua-nao-faturada` (or the reading page that exists), `/a-verificar`, `/404`; the note.

## 4. Exit criteria

Build green with the tether accepting the Método entry; the `data-correcao` door extension proven on two plants; the register rendering the struck-through form on every entry; the two sentences present in both editions exactly as written; no `var(--yellow)` left in your files; screenshots; the invariance summary; EN keys listed; the note complete (commands, outputs, deltas, ISSUES, requests, model, tokens). Rough scale: 400k to 550k tokens; stop at the next commit boundary past 700k with the note written.

Session URL for the trailer: https://claude.ai/code/session_01EY7AceRq6jcySfcNyXcBTb
