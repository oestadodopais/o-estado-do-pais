# BRIEF · etapa 1f · the director's decisions in the shared chrome: eighth item, the provisional word, the dark rule

*For builder A (Opus; the shared-files owner) or, if that thread cannot be resumed, a fresh Opus builder who reads `BRIEF-etapa-1.md` §1, §2 and §6 and `../notas/stage-1.md` first. 20.08.2026, night. The director answered the plan's §3 decisions (recorded at `PLANO-redesenho-v3.md` §3, «Decided by the director»); three of them are in the shared files. Same rules as stage 1 (nothing outward; no governed byte; no new gate; no invented numbers; PT and EN together; `npm run build` green; `npm run ledger:check` after every edit of `DECISIONS.md`). Do not touch `src/views/`, `src/components/inicio/`, `InstrumentoConvergencia.astro`, `InstrumentoMapa.astro`, `public/js/inicio.js` (stage 2's files) beyond what item 2 strictly needs in `Claim.astro`.*

## 1. The eighth item (decision a) · commit 1

`Masthead.astro`: `OITAVO_ITEM` on, Correções in the navigation between Método and Sobre (the order of the footer's list, which already carries it), both editions, `aria-current` as for the others. `design/especime-v3/direcao.md`: mirror the vault's Emenda 9 (the text is below) so the repo copy follows the source; and in §4 («Cabeçalho neutro») the words «sete itens» become «oito itens» with a parenthesis «(Emenda 9)». Re-measure the masthead at 320, 390 and 1280 with eight items (closed menu below 640px: height unchanged; at 1280 one line, overflow 0) and retake the four `inicio-*` captures of stage 1 plus one `inicio-1280-pt-claro` for the record.

Emenda 9, verbatim from the vault note (`~/Obsidian/Experiments/O Estado do País — Constituição visual v3.1.md`):

> 9. **Cabeçalho, oito itens (§4 emendado; diretor, 2026-08-20, noite):** a navegação do cabeçalho passa de sete a **oito** itens, com «Correções» a entrar (era o segundo dos oito defeitos da crítica cruzada: a página das correções só tinha porta no rodapé e nas páginas). A emenda é feita no mesmo commit em que o oitavo item entra em `Masthead.astro` (ramo `redesenho-v3`, etapa 1f; a cópia do repositório, `design/especime-v3/direcao.md`, segue esta nota). Decidido com as outras seis respostas do dia, registadas em `design/especime-v3/PLANO-redesenho-v3.md` §3: «por ler» é rótulo de cobertura e não estado; a palavra «provisório» ao pé de um valor que a fonte marca `p`; a régua sem distância numérica na fase 1; a paleta escura como regra provisória com duas condições (contornos dos marcadores na tinta do tema; auditoria de contraste inteira no papel escuro, âmbar sobre papel incluído); o registo de correções com a porta do selo da linha; as duas frases do Método, «A cor» condicionada à forma riscada do registo e «A letra» no texto verbatim do diretor.

## 2. The provisional word (decision d) · commit 2

`Claim.astro`: when the row's `source_flag` is `"p"` (read the row through the existing `getClaim`/`loadClaims`, never a hand list), render the word from a new key pair `prov.provisorio` (PT «provisório», EN «provisional») **beside** the value: outside the `[data-claim]` element (the gate compares the string inside it), outside the seal (the gate compares the seal's visible text), as its own `<span class="claim-provisorio">` in `--f-instr`, lowercase, `--g1`, after the unit suffix when there is one. Read `gate-html.mjs` `formaDoValor()`/`textoTranscrito()` and `seloDaLinha()` before placing it, and say in the note why the placement passes each. The seal stays the door; the row page's flag note stays the content (it already renders as a field). Inventory, after the build, of every page where the word now renders (grep `claim-provisorio` in `dist/`): expected the home (Instrumento n.º 1 in País scope, the Portugal marker of the Região band, if those render the value through `<Claim>`), the row page of `pib-pc-portugal-2024`, the ledger index row, in both editions; list what you find and anything unexpected. Plant: temporarily set `source_flag` to `"p"` on a scratch copy of another row and show the word appears there and nowhere else; revert. `CHAVES-EN.md`: the pair under «Etapa 1f».

## 3. The dark rule (decision f) · commit 3

`tokens.css`: the dark block's comment changes from «proposta» to «regra provisória, decidida pela direção a 20.08.2026 com duas condições: os contornos dos marcadores de estado na tinta do tema (`#ECEEEA` no escuro), e a auditoria de contraste inteira repetida no papel escuro, âmbar sobre papel incluído; a pré-visualização n.º 1 é o seu primeiro teste real». Verify that every marker contour in `site.css` and in stage 2's components is `var(--ink)` and never a literal or a light-only token (grep; list the rules); if a contour is not `--ink`, say so in the note for the seat (stage 2's files are not yours). `IDENTIDADE.md` §2: one sentence that the dark palette is a provisional rule under those two conditions (no governed quotation). Run `node scripts/medir-contraste.mjs` and paste the **complete** dark table in the note, every pair, with the two the director named called out by name: amber on dark paper and the contour on amber; 0 text failures is the bar, interface pairs below 3:1 listed with why. `npm run ledger:check` after the `IDENTIDADE.md` edit.

## 4. `DECISIONS.md` §1.51 · commit 4

A short entry, `**Afecta:** nenhum`, recording the director's seven answers of 20.08 (the table of the plan's §3, in prose, with the date), what this substage implemented (items 1 to 3) and what waits for its stage (c and g/h in stage 4, b and e as stage 2's parked defaults). It explicitly says the Método bytes do not change here and will change in stage 4 with their own entry and digest. `npm run ledger:check` green.

## Exit

Four local commits; build green; `grep -r "fonts.googleapis" dist/ | wc -l` still 0; the masthead measurements; the provisional-word inventory and its plant; the full dark contrast table; the note appended to `notas/stage-1.md` under «1f»; `CHAVES-EN.md` and `ISSUES.md` updated. Rough scale: 120k to 200k tokens.

Trailers: `Co-Authored-By: Claude Opus <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01EY7AceRq6jcySfcNyXcBTb`.
