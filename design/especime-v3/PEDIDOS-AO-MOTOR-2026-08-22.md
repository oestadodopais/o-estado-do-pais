# Pedidos ao motor · sessão própria em `~/Instruments/ResearchHub` (escritos a 22.08.2026 pelo lugar de direção, Claude Fable 5)

*O prompt de 22.08 separa o que é tarefa deste repositório do que é pedido ao motor. Estes são os pedidos, na ordem em que o sítio precisa deles, com o que o sítio já sabe sobre cada um. A sessão do motor é plan-gated: apresenta o plano e a estimativa antes de construir, e nada do que sair daqui atravessa para o sítio sem o bloco de republicação (`DECISIONS.md` §1.49) e a leitura cruzada de outra família. Sem travessões neste ficheiro por escolha.*

## B2 · O registo de conteúdo de cada estudo (decisão de 21.08: a apresentação separa-se do conteúdo, e a garantia passa dos bytes ao conteúdo)

**O que é.** Cada estudo exportado como **registo de conteúdo, e não como página**: um ficheiro por edição, ao lado de `ledger.json`, fixado por resumo (SHA-256), em que cada parágrafo da prosa é texto e cada algarismo é uma referência à sua linha do livro-razão (o id da linha, o valor como cadeia tal como a linha o guarda, a posição no parágrafo). Nada de HTML, nada de CSS, nada de script: o registo é o que o documento diz, e só isso. O sítio (parte 3, etapa própria depois desta) constrói a página do estudo a partir dele com o gabarito v3, e o `check:documentos` passa a comparar o texto rendido carácter a carácter com o registo fixado, e cada algarismo através do `<Claim>` contra a sua linha.

**O que o motor já tem, lido a 22.08.** `content/<estudo>/<edição>.md` é a prosa; `core/receipts.py` (`annotate()`, `choose_row()`, `assert_matches_reconcile()`) já resolve cada algarismo do corpo à sua linha de `ledger.json` para construir a ilha `rcpt-data` do HTML; `core/editions.py` compara edições contra o livro-razão. O registo de conteúdo é, em princípio, a saída de `annotate()` serializada sem a vista: parágrafos, e por parágrafo a lista de `(linha, valor, intervalo)`. O que falta é a forma do ficheiro, a fixação por resumo e a decisão sobre o que a prosa do registo leva.

**A passagem de voz, e a regra.** Uma passagem sobre a prosa que **tira só o que descreve o sítio ou o ficheiro** («Cada valor abaixo tem uma linha em `ledger.json` junto a este ficheiro», «Research Hub.», «Edição pt-PT, gerada da edição inglesa por `Technical Source/make_pt.py`», e afins) e **deixa o que descreve os dados** (os limites, «Ler esta parte primeiro», as definições, as bandeiras de provisório). É a regra da Emenda 15 aplicada à voz do próprio documento: sobre os dados fica, sobre o sítio ou o ficheiro sai. O resultado é mostrado **como diff** (o que sai, o que fica, por edição), lido pelo Codex com plantas (uma frase sobre os dados retirada de propósito, uma frase sobre o ficheiro deixada de propósito), e **posto ao diretor antes de fixar o resumo**. Nada é fixado antes da sua palavra.

**O que NÃO se faz.** Não se re-templatiza o documento dentro do motor: o gabarito v3 vive no sítio; o motor exporta conteúdo e fica com o seu HTML como está (a edição de registo, arquivada byte a byte, continua a ser alcançável do sítio).

**Estimativa a apresentar antes de construir:** o formato do registo (um esquema, com um exemplo do 04 pt-PT), a lista das 15 edições e das frases que a passagem de voz tiraria de cada uma (contagem por edição), o custo em tokens por modelo, e as plantas que o Codex recebe.

## C1 · As linhas do Procedimento que «faltam» ao painel (ISSUES I32; Emenda 16): **RETIRADO pelo diretor a 22.08.2026: a verificação diz que não falta nenhuma**

A Emenda 16 e a ISSUES I32 dizem que as 13 linhas do livro-razão com «Limiar do Procedimento» cobrem 11 dos 13 indicadores principais do AMR de 2026, «porque o fluxo de crédito e a dívida do setor privado estão divididos em famílias e empresas, e faltam pelo menos o desemprego de longa duração e o desemprego jovem». **Lida a página da Comissão a 22.08.2026** (`economy-finance.ec.europa.eu/economic-and-fiscal-governance/macroeconomic-imbalance-procedure/scoreboard_en`, 74 KB descarregados e lidos no HTML, não num resumo): «The headline indicators consist of the following 13 indicators and indicative thresholds», e os 13 são: balança corrente (+6/−4), PII (−35), taxa de câmbio efectiva real (±3 AE), desempenho das exportações (−3), custo unitário do trabalho (+9 AE), dívida pública (60), **dívida das famílias (55)**, **dívida das SNF (85)**, **fluxo de crédito às famílias (14)**, **fluxo de crédito às SNF (13)**, preços da habitação (9), taxa de desemprego (10), taxa de participação (−0,2 em três anos). A própria Comissão separa a dívida e o crédito em famílias e empresas, e **nem o desemprego de longa duração nem o desemprego jovem estão entre os 13** (a página diz que os 13 são «complemented by 23 auxiliary indicators» sem os enumerar).

As 13 linhas do livro-razão (`grep -l "Limiar do Procedimento" ledger/claims/*.yml`) correspondem **uma a uma** a esses 13, com os mesmos limiares: `saldo-da-balanca-corrente-2025`, `posicao-de-investimento-internacional-2025`, `taxa-de-cambio-efectiva-real-2025`, `desempenho-das-exportacoes-2025`, `custo-unitario-do-trabalho-2025`, `divida-publica-2025`, `divida-das-familias-2025`, `divida-das-empresas-2025`, `fluxo-de-credito-as-familias-2025`, `fluxo-de-credito-as-empresas-2025`, `precos-da-habitacao-2025`, `taxa-de-desemprego-mip-2025`, `taxa-de-actividade-2025`. **O painel da primeira página já é o painel inteiro: 13 de 13.** A manchete nunca afirmou um total, e por isso nada do que está no ar é falso; o que está errado é a frase da Emenda 16, a I32 e a §1.53 do `DECISIONS.md`, corrigidas a 22.08 com a palavra do diretor: nota datada sob a Emenda 16 (cofre e `direcao.md`), I32 fechada, este pedido retirado.

**O que fica para o motor:** nada de novo no painel. Ficam úteis, fora do Procedimento, o `desemprego-de-longa-duracao-2025` (já existe, `tesem130`) e uma eventual linha do desemprego jovem como indicadores do Painel Social ou auxiliares, se o registo do motor os colocar nesses quadros; sem limiar do Procedimento, porque não o têm.

## C2 · A ilha `rcpt-data` duplicada num documento alojado (ISSUES I60)

Um documento alojado traz a ilha `rcpt-data` **duas vezes** (606 KB cada, o mesmo id duas vezes) nos bytes originais do motor. Os bytes fixados não se tocam do lado do sítio; a correção é no gerador do motor, e o documento volta pelo bloco de republicação. Qual documento, conferido a 22.08: `grep -c 'id="rcpt-data"' studies-src/*/*.html` dá **2** só em `evora-os-pelouros-quem-os-teve-o-que-fizeram/pt.html` (o estudo 09, edição pt-PT; os outros 14 ficheiros dão 1). No motor é `content/09 Évora Pelouros/`. Com o B2 feito, esta ilha deixa de ser servida pelo sítio, e o pedido passa a ser só de higiene do motor.

## C3 · Uma conjunção portuguesa numa `reason_en` (ISSUES I63)

`ledger/claims/evora-prr-vencido-aprovado-2026.yml`, entrada da história de 18.08 (linha 89 no sítio): «…recomputed over the 2026-08-17 snapshot (listagem-de-entidades-prr-20260817.xlsx e listagem-de-projetos-prr-20260817.xlsx)…». O «e» é português; a entrada seguinte da mesma linha (20.08) escreve «and». Achado da leitura cruzada de 22.08. Corrige-se na fonte da entrada (o exportador ou o registo de onde ele a lê), nunca no ficheiro do sítio, e atravessa com a próxima passagem das linhas.

## B4 · Os dois reforços à cadeia (diretor, 22.08.2026, noite: «can you do that?»)

A cadeia dos estudos, depois de B2 e de B3, é: fonte → linha do motor (URL, carimbo, excerto ou resumo do ficheiro mais aritmética, releituras) → linha do sítio (exportador V13 a V16, `ledger:check`) → registo de conteúdo fixado por resumo (parágrafos e referências a linhas) → página rendida (`check:documentos` carácter a carácter; cada algarismo por `<Claim>` contra a sua linha) → no ar (`verify:deploy`) → edição de registo arquivada byte a byte. Dois reforços, para que a cadeia seja percorrida e mostrada, e não só imposta:

**B4a · `check:cadeia`, um guião da construção do sítio (etapa B3, site), com um campo do motor.** Para cada estudo, o guião percorre: o resumo do documento de origem de cada linha (`document.sha256`, ou o resumo do ficheiro contado, ou o da cópia arquivada) → a linha do motor (o id) → a linha do sítio (o mesmo id, o mesmo valor como cadeia) → a posição no registo de conteúdo → o `<Claim>` na página rendida; e imprime as contagens: algarismos no registo, algarismos resolvidos, por resolver (tem de ser 0), linhas com resumo de origem, linhas sem (com o motivo da lista fechada). As contagens entram na prova (`dist/prova.json`) e a página do estudo mostra-as por `data-prova` (por exemplo «47 algarismos · 47 linhas»), recontadas pelo portão e nunca escritas. **O que o motor exporta para isto:** o registo de conteúdo leva, por referência a linha, o resumo de origem que a linha já guarda; nada novo se inventa, só se transporta. Provado em estragos plantados: um algarismo sem linha, uma linha sem resumo de origem, uma contagem escrita à mão na página.

**B4b · Releituras periódicas das fontes das linhas dos estudos (motor).** O refresco semanal (`indicators/refresh.py`, segunda-feira 09:30) relê hoje as 32 linhas do painel contra a fonte e escreve `verifications[]`; o varrimento mensal relê o PRR. Estende-se a mesma disciplina às linhas citadas pelos estudos: uma leitura fresca da fonte comparada com o excerto guardado (ou, onde a fonte roda ou desaparece, o resumo da cópia arquivada comparado com o guardado), a escrever uma `verification` por linha e, quando um valor mexer, uma `atualizacao` tipada pelo exportador (V16), nunca uma reescrita silenciosa. Cadência: a do painel para fontes vivas (Eurostat, INE quando responder), a mensal para instantâneos; o relatório de cada corrida diz quantas linhas leu, quantas bateram certo, quantas mexeram e quantas não responderam, e uma fonte que não responde é «não lida», nunca «confirmada». Plan-gated com B2: a lista das linhas dos estudos por fonte e o custo por corrida vêm com a estimativa.

## Ordem e dependências

C1 foi retirado (22.08); C2 e C3 são pequenos; B2 é o bloco, plan-gated, e B4b vem com a sua estimativa; B4a é do sítio (etapa B3) e só precisa do motor para transportar o resumo de origem no registo. O sítio fica à espera de B2 para a parte 3 (o renderizador e a extensão do `check:documentos`); até lá a faixa dos documentos está na v3 (B1, neste ramo) e os documentos são servidos como estão.
