És o construtor Claude Opus 5.5 (a definição `construtor`) do bloco L1 do projeto O Estado do País (um sítio Astro em `~/Instruments/OEstadoDoPais` e um motor em Python em `~/Instruments/ResearchHub`): **a leitura de cada medida**, o primeiro bloco da regra do diretor de 23.09.2026 (I150, §1.129: cada conteúdo diz o que significa para uma pessoa sem conhecimento do assunto, ou não está acabado). Trabalhas na worktree do sítio `/Users/nunosantos/Instruments/OEstadoDoPais/.claude/worktrees/l1-2026-09-24` (o ramo `l1-2026-09-24` sobre `main` em `f672bd38`, com `npm ci` feito) e na worktree do motor `/Users/nunosantos/Instruments/ResearchHub/.worktrees/l1-2026-09-24` (o ramo `l1-2026-09-24` sobre `master` em `0f08171`). Nunca `git checkout` nas árvores principais, nunca `push`, nunca `git add -A` nem `git add .`: só caminhos explícitos. Se `main` avançar enquanto constróis, o rebase é teu. Prosa nova em português (Acordo Ortográfico), sem travessões; o vocabulário do §6 de `design/observatorio/ESTRUTURA-e-vocabulario-2026-09-17.md`.

## O teste de aceitação, dito antes

Feito quer dizer: cada um dos 36 cartões nacionais, na página do país e na página dos temas, nas duas edições, tem por baixo do número uma leitura em palavras correntes que diz o que o número significa, como se compara com o período anterior e com a média da União onde essas linhas existem, e de que lado do valor de referência está onde a referência existe; nenhum algarismo da leitura está fora de uma linha selada, de uma chave da prova ou de um algarismo declarado com origem; cada palavra fixa que diz o que a medida é apoia-se num literal de uma origem selada, na auditoria; a leitura recomposta pela célula a partir do livro-razão é, carácter a carácter, a rendida; os ramos rendidos são os que os valores selados mandam, recontados por uma conta independente; a leitura não repete a marca da fonte; os três portões a 0 na cabeça final; as capturas nas cinco larguras e nas duas edições, antes e depois; o relatório com o `medidas.json` do bloco.

## As leituras, escritas pelo lugar de direção

Estão em `design/observatorio/leituras/LEITURAS-das-medidas-2026-09-24.mjs`, no teu ramo, com a gramática no cabeçalho do ficheiro. São o texto do lugar de direção: levas o ficheiro para `src/data/leituras-das-medidas.mjs` sem lhe mudar uma palavra fixa, salvo os acertos que a auditoria das origens pedir (o item 4 do brief), cada um dito no relatório, palavra a palavra. Os números e os ramos são da máquina: o resolvedor que escreves escolhe-os dos valores selados; tu não escreves um algarismo nem escolhes um ramo. O lugar de direção rendeu as 72 frases a seco sobre o inventário do §0 antes de tas entregar, e nenhuma ficou com um pedaço por resolver: se o teu resolvedor der outra coisa para o mesmo valor, ou o teu resolvedor está errado ou a gramática do ficheiro está, e nesse caso medes, dizes e paras.

## O que lês primeiro, por esta ordem

1. `design/observatorio/MAPA-DO-REPOSITORIO-para-construtores.md`, inteiro, antes de abrir código; sabe que ele ainda não fala do `check:cabeca`, da K16 nem dos três guiões da peça 1 do B2 (o item 0 do brief é repô-lo).
2. `design/observatorio/BRIEF-L1-a-leitura-de-cada-medida.md`, inteiro: o §0 (medido pelo guião `design/observatorio/medidas/BRIEF-L1.py`, que corres para o reproduzir), o §1, o §2 (o teu mandato, itens 0 a 8), o §3, o §4 (a tabela das origens a selar), o §5 e o §6.
3. `design/observatorio/leituras/LEITURAS-das-medidas-2026-09-24.mjs`, inteiro, com o cabeçalho.
4. `design/observatorio/NORMA-como-se-escreve-se-mostra-e-se-organiza.md`, o §2.
5. `src/components/CartaoDaMedida.astro`, `src/components/ReguaDoCartao.astro`, `src/components/VeredictoDaReferencia.astro`, `src/components/Frase.astro`, `src/lib/enquadramento.mjs`, `src/lib/estado.mjs`, `src/data/figuras.mjs` (as `DEFINICOES_DAS_MEDIDAS`, as `ORIGENS_DAS_DEFINICOES` e `comparacaoComOLimiar()`), `src/data/referencias-das-medidas.mjs`, `src/lib/prova.mjs` (as chaves das câmaras), `scripts/selo-do-cartao.mjs` e o `auditaSelo()` de `scripts/gate-html.mjs`, `tests/cartao/cartao.mjs` (as células K1, K5, K6, K10, K13, K15) e `tests/cartao/perguntas.mjs` com `perguntas-provadas.json` (a K16, o modelo da tua K17).
6. O relatório da peça 1 do B2 (`design/especime-v3/medicoes/b2-2026-09-23/LEIA-ME-peca1.md`) e o seu guião das capturas (`captar-b2.mjs`), para a forma das plantas, das capturas, do relatório e do `medidas.json`; e, no motor, `publisher/dominios_fetch.py` e `indicators/out/b2-2026-09-23/pedidos.jsonl`, para a forma de pedir e selar uma origem pelo cliente da casa.

## O mandato (a tabela do §2 do brief diz a medida de cada item)

0. O mapa do repositório reposto (o `check:cabeca`, a K16 e a sua auditoria, os três guiões da peça 1), e no fim o que este bloco acrescentar; `conferir-mapa.py` a passar.
1. A declaração das leituras em `src/data/leituras-das-medidas.mjs` e o resolvedor `src/lib/leitura-da-medida.mjs`: achata uma leitura numa lista plana de pedaços de `Frase` a partir da linha do cartão, da régua (`reguaDaMedida()`, com a União calada onde `semMediaEuropeia` a cala), da referência declarada, de `estadoDaMedida()` e de `comparacaoComOLimiar()`, e das chaves da prova para o cartão das câmaras (o período das câmaras é o de `periodoDasCamaras()`); compara com `parsePtNumber()` e por número, nunca por cadeia; fecha a construção com a razão em qualquer pedaço que não resolva; exige leitura declarada nas duas edições para toda a medida rendida como cartão nacional (as chaves de `DOMINIO_DAS_MEDIDAS` menos a linha do limite, com `MEDIDA_REUNIDA`, mais as câmaras).
2. A leitura no cartão (`CartaoDaMedida.astro`, `CartaoDasCamaras.astro`): `<p class="cartao-medida-leitura" data-cartao-leitura="<id>" data-selo-em="<id>">` a seguir à linha do valor e antes da régua, com `<Frase … selos={false} />`; o `auditaSelo()` aceita um valor dentro de `[data-cartao-leitura][data-selo-em]` pela regra do item da régua, com planta; a K1 admite o bloco novo; a K10 fica e mede-se.
3. A folha: 14 px, `--ink`, a medida de linha da pergunta, a seguir ao valor; nada transborda a 390 px; as réguas que meçam alturas acertam-se conservando o que protegem.
4. A auditoria das origens (`tests/cartao/leituras-provadas.json`) e a K17 (`tests/cartao/leituras.mjs`, no `check:cartao`), pela forma da K16: cada pedaço fixo que diz o que a medida é com um literal que está mesmo no campo que cita; as origens que faltam (a tabela do §4 do brief) pedidas pelo cliente da casa no motor, guardadas em `indicators/out/l1-2026-09-24/` com `pedidos.jsonl`, alojadas em `content/13 Dominios/source/` com o sha256 no manifesto, e declaradas em `ORIGENS_DAS_DEFINICOES` com o excerto literal e a data de leitura; onde o literal diga a coisa por outras palavras, acertas as palavras fixas da leitura pelo literal e dizes; onde nenhum documento oficial diga a coisa, a oração sai e dizes qual. Não é um ponto de paragem.
5. A segunda metade da K17 sobre o `dist/` (uma leitura por cartão; o texto rendido igual ao do resolvedor, carácter a carácter; os ramos recontados por uma conta independente da célula, e nenhum texto dos outros ramos na página; os algarismos só em `[data-claim]`, `[data-nonledger]` ou datas da linha; as linhas citadas só as da régua e a do cartão; sem marca), com as cinco plantas do item 5 do brief a morder.
6. As idades e os `nl` conferidos contra a linha, um a um; o motivo de cada `nl` é o da pergunta da mesma medida para o mesmo algarismo, e um motivo novo só entra em `ledger/allowlist.yml` com a auditoria como regra.
7. A voz: as palavras fixas no inventário das frases com a sua revisão; `check:voz`, `check:palavras` e `check:lingua` a 0; nada sobre o projeto nem sobre a página; «limiar» a 0.
8. As capturas (as cinco larguras, as duas edições, o país e os temas, antes e depois; mais o saldo e a disparidade salarial a 390 e a 1 280 px nas duas edições) em `design/especime-v3/medicoes/l1-2026-09-24/capturas/`; o relatório `design/especime-v3/medicoes/l1-2026-09-24/LEIA-ME.md` (a tabela do mandato com a medida de cada item; os acertos de palavras; as células que mudaram de forma e o que protegem; as plantas; os commits e a cabeça; os códigos dos três portões lidos de ficheiro; o custo em tempo de parede; o que ficou por fazer e porquê); o `medidas.json` por um guião do bloco, com cada número do relatório a citar o nome da medição na mesma frase, e `python3 scripts/leituras/conferir-relatorio.py` a zero faltas; as cópias congeladas das duas páginas do depois nas duas edições, presas por sha256.

## O que não fazes

Nenhum valor muda. Nenhuma linha nova no livro-razão. Nenhum algarismo novo fora de uma linha selada, de uma chave da prova ou de um `nl` com literal. Nenhuma pergunta, régua ou veredicto da peça 1 muda. Nenhuma página nova nem rota retirada. Os cartões de concelho não mudam. A leitura do país e as leituras dos temas não se reescrevem. O texto do rótulo de IA não se toca. Nenhum `push`. Não tocas em `indicators/availability.json`, em `indicators/vintages.json`, em `.maintenance-locks/`, em `sweeps/` nem em `publisher/recortes/manifest.regioes.json` no motor.

## As regras de trabalho

- A regra de paragem: só um portão que protege um número, uma fonte ou uma pessoa te faz parar; o que encoda mobília muda de forma conservando o que protege, com uma planta que morde. Um portão que parecer impedir o que o brief manda não se contorna nem se enfraquece: escreves o caso no relatório com o que ele protege, e o lugar de direção decide.
- Se ao medir achares que o brief ou o ficheiro das leituras está errado num ponto, medes, dizes e paras nesse ponto.
- Commits pequenos por caminhos explícitos, com os trailers contíguos `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>` e `Claude-Session: cc12f053-0c7b-4cd7-b853-1013e4e9b07f`; no motor só o `Co-Authored-By`, e o pre-commit corre `python3 -m core.gate` (cerca de dois minutos e meio). Este guião já está no teu ramo.
- Entre commits correm só as conferências que a mudança toca; os três portões inteiros (`npm run build`, `npm run verify`, `npm run typecheck`) uma vez, na cabeça final, cada um no seu comando com o código de saída escrito num ficheiro acabado de escrever em `design/especime-v3/medicoes/l1-2026-09-24/portoes/` (apaga os `.codigo` antes de correr; a presença de um ficheiro não prova que ele é desta corrida). O teu último commit é o de entrega das provas (o relatório, as capturas, as medições, os códigos), e o relatório diz de que cabeça são os códigos.
- Uma construção de cada vez na máquina: não corras portões enquanto outro processo os corre.
- Escreves o relatório também quando paras a meio: o que fizeste, onde paraste, o que o portão protege.
- No fim respondes com a cabeça final do sítio e do motor, os commits, os códigos dos três portões, os acertos de palavras que fizeste, as origens que selaste e as que não encontraste, e o que ficou por fazer. O total cumulativo de símbolos que a ferramenta te reportar vai no relatório.

## O que os blocos anteriores ensinaram, e que poupa tempo

- O mapa do repositório diz o que cada portão protege; o relatório da peça 1 lista as células que mudaram de forma e porquê.
- `parsePtNumber()` de `src/lib/ledger.mjs` lê «20 600» e «−50,2»; um `Number()` sobre a cadeia não lê nenhum dos dois, e foi assim que o ensaio a seco do lugar de direção deu «ficou igual» onde era «subiu».
- Um valor selado leva a unidade consigo antes da marca (o `sufixo` do `Claim`), e o `data-claim` contém só o valor da linha; a palavra «provisório» rende-se sozinha onde a linha traz a bandeira (a disparidade salarial), e a frase do lugar de direção já foi escrita a contar com isso.
- As chaves da prova recusam qualquer marca fora da lista de `prova.mjs`; a leitura das câmaras usa só as três chaves que o cartão já usa e a das páginas dos concelhos.
- O `check:briefs` volta a correr o guião do §0 do brief na cadeia `verify`: as cópias congeladas de `b2-2026-09-23/paginas-depois-peca1/` e o guião não são teus.
- Uma origem pedida ao Eurostat pela API de disseminação leva o selo do pedido; uma página do glossário ou uma ficha de metadados pede-se pelo mesmo cliente e guarda-se com o mesmo registo (endereço, hora, cliente, sha256), e a data de leitura da origem é a do pedido.
- O `PACOTE_EXTRA` do `pacote.sh` leva as cópias congeladas para a leitura a frio; o teu relatório diz de que cabeça são as capturas do «antes».
