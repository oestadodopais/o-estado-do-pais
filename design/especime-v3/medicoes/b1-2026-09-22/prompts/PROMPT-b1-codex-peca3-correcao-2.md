És o construtor da segunda passagem de correção da terceira peça do bloco B1 do projeto O Estado do País (um sítio Astro): **o país**. A peça foi construída e corrigida pelo Codex nesta worktree (`/Users/nunosantos/Instruments/OEstadoDoPais/.claude/worktrees/b1-peca3-2026-09-22`, ramo `b1-peca3-2026-09-22`, cabeça `7d323c5f` mais o commit deste guião; confirma com `git log -1`), e o Claude Opus leu-a a frio com cinco estragos plantados só nas cópias do pacote: apanhou os cinco e deixou achados reais, que o lugar de direção triou. A leitura está em `design/especime-v3/critica/LEITURA-b1-peca3-2026-09-22.md` (lê-a inteira; os achados 1 a 6 são as plantas e a sua consequência, e não existem no ramo). O teu relatório é `design/especime-v3/medicoes/b1-2026-09-22/LEIA-ME-peca3.md`. As regras de sempre: nunca `git checkout` noutra árvore, nunca `push`, nunca `git add -A` nem `git add .`; commits pequenos por caminhos explícitos, com os dois trailers num bloco contíguo no fim da mensagem, sem linha vazia entre eles: `Co-Authored-By: Codex gpt-6-astra <noreply@openai.com>` e `Claude-Session: https://claude.ai/code/session_016dojDrtR3Thizhpckp9ENd`. Prosa nova em português, sem travessões; o vocabulário é o do §6 da estrutura. Só te faz parar um portão que proteja um número, uma fonte ou uma pessoa; o que encoda mobília muda de forma conservando o que protege, com uma planta que prova que ainda morde.

## O mandato, em oito pontos (cada um com a célula e a planta decididas)

**1 · A descrição da primeira página (achado 7).** O `<title>` e a `description` das duas edições descrevem a página antiga («As medidas de cabeça de cada domínio, a busca e o mapa dos concelhos…»). Passam a: PT «A leitura do país e os números oficiais por tema, cada um com a sua fonte, o mapa dos 308 concelhos e os estudos mais recentes.»; EN «The country's reading and the official numbers by theme, each with its source, the map of the 308 municipalities and the most recent studies.». O título fica como está se for «O Estado do País»; se disser mais, diz só isso. *Célula:* o `check:datas` ou o `gate:html` já comparam a descrição com o `strings.mjs`; se nenhum o fizer, o `check:pais` ganha a célula D1 (a descrição da primeira página é a declarada, nas duas edições). *Planta:* a descrição antiga reposta.

**2 · O título e a descrição de `/temas/` (achado 13).** Passam a `<title>` «Temas · O Estado do País» / «Themes · O Estado do País» e à descrição PT «Todas as medidas do país, por tema, cada uma com o seu valor, a sua fonte e o seu recibo.»; EN «All the country's measures, by theme, each with its value, its source and its receipt.». *Célula e planta:* as do ponto 1, para esta rota.

**3 · A linha «4 → 4» em «O que mudou» (achado 8).** A entrada de 12.08.2026 de «Estudos publicados sobre Évora» rende o valor riscado da correção 0 e o valor novo da correção 1, com o `<time>` da correção 1: a composição junta duas correções da mesma linha no mesmo dia. Cada linha de mudança passa a nascer de UMA correção (as três marcas `data-correcao-n` com o mesmo `n`, e a data dela), e duas correções da mesma linha no mesmo dia são duas linhas. *Célula:* `check:pais` M3: em cada linha de mudança as três marcas têm o mesmo `n` e o valor riscado é diferente do novo. *Planta:* uma linha com o `n` misturado; uma linha «4 → 4».

**4 · A célula H10 do `check:alvos` (achado 9).** A condição `(dist.expanded === 0 || provasDoGuiao.length > 0)` deixa a célula verde sem medir nada, e o conhecido-positivo está atrás de uma bandeira que a cadeia não passa. A forma nova: com zero `aria-expanded` no `dist`, a célula mede isso e di-lo, e o conhecido-positivo (uma página injetada com `aria-expanded` num elemento sem comando) corre SEMPRE na cadeia e tem de ficar vermelho, sem bandeira; qualquer `aria-expanded` real que exista tem de abrir e fechar no navegador. *Planta:* o conhecido-positivo a passar (a célula fecha); a bandeira `--planta-b1` sai.

**5 · A isenção nova do `auditaSelo` no `gate:html` (achado 10).** O ramo `-ue` é mais fraco do que o ramo `anterior`: não exige a mesma `document.edition` nem a mesma `unit`. Passa a exigir as duas, e que a linha `-ue` seja o agregado da União da mesma série (a `note` da linha da União di-lo; a relação de identificadores não chega). A isenção não se limita por rota (a forma é a mesma em todas as réguas de cartão), mas fica tão apertada como o ramo `anterior`. *Planta:* um valor da União de outro conjunto de dados (outra `edition`) posto na régua de um cartão: o portão fecha.

**6 · O tema escuro sem comando (achado 12).** O comando saiu com a mobília antiga e o guião de arranque em `src/layouts/Base.astro` continua a aplicar `data-theme="dark"` a quem o tinha guardado, sem caminho de volta. Decisão do lugar de direção: o tema segue o sistema. O guião de arranque sai; o `:root[data-theme='dark']` de `src/styles/site.css` passa a `@media (prefers-color-scheme: dark)` sobre `:root`; nenhuma página lê nem escreve `localStorage` para o tema. *Célula:* `check:pais` N3: nenhuma página do `dist` traz o guião do tema nem `data-theme`. *Planta:* o guião reposto na `Base.astro`.

**7 · Uma porta, uma grafia (achado 16).** O menu liga `/lugares` e `/temas` (e `/en/places`, `/en/themes`) sem a barra final, e o resto da página liga `/lugares/` e `/temas/`. O menu passa a usar `routePath()` como as outras portas, e a L1 do `check:lugar` normaliza a barra final antes de contar portas repetidas. *Planta:* a grafia sem barra reposta no menu: a L1 conta a porta duas vezes.

**8 · O relatório (achados 11, 14 e 15).** Um prefácio de cinco linhas no topo do `LEIA-ME-peca3.md`, antes do primeiro parágrafo, a dizer o estado final: as três passagens, a cabeça final, e que os três portões só se leem na tabela da última passagem (o primeiro parágrafo descreve a construção e é história). Uma secção nova no fim, «A segunda passagem de correção, 22.09.2026», curta, com os sete pontos, a célula e a planta a morder de cada um. A saída inteira do `porta.mjs` da passagem anterior e desta guardada em `porta-correcao-peca3.json` na pasta das medições. Dois achados que não são defeitos, ditos no relatório com a razão: o 17 (o U+202F do livro-razão rende-se como U+00A0, e é a regra da §1.47, T4: o mesmo separador noutro ponto de código) e o 20 (as duas linhas derivadas do PRR não têm marcador porque o marcador das origens está no campo `excerpt`, e os valores das origens estão confirmados e conferidos pela expressão `check` na construção).

## O que não fazes

Nada mais nas páginas além dos oito pontos. Nenhum valor, nenhuma linha do livro-razão, nenhuma frase da leitura. Nenhum `push`. **Não corres os três portões inteiros no fim**: o lugar de direção vai acrescentar os registos da sessão a este ramo e corre-os ele próprio na cabeça final; entre os teus commits corres só as conferências que cada mudança toca, e dizes quais. Refazes as capturas «depois» da página do país e da página dos temas (as cinco larguras em português, 390 e 1 280 em inglês) com o `captar-peca3.mjs`, com os mesmos nomes, e reconferes os resumos. A tua resposta final, curta: a cabeça, os sete pontos com a planta a morder, e o total de símbolos que a ferramenta te reportar.
