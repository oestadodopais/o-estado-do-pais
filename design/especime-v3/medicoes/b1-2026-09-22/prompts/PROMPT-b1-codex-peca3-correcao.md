És o construtor da passagem de correção da terceira peça do bloco B1 do projeto O Estado do País (um sítio Astro): **o país**. A peça foi construída pelo Codex nesta mesma worktree a 21.09.2026 (o relatório dele é `design/especime-v3/medicoes/b1-2026-09-22/LEIA-ME-peca3.md`, e é a primeira coisa que lês inteira), e o lugar de direção leu-a como editor sobre as capturas e conferiu no código as duas paragens. Trabalhas nesta worktree e só nela: `/Users/nunosantos/Instruments/OEstadoDoPais/.claude/worktrees/b1-peca3-2026-09-22` (ramo `b1-peca3-2026-09-22`, cabeça `b770abca` mais o commit deste guião; confirma com `git log -1`). Nunca faças `git checkout` noutra árvore, nunca `push`, nunca `git add -A` nem `git add .`; commits pequenos por caminhos explícitos, com os dois trailers num bloco contíguo no fim da mensagem, sem linha vazia entre eles: `Co-Authored-By: Codex gpt-6-astra <noreply@openai.com>` e `Claude-Session: https://claude.ai/code/session_016dojDrtR3Thizhpckp9ENd`. `node_modules` já existe. Prosa nova em português, sem travessões; o vocabulário é o do §6 da estrutura. A regra de paragem é a de sempre: só te faz parar um portão que proteja um número, uma fonte ou uma pessoa (P no mapa do repositório); o que encoda mobília muda de forma conservando o que protege, com uma planta que prova que ainda morde.

## As duas paragens da construção, decididas pelo lugar de direção (nada a construir)

1. **A atribuição da Carta fica por baixo do mapa, como está.** A R6 do `check:mapa` protege uma obrigação de licença (CC BY 4.0: a menção escrita onde o mapa está, Emenda 20e). O guião da construção mandava passá-la ao Método e estava errado; o construtor parou bem. Não tocas na legenda nem na R6.
2. **A leitura inglesa fica com a vírgula decimal, como está.** O `Claim` rende o valor tal como o livro-razão o escreve, e o `gate:html` compara-o com o livro (§1.47, T4): é um portão P. Medido na cabeça `45f056f8`: a página inglesa de Évora tem 45 valores decimais com vírgula e 0 com ponto; a edição inglesa inteira já é assim, e mudá-la é uma decisão para o bloco das palavras, não para esta peça. Não tocas no `Claim`, no livro nem na comparação.

## O mandato, em quatro pontos (cada um com a célula e a planta decididas)

**1 · Os temas passam a toda a largura, por baixo da cabeça.** Na página do país a 1 280 px, os nove temas correm em duas colunas dentro da coluna esquerda ao longo de cerca de 2 800 px, com o terço direito vazio abaixo do mapa (medido na captura `depois-pais-pt-1280.png`: 6 470 px de altura). A causa foi o teste de aceitação do guião, que exigia a primeira fila de cartões dentro dos 800 px do primeiro ecrã: o lugar de direção retira essa exigência. A forma nova: a cabeça continua em duas colunas a partir de 1 024 px, mas só com a leitura à esquerda e o mapa à direita; a secção «Temas» começa por baixo da cabeça, a toda a largura do conteúdo, com a mesma grelha de cartões que `/temas/` usa a cada largura (a 390 px nada muda). O teste de aceitação passa a ser: a 1 280 × 800, o primeiro ecrã mostra o menu, o nome, a leitura inteira, a porta do mapa com «308 concelhos», e o olho «Temas» com o nome do primeiro tema. *Célula:* `tests/inicio/porta.mjs` muda de forma para medir isto (a leitura inteira e o olho dentro dos 800 px; a secção dos temas com a largura do conteúdo a 1 280). *Plantas:* o olho «Temas» empurrado para lá dos 800 px; a grelha dos temas mais estreita do que o conteúdo a 1 280 px. *Medida:* a altura da página a 1 280 antes e depois, e a largura da secção dos temas.

**2 · O cartão do limite legal diz o que o número é.** Na primeira fila do tema «Economia e finanças públicas», e em `/temas/`, o cartão da linha `indice-de-divida-limite-legal` chama-se «Dívida da câmara contra o limite legal» e mostra «150 · %»: um leitor lê 150 como uma dívida, e é o limite. O nome do projeto para esta linha passa a «Limite legal da dívida das câmaras» (en: «Legal cap on municipal debt»), no único sítio onde está declarado (`src/data/dominios.mjs`, o campo `nome` da entrada desta linha, que é o degrau 1 da escada de `nomeDoCartao()` em `src/lib/nomes.mjs`); a `pergunta` e o `rotuloDoValor` ficam. O nome muda em todas as páginas onde o cartão se rende, incluindo a página de domínio antiga, e é isso que se quer. *Célula:* as que já existem (`check:cartao` K1 e K2 comparam o título do cartão com a declaração; o `check:nomes` N6 confere que nenhum nome oficial do ficheiro do motor se rende fora dos elementos marcados). *Planta:* o nome antigo escrito à mão na vista, com a declaração nova: o `check:cartao` fecha. *Medida:* as páginas onde o nome novo aparece, contadas no `dist`.

**3 · O olho dos estudos na página do país.** Diz «Estudos sobre este lugar» e lista os três estudos mais recentes do projeto, dois deles sobre Évora: o olho contradiz o que está debaixo dele. Na página do país o olho passa a «Estudos recentes» (en: «Recent studies»), um rótulo novo `estudosRecentes` em `ROTULOS_B1` (`src/data/rotulos-b1.mjs`), usado só pela `HomeView`; as páginas de lugar continuam com `estudosDoLugar`. *Célula:* a lista fechada do país no `check:voz` ganha o rótulo. *Planta:* o olho antigo reposto na página do país: a lista fechada fecha. Não mexes na maqueta (`design/especime-v3/maquetas/b1/pais.html`); dizes no relatório que ela ficou com o olho antigo.

**4 · As linhas de publicação em «O que mudou».** Leem-se «publicado a · Évora 2027: …» com a data na coluna da esquerda, e «publicado a» fica pendurado sem data. A linha passa a «Estudo publicado · <título>» (en: «Study published · <title>»), com um rótulo novo `estudoPublicado` em `ROTULOS_B1`, e o `publicado` («publicado a») continua a servir a lista dos estudos, onde a data vem a seguir. *Célula:* a lista fechada do `check:voz` e o que o `gate:html` já confere nas mudanças (cada campo contra a declaração ou a data da edição). *Planta:* o rótulo substituído por prosa solta na vista: a lista fechada fecha.

## O que não fazes

Nada mais na página do país, em `/temas/`, no menu, no rodapé nem em «O que mudou» além dos quatro pontos. Nenhum valor, nenhuma linha do livro-razão, nenhuma frase da leitura. Nenhuma página nova, nenhum redirecionamento novo. Nenhum `push`. Não regeneras a maqueta.

## O fim: os portões uma vez, na cabeça final, e o relatório

1. Os registos primeiro (o relatório e as capturas), os portões depois: qualquer commit depois dos portões obriga a corrê-los outra vez.
2. As capturas «depois» refeitas na cabeça final, com o mesmo guião (`captar-peca3.mjs`) e os mesmos nomes de ficheiro: a página do país nas cinco larguras e em inglês a 390 e a 1 280; as outras quatro famílias a 390 e a 1 280. Os resumos SHA-256 reconferidos contra os PNG.
3. Uma secção nova no fim do `LEIA-ME-peca3.md`, «A passagem de correção de 22.09.2026», curta: os quatro pontos com a célula, a planta a morder (a saída do comando) e a medida de cada um; as duas paragens ditas como decididas; e a tabela dos três comandos completos na cabeça final. O que o relatório já dizia não se reescreve.
4. **Os três portões inteiros, cada um no seu comando, na cabeça final, com o código lido de um ficheiro** (nunca atrás de um `|`): `npm run build > build-peca3.log 2>&1; echo $? > build-peca3.codigo`, e o mesmo para `npm run verify` e `npm run typecheck`, com `.inicio`, `.fim` e `.cabeca` ao lado, na pasta `design/especime-v3/medicoes/b1-2026-09-22/`. Os três a 0. Se algum não ficar a 0, não emendas o código do portão para o calar: lês a falha, corriges a causa, voltas a correr os três, e dizes no relatório o que foi. Os ficheiros dos códigos entram no último commit, que só pode conter esses ficheiros e o relatório com a tabela deles; se esse commit tocar noutra coisa, os portões correm outra vez.
5. A tua resposta final, curta: a cabeça final, os três códigos, a altura da página do país a 1 280 antes e depois, e o total de símbolos que a ferramenta te reportar.
