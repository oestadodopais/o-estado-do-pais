# Cold read of B1, piece 1 — package `pacote-b1p1`

Reader: Claude Opus 5 (1M context), different family from the builder (Codex gpt-6-astra). Read-only, no network, no git, no running site. Every claim below was reproduced from the files in this package; paths are relative to the package folder. The built pages are minified to two lines, so a reference of the form `built/…/index.html:2` is the page body; the exact string is quoted so it can be found.

## Blocking

1. **The Portuguese studies list prints a publication date that is not the edition's date, and the English list prints a different one for the same edition.** `built/estudos/index.html` renders `<time datetime="2026-08-24" data-nonledger="data-do-repositorio">24.09.2026</time>` for `penalizacoes-por-reforma-antecipada-2026`: the machine-readable value says 24 August and the reader is shown 24 September. The English twin renders `24.08.2026` for the same `datetime="2026-08-24"`, and the study's own page renders `24.08.2026` as well, so the site states two different publication dates for one edition. The block's own planted fault for exactly this defect (`planta-data-da-lista.txt`) shows `check:datas` failing with «a edição penalizacoes-por-reforma-antecipada-2026/pt imprime «16.09.2026» e src/data/datas-de-publicacao.json diz que … entrou a 24.08.2026», so the cell that should have caught this was proved to bite and the report still claims `check:datas` green. A date the reader is shown that the repository does not declare for that edition is the defect this whole family of cells exists to prevent. `built/estudos/index.html:2`, `built/en/studies/index.html:2`, `built/estudos/penalizacoes-por-reforma-antecipada-2026/index.html:2`, `design/especime-v3/medicoes/b1-2026-09-17/planta-data-da-lista.txt:9`, `design/especime-v3/medicoes/b1-2026-09-17/check-datas-trabalho.log:2`

2. **The copied `src/data/studies.mjs` declares a theme the diff does not add and the domain table does not contain, which makes the build throw rather than pass.** The file in the package reads `tema: 'culturas'`; the diff's post-image for that same line reads `tema: 'cultura'`, and the block's own plant script in the diff hardcodes the target as `h.replace("    tema: 'cultura',\n", '')`. `temas-aplicados-peca1.json` and `temas-e-documentos-peca1.json` both record `cultura` for this study, `valido: true`, against 18 declared domains; the string `culturas` occurs nowhere in the diff, in any script, or in any measurement file. With `culturas` in place, `fichaDoEstudo` throws `B1 tema em falta ou desconhecido` and `voz-b1.mjs` pushes `B1 tema: … sem tema válido`, so neither `npm run build` nor `npm run check:voz` can be green on the file as shipped — and the `tema-em-falta` plant becomes a silent no-op, because its `replace` no longer finds its target. This is the first of the diff-versus-copy disagreements, and it is the one that decides whether the block builds at all. `src/data/studies.mjs:205`, `diff.patch:79561`, `diff.patch:74700`, `design/especime-v3/medicoes/b1-2026-09-17/temas-aplicados-peca1.json:10`, `design/especime-v3/medicoes/b1-2026-09-17/temas-e-documentos-peca1.json:3`, `src/lib/estudos-b1.mjs:13`, `scripts/voz-b1.mjs:23`

3. **Two of the four built pages in the package are not the files the gates and the sixteen plants were run against, by sha256.** `plantas-peca1.json` records the target `dist/estudos/index.html` at `559852b965791b29c6baaeb5c47fcdb90cb67d79f32fdaef306928403f360619` and `dist/estudos/evora-2027-prometido-painel-dinheiro/index.html` at `661f29b89757cc13c922a5227dc1aea3f61bb859a3f2f0e9b842b326a8dfb6c8`, both «antes» equal to «reposto». The copies shipped here hash to `a66791236cc82038a4c62294960103327cfce0e10697b8513d15d369b1c92ec5` and `b90daed6b7187b25a8de44b1aefd5fbb10e1ae33b16fbe9a43b9ed7b6d36828c`. The redirect page `built/estudos/evora-2027-prometido-painel-dinheiro/texto/index.html` does match its recorded `0964e55990c71fcfef724836ed6b2b31ea272f15dccd15c05a8e97e99bb69341`, which shows the sha comparison is meaningful and only these two files diverge. Every green run cited in the report was therefore run on pages that are not the pages in this package, and findings 1 and 5 are what that divergence contains. `design/especime-v3/medicoes/b1-2026-09-17/plantas-peca1.json:9`, `design/especime-v3/medicoes/b1-2026-09-17/plantas-peca1.json:47`, `design/especime-v3/medicoes/b1-2026-09-17/plantas-peca1.json:116`, `built/estudos/index.html:1`, `built/estudos/evora-2027-prometido-painel-dinheiro/index.html:1`

4. **No gate in this package was run on the head the diff delivers.** The package states the diff runs from base `2bf8b238` to head `3da02ca5`. Every measurement that names a head names `dd33e1eff6bb09ded152ab4b6bdf636f52c292f6`: `cabeca-verificacao-final.txt`, `portoes-peca1.txt`, `integridade-peca1.json`, `capturas-depois-peca1.json`, and the report itself («cabeça final conferida»). The three green codes the report tables — build 0, verify 0, typecheck 0 — are therefore claimed for a commit that is not the head under review, and at least one commit of the diff has never been through `npm run build`, `npm run verify` or `npm run typecheck`. An unshown measurement is a claim, not a measurement, and here even the measured object is the wrong one. `relatorio-construtor.md:85`, `relatorio-construtor.md:104`, `design/especime-v3/medicoes/b1-2026-09-17/cabeca-verificacao-final.txt:1`, `design/especime-v3/medicoes/b1-2026-09-17/integridade-peca1.json:3`, `design/especime-v3/medicoes/b1-2026-09-17/portoes-peca1.txt:1`

5. **The Portuguese studies list still prints a sentence that this block's own inventory records as withdrawn, that the closed list does not contain, that the source cannot produce, and that describes furniture the block removed.** `built/estudos/index.html` carries `<p class="sub">Cada estudo publicado, com as suas edições e datas.</p>`. `INVENTARIO-FRASES.md` marks that phrase `B1-peca1 · retirada · explica a página`, `REVISOES-DO-INVENTARIO.md` lists it among the six `lista` withdrawals, and `check-voz-trabalho.log` asserts «432 retiradas, nenhuma rendida». `EstudosView.astro` emits no such paragraph, and the English list does not carry it, so the two editions differ and the built page does not follow the copied source. A walk over `<main>` with `voz-b1.mjs`'s own dispensation rules finds this as the single string on that page outside the closed list, outside the data and outside every dispensation — the exact shape the `planta-frase-fora-da-lista` plant proves `check:voz` catches — and it tells the reader about «edições» that no longer exist on the page. `built/estudos/index.html:2`, `design/especime-v3/INVENTARIO-FRASES.md:1108`, `design/especime-v3/critica/REVISOES-DO-INVENTARIO.md:268`, `src/views/EstudosView.astro:17`, `design/especime-v3/medicoes/b1-2026-09-17/check-voz-trabalho.log:3`, `design/especime-v3/medicoes/b1-2026-09-17/planta-frase-fora-da-lista.txt:9`

## Major

6. **The English edition never tells its reader that a study is in Portuguese, while the Portuguese edition does tell its reader when one is in English.** The marker is guarded by `lang === 'pt' && e.edicao.lang === 'en'`, and `r` is `ROTULOS_B1[lang]`, so `ROTULOS_B1.en.emIngles` («(in English)») can never render: on the English page `lang` is `en` and the condition is false. The built pages confirm it — the Portuguese list renders `<span class="estudo-em-ingles"> (em inglês)</span>` once, the English list renders the class only inside its stylesheet. On `/en/studies` five of the six listed studies are Portuguese-titled (`Penalizações por Reforma Antecipada em Portugal`, `Onde está a água?`, `Água Não Faturada`, `Avaliação Económica Regional de Portugal 2026`, `Evolução de Portugal desde 1981`) and the page says nothing about it; the markup even carries `lang="pt-PT"` on those titles, so the fact is known and withheld. A label declared in the closed list that no code path can reach is also a label no gate can prove. `src/views/EstudosView.astro:24`, `src/data/rotulos-b1.mjs:10`, `built/estudos/index.html:2`, `built/en/studies/index.html:2`

7. **The section renamed «Fontes e verificação» contains neither a source nor a verification for any of its rows.** On the Évora 2027 page it lists 122 rows (118 in English) and every one has `resumo de origem` equal to the placeholder `pdf-sem-resumo` (117 rows) or `raw-sem-manifesto` (5 rows); there are zero `texto-linha-porta` doors and zero `src-chip` marks on either edition. Each row therefore shows an engine identifier, the same number twice (`valor` equal to `impresso` in 120 of 122), and a string that says the digest is missing. The label it replaced was `textoLinhasK`, «As linhas deste documento» / «The rows of this document», which described exactly that; the new label is house prose promising the reader something the section does not carry, and the report's L6 row claims «Mantidos id, ordem, valores, impresso, origem e portas» when no door renders here at all. `built/estudos/evora-2027-prometido-painel-dinheiro/index.html:2`, `built/en/studies/evora-2027-prometido-painel-dinheiro/index.html:2`, `src/views/EstudoView.astro:123`, `src/views/EstudoView.astro:145`, `diff.patch:79687`, `relatorio-construtor.md:26`

8. **The report's L1 ceiling numbers are not the numbers in the code or in the log.** The report writes «O teto desce de 2286 para 2261 páginas, sem alargamento». The diff changes `l1_paginas: 2286` to `l1_paginas: 2271`, the copied `check-lugar.mjs` reads `2271`, and `check-lugar-trabalho.log` prints `L1 · páginas com dois destinos iguais fora da mobília 2271 (teto 2271) ok`. The reported drop of 25 is a drop of 15, and the figure 2261 is reproducible from nothing in this package. `relatorio-construtor.md:32`, `scripts/check-lugar.mjs:200`, `diff.patch:78692`, `design/especime-v3/medicoes/b1-2026-09-17/check-lugar-trabalho.log:6`

9. **The L1 drop is a change of what is counted, and the report's «sem alargamento» is not established by anything here.** The cell now skips a link when `daMobilia.has(a) || a.closest('[data-registo-unidade]')`; the second clause is new and removes from the count every link inside a transcribed unit, on every page of the site, not only the merged study pages. Nothing in the package measures how many of the 15 pages left the count because a duplicate door was removed versus because its links stopped being looked at. The `planta-segunda-porta` plant is run on `dist/estudos/onde-esta-a-agua/index.html`, a study with no content record and therefore no `data-registo-unidade` at all, so it exercises the cell only where the new exemption cannot fire and cannot distinguish the two cases. `scripts/check-lugar.mjs:977`, `relatorio-construtor.md:32`, `design/especime-v3/medicoes/b1-2026-09-17/plantas-peca1.json:136`, `design/especime-v3/medicoes/b1-2026-09-17/planta-segunda-porta.txt:1`

10. **The AI disclosure was removed from the top of the reading pages, and the reason it was there is still written, unchanged, three lines below the change.** `gate-html.mjs` now sets `const esperadoNoTopo = 0;` and the error text immediately under it still reads «O topo é das páginas de leitura, que são texto longo e onde o rodapé chega tarde para o "momento da primeira exposição" do n.º 5 do artigo 50.º». The removed call was `<RotuloDeIA lang={lang} path={path} onde="topo" />` in the old reading view; the report describes this as «Sai a duplicação no topo», which is the one reading the gate's own prose rules out. The Évora page is 255 KB of body, so the single footer label now arrives after the whole document. The invariant the cell protected — disclosure at first exposure on a long page — is now protected by nothing, and the plant that proves the cell still bites (`planta-rotulo-ia-retirado`) only proves the footer label cannot be removed. `scripts/gate-html.mjs:4884`, `scripts/gate-html.mjs:4889`, `diff.patch:80990`, `relatorio-construtor.md:27`

11. **The ten redirect pages ship English prose on the Portuguese site, with no declared language, and every cell that used to read them now skips them.** `built/estudos/evora-2027-prometido-painel-dinheiro/texto/index.html` is `<!doctype html><title>Redirecting to: …</title>…<body><a href="…">Redirecting from <code>…</code> to <code>…</code></a></body>`: no `<html>` element, so no `lang`, and reader-facing English on a `pt-PT` route. `gate-html.mjs` `continue`s out of the `texto` branch before the `<html lang>` check and before the AI-disclosure check; `check-lugar.mjs` `continue`s on `chaveDaRota === 'texto'`; `medir-defeitos.mjs` `continue`s on the same key, so the phrase inventory never sees the text; `cabeca-b1.mjs` exempts the route from H3. What remains is a check of the refresh target, the canonical, one link and the destination file — nothing about language, prose or the disclosure the same gate calls «de cada página». The reader who lands there before the refresh fires, or whose client does not follow it, sees an English sentence the house never wrote in Portuguese. `built/estudos/evora-2027-prometido-painel-dinheiro/texto/index.html:1`, `scripts/gate-html.mjs:4041`, `scripts/gate-html.mjs:4054`, `scripts/check-lugar.mjs:952`, `scripts/medir-defeitos.mjs:1025`, `tests/acessibilidade/cabeca-b1.mjs:5`

12. **The new place-count recount reads only the Portuguese place page and returns a silent zero when it cannot read anything.** `contagensDoPortao` builds `routePath(lugar === 'evora' ? 'municipio' : 'regiao', 'pt', …)` — the language is hardcoded `'pt'` — so the number printed on `/en/studies` (`Évora 6`, `Alentejo 1`) is compared against a count taken from `/municipios/evora`, and an English place page listing a different set of studies is not a case this cell can see. If the file is absent `doc` is `null`, if `#trabalhos` is absent `tit` is `null`, and `?? 0` turns both into the value `0` with no error raised. The `contagem-do-lugar` plant is planted on `dist/estudos/index.html`, the list page, so it proves the declared-versus-rendered comparison and never exercises the recount's own failure paths. `scripts/gate-html.mjs:3386`, `scripts/gate-html.mjs:3391`, `src/lib/prova.mjs:766`, `design/especime-v3/medicoes/b1-2026-09-17/plantas-peca1.json:126`, `built/en/studies/index.html:2`

## Minor

13. **`check:voz` prints «13 temas conferidos», and that number is the count of studies, not of themes verified.** `verificaB1` returns `temas: WORKS.length`, and `check-voz.mjs` prints it as a count of themes checked. It happens to equal 13 because there are 13 studies; it would print 13 whatever the themes were. A count that cannot move with the thing it names is not a measurement of that thing. `scripts/voz-b1.mjs:83`, `scripts/check-voz.mjs:108`, `design/especime-v3/medicoes/b1-2026-09-17/check-voz-trabalho.log:1`

14. **The split between the reading and the text is a hardcoded ordinal with no check behind it.** `EstudoView.astro` takes `titulos.filter(t => t.nivel === 2)[3]` — the fourth level-2 heading — as the boundary, and only when `titulos[0].texto` is exactly `Em resumo` or `In brief`. On a record with fewer than four level-2 headings `depoisDaLeitura` is `undefined` and the reading silently merges into the text; on a record whose first heading is worded differently the same happens. No cell in `scripts/` or `tests/` mentions `estudo-leitura`, `estudo-texto` or the boundary — the only other occurrences are three CSS rules — so a record that changes shape loses the reading with a green run. `src/views/EstudoView.astro:56`, `src/views/EstudoView.astro:57`, `src/styles/estudos-b1.css:9`

15. **The H3 plant does not exercise the branch the block added.** `planta-h3-titulo-retirado.json` records only `{"antes": true, "planta": false, "reposto": true}` from removing a title in memory, which drives `cabecaValida` down the path where the path pattern does not match and the function returns `false` at its second line. The new behaviour is the exception below that: a redirect page with a wrong destination, a wrong refresh target or a second link must still be refused. No plant in `plantas-peca1.json` puts a bad redirect through `cabecaValida`, so the exception itself is proved by nothing, and the report's phrase «foi recusada» describes the old path. `design/especime-v3/medicoes/b1-2026-09-17/planta-h3-titulo-retirado.json:1`, `tests/acessibilidade/cabeca-b1.mjs:5`, `tests/acessibilidade/cabeca-b1.mjs:8`, `relatorio-construtor.md:41`

16. **The redirect page and its destination declare two different canonical URLs for the same page.** The redirect declares `https://xn--oestadodopas-2fb.pt/estudos/evora-2027-prometido-painel-dinheiro/` and the study page declares the same address without the trailing slash. The gate compares them only after stripping a trailing slash (`canonica?.replace(/\/$/, '') !== canonicalUrl(destino)`), so it is written to tolerate the difference rather than to resolve it. The redirect carries `noindex`, which limits the consequence, but two spellings of one canonical is still two answers to one question. `built/estudos/evora-2027-prometido-painel-dinheiro/texto/index.html:1`, `built/estudos/evora-2027-prometido-painel-dinheiro/index.html:1`, `scripts/gate-html.mjs:4049`

17. **The word-list exception for the study route removes the whole transcribed article, not only the transcribed units.** The exception retires `[data-registo-unidade], .texto-artigo`, and `.texto-artigo` is the container of both the reading and the text, so the house's own composed strings inside it — `Secção n de 11` and `Subir ↑`, written by `EstudoView` from `strings.mjs`, not by the record — leave the forbidden-words sweep with the transcription. The stated reason names only «as unidades e os artigos transcritos». The closed list still sees those strings, so the consequence is bounded, but the exemption is wider than its justification. `scripts/voz-palavras.mjs:215`, `scripts/voz-palavras.mjs:216`, `src/views/EstudoView.astro:37`, `src/views/EstudoView.astro:38`

18. **Two claims in the report cite evidence that is not in the package.** The theme table is said to follow `design/especime-v3/maquetas/b1/fazer.py`, «intacto» — that file is not here, and `temas-aplicados-peca1.json` asserts `"coincide": true` without showing what it compared against. The seven commits, their two trailers, the absence of a push and the untouched main tree are stated in prose with no log, no `git` output and no hash list beyond the head. Both are claims, not measurements. `relatorio-construtor.md:74`, `relatorio-construtor.md:97`, `relatorio-construtor.md:112`, `design/especime-v3/medicoes/b1-2026-09-17/temas-aplicados-peca1.json:3`

## «What is fine»

19. **The sixteen file plants each returned a non-zero code with the expected message and each file came back at its starting sha256.** `design/especime-v3/medicoes/b1-2026-09-17/provar-peca1.log:22`, `design/especime-v3/medicoes/b1-2026-09-17/plantas-peca1.json:1`

20. **The five clean reference runs returned 0 and the five post-restoration runs are byte-identical to them.** `design/especime-v3/medicoes/b1-2026-09-17/planta-limpa-gate-html.txt:2`, `design/especime-v3/medicoes/b1-2026-09-17/planta-reposta-gate-html.txt:2`

21. **L8 is unchanged: the same sha256 before and after, at the line the report names.** `design/especime-v3/medicoes/b1-2026-09-17/l8-intacta-peca1.json:1`, `scripts/gate-html.mjs:2208`

22. **The 84 withdrawn strings break down exactly as the report's table says: estudo 13 + 57 + 8, lista 6.** `design/especime-v3/medicoes/b1-2026-09-17/cadeias-retiradas-peca1.json:1`, `relatorio-construtor.md:47`

23. **The index of each edition matches its body heading for heading: all 29 entries resolve to an existing `#bloco-N` whose text is identical, in both editions.** `built/estudos/evora-2027-prometido-painel-dinheiro/index.html:2`, `built/en/studies/evora-2027-prometido-painel-dinheiro/index.html:2`

24. **Every record key on a study page carries that page's own language and slug — 29 index keys and 366 row keys in Portuguese, 29 and 354 in English, with no cross-edition leakage.** `built/estudos/evora-2027-prometido-painel-dinheiro/index.html:2`, `built/en/studies/evora-2027-prometido-painel-dinheiro/index.html:2`

25. **The collection counts the report gives are the counts the ruler read: estudo 26, texto 10, indice 2, edicoes 26, linhas 12.** `design/especime-v3/medicoes/b1-2026-09-17/check-lugar-trabalho.log:4`, `relatorio-construtor.md:20`

26. **The thirteen studies are all reachable — six on the country list, six under Évora, one under Alentejo — and the coverage cell fails on any study that is not.** `scripts/check-lugar.mjs:1924`, `built/estudos/index.html:2`, `design/especime-v3/medicoes/b1-2026-09-17/planta-estudo-sem-acesso.txt:1`

27. **All ten old addresses are preserved and each was walked to its body in the browser.** `scripts/gate-html.mjs:7013`, `design/especime-v3/medicoes/b1-2026-09-17/navegacao-peca1.json:1`

28. **The study pages themselves are clean against the closed list: twelve distinct undispensed strings on the Évora page and five on a study without a record, all of them labels in `ROTULOS_B1` or `strings.mjs`, in both editions.** `built/estudos/evora-2027-prometido-painel-dinheiro/index.html:2`, `built/estudos/penalizacoes-por-reforma-antecipada-2026/index.html:2`, `src/data/rotulos-b1.mjs:4`

29. **Twenty «antes» and twenty «depois» captures exist at five widths in both editions and families, every one with zero lateral displacement.** `design/especime-v3/medicoes/b1-2026-09-17/capturas-antes-peca1.json:1`, `design/especime-v3/medicoes/b1-2026-09-17/capturas-depois-peca1.json:1`

30. **The chain and date counts the report quotes are the counts in the logs: ten records, 1 038 blocks, 2 976 figures, 18 dated editions, 38 dates bound on 28 pages.** `design/especime-v3/medicoes/b1-2026-09-17/check-cadeia-trabalho.log:15`, `design/especime-v3/medicoes/b1-2026-09-17/check-datas-trabalho.log:2`, `relatorio-construtor.md:110`

31. **The three run times add up: 995 + 563 + 4 209 = 5 767 s = 96 min 7 s, and each duration matches its own start and end stamp.** `design/especime-v3/medicoes/b1-2026-09-17/tempo-peca1.json:1`, `relatorio-construtor.md:144`

32. **The «Onde está a água?» opening is the first sentence of `p.standfirst` and the cell compares it against the hosted bytes, `VERBATIM` and `WORKS`, in both editions.** `scripts/voz-b1.mjs:27`, `src/data/verbatim.mjs:61`, `built/en/studies/index.html:2`

33. **The index and the sources fold are `<details>` closed by default, so they work with no JavaScript and the browser run recorded both as closed and openable.** `src/views/EstudoView.astro:85`, `src/views/EstudoView.astro:117`, `design/especime-v3/medicoes/b1-2026-09-17/navegacao-peca1.json:1`

34. **The date cell refuses to pass on an empty measurement: it fails when nothing was printed, when no page was bound, when no date was tied to an edition, and when a declared edition has no page printing its date.** `scripts/check-datas.mjs:189`, `scripts/check-datas.mjs:349`, `scripts/check-datas.mjs:356`, `scripts/check-datas.mjs:367`
