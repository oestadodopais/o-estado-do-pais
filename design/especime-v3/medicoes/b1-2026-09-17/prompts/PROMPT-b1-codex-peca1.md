És o construtor da primeira peça do bloco B1 da reconstrução da superfície do sítio O Estado do País, e trabalhas nesta worktree e só nela. Lê primeiro, por esta ordem e por inteiro: `CLAUDE.md` (as regras do projeto), `design/observatorio/BRIEF-B1-as-quatro-paginas.md` (o brief do bloco; esta peça faz os itens 4, 5, 7c e a parte do 8 que toca às duas famílias, e mais nada), `design/observatorio/ESTRUTURA-e-vocabulario-2026-09-17.md` (§3, §5 e §6: a página do estudo, as três regras, o vocabulário), `design/observatorio/NORMA-como-se-escreve-se-mostra-e-se-organiza.md` (§1 e §4), e as maquetas em `design/especime-v3/maquetas/b1/` (`estudo.html` e `estudos.html` com o CSS ao lado, e as capturas `estudo-*.png` e `estudos-*.png`), que são a especificação visual: o que elas mostram é o que a página faz; o que não mostram não entra.

A peça: (1) a página do estudo (`src/views/EstudoView.astro` e `src/views/TextoView.astro` fundidas numa só vista): `/estudos/<slug>/` passa a ser o texto composto do registo de conteúdo, com o título, a leitura (as três secções da abertura quando o estudo a tem) e o texto; no fim, uma linha «Documento original (PDF) · publicado a <data>» ligada aos bytes fixados em `studies-src/`; a edição inglesa pela troca de língua do sítio; `/estudos/<slug>/texto/` passa a redirecionar para `/estudos/<slug>/` (e o mesmo em `/en/studies/`); a caixa «Edições», as portas «Ler no sítio» e «Ler o documento», a frase «O que cada porta abre», a secção do tema e a das descrições saem; os estudos sem registo de conteúdo (os alojados de fora) mostram o título, a descrição e a linha do documento original, e mais nada. (2) A lista dos estudos (`src/views/EstudosView.astro`): primeiro os estudos sobre o país, do mais recente ao mais antigo; depois a secção «Por lugar» com os lugares que têm estudos (hoje o Alentejo e Évora), cada um ligado à secção dos estudos da sua página e com o número de estudos ao lado; cada estudo com o título (e «(em inglês)» a cinzento quando não há edição portuguesa), o lugar, o tema, a data e as duas primeiras frases da leitura ou, sem leitura, da descrição; sem subtítulo, sem contagens no cabeçalho, sem agrupamento por assunto; a descrição «[a verificar]» de «Onde está a água?» substitui-se pela primeira frase do próprio estudo, transcrita da sua edição. (3) `src/data/studies.mjs`: cada trabalho ganha `tema` (o identificador de um dos dezoito domínios de `src/data/dominios.mjs`) pela tabela do §5.5 do brief, e `SUBJECTS` passa a ser só o lugar; um portão pequeno (em `scripts/`, chamado do `verify`) falha se um trabalho ficar sem `tema` ou com um tema que não exista, com uma planta que o prova. (4) O inventário das frases (`design/especime-v3/INVENTARIO-FRASES.md` e `critica/REVISOES-DO-INVENTARIO.md`): as cadeias do leitor destas duas famílias reduzem-se aos rótulos da lista fechada da estrutura (§5.1) e às cadeias de dados; cada cadeia retirada entra em `REVISOES-DO-INVENTARIO.md` com a razão («explica a página», «segunda porta», «palavra fora do lugar»); o `check:voz` tem de continuar a 0.

As regras que não se quebram, além das do `CLAUDE.md`: commits pequenos, por caminhos explícitos, nunca `git add -A`, cada um com os dois trailers exatos no fim da mensagem, `Co-Authored-By: Codex gpt-6-astra <noreply@openai.com>` e `Claude-Session: https://claude.ai/code/session_01PLc5X7FKC4VzUqmHuus2Ub`; mensagens em português na voz do repositório (lê `git log --oneline -30`); nunca `git push`, nunca `git checkout` fora desta worktree, nunca tocar em `main`; os três portões a 0 na cabeça final, cada um no seu comando com o código de saída escrito num ficheiro em `design/especime-v3/medicoes/b1-2026-09-17/portoes-peca1.txt` (`npm run build`, `npm run verify`, `npm run typecheck`); nenhum portão se enfraquece nem se contorna: se um portão parecer impedir o que o brief manda, escreve o caso no relatório com o que o portão protege e para nesse ponto; nenhum número escrito à mão; nada novo em largura além do CSS que a maqueta mostra (`maqueta.css`, passado para a folha do sítio no mesmo espírito); prosa nova em português do Acordo Ortográfico, sem travessões; o vocabulário do §6 da estrutura em cada cadeia nova. As capturas: as duas páginas nas cinco larguras (390, 768, 1 024, 1 280, 1 600) e nas duas edições, antes (o `dist/` de `main`, construído antes de mudares o que quer que seja) e depois, em `design/especime-v3/capturas/b1-2026-09-17/`, com o Playwright que está em `node_modules`; confere em cada captura «depois» que a página não desloca de lado a 390.

No fim escreve o relatório em `design/especime-v3/medicoes/b1-2026-09-17/LEIA-ME-peca1.md`: a tabela dos quatro pontos com a medida de cada um; a tabela das cadeias que saíram, por família, com a razão; a tabela dos treze temas; os commits e a cabeça; os três códigos; as capturas; o custo em tempo de parede; o que ficou por fazer e porquê. Prosa em português, curta, sem travessões. Responde com esse relatório.
