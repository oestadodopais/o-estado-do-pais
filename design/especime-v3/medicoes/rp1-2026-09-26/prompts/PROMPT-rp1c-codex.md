És o construtor da peça RP1c do bloco RP1 do projeto O Estado do País (um sítio Astro em `~/Instruments/OEstadoDoPais` e um motor em Python em `~/Instruments/ResearchHub`): **a passagem de correção depois da leitura a frio** das oito medidas da primeira entrega, feita pelo Claude Opus 5.5 e guardada em `design/especime-v3/critica/LEITURA-rp1-2026-09-26.md` (com as cinco plantas registadas em `LEITURA-rp1-2026-09-26.plantas.json`, todas achadas). Trabalhas na worktree do sítio em que foste lançado (o `-C` da linha de comando: o ramo `rp1-2026-09-26`, cuja cabeça já tem as oito medidas da primeira entrega, as quatro da peça RP1b e a terceira redação das leituras do lugar de direção) e na worktree do motor `~/Instruments/ResearchHub/.worktrees/rp1-2026-09-26` (o ramo `rp1-2026-09-26`). Nunca `git checkout` noutra árvore, nunca `push`, nunca `git add -A` nem `git add .`: só caminhos explícitos. Prosa nova em português (Acordo Ortográfico), sem travessões; o vocabulário do §6 de `design/observatorio/ESTRUTURA-e-vocabulario-2026-09-17.md`. **Nenhum ficheiro que escrevas leva um caminho da máquina** (nada que comece por `/Users/`): nos relatórios, nos registos e nos guiões, a worktree diz-se `<worktree do sítio>` e o motor `~/Instruments/ResearchHub`.

## O teste de aceitação, dito antes

Feito quer dizer: cada achado da leitura a frio que a tabela abaixo manda corrigir está corrigido no sítio ou no motor, com a planta que a tabela pede a morder; as leituras do sítio são iguais, palavra a palavra, à terceira redação de `design/observatorio/leituras/LEITURAS-rp1-2026-09-26.mjs`, salvo os acertos que a auditoria K17 exigir, cada um dito com o literal; nenhum ficheiro do bloco (a pasta `design/especime-v3/medicoes/rp1-2026-09-26/` inteira, incluindo os guiões, as respostas e os registos) contém um caminho da máquina, e o guião das medições mede esse zero com um conhecido-positivo; os três portões a 0 na cabeça final; as capturas dos cartões que mudaram; o relatório com a secção desta peça, a lista de todos os commits do ramo e o `medidas.json` estendido.

## O que lês primeiro, por esta ordem

1. `design/observatorio/MAPA-DO-REPOSITORIO-para-construtores.md`, inteiro, antes de abrir código.
2. `design/especime-v3/critica/LEITURA-rp1-2026-09-26.md`, inteira: cada achado tem as referências `path:line` do pacote da leitura, que correspondem aos caminhos do repositório (o `built/` do pacote é o `dist/`; o `motor/` é o motor).
3. `design/observatorio/BRIEF-RP1-rendimentos-e-precos-os-cartoes.md` (o §8 e o §2), `design/observatorio/leituras/LEITURAS-rp1-2026-09-26.mjs` (a terceira redação, com o cabeçalho), e os relatórios das duas entregas anteriores em `design/especime-v3/medicoes/rp1-2026-09-26/LEIA-ME.md` (a secção RP1b no fim) e as respostas `RESPOSTA-codex-rp1.md` e `RESPOSTA-codex-rp1b.md`.
4. No sítio: `src/components/Claim.astro`, `CartaoDaMedida.astro`, `DataDaLinha.astro`, as páginas de linha (o recibo: `src/pages/livro-razao/[id].astro` ou o caminho que o mapa disser), `src/data/medidas-rp1.mjs`, `origens-rp1.mjs`, `leituras-rp1.mjs`, `tests/cartao/leituras.mjs` (a K17 e `PALAVRAS_DE_LIGACAO`), `tests/cartao/rp1.mjs`, `tests/pais/enquadramento-b2.mjs`, `scripts/check-formas.mjs`, `scripts/leituras/conferir-relatorio.py`, `design/especime-v3/medicoes/rp1-2026-09-26/{medir-rp1.mjs,medir-l1-rp1.mjs,origens-rp1.py,acertos-rp1.py,relatorio-rp1.py,captar-rp1.mjs}`.
5. No motor: `publisher/dominios_rp1.py`, `publisher/dominios_readers.py` (`ine_indicator()`, o excerto cortado na bandeira), `indicators/out/rp1-2026-09-26/pedidos.jsonl`, `pedir.py`, `alojar.py`.

## A tabela da correção (o número é o do achado na leitura a frio)

| achado | o que se faz | a prova |
|---|---|---|
| 1, 2, 3, 4, 12 | Nada: são as cinco plantas do pacote (o valor 2,41; o ramo «Mais do que» do RSI; «inserção profissional»; a régua dos alimentos; as 18 linhas), todas achadas. O relatório di-lo. | o registo `LEITURA-rp1-2026-09-26.plantas.json` |
| 5 | Já feito na segunda redação (o verbo pelo sinal nas médias dos doze meses); confirmas que o sítio a tem. | as duas frases em `leituras-seladas.json` a dizer «subiram» / «rose» |
| 6 | A terceira redação do RSI: «havia N pessoas a receber o rendimento social de inserção por cada mil pessoas em idade ativa, dos 15 aos 64 anos: é o apoio da Segurança Social a quem vive em carência económica grave, com um programa de inserção no trabalho e na comunidade». Os literais: a idade ativa na metainformação do INE `0013420` (a página `minfo.jsp?var_cd=0013420`, já alojada em `014-ine-0013420-minfo.html`; se ela não fixar os 15 e os 64, a página do INE do conceito «população em idade ativa» pedida pelo cliente da casa; se nenhuma origem oficial fixar as idades, os dois `nl` saem e o relatório di-lo); «carência económica grave» na página oficial da Segurança Social sobre o RSI (`seg-social.pt`, pedida pelo cliente da casa e alojada no estudo 13, uma origem nova em `origens-rp1.mjs`); se a página disser a coisa por outras palavras, a leitura usa as palavras dela e o acerto vai no relatório com o literal. | a K17 a 0; `acertos-rp1.json` com o acerto, se houver |
| 7 | Já feito na segunda redação (frases curtas, sem «to live on», as duas edições a dizer o mesmo); confirmas. | `leituras-seladas.json` |
| 8 | A bandeira «provisório» deixa de ser uma palavra colada ou solta a seguir à unidade: rende-se entre parênteses com as palavras da nota da fonte, « (dado provisório)» em português e « (provisional data)» em inglês, no valor do cartão, dentro das leituras e no cabeçalho do recibo, com o espaço antes do parêntese no texto acessível; a K17, a M8 (`tests/inicio/areas.mjs`) e a conferência das marcas em `tests/cartao/rp1.mjs` seguem a forma; o literal é a nota da própria resposta («Dado provisório») e a nota inglesa que o motor já escreve. | a planta da bandeira colada a morder; as capturas do cartão da remuneração nas duas edições |
| 9 | Nenhum caminho da máquina nem o nome do utilizador da máquina em ficheiro nenhum do bloco: trocas `/Users/<o utilizador>/Instruments/OEstadoDoPais/.claude/worktrees/rp1-2026-09-26` por `<worktree do sítio>`, `/Users/<o utilizador>/Instruments/ResearchHub` por `~/Instruments/ResearchHub`, e qualquer caminho de `/private/tmp/claude-501/-Users-<o utilizador>-…` por `<scratchpad da sessão>`, em todos os ficheiros de `design/especime-v3/medicoes/rp1-2026-09-26/` (respostas, guiões, registos, portões, prompts) e de `design/especime-v3/critica/LEITURA-rp1-*`; o nome do utilizador (a palavra que vem a seguir a `/Users/` nesta máquina) não fica em nenhum deles, em nenhuma forma; `medir-rp1.mjs` mede o número de ficheiros do bloco com `/Users/` ou com esse nome (tem de ser 0, com um conhecido-positivo: um ficheiro de ensaio, fora do repositório, com o caminho que o detetor vê). Os ficheiros antigos de outros blocos não se tocam: ficam para uma decisão do diretor. | a medição a 0 no `medidas.json` |
| 10 | «É a inflação» precisa de um literal que ligue a palavra «inflação» à taxa de variação homóloga do IPC: procuras nos corpos alojados (o documento metodológico `017-ine-ipc-metodologia.txt`, o destaque `025-ine-ipc-agosto-2026.txt`, a metainformação) uma frase em que o INE diga que o IPC mede a inflação ou que a variação homóloga é a taxa de inflação; se a achares, ela sustenta a folha e substitui a citação da média dos doze meses; se não a achares, a leitura passa a «É a subida geral dos preços, que o INE mede num cabaz de bens e serviços que representa o que as famílias compram.» e «That is the general rise in prices, which the INE measures on a basket of goods and services that represents what households buy.», sustentada pela definição do IPC já alojada, e o acerto vai no relatório. | a K17 a 0 com a folha nova; o literal no relatório |
| 11 | `PALAVRAS_DE_LIGACAO` passa a ser por língua (uma lista para o português, outra para o inglês); «no» fica só na portuguesa. | uma planta em `tests/cartao/rp1.mjs`: uma parte inglesa «There were no» sem literal é recusada |
| 13 | O recibo toma o `lang` de cada origem do seu campo `lingua` (a ligação do documento e o bloco do excerto), nas duas edições. | uma planta: uma origem `lingua: 'pt'` rendida com `lang="en"` é recusada por `check:lingua` ou pela célula que o mapa disser |
| 14 | No recibo, o cabeçalho ordena as três peças como o cartão (o valor, a unidade, a bandeira entre parênteses). No motor, o excerto de um valor do INE com bandeira passa a ser o objeto literal inteiro da categoria, como nas linhas sem bandeira (com `"ind_string" : "1 835 &"` e os campos da bandeira tal como a resposta os escreve), e o sítio lê a bandeira do campo da linha, não do excerto; `dominios_rp1_test.py` cobre-o; a linha da remuneração é reexportada com o excerto inteiro (é uma mudança de excerto, não de valor: a V16 não pede entrada, mas o relatório di-lo). | a planta de um excerto cortado a meio de uma cadeia JSON a ser recusada pelo teste do motor; o recibo construído |
| 15 | A linha do valor do cartão diz «no 2.º trimestre de 2026» e «in the 2nd quarter of 2026» (a preposição pela forma do período: «em» e «in» para meses e anos, «no» e «in the» para trimestres), pela `DataDaLinha` ou pelo cartão, com o `check:formas` F1 a recompor a forma. | a planta em `check:formas`: «em 2.º trimestre» é recusado |
| 16 | Os nomes: `pensao-media-anual-2025` passa a «Pensão média anual» / «Average annual pension» (e a linha de 2024 igual); `ipc-alimentacao-variacao-homologa` passa a «Preços dos alimentos e das bebidas não alcoólicas» / «Prices of food and non-alcoholic drinks» (e a linha do período anterior igual); `lingua-dos-titulos.mjs` e a K7 seguem. | as capturas dos dois cartões nas duas edições |
| 17 | Nada aqui: fica registado pelo lugar de direção como assunto do motor (os excertos do Eurostat montados a partir das etiquetas). | |
| 18 | A origem `rp1-rendimento-equivalente` passa a citar um pedido do próprio bloco: o cliente da casa volta a pedir a página do glossário do Eurostat, regista-o em `pedidos.jsonl` e aloja-o; `origens-rp1.py` confere o alojamento de TODOS os selos, seja qual for a pasta. | o guião a passar com a origem nova; a planta de um selo sem ficheiro alojado a ser recusada |
| 19 | A terceira redação das pensões («no total das pensões de velhice, de invalidez e de sobrevivência») apoia-se nas categorias da dimensão «Tipo de pensão» da resposta alojada do INE `0014532` («Total», «Velhice», «Invalidez», «Sobrevivência», como a resposta as escreve), e não no literal da doença profissional; a origem `rp1-pensoes-tipos` cita esses literais. | a K17 a 0 com os literais novos |
| 20 | `tests/pais/enquadramento-b2.mjs` passa a percorrer também as réguas declaradas (`REGUAS_DECLARADAS`), de modo que a prova de que nenhuma linha de régua ganha cartão próprio cubra as do RP1; o relatório deixa de citar esse teste como prova do que ele não via. | o registo `enquadramento-b2.json` com as linhas do RP1 |
| 21 | Nada muda na regra (uma estimativa rápida publica-se antes de o período acabar); acrescentas a planta que falta (uma publicação dentro do período passa) e a nota das linhas do período anterior diz que o `published_at` é a data da última atualização do quadro, que a API dá por quadro e não por período. | a planta em `tests/cartao/rp1.mjs`; as notas das linhas |
| 22 | O relatório lista todos os commits do ramo (os do lugar de direção e os teus, das três peças), e diz de que cabeça são os códigos dos portões. | a lista no relatório, conferida por `relatorio-rp1.py` |
| 23 | `medir-rp1.mjs`: o guarda das classes usa os códigos como o INE os escreve («045», «0722», «041») e mede que nenhum deles está na dimensão 3 do `0014663`; `medir-l1-rp1.mjs` perde as funções que não chama, e o ficheiro de referência do B2 que ele lê (`l1-depois.json`) fica citado pelo caminho no relatório, para o pacote da leitura o levar. | o `medidas.json` regenerado |

## O que não fazes

Nenhum valor de linha nenhuma muda. Nenhuma medida nova. Nenhum gráfico. Nenhuma mudança à leitura do país nem à regra da primeira página. O texto do rótulo de IA não se toca. Nenhum ficheiro de outro bloco se toca por causa dos caminhos da máquina. Nenhum `push`. Não tocas em `indicators/availability.json`, em `indicators/vintages.json`, em `.maintenance-locks/`, em `sweeps/` nem em `publisher/recortes/manifest.regioes.json` no motor.

## As regras de trabalho

- A regra de paragem: só um portão que protege um número, uma fonte ou uma pessoa te faz parar; o que encoda mobília muda de forma conservando o que protege, com uma planta que morde. Se ao corrigir achares que a tabela está errada num ponto (um literal que não existe, uma forma que a célula não admite), medes, dizes e paras nesse ponto, e corriges o resto.
- Commits pequenos por caminhos explícitos, com os trailers contíguos `Co-Authored-By: Codex gpt-6-astra <noreply@openai.com>` e `Claude-Session: https://claude.ai/code/session_01B4Zx94pG7dxxQNpoEn1bjP`; no motor só o `Co-Authored-By`, e o pre-commit corre `python3 -m core.gate` (cerca de dois minutos e meio). Este guião já está no ramo.
- Entre commits correm só as conferências que a mudança toca; os três portões inteiros (`npm run build`, `npm run verify`, `npm run typecheck`) uma vez, na cabeça final, cada um no seu comando com o código de saída escrito num ficheiro acabado de escrever em `design/especime-v3/medicoes/rp1-2026-09-26/portoes/rp1c/`. O teu último commit é o de entrega das provas, e o relatório diz de que cabeça são os códigos.
- Uma construção de cada vez na máquina: não corras portões enquanto outro processo os corre.
- As capturas dos cartões que mudaram (a remuneração, as pensões, o RSI, os alimentos, a inflação) nas duas edições, com o prefixo `rp1c-`, e as cópias congeladas das duas páginas do depois regeneradas com o `INDICE.json` na cabeça final; o relatório `LEIA-ME.md` ganha a secção «RP1c» no fim (a tabela acima com o resultado de cada linha; as células que mudaram de forma e o que protegem; as plantas; os commits; os códigos; o custo); `medir-rp1.mjs` estende `medidas.json` numa chave `rp1c`; `python3 scripts/leituras/conferir-relatorio.py` a zero faltas; a tua resposta curta em `design/especime-v3/medicoes/rp1-2026-09-26/RESPOSTA-codex-rp1c.md`.
- Escreves o relatório também quando paras a meio: o que fizeste, onde paraste, o que o portão protege.
