RP1c concluída: os achados mandados corrigir estão tratados, sem mudar valores nem acrescentar medidas. As leituras seguem a terceira redação, com quatro acertos apoiados por literais: «pobreza extrema» no RSI e a alternativa «subida geral dos preços» no IPC, nas duas edições.

`build`, `verify` e `typecheck` terminaram a 0 na cabeça `68215ae9a73910f59eb103c16961be52bd901fae`. O motor ficou em `4eb2867936dd14ad2654751722e390804e68dda3`, com o pre-commit a 0. A K17, as plantas pedidas e a conferência dos números do relatório passaram. O detetor de caminhos pessoais deu 0 e encontrou o conhecido-positivo.

Estão guardadas 20 capturas de página, 50 recortes `rp1c-` e as páginas congeladas. Foram abertas 20 imagens para inspeção. O relatório em `LEIA-ME.md`, secção «RP1c», contém os literais, o resultado de cada achado, a diferença entre a K7 citada no mandato e as células dos nomes, todos os commits e os códigos dos portões. `medidas.json` tem a chave `rp1c`.

O último commit entrega apenas as provas e é identificado por `HEAD` no relatório, com o pai conferido por `relatorio-rp1.py --verifica`. O achado dos excertos Eurostat permanece fora desta peça, como mandado. Sem `push`.
