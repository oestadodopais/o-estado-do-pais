Green light for the round after the merge of phase 1 of the redesign v3, site repo ~/Instruments/OEstadoDoPais, on a branch `pos-fusao-v3` from `origin/main`. Site repo only: anything that needs the engine (~/Instruments/ResearchHub) is an ask, not a task, except where this prompt says «engine session» below, which is its own session in that repo. Nothing outward, no deploy, no merge, without my explicit go. Standing arrangement: Fable manages and reviews, Opus builds, Sonnet does mechanical passes and blind measurements, Codex reads across families with planted defects; say which model ran which part in every report. Verification policy as set on 21.08: the five gates at every commit; full matrices, captures and blind reads only at stage exits and before a preview or merge; fix rounds measure only what they touch; my notes per preview batched into one round.

READ FIRST, IN THIS ORDER
1. `design/especime-v3/ESTADO-DO-MAIN-2026-08-21.md`: what `main` is today (`53272ac` at the time of writing; say what moved since).
2. `design/especime-v3/PLANO-redesenho-v3.md` §3 (the decided table), §14a/§14b (previews and merge), §15 (deviations); `DECISIONS.md` §1.50 to §1.56 and §4.
3. The constitution: ~/Obsidian/Experiments/O Estado do País — Constituição visual v3.1.md with Emendas 1 to 17 (binding; the repo copy `design/especime-v3/direcao.md` follows it), including the sentence rule under Emenda 15: a sentence survives on a reader's page only if removing it would make a reader misread a number.
4. `design/especime-v3/ISSUES.md` (open items), `INVENTARIO-FRASES.md`, `CHAVES-EN.md`, and `notas/stage-5.md` §10 (the last requests).
5. The vault: ~/Obsidian/Experiments/O Estado do País.md, §Thirteenth block.

SCOPE OF THIS ROUND, IN ORDER
A. Post-merge duties left at the merge: rewrite `scripts/design-bundle.mjs` for the v3 identity (tokens, the three faces, the seal and marker, the ruler grammar, the map in dots, the state pair, the cards), regenerate the design bundle from `dist/`, run DesignSync against the Claude Design project «O Estado do País» (id `ecbc1701-fb7a-4223-b5a4-bfa7188c398b`), and rewrite `README.md`'s identity section to v3 (ISSUES I16). Then the small ISSUES: I13/I14 (the v2 convergence instrument's inline seals below 44px: give the instrument the piece grammar of the home or a measured exception), I59 (card PNGs: a palette encoder if it adds no risk, else record why not), I53 (English 404: put the `vercel.json` rule to me as a one-line decision before touching it).
B. The studies' transition to the new approach, staged: (1) site, now: the hosted-document banner in the v3 chrome (wordmark, «documento do estudo» / «study document», the door back to the study page; no sentence about the site; `check:documentos` must still prove the document bytes untouched); (2) engine session, plan-gated (estimate first): a v3 document template (light paper, Spectral and Bitter self-hosted by the site, the state pair, the receipt island as today, no self-explaining prose: the sentence rule applies to the document's own voice), the 15 editions re-rendered from the engine's ledgers, `ledger.json` pinned beside each document so its sentence becomes true, then the republication block on the site exactly as `DECISIONS.md` §1.49 did it (manifest, digests, gate); (3) the six works without a reading page listed to me with what each needs, because writing them is editorial and mine.
C. Engine asks, in the engine session: the MIP rows missing from the ledger (long-term unemployment, youth unemployment; confirm the 13th headline indicator of the 2026 AMR against the Commission's list and add it if a row can be sourced) so the home's counts move by themselves; the duplicated `rcpt-data` island in one hosted document (I60).
D. Watch Monday's panel refresh (24.08): the first automatic rebuild on the new base with the cards and the generated lede; report whether any value or count moved and whether the build stayed green.

DECIDE WITH ME BEFORE THE STAGE THAT NEEDS THEM
- the voice phase (frame phrases 92 distinct against the constitution's ≤ 12): this round after A and B1, or after the housing reading page (my leaning: after the housing page, since the inventory already holds self-reference at zero);
- the English 404 route (a `vercel.json` rule);
- the studies' document template: what the document keeps of its own voice («Read this part first», the limits) and what goes.

STOP AND ASK, DO NOT IMPROVISE AROUND: the same five cases as the «luz verde» prompt of 20.08 (a conflict with repo reality, a gate or a standing decision; a value or copy with no row; an editorial call; a new gate; a constitution rule that cannot be met as specified).

REPORTING: as before, at each stage boundary: what shipped, screenshots at 1280 and 390 in both editions where a page changed, gate and ruler status with deltas, EN keys, the ISSUES list, which model ran which part and the tokens. Start by telling me in ten lines what you read, what moved on `main` since `53272ac`, what the Monday refresh did if it has run, and what stage A will commit. Then go.
