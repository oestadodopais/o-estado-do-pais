# Leitura a frio do Codex ao bloco F2.6 (o calendário a sério, no motor), 04.09.2026

*Codex `gpt-5.6-sol`, xhigh, só leitura, 09:34 a 09:48 UTC, sobre um pacote do motor com cinco plantas de três classes (5 de 5 vistas; o registo está no `.plantas.json` ao lado): P1a e P1b, o caminho da difusão atrasada a cair outra vez de `pedir`, na cópia e no diff, apanhada no terceiro Major («`atrasados` is counted but never unioned into `pedir`»); P2, a célula do provador a passar com uma só contagem certa, apanhada no primeiro Blocking e no quarto Major; P3, o relatório a dizer 16 de 79 na tabela e 6 no resto, apanhada no primeiro Blocking; P4, um evento do calendário sem origem da data, apanhada no primeiro e no segundo Blocking. Triagem do lugar de direção (pela delegação de 04.09, §1.98): o terceiro Blocking é do pacote (as cópias do ensaio foram como ficheiros e não no diff); tudo o resto é real e pesa: a regra do INE era documentação e não guarda (o leitor do INE corre em qualquer máquina e em cada corrida, porque a marca vive numa pasta nova por corrida); os resultados vivos do Eurostat nunca entram no `calendar.json` (o escritor copia só resumos, o montador ignora-os e o fluxo não passa a bandeira), o que esvazia o leitor; datas escritas à mão na função que devia lê-las (2026-11-04, a constante 2026-09-01, os valores por omissão da linha de comandos); 35 dos 38 eventos sem `afecta_linhas`, e por isso sem efeito no meio-dia, com menções de datas de outras operações e excertos truncados; a busca por janelas de 31 dias a parar na primeira janela vazia e a confundir um JSON malformado com um calendário vazio; um endereço com a única data no passado a ser pedido todos os dias por falta de um estado de confirmação; os provadores fracos (cinco eventos onde o relatório diz dez, datas ordenadas, JSON sintético, o `calendar.json` entregue nunca validado); e a contabilidade da rede que o construtor fez com quatro leituras por `WebFetch` a páginas de documentação fora do cliente da casa, contra a regra do bloco. O Python 3.12 do fluxo fica como o F0.7 o deixou (o congelamento resolve em 3.12 e 3.14). A segunda passagem é grande e fica escrita; corre nesta sessão se houver tempo, ou na seguinte. O texto do leitor fica como veio.*

---

## Blocking

- The copies are not the patch. Three lines differ: C1 is changed from **6/79** to **16/79** (`diff.patch:68`, `relatorio-construtor.md:22`); `origem_da_data` is renamed to `origem_da_data_apagada` (`diff.patch:579`, `indicators/calendar.json:17`); and the past-date assertion is weakened from `or` to `and` (`diff.patch:2919`, `indicators/calendario.py:1232`). The report consequently contradicts itself, saying 6, 16, and 6 again (`relatorio-construtor.md:12`, `relatorio-construtor.md:22`, `relatorio-construtor.md:240`).

- C2 and C7 are false in the delivered copy. `inventario-2026-06-22` is dated but has no recognized `origem_da_data`; the measurement code would list it as missing origin (`indicators/calendar.json:6`, `indicators/calendar.json:14`, `indicators/calendar.json:17`, `indicators/calendario.py:1121`). Thus the result is 19 inventory dates present, **18 with valid origin**, and **1 date without origin**, not 19/19 with origin and zero missing (`relatorio-construtor.md:23`, `relatorio-construtor.md:28`).

- The patch omits the archived calendar fixtures, manifest, and `LEIA-ME.md`, although the report declares that directory new. The patch’s final section is `provas_test.py` and then ends; applying it would leave the new provers without their required files (`relatorio-construtor.md:468`, `diff.patch:3195`, `diff.patch:3222`, `indicators/calendario.py:1242`, `indicators/calendario.py:1304`).

- The INE restriction is documentation, not a guard. `reler_calendarios` always calls `reler_ine`; the corredor does not test GitHub, `ensaio`, or machine identity before doing so, and the CLI exposes the same path locally (`indicators/calendario.py:559`, `indicators/calendario.py:642`, `indicators/calendario.py:655`, `indicators/corredor.py:615`, `indicators/corredor.py:624`, `indicators/corredor.py:2414`). The workflow invokes that path in both `ensaio` and scheduled `real` (`.github/workflows/corredor.yml:223`, `.github/workflows/corredor.yml:424`, `.github/workflows/corredor.yml:429`). Worse, its marker lives in a fresh per-run `relatorio/`; a missing marker means “read now”, and no previous artifact is restored, so the purported weekly read occurs on every active run (`.github/workflows/corredor.yml:153`, `indicators/calendario.py:243`, `indicators/calendario.py:246`). The reported laptop `.last` timestamp is not packaged and proves historical through-client traffic only, not enforcement; whether an INE request actually left the laptop remains `[verify]` (`relatorio-construtor.md:34`, `relatorio-construtor.md:40`).

- The network accounting violates the stated rule. Besides the one cited house-client request, the report admits four Eurostat `WebFetch` requests that bypass `core/http.py` and the house User-Agent, plus four `eventsJson` requests in the runner: at least five Eurostat-calendar requests before counting WebFetch (`relatorio-construtor.md:48`, `relatorio-construtor.md:52`, `relatorio-construtor.md:79`, `relatorio-construtor.md:89`, `relatorio-construtor.md:337`, `relatorio-construtor.md:340`). The implemented reader itself makes two forms per first window and can make up to seven requests over six windows, not one (`indicators/calendario.py:484`, `indicators/calendario.py:496`, `indicators/calendario.py:608`, `indicators/calendario.py:613`).

- Live calendar results cannot enter `calendar.json`. `escrever_calendario` copies only `relido_em` and publisher summaries, never `lidas[*].datas`; `montar` consumes archived INE plus the inventory but never Eurostat live results; and the workflow does not pass `--escrever-calendario` at all (`indicators/calendario.py:703`, `indicators/calendario.py:705`, `indicators/calendario.py:1071`, `indicators/calendario.py:1078`, `.github/workflows/corredor.yml:424`). Therefore a successful 31-day response cannot attach `datasetCodes`, raise coverage toward 41, or affect noon selection as claimed (`relatorio-construtor.md:444`, `relatorio-construtor.md:447`).

- Measurable provenance is hard-coded. The new employment event contains literal date `2026-11-04`, although the same function already reads dates from the inventory; all inventory access dates and labels use the constant `2026-09-01`; CLI defaults also hard-code the rehearsal ID and reading date (`indicators/calendario.py:818`, `indicators/calendario.py:901`, `indicators/calendario.py:907`, `indicators/calendario.py:1023`, `indicators/calendario.py:1028`, `indicators/calendario.py:1418`). A rebuild with newer inputs can therefore stamp old provenance.

## Major

- The event contract is not met semantically. Of 38 dated/window events, 35 have no `afecta_linhas`; only three events carry the six affected line IDs (`indicators/calendar.json:636`, `indicators/calendar.json:692`, `indicators/calendar.json:722`, `relatorio-construtor.md:405`). Some “events” are merely date mentions explicitly belonging to another operation, such as 22 June (`indicators/calendar.json:14`, `indicators/calendar.json:20`). One origin points to Eurostat while its excerpt attributes the date to INE (`indicators/calendar.json:509`, `indicators/calendar.json:512`), and several alleged full sentences are fragments or truncated at `3.` (`indicators/calendar.json:615`, `indicators/calendar.json:729`, `indicators/calendar.json:731`). The eight undated placeholders also literally lack origins, though they do not fabricate dates: `ce-*`, DGAL, ERSAR, Évora, IEFP, and two INE placeholders (`indicators/calendar.json:951`, `indicators/calendar.json:998`, `indicators/calendar.json:1051`, `indicators/calendar.json:1076`, `indicators/calendar.json:1100`, `indicators/calendar.json:1153`, `indicators/calendar.json:1206`, `indicators/calendar.json:1233`).

- The 31-day hypothesis is implemented and honestly labelled unproven, but its control flow is unsound. A release-free first 31-day window stops the entire 180-day search, so releases in later windows are never requested (`indicators/calendario.py:484`, `indicators/calendario.py:608`, `indicators/calendario.py:630`, `relatorio-construtor.md:195`, `relatorio-construtor.md:200`). Malformed JSON and unexpected response shapes also become `[]`, indistinguishable from a valid empty calendar (`indicators/calendario.py:448`, `indicators/calendario.py:451`, `indicators/calendario.py:462`). It is not literally silent—the report says zero recognized dates—but it does not distinguish “no events” from parser/shape failure (`indicators/calendario.py:659`, `indicators/corredor.py:2757`).

- Noon selection is correct only for the narrow past-only case. With no releases and every address future-dated it asks zero; with one date today it asks that address; with the real ledger on 04.09 it asks the 73 addresses without future calendars (`indicators/calendario.py:1170`, `indicators/calendario.py:1183`, `relatorio-construtor.md:240`, `relatorio-construtor.md:243`). An address whose sole date is past is asked every subsequent day as “without calendar”, because no confirmation state exists (`indicators/calendario.py:185`, `indicators/calendario.py:216`, `indicators/calendario.py:225`). Conversely, an address with both an overdue event and a future event is not asked: `atrasados` is counted but never unioned into `pedir`, contradicting the report’s claimed three paths (`indicators/calendario.py:218`, `indicators/calendario.py:225`, `relatorio-construtor.md:227`).

- The provers can pass broken readers. The INE fixture requires only five events despite the claimed ten and does not assert correct title/date/period association; its swapped-date test compares sorted dates only (`indicators/calendario.py:1248`, `indicators/calendario.py:1249`, `indicators/calendario.py:1288`). The Eurostat tests exercise synthetic JSON, not request generation, live shape, window traversal, or persistence (`indicators/calendario.py:1316`, `indicators/calendario.py:1336`). The past test’s planted `and` lets either wrong counter pass (`indicators/calendario.py:1232`). Nothing in `--provar` loads the delivered `calendar.json`, so the missing origin and false report count survive. The two reader mutants described in prose would be caught, but that prose is not an executable mutation test and proves only those two cases (`relatorio-construtor.md:363`, `relatorio-construtor.md:369`).

- The acceptance evidence is not reproducible from this package. `calendario.py` imports the absent `indicators.upcoming`; the aggregate prover invokes absent `arquivo.py` and `provar_fluxo.py`; and `--medir` requires an external ledger and inventory (`indicators/calendario.py:53`, `indicators/provas_test.py:91`, `indicators/provas_test.py:99`, `indicators/calendario.py:1393`, `indicators/calendario.py:1414`). The code protects only a floor of 109, not the reported exact 124, and no test log is included (`indicators/provas_test.py:90`, `indicators/provas_test.py:106`, `indicators/provas_test.py:174`, `relatorio-construtor.md:27`). The run ID is repeated consistently, but “green”, duration, commit, the previous run, and 79-address denominator remain `[verify]`; the manifest contains no run ID or commit tying its bytes to that run (`relatorio-construtor.md:107`, `relatorio-construtor.md:115`, `indicators/calendarios/manifesto.json:1`, `indicators/calendarios/LEIA-ME.md:5`).

- The delivered rehearsal workflow tests Python 3.12, not the repository’s declared Python 3.14 runtime (`.github/workflows/corredor.yml:239`, `.github/workflows/corredor.yml:242`).

## Minor

- The module-level measurement note still says 91 machine addresses, while the brief, workflow comment, and report use 79; the stale denominator makes future reviews ambiguous (`indicators/calendario.py:24`, `brief.md:34`, `.github/workflows/corredor.yml:88`).

## What is fine

- Structurally, the file has 46 events, 38 dated/window events, 19 distinct `inventario-*` entries, and the three linked events name six line IDs; the sole origin defect is the one listed above (`relatorio-construtor.md:376`, `relatorio-construtor.md:384`, `indicators/calendar.json:627`, `indicators/calendar.json:681`, `indicators/calendar.json:715`).

- The archived files’ byte counts and SHA-256 values agree with the manifest and `LEIA-ME.md` (`indicators/calendarios/manifesto.json:12`, `indicators/calendarios/manifesto.json:20`, `indicators/calendarios/LEIA-ME.md:12`, `indicators/calendarios/LEIA-ME.md:15`).

- The Python calendar call sites themselves use `cliente.condicional`; the corredor constructs that client with the house User-Agent, and `core/http.py` applies the host gate (`indicators/calendario.py:577`, `indicators/calendario.py:613`, `indicators/corredor.py:606`, `core/http.py:397`, `core/http.py:436`).

- The noon cron remains commented and carries the 6/79 count (`.github/workflows/corredor.yml:87`, `.github/workflows/corredor.yml:106`).

**13 distinct findings.**