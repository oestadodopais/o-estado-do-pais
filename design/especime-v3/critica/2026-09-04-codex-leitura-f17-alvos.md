# Leitura a frio do Codex ao bloco F1.7 (acessibilidade e alvos), 04.09.2026

*Codex `gpt-5.6-sol`, xhigh, só leitura, 03:06 a 03:19 UTC (a primeira tentativa, às 02:24, parou aos nove minutos no limite de uso da conta do Codex), sobre um pacote com cinco plantas de três classes (5 de 5 vistas; o registo está no `.plantas.json` ao lado): P1a e P1b, o registo dos cartões a escrever a unidade do livro-razão em vez da traduzida, na cópia e no diff, apanhada no Blocking 3; P2, a régua a aceitar 20 px como 44, apanhada no Blocking 1; P3, o relatório a dizer 20 unidades em português na tabela H8 e 0 no resumo, apanhada no Major 6; P4, «% do PIB» de volta numa página inglesa construída (no `<title>`), apanhada no Major 7. Triagem do lugar de direção (pela delegação de 04.09, §1.98): o Blocking 2 é real (a célula H2 tem de contar o buraco dos 641 a 1 023 px, e dos 129 alvos que ficam só 36 são polígonos do mapa; os outros sobem a 44 px); o resto do Blocking 3 é real (a régua H8 confere a unidade rendida contra a tabela e não o texto inglês contra si próprio; a tabela `src/i18n/unidades.mjs` não foi no pacote, o que é do pacote); o Blocking 4 é real (a porta das correções dentro de `<footer>` ou de um `<nav>` com nome, nunca aceite dentro de `<main>`); o Blocking 5 é real na régua (a exceção dos nove algarismos só vale quando o selo que partilha a área abre a mesma linha que o algarismo, e a régua confere-o); o Major 6 é em parte do pacote (as capturas e a régua das capturas não foram) e em parte real (o JSON da medição «depois» tem H2 vermelha e o relatório diz tudo verde: o relatório diz a verdade do JSON); os Major 8 a 11 e o Minor 12 são reais e consertam-se na segunda passagem (as plantas a cobrir o caso do `<main>` e a ler o HTML rendido; H1 a exigir zero violações de qualquer grau, como o relatório afirma; a geometria dos alvos a sondar também os cantos; a régua do `aria-expanded` a exigir o guião que o controla presente e a mudar o estado; o reconhecedor de diplomas com o sufixo `-A`, «Portaria», «Resolução» e «Despacho»; «Procurar» no inventário da voz). O texto do leitor fica como veio.*

---

# Blocking

1. **The supplied ruler tests 20 px, not 44 px.** The patch specifies `ALVO = 44`, but the copied test contains `ALVO = 20`; therefore a 30 px municipality card can pass. The recorded plant says that exact 30 px mutation failed H2, so those results were not produced by the supplied ruler. `diff.patch:2870-2874`; `tests/acessibilidade/alvos.mjs:122-126`; `tests/acessibilidade/alvos.mjs:218-224`; `design/especime-v3/medicoes/alvos-plantas.json:168-182`

2. **H2 passes while 129 restored-band targets remain below 44 px.** `buracoMau` is calculated but omitted from H2’s pass predicate. The report says all 129 are map polygons, while the measurement divides them among ordinary links, headline digits, text links and seals; only 36 are `a.uni-porta`. `tests/acessibilidade/alvos.mjs:1137-1156`; `relatorio-construtor.md:353-361`; `design/especime-v3/medicoes/alvos-medidas-depois.json:193-202`; `design/especime-v3/medicoes/alvos-plantas.json:65-67`

3. **The card-unit implementation and H8 proof are broken.** `valorDaLinha()` leaves the unit untranslated through a no-op conditional, although the card headline uses the translated value. The mandatory HTML gate then expects `v.texto` to contain that translation, so the supplied source cannot produce the claimed green build for translated-unit cards. `src/lib/cartoes.mjs:252-285`; `src/lib/cartoes.mjs:419-428`; `scripts/gate-html.mjs:7148-7157`; `package.json:12-18`

   H8 compounds this by feeding `u.texto`, already intended to be English, back into the Portuguese-unit lookup. The 204 former failures plus six genuinely untranslated records consequently become 210 “without translation”, and no translated unit is really checked. `tests/acessibilidade/alvos.mjs:868-883`; `design/especime-v3/medicoes/alvos-medidas-antes.json:139-149`; `design/especime-v3/medicoes/alvos-medidas-depois.json:131-137`; `relatorio-construtor.md:251-263`

   The units table itself is not supplied despite being imported, so exhaustive translation fidelity and the “table grows only as used” rule cannot be audited. `src/lib/cartoes.mjs:257-259`; `tests/acessibilidade/alvos.mjs:849-851`; `brief.md:18`

4. **The corrections door fails the brief on both ledger samples.** It is inside `<main><aside>`, not `<footer>` or a labelled `<nav>`. Both gate and ruler wrongly accept `<main>`, and the report explicitly preserves 6,482 such cases. `brief.md:11`; `depois/linha-en.html:2`; `depois/linha-pt.html:2`; `scripts/gate-html.mjs:4633-4646`; `tests/acessibilidade/alvos.mjs:729-736`; `relatorio-construtor.md:88-94`

5. **Nine headline digits do not have independent 44 px hit areas.** Their areas are partly occupied by seals. H7 accepts any other anchor inside the same `<h1>` without checking that it opens the same row, so even a wrong-row seal can satisfy the exception. No digit-to-digit collision is recorded; the demonstrated defect is digit-to-seal overlap. `design/especime-v3/medicoes/alvos-medidas-depois.json:89-92`; `relatorio-construtor.md:232-241`; `tests/acessibilidade/alvos.mjs:575-597`; `tests/acessibilidade/alvos.mjs:1243-1247`; `brief.md:17`

# Major

6. **The package disagrees with its own diff, report and saved results.** The patch’s report says H8 has zero Portuguese units, while the supplied report says 20. The named after-measurement file has H2 red, although the report claims every cell was green. The diff adds a capture ruler, and the report claims 120 PNGs, but neither the ruler copy nor captures are supplied. `diff.patch:84-89`; `relatorio-construtor.md:79`; `design/especime-v3/medicoes/alvos-medidas-depois.json:65-67`; `relatorio-construtor.md:418-428`; `diff.patch:4217-4233`; `relatorio-construtor.md:463-474`

7. **English visible metadata regressed.** The English ledger `<title>` changed from “89,7% of GDP” to “89,7% do PIB”, while the body and Open Graph title remain English. The copied source still constructs the title through the language-aware formatter, so the after HTML does not agree with that source or the stated scope. `antes/linha-en.html:1`; `depois/linha-en.html:1-2`; `src/lib/livro.mjs:79-87`; `relatorio-construtor.md:455-461`

8. **The planted defects do not prove the claimed ruler coverage.** The second-H1 plant is genuine and H3 catches it. The 30 px plant contradicts the supplied 20 px threshold. The door plant tests relocation outside every landmark, not the forbidden-but-accepted `<main>` case. The Portuguese-unit plant mutates the sidecar JSON because H8 never inspects the PNG, so a Portuguese visible card can pass if its JSON says English. `tests/acessibilidade/alvos.mjs:211-247`; `tests/acessibilidade/alvos.mjs:790-805`; `tests/acessibilidade/alvos.mjs:839-883`; `relatorio-construtor.md:381-400`

9. **Other rulers under-enforce their stated conditions.** H1 passes whenever serious/critical axe violations are zero, even if moderate or minor violations remain. Target geometry probes only four axial points, so it can accept a target whose corners are unavailable or overlapped. `brief.md:35`; `tests/acessibilidade/alvos.mjs:1104-1110`; `tests/acessibilidade/alvos.mjs:374-407`; `tests/acessibilidade/alvos.mjs:583-597`

10. **Control of `aria-expanded` is not established.** Every supplied page uses the permitted `details > summary[aria-controls]` shape and references `/js/tema.js`, but that controller is not in the package. The checks validate only element shape, not the script’s presence or state updates. `depois/linha-en.html:2`; `depois/metodo.html:2`; `scripts/gate-html.mjs:4676-4708`; `tests/acessibilidade/alvos.mjs:738-758`; `brief.md:20`

11. **The law-title count has false negatives.** The matcher recognizes only `Lei` and `Decreto-Lei`, and its advertised `Lei n.º 73/2013-A` form is cut before `-A`; other diploma kinds such as `Portaria` are not recognized and therefore cannot be reported unmarked. H9 reuses this matcher and skips unmatched files. `src/i18n/nomes-de-lei.mjs:39-57`; `src/components/ProsaComLeis.astro:24-35`; `tests/acessibilidade/alvos.mjs:930-967`; `brief.md:19`

# Minor

12. **The governed-copy evidence is incomplete.** The Método text is unchanged except for filling the empty heading with the existing menu string, which is permitted. No Sobre before/after page is supplied, so only the absence of a corresponding view hunk can be assessed. Outside governed pages, “Procurar” becomes newly visible through an existing string key, but the promised voice inventory is not included. `brief.md:7`; `brief.md:21-25`; `antes/metodo.html:2`; `depois/metodo.html:2`; `src/views/MetodoView.astro:83-96`; `src/views/MunicipiosView.astro:233-249`; `depois/municipios.html:2`

## What is fine

- Every one of the ten supplied before/after HTML samples has exactly one `<h1>`; the saved full-build count also reports zero bad pages after. `antes/linha-en.html:2`; `antes/linha-pt.html:2`; `antes/metodo.html:2`; `antes/municipios.html:2`; `antes/regiao.html:2`; `depois/linha-en.html:2`; `depois/linha-pt.html:2`; `depois/metodo.html:2`; `depois/municipios.html:2`; `depois/regiao.html:2`; `design/especime-v3/medicoes/alvos-medidas-depois.json:117-129`
- The CSS itself contains 44 px rules through 1,023 px for the named target classes, 44 px municipality cards, and the four 32 px desktop text targets. `src/styles/site.css:3386-3419`; `src/styles/site.css:4270-4301`; `src/styles/site.css:4720-4729`; `src/styles/site.css:4819-4835`; `src/styles/inicio.css:1370-1376`; `src/styles/inicio.css:2662-2675`; `src/styles/inicio.css:2957-2968`
- The auditable translations preserve meaning: `% do PIB` → `% of GDP`, `pontos de índice` → `index points`, `lugares` → `seats`, and `pelouros` → `portfolios`. No meaning or precision change is evidenced among those examples; the missing table prevents a complete per-unit verdict. `depois/linha-pt.html:2`; `depois/linha-en.html:2`; `design/especime-v3/medicoes/alvos-medidas-antes.json:145-149`
- The CSS preserves the digits’ typography and adds only anchor normalization and a 44 px pseudo-element; exact visual equivalence remains unproved because the claimed captures are absent. `src/styles/site.css:684-690`; `src/styles/site.css:702-728`; `relatorio-construtor.md:226-230`

**Distinct findings: 12.**