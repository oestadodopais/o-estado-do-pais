# Cold reading · B1, piece 3 (the country) · 22.09.2026

Reader: Claude Opus 5 (1M context), a family other than the builder's. Read-only, no network, no repository, no running site. Every count below was reproduced with Python over the package files; nothing is taken from the report. Paths are relative to `pacote/`. The built pages are minified, so a line number on a `built/*.html` file points at the line that carries the whole body.

## Blocking

**1. The Portuguese front page prints `89,9` for public debt where the ledger row it seals says `89,7`, and the English page prints `89,7`.** The reading's second value renders `<span data-claim="divida-publica-2025" class="claim-value">89,9</span><span class="claim-sufixo"> % do PIB</span>`, sealed to `/livro-razao/divida-publica-2025`. The row in this package writes `value: "89,7"`. The English edition of the same sentence renders `89,7`. So the seat's approved sentence ("desceu de 93,5 % para 89,7 %") is printed with a different number in the edition most readers will see, the two editions state different figures for the same row, and the figure the reader is offered a receipt for is not the figure on the receipt. The source component carries a hard guard (`'divida-publica-2025': '89,7'`, `throw` on any difference) and `voz-pais.mjs` rebuilds the whole sentence from `getClaim(id).value` and compares it with the page, so neither the source nor the rulers in this package can produce this page. `built/index.html:4`, `built/en/index.html:4`, `ledger/claims/divida-publica-2025.yml:9`, `src/components/inicio/LeituraDoPais.astro:9`, `src/components/inicio/LeituraDoPais.astro:15`, `scripts/voz-pais.mjs:23`

**2. The English front page carries a sixth menu entry, «Agenda», which is also in its footer.** `built/en/index.html` renders `<nav class="menu-cinco" id="nav-principal">` with six anchors: Portugal, Places, Themes, Studies, About, Agenda (`/en/agenda`). The Portuguese front page has five. The mandate is five entries in both editions with Agenda only in the footer and in About; the same page's footer already links `/en/agenda`, so the English home page has two doors to one thing and the two editions do not behave the same. The component that draws the menu maps `ROTAS_NAV`, which is exactly five keys, so this page cannot come from the source in this package either. A structural diff of the two front pages shows this extra anchor as the only element-level asymmetry between them. `built/en/index.html:4`, `src/components/Masthead.astro:18`, `src/lib/navegacao.mjs:3`

**3. On the Portuguese themes page the public-debt card is titled «Saldo das administrações públicas» — the name of the card immediately above it.** `/temas/` renders `<span class="cartao-medida-nome" data-nome="figuras" data-de-linha="divida-publica-2025">Saldo das administrações públicas</span>` over the value `89,7 · % do PIB`, four cards after the card `saldo-das-administracoes-publicas-2025` which shows `0,7 · % do PIB` under the identical name. The declared name for this row is «Dívida pública» / «Government debt», and the English page prints «Government debt» correctly. A Portuguese reader is shown two cards with one name and two unrelated numbers, and the wrong one is the headline figure of the country's reading. This is a name attached to a row it does not name, in a reader's page. `built/temas/index.html:3`, `built/en/themes/index.html:3`, `src/data/dominios.mjs:310`

**4. The declared theme table in this package puts `taxa-de-emprego-2025` in «Educação», and every built page renders it in «Trabalho».** `DOMINIO_DAS_MEDIDAS` writes `'taxa-de-emprego-2025': 'educacao'` — under the section comment `/* 2 · Trabalho (carta §2 e §3, domínio 2). */`. Counting the table gives Economia 13, Trabalho 10, Educação 4; counting the four built pages gives Trabalho 10 rendered (11 entries less the deduplicated unemployment row) and Educação 3. The mandate is that a measure's theme is read from this table and nowhere else, `temasDoPais()` does read it, and `check:pais` cell T3 exists to fail on exactly this mismatch. Whichever side is right, the employment rate is filed under Education in the package's only declaration of where it belongs, and one of the two artefacts shipped for reading is wrong. `src/data/dominios.mjs:814`, `built/temas/index.html:3`, `built/index.html:4`, `src/lib/pais.mjs:19`, `scripts/check-pais.mjs:61`

**5. The three green exit codes cannot be evidence for the pages in this package: four cells that are in the chains and do bite would each have failed on them.** `build`, `verify` and `typecheck` all read `0` from their own files on head `6e3baae0`, and `check:pais` is in both chains. But on those pages: `check:pais` N1 compares the whole menu array against five entries and its own plant «sexta entrada no menu» proves it returns 1 on a six-entry menu (finding 2); `check:pais` T3 compares each card's theme against the table (finding 4); `check:cartao` K1, added by the fix pass, compares each card title against `MEDIDAS_DO_DOMINIO_1` (finding 3); `gate:html` and `voz-pais` compare the reading's values against the ledger (finding 1). The recorded run instead printed `B1 país: 35 medidas, 9 temas, 7336 menus, 1 mudanças declaradas.` followed by `todas as conferências a 0`, and the capture manifest records the English menu as five entries on all ten English captures. So the `dist` that was gated and photographed is not the `dist` in `built/`, and no statement in the report about these pages is backed by the exit codes it prints. `medicoes/verify-peca3.codigo:1`, `medicoes/verify-peca3.log:728`, `medicoes/plantas-pais.json:80`, `scripts/check-pais.mjs:110`, `medicoes/capturas-depois-peca3.json:787`, `relatorio-construtor.md:185`

## Major

**6. The report's headline measurement for the fix pass contradicts the measurement file in the package, and reverses the sign of the change.** The report states «A altura portuguesa a 1 280 **diminuiu de 6 470 para 6 322 px**, menos 148 px». The capture manifest records `depois-pais-pt-1280.png` with `"altura": 6622`. Against the 6 470 px the fix brief quotes for the same capture before the pass, the page grew by 152 px; the report says it shrank by 148. The manifest's other numbers for that capture reproduce the report exactly (leitura 418,3 pt and 380,2 en, olho 769,4, primeiro tema 797,8, temas 1 092 = conteúdo 1 092), which makes the one figure that does not reproduce the one the report puts in bold. `relatorio-construtor.md:167`, `medicoes/capturas-depois-peca3.json:368`, `brief-correcao.md:10`

**7. The front page's `<title>` and meta description, in both editions, describe a page this block deleted.** Both front pages ship `description`/`og:description` = «As medidas de cabeça de cada domínio, a busca e o mapa dos concelhos, e as portas para os estudos e para os números com as suas fontes.» and its English twin. The head measures band is gone, the search moved to Lugares (the report says so), and the page is now organised by *tema*, not by *domínio* — the word the block's own vocabulary replaces. This is the first thing a reader sees in a search result or a shared link, and it is false. The string's own comment in the file records that it was rewritten on 09.09.2026 for precisely this reason ("ficou a descrever uma página que já não existe"); the same thing has now happened again and the report does not mention the head. `src/i18n/strings.mjs:1725`, `src/i18n/strings.mjs:3413`, `built/index.html:1`, `built/en/index.html:1`, `relatorio-construtor.md:60`

**8. «O que mudou» prints a dated line that says nothing changed: «Estudos publicados sobre Évora 4 → 4 estudos», in both editions.** The 12.08.2026 entry renders `<s data-correcao-n="0">4</s> → <span data-correcao-n="1">4</span>`: the struck value is taken from correction 0 and the new value from correction 1, while the `<time>` declares `data-correcao-n="1"`. Every other correction line in the block carries one and the same `n` on all three marks. It is the only no-change line of the 29 and it is the oldest, so it is the last thing a reader of the section reads. `check:pais` C1 cannot see it — it compares only the set of `id|date` keys against the ledger — and the `gate:html` cell this block added compares the changes' *date and text*, not `old_value` against `new_value`. `built/index.html:4`, `built/en/index.html:4`, `scripts/check-pais.mjs:90`, `scripts/gate-html.mjs:5164`

**9. `check:alvos` H10 was changed so that it passes without measuring anything, and on this `dist` that is what it does.** The cell's condition became `(dist.expanded === 0 || provasDoGuiao.length > 0)`; before, `provasDoGuiao.length > 0` was required outright. The final run prints `aria-expanded no dist/: 0 ocorrência(s) … abrir e fechar o <details>: 0 prova(s) no navegador` and the cell is green — the green comes entirely from the new escape, and the browser proof that was the point of the cell ran zero times. The compensating known-positive (`--planta-b1`, which injects `aria-expanded` on an element with no command) is behind a flag; `"check:alvos": "node tests/acessibilidade/alvos.mjs"` in the chain passes no flag, so nothing in `build` or `verify` exercises it. `tests/acessibilidade/alvos.mjs:1872`, `tests/acessibilidade/alvos.mjs:234`, `package.json:30`, `medicoes/verify-peca3.log:588`

**10. `gate:html` — a gate the block itself classifies P — gained a new site-wide exemption from the requirement that a value carry its own receipt.** `auditaSelo` now returns early for any value inside `[data-regua][data-selo-em]` whose id is `${principal}-ue`, or the previous year of the same series, provided the enclosing card links to the principal's ledger page. The report's table justifies this as admitting on the two new routes a shape the old pages already used, but the code is not route-limited: it applies to every page in `dist`. The `ue` branch is also weaker than its sibling — the `anterior` branch requires matching `document.edition` and `unit`, the `ue` branch checks only that the id ends in `-ue`. The plant listed for this cell («referência atribuída ao selo de outra linha») exercises the mis-attribution case, not the new blanket path. `scripts/gate-html.mjs:3126`, `scripts/gate-html.mjs:3135`, `scripts/gate-html.mjs:3140`, `relatorio-construtor.md:34`

**11. The report's opening paragraph and its closing table state opposite things about the gates, and the opening one is what a reader of the report meets first.** Line 3 says «O único `verify` completo ficou a 1 na planta I11 … Não há, portanto, três códigos finais a 0», and the build-pass table prints `npm run verify | 1`. The fix-pass table at the end prints 0, 0, 0 on `6e3baae0`, and the three `.codigo` files in the package read 0. The fix brief did instruct that what the report already said was not to be rewritten, but the result is a report whose first paragraph contradicts its own conclusion and its own measurement files. `relatorio-construtor.md:3`, `relatorio-construtor.md:133`, `relatorio-construtor.md:186`, `medicoes/verify-peca3.codigo:1`

**12. The light/dark control is gone from every page in this package while the boot script still applies a stored `dark`.** Each built page still runs `(function(){try{if(localStorage.getItem('tema')==='dark'){document.documentElement.setAttribute('data-theme','dark')}}catch(e){}})()` in the head, and no page in the package renders a control to change it — the inventory records «Tema» and «Theme» as chains retired with the reason "segunda porta". A reader who chose dark before this block keeps dark and, on these seven pages, has no way back. The report mentions the removal in one clause and does not mention the stranded preference. I can only speak for the eight pages shipped here; whether some page outside the package still carries the control cannot be checked from this folder. `built/index.html:1`, `built/temas/index.html:1`, `built/sobre/index.html:1`, `design/especime-v3/critica/REVISOES-DO-INVENTARIO.md:352`, `relatorio-construtor.md:60`

**13. `/temas/` and `/en/themes/` ship a one-word `<title>` and a one-word description, breaking the pattern every other page follows.** Both pages render `<title>Temas</title>` / `<title>Themes</title>`, `description="Temas"`, `og:title="Temas"`, `og:description="Temas"`, and the front page's social card image. Every other page in the package is «X · O Estado do País» with a written sentence (`sobre/index.html`: «Sobre · O Estado do País», «O que é O Estado do País, em duas frases, e a porta para o método que as sustenta.»). The view passes `title={r.temas} description={r.temas}` — the section label reused as both. A shared or indexed link to the site's second most important page announces one word and nothing about the site. `built/temas/index.html:1`, `built/en/themes/index.html:1`, `built/sobre/index.html:1`, `src/views/TemasView.astro:10`

## Minor

**14. Six proof files the report cites as evidence are not in the package, so the measurements they carry are claims.** The report points to `alvos-peca3.json` for the H2/H6/H10 plants, `medidas-correcao-peca3.json` for the twelve pages carrying the new name, `porta-correcao.log` and `provas-correcao.log` for the full plant output and its English twins, `cartao-antes-correcao.*` for the K1 zero-before proof, `autoria-peca3.log` for the commit bodies and `tempo-peca3.json` for the wall time. None is in `medicoes/`, which holds only the seven files listed in its directory. The 55 PNGs and their SHA-256 are likewise absent, so «os 35 resumos SHA-256 foram reconferidos contra os PNG» and «55 PNG idênticos aos arquivados» cannot be checked here. `relatorio-construtor.md:76`, `relatorio-construtor.md:163`, `relatorio-construtor.md:171`, `relatorio-construtor.md:189`, `relatorio-construtor.md:148`

**15. The only `porta.mjs` output in the package is the superseded build-pass run, not the 66-cell run the fix pass describes.** `porta-peca3.json` holds 14 cells (N1×4, N2×4, P1×2, planta-menu×2, planta-dobra×2) — the old shape, with no P2, no G1 and no `reposicao-*`. The fix-pass table claims «66 células verdes, incluindo seis plantas e seis reposições»; that total does reproduce from the rewritten ruler (5 widths × (2 routes × 2 cells + G1) = 25 per language, plus P1 and P2 at 1 280, plus 3 plants and 3 restorations per language = 33 × 2 = 66), but the run itself is not shown. `medicoes/porta-peca3.json:1`, `tests/inicio/porta.mjs:46`, `relatorio-construtor.md:162`

**16. One door is spelled two ways on the same page, in both editions.** The front page links `/lugares` from the menu and `/lugares/` from the map door, and `/temas` from the menu against `/temas/` from the nine theme doors; the English page does the same with `/en/places` and `/en/themes`. `check:lugar` L1 keys duplicate destinations on `href.split('#')[0]`, so the two spellings are never seen as the same door. The brief asks for «308 concelhos» linked to `/lugares/`, which the map door honours; the menu does not use the same string. `built/index.html:4`, `built/en/index.html:4`, `scripts/check-lugar.mjs:992`

**17. `pib-real-per-capita-2025` renders a different space character from the one the ledger writes.** The row writes `value: "20 600"` with U+202F (narrow no-break space); all four new pages render `20 600` with U+00A0. The house rule is that values render exactly as the ledger writes them. The substitution is invisible to the eye and applies site-wide rather than only to this block's pages, but it is a value that does not render as written. `ledger/claims/pib-real-per-capita-2025.yml:9`, `built/index.html:4`, `built/temas/index.html:3`

**18. Two of the report's secondary counts are not distinguishable in the file they cite.** «23 ficam retiradas com a razão e duas cadeias curtas saem do inventário porque ainda pertencem a frases maiores» describes a 23/2 split of the 25 retired chains; `cadeias-peca3.json` holds 25 rows that all carry a `razao`, with no field separating the two kinds. The family/reason totals do reproduce exactly (menu e rodapé 11, país/palavra fora do lugar 10, país/leitura substituída 2, país/explica a página 2), as does «18 cadeias distintas» from 19 rows with `→` twice. `medicoes/cadeias-peca3.json:1`, `relatorio-construtor.md:51`

**19. Three of the report's figures cannot be reproduced here because the package ships 13 of the ledger's 2 975 rows.** C1 counts the page's 15 correction lines against every `corrections` entry of kind `correcao`/`atualizacao` in `ledger/claims/`; the 13 rows here all have `corrections: []`. T0, which guards the claim that the two unemployment rows are the same measure, needs `taxa-de-desemprego-mip-2025.yml`, which is absent. Likewise the derivation behind «Estudos publicados sobre Évora». The 15/13/1 split of the 29 lines does reproduce from the built page. `scripts/check-pais.mjs:40`, `scripts/check-pais.mjs:24`, `ledger/claims/indice-de-divida-limite-legal.yml:43`, `built/index.html:4`

**20. Two derived PRR values sit in «O que mudou» without the marker their same-day siblings carry.** `ledger:check` in the final run lists eight rows with `[a verificar]` fields, among them `evora-prr-aprovado-2026`, `evora-prr-pago-2026` and `evora-prr-vencido-aprovado-2026`; all three appear on the front page with `src-chip is-unverified`, `[a verificar]` and the one-line definition, which is right. On the same two dates the page also prints `evora-prr-execucao-2026` and `evora-prr-vencido-quota-2026` with a plain seal reading «calculado» and no marker. Those two rows are not in the package, so whether they are derived from the flagged ones — and therefore whether a status is being dropped — cannot be settled from this folder; it is the one question about item 6 that the package does not let me close. `medicoes/verify-peca3.log:19`, `built/index.html:4`, `ledger/claims/divida-publica-2025.yml:3`

**21. The editor's question, one line per built page.** `built/index.html` — no: the debt figure is wrong (finding 1), the search-result description is false (finding 7), and a "what changed" line reads 4 → 4 (finding 8). `built/en/index.html` — no: six menu entries with Agenda twice on the page (finding 2), the same false description, and «93,5 %» / «17,6 %» with a decimal comma in an English text, which is a decided house rule but which an editor would still stop at. `built/temas/index.html` — no: two cards share one name over two unrelated numbers (finding 3), and the page announces itself as «Temas» and nothing else (finding 13). `built/en/themes/index.html` — yes, except for the one-word title. `built/sobre/index.html` and `built/en/about/index.html` — yes: two sentences, four maintenance doors, the language switch, nothing to cut. `built/municipios/evora/index.html` and `built/estudos/agua-nao-faturada/index.html` — yes for what this block touched: five entries, no drawer, footer intact. `built/index.html:4`, `built/en/index.html:4`, `built/temas/index.html:3`, `built/en/themes/index.html:3`, `built/sobre/index.html:1`, `built/en/about/index.html:1`, `built/municipios/evora/index.html:1`, `built/estudos/agua-nao-faturada/index.html:1`

## «What is fine»

**22.** The diff and the copied files agree line for line: reconstructing every hunk's post-image and matching it at its stated line number across all 40 files produced no mismatch (the one flagged case was my parser counting the patch's terminating blank line). `diff.patch:1`

**23.** `check:pais` is in both chains, once each, at the same position relative to `check:lugares` and `check:voz`. `package.json:12`, `package.json:42`

**24.** The card counts reproduce: 36 table entries, 35 unique after the declared unemployment merge, 35 cards on each themes page, 19 on each front page, 9 theme groups in table order on all four pages, every front-page row at most four and in ascending table order, no card twice, and the three rows the reading uses (`divida-publica-2025`, `taxa-de-desemprego-mip-2025`, `precos-da-habitacao-2025`) absent from the front page. `src/lib/pais.mjs:14`, `built/temas/index.html:3`, `built/index.html:4`

**25.** Every card unit I could check renders the ledger's `unit` string character for character, including the parenthetical in `euros por habitante · volumes encadeados (2015)`. `ledger/claims/pib-real-per-capita-2025.yml:10`, `built/temas/index.html:3`

**26.** The reading carries six values and seven receipts in both editions, and the seventh receipt is the European comparison attached to «a par da média europeia» exactly as the brief asks. `built/index.html:4`, `scripts/check-pais.mjs:50`

**27.** The Carta attribution stands under the map in both editions, unchanged and whole: «Direção-Geral do Território · Carta Administrativa Oficial de Portugal (CAOP) 2025 · CC BY 4.0», with cell R6 untouched and its plant red. `built/index.html:4`, `built/en/index.html:4`, `medicoes/plantas-portoes.json:3`

**28.** The footer is complete on every page: the eight retired destinations, the corrections address, the language switch, and the AI label whose full stop sits inside `<span class="rotulo-ia-final">` with the link, which is the I120 fix; `check:pais` N2 counts exactly one per page. `built/index.html:4`, `src/components/SiteFooter.astro:18`, `scripts/check-pais.mjs:111`

**29.** The drawer is gone everywhere: zero `<details>`, zero `<summary>`, zero `aria-expanded` and no `.nav-menu` on any of the eight built pages, and the same across `dist` in the final run. `built/municipios/evora/index.html:1`, `medicoes/verify-peca3.log:588`

**30.** The map is a door without JavaScript: 29 `<a class="uni-porta">` around the paths, the `308 concelhos` count inside a plain `<a href="/lugares/">`, and the two JS-only controls shipped `hidden`. `built/index.html:4`

**31.** The four labels the fix pass ordered are in place and language-correct: «Estudos recentes» / «Recent studies» on the front page, «Estudos sobre este lugar» still on Évora, «Estudo publicado · <título>» / «Study published · <title>» on all thirteen publication lines, and «Limite legal da dívida das câmaras» / «Legal cap on municipal debt» on the card that shows 150 %. `src/data/rotulos-b1.mjs:25`, `built/index.html:4`, `built/municipios/evora/index.html:1`, `src/data/dominios.mjs:371`

**32.** The declared change of 21.09.2026 renders word for word as `MUDANCAS_DO_PROJETO` writes it, in both editions, dated 21.09.2026, and the 29 lines run strictly newest to oldest. `src/data/mudancas-do-projeto.mjs:9`, `built/index.html:4`, `built/en/index.html:4`

**33.** The project's uncertainty marker reaches the reader where the data carries it: six `[a verificar]` on each front page, each with `src-chip is-unverified` and the one-line definition, and two in card definitions on each themes page with the English gloss «(to verify)» present only in the English edition. `built/index.html:4`, `built/en/themes/index.html:3`

**34.** No official INE or PORDATA name renders anywhere: the final run reports 0 in receipts, 0 in card titles, 36 refused and 21 plants correctly handled. `medicoes/verify-peca3.log:901`

**35.** Language separation holds: the English pages carry Portuguese only in proper names (island titles inside the SVG, the DGT/CAOP attribution, Portuguese study titles marked `lang="pt-PT"` and flagged «(in Portuguese)»), and the Portuguese pages carry no English. `built/en/index.html:4`, `built/index.html:4`

**36.** No duplicate `id` on any of the eight built pages, and the front page's heading order is h1 (the project's name) → h2 Temas → h3 per theme → h2 Estudos recentes → h3 per study → h2 O que mudou. `built/index.html:4`, `src/views/HomeView.astro:40`

**37.** The capture manifest's quality columns are clean across all 35 captures: side-scroll 0 everywhere, zero broken values, zero broken seals, and the AI full stop unbroken. `medicoes/capturas-depois-peca3.json:1`

**38.** `check:alvos` H6 was strengthened, not weakened: `.pais-porta-lugares`, `.pais-porta-tema a` and the footer's mail link joined the I105 class, and the cell measured 22 targets on the front page at 1 280 with none under 32 px. `tests/acessibilidade/alvos.mjs:805`, `medicoes/verify-peca3.log:583`

**39.** `check:lugar` L4 was inverted rather than dropped — from "exactly one definition sentence on the front page" to "exactly zero" — and its plant (restoring the sentence) is red. `scripts/check-lugar.mjs:1827`, `medicoes/plantas-portoes.json:64`

**40.** The approved reading is locked twice from two directions: `LeituraDoPais.astro` throws at build time if any of the seven ledger values moves off the seat's numbers, and `voz-pais.mjs` rebuilds the whole sentence from the ledger and compares it with the rendered page. Neither alone would be enough; together they close the loop. `src/components/inicio/LeituraDoPais.astro:8`, `scripts/voz-pais.mjs:22`
