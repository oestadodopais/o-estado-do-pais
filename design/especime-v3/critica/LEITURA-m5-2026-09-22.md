## Blocking

1. **Two public front-page claims suppress their unresolved `[a verificar]` status.** The build records three unresolved markers in notes belonging to `disparidade-salarial-entre-sexos-2024` and `retribuicao-minima-mensal-garantida-continente-2026`, then explicitly says those notes never reach the page or affect the seal. Both claims nevertheless render as `data-estado="completa"` in both editions; the pay-gap card shows “provisional”, which is not the site’s dashed `[a verificar]` disclosure, and the minimum-wage card shows no warning at all. This is precisely a verification status dropped between data and reader.  
design/especime-v3/medicoes/m5-2026-09-22/build.log:21, design/especime-v3/medicoes/m5-2026-09-22/build.log:22, built/index.html:4, built/en/index.html:4

2. **Four copied changed regions do not match the additions in `diff.patch`, so the package is not one auditable head.** The diff records 33 date exemptions, strict `is not True` validation, an empty-source test using `=== 0`, and a normal membership comparison. The copied files instead contain 32, reject only literal `False`, test the impossible condition `length < 0`, and add `or not valores`. Consequently, the packaged green logs do not verify the code and data actually supplied for review.  
diff.patch:13957, design/observatorio/medidas/BRIEF-M5.json:34, diff.patch:14454, scripts/check-briefs.py:216, diff.patch:14703, scripts/gate-html.mjs:3997, diff.patch:15151, scripts/leituras/numeros.py:247

3. **The M5 brief does not comply with its own measured-§0 rule.** Its §0 claims 33 briefs exempt by date while its copied measurement JSON declares 32. It also asserts counts such as two erroneous §0s, sixteen dated lines, one repeated definition, one exported constant, three comment mentions and one CSS mention without citing corresponding measurement names. The JSON contains only seven named measurements and none for those factual counts.  
brief.md:7, brief.md:9, brief.md:13, design/observatorio/medidas/BRIEF-M5.json:4, design/observatorio/medidas/BRIEF-M5.json:34

4. **Both number gates accept a wrong claim whenever the same numeral occurs anywhere else in any measurement JSON.** The reader collects numbers from every JSON key, value and string into global sets, then checks only membership; it does not bind a number to a measurement name or the sentence making the claim. `check:briefs` likewise never requires the §0 to cite a measurement name. Thus a false “37” can pass because 37 is an unrelated brief count, while the plants test only wholly absent values 41 and 314159 and cannot distinguish this defect.  
scripts/leituras/numeros.py:180, scripts/leituras/numeros.py:207, scripts/leituras/numeros.py:243, scripts/check-briefs.py:226, scripts/check-briefs.py:228, design/especime-v3/medicoes/m5-2026-09-22/plantar-m5.py:112, design/especime-v3/medicoes/m5-2026-09-22/plantar-m5.py:120

5. **One M5 known-positive is not a positive for the detector that produces its value, and its declared commands do not reproduce the measurements.** The export value tests whether the director’s name occurs in an export’s right-hand side, but its positive merely checks that an unrelated export named `LINGUA_DO_RESPONSAVEL` exists; the name-membership predicate can be blind while the positive stays green. The corresponding command lists every export without filtering or counting, while the source-name command reports matching lines although the value counts files. The gate merely requires the fields to exist and never runs or validates those commands.  
design/observatorio/medidas/BRIEF-M5.json:53, design/observatorio/medidas/BRIEF-M5.json:62, design/observatorio/medidas/BRIEF-M5.json:64, design/observatorio/medidas/BRIEF-M5.py:189, design/observatorio/medidas/BRIEF-M5.py:193, scripts/check-briefs.py:212

6. **The copied brief gate accepts a missing, null or malformed known-positive as successful.** It closes only when `encontrado is False`; omission, `null`, `0`, or a string all pass even though none is the required boolean `true`. The known-positive plant changes the value specifically to boolean false, so it does not expose this weakness. The diff’s stricter `is not True` condition confirms that the copied version is weakened.  
scripts/check-briefs.py:216, design/especime-v3/medicoes/m5-2026-09-22/plantar-m5.py:104, design/especime-v3/medicoes/m5-2026-09-22/planta-conhecido-positivo-cego.log:5, diff.patch:14454

7. **The block’s own report fails its stated zero-miss acceptance test.** The report says B1c has 45 unsupported numbers, while the captured B1c result, `medidas.json`, and repository map all say 50. Running the packaged checker against the packaged report therefore identifies line 64 as missing and exits 1, contradicting both packaged files that claim 62 of 62 numbers have evidence and zero misses.  
relatorio-construtor.md:5, relatorio-construtor.md:64, design/especime-v3/medicoes/m5-2026-09-22/conferir-relatorio-b1c.txt:7, design/especime-v3/medicoes/m5-2026-09-22/medidas.json:71, design/observatorio/MAPA-DO-REPOSITORIO-para-construtores.md:192, numeros-do-relatorio.txt:7

## Major

8. **The copied HTML gate’s safeguard against a vacuous source scan can never fire.** An array length cannot be negative, so missing or empty `src/` and `public/` trees are reported as zero name occurrences rather than as an invalid measurement. Both name plants run against non-empty trees and therefore do not exercise this condition.  
scripts/gate-html.mjs:3997, diff.patch:14703, design/especime-v3/medicoes/m5-2026-09-22/plantar-m5.py:124, design/especime-v3/medicoes/m5-2026-09-22/plantar-m5.py:130

9. **A new brief can evade the gate by placing an old date anywhere in its filename, and any later edit to an old brief remains invisible.** The first date-like substring in the filename takes unconditional precedence over the header. The main loop then skips every brief whose resulting tuple predates the cutoff, without checking when or how that file changed.  
scripts/check-briefs.py:109, scripts/check-briefs.py:111, scripts/check-briefs.py:114, scripts/check-briefs.py:263, scripts/check-briefs.py:266

10. **Different briefs can share one measurement script and JSON without the gate noticing.** The key uses only the first two hyphen-separated filename fields, so every `BRIEF-M5-*.md` resolves to `BRIEF-M5`. The gate never validates the JSON’s top-level `brief` or `guiao` fields against the brief under review. A second brief with that prefix can therefore reuse the first brief’s artifacts and pass if its numerals happen to exist in the pooled JSON values.  
scripts/check-briefs.py:121, scripts/check-briefs.py:123, scripts/check-briefs.py:169, scripts/check-briefs.py:187, design/observatorio/medidas/BRIEF-M5.json:2, design/observatorio/medidas/BRIEF-M5.json:3

11. **The claimed independent comparison of all brief counts silently skips the number of checked briefs.** The gate looks for a measurement named `briefs_conferidos_pelo_portao`, but the M5 script emits `briefs_com_guiao_de_medicoes`. Because comparison is conditional on the expected key existing, this discrepancy produces no error. The report’s claim that the two readers cross-check their counts is therefore overstated.  
scripts/check-briefs.py:279, scripts/check-briefs.py:280, scripts/check-briefs.py:284, design/observatorio/medidas/BRIEF-M5.py:152, relatorio-construtor.md:51

12. **The report checker permits measured claims to hide as words, inline code, or year-shaped integers.** Its number expression recognizes digits only, while cleanup removes every inline-code span and every isolated integer from 1900 through 2100 regardless of semantic context. The known-positive tests only an ordinary decimal integer, so it does not prove visibility in any excluded form. Count claims such as the report’s “three new citations” are therefore not examined at all.  
scripts/leituras/numeros.py:56, scripts/leituras/numeros.py:74, scripts/leituras/numeros.py:90, scripts/leituras/conferir-relatorio.py:86, relatorio-construtor.md:122

13. **`medir-m5.py` can emit a successful measurement file after its subordinate checks fail.** A report-checker infrastructure failure is converted into an error object, and a failed brief gate is merely recorded as a numeric code. The known-positive total is then read from the already stored JSON rather than from a successful fresh run, and `main()` returns zero unconditionally.  
design/especime-v3/medicoes/m5-2026-09-22/medir-m5.py:37, design/especime-v3/medicoes/m5-2026-09-22/medir-m5.py:137, design/especime-v3/medicoes/m5-2026-09-22/medir-m5.py:149, design/especime-v3/medicoes/m5-2026-09-22/medir-m5.py:180, design/especime-v3/medicoes/m5-2026-09-22/medir-m5.py:197

14. **The four historical report results and the claimed B1c package trial are not reproducible from this package.** Their captured outputs point to reports and measurement directories that are absent here, so the lists can be read but not rerun. The only packaged `numeros-do-relatorio.txt` targets M5, not the B1c trial claimed in the report. The claimed repository-map checker run is likewise stated without a packaged output.  
relatorio-construtor.md:61, relatorio-construtor.md:66, design/especime-v3/medicoes/m5-2026-09-22/conferir-relatorio-b1c.txt:1, design/especime-v3/medicoes/m5-2026-09-22/conferir-relatorio-b1c.txt:2, relatorio-construtor.md:80, numeros-do-relatorio.txt:1, relatorio-construtor.md:124

15. **The report gives a false commit hash for `medidas.json`.** It says both plant and measurement indexes record `415b6866`, but only `plantas-m5.json` does; `medidas.json` records the final `3571da4c…` head. Hashes are excluded by the number checker, so its claimed green result cannot detect this contradiction.  
relatorio-construtor.md:153, design/especime-v3/medicoes/m5-2026-09-22/plantas-m5.json:3, design/especime-v3/medicoes/m5-2026-09-22/medidas.json:2, scripts/leituras/numeros.py:64

16. **`pacote.sh` leaves a substantially assembled package behind when the checker exits with fatal code 2.** It creates the destination and copies the brief, report, diff, changed files and built pages before running the checker. Its later exit therefore does not make true the claim that such a package “does not mount”; stale partial packages remain available for use.  
scripts/leituras/pacote.sh:10, scripts/leituras/pacote.sh:24, scripts/leituras/pacote.sh:32, scripts/leituras/pacote.sh:46, scripts/leituras/pacote.sh:48, scripts/leituras/pacote.sh:51

## Minor

17. **The packaged checker output leaks an absolute workstation path and is not portable evidence.** Both the report and measurement-folder paths expose the local username and worktree location instead of referring to files inside the cold-read package.  
numeros-do-relatorio.txt:1, numeros-do-relatorio.txt:2

## «What is fine»

18. **`check:briefs` is present in the `verify` chain at the documented position.**, package.json:43, design/especime-v3/medicoes/m5-2026-09-22/verify.log:152

19. **The intended diff removes the director’s exported constant, and the exact name is absent from the six included built pages in both editions.**, diff.patch:15271, built/index.html:4, built/en/index.html:4, built/sobre/index.html:1, built/en/about/index.html:1, built/metodo/index.html:1, built/en/method/index.html:1

20. **The Portuguese and English front pages contain the same 19 card identifiers with matching values and reference periods.**, built/index.html:4, built/en/index.html:4

21. **Both Method editions visibly render the structural `[a verificar]` marker beside its one-line definition.**, built/metodo/index.html:1, built/en/method/index.html:1

22. **The recorded build, verify and typecheck durations are arithmetically consistent with their timestamp files and all three recorded exit codes are zero.**, design/especime-v3/medicoes/m5-2026-09-22/medidas.json:83, design/especime-v3/medicoes/m5-2026-09-22/medidas.json:92, design/especime-v3/medicoes/m5-2026-09-22/medidas.json:101

23. **The root brief is byte-for-byte the same as the copied brief at its repository path.**, brief.md:1, design/observatorio/BRIEF-M5-as-medicoes-provam-que-veem.md:1