# Leitura a frio do Codex ao bloco F1.4 (os nomes humanos, as datas, o índice do livro-razão), 04.09.2026

*Codex `gpt-5.6-sol`, xhigh, só leitura, 02:14 a 02:27 UTC, sobre um pacote com cinco plantas de três classes (5 de 5 vistas; o registo está no `.plantas.json` ao lado): P1a e P1b, `nomeDaMedida()` a devolver o identificador em vez do nome, na cópia e no diff, apanhada no Blocking 1; P2, a régua I2 a exigir um `Z` no fim da data ISO, apanhada no Blocking 3; P3, o relatório a dizer 30 datas ISO na tabela G2 e 0 no resumo, apanhada no Blocking 3; P4, uma medida do índice a chamar-se pelo slug na página construída, apanhada no Blocking 2. Triagem do lugar de direção (pela delegação de 04.09, §1.98): o Blocking 4 é em parte do pacote (`figuras.mjs` e `dominios.mjs` não foram no pacote) e em parte real (a régua tem de conferir que o nome rendido é o da mesma linha, e não um nome qualquer do ficheiro); o Major 5 fica como está (o total «2 916 afirmações» é o denominador de si próprio, e as duas contagens que são partes levam-no); o Major 6 é real e a decisão é a do F1.10 (um só índice: a busca cobre as 2 916 linhas por um índice compacto que o guião carrega, com as linhas dos concelhos incluídas, e sem guião a página lista as 149 gerais e a porta para a coleção dos concelhos); o Major 7 fica como está (o marcador dentro da ligação de uma linha é texto dessa ligação, e a régua di-lo); o Major 8 é do F1.10 (a palavra sai de todo o sítio lá); os Major 9 a 13 são reais e consertam-se na segunda passagem (o título do documento como ligação nas duas ocorrências; a conta dos estudos sem data dita uma vez e a régua a contar os dois marcadores; o inventário da voz com os rótulos da busca e os sufixos das áreas; o corredor das plantas a falhar quando uma não fica vermelha e as células do navegador dentro do `verify`, agora que a CI tem o Chromium; as réguas a apanhar os equivalentes: o nome de outra linha, `12/08/2026`, um `action` para uma página que não existe, um filtro só por id, um agregado sem marca). A decisão sobre as datas de publicação dos estudos (a data do commit que pôs a edição no repositório público, dita com essa origem) entra na mesma segunda passagem. O texto do leitor fica como veio.*

---

## Blocking

1. **The built HTML was not produced from the supplied source.** For rows with `claim.name`, the patched resolver returns `claim.id`, not `claim.name`; the component then renders that value as the name. Yet the built ledger renders source names for its 17 `name` rows. The guard checks only that the selected field is `"name"`, never that `texto === claim.name`. (`src/lib/nomes.mjs:114-123`, `diff.patch:1744-1752`, `src/components/NomeDaMedida.astro:52-58`, `scripts/provar-guardas.mjs:447-451`, `built/livro-razao.html:1`)

2. **G1 is already red in the supplied build.** The Portuguese entry `abandono-escolar-precoce-2025` visibly uses that slug as `.livro-item-nome`, contradicting the reported zero. I1 explicitly rejects this exact condition, so the claimed clean ruler run cannot correspond to this HTML. (`built/livro-razao.html:1`, `relatorio-construtor.md:273-275`, `tests/livro/indice.mjs:214-220`)

3. **G2 fails and its evidence was altered.** The brief requires zero visible ISO dates, but the root report admits 30 after the change and explicitly permits ISO in transcribed prose; the report embedded in the patch says “0 and 0” instead. Moreover, I2 counts with a regex requiring a trailing `Z`, so ordinary `2026-08-12` failures increment the reported count by zero; this ruler cannot have produced the stated 28,300 count. (`brief.md:12`, `brief.md:34`, `relatorio-construtor.md:135-139`, `relatorio-construtor.md:276`, `diff.patch:348`, `tests/livro/indice.mjs:98-102`, `tests/livro/indice.mjs:260-268`)

4. **The card-name equality and “no source typo” claims are unverifiable and not enforced.** The required `src/data/figuras.mjs` and `src/data/dominios.mjs` copies are absent. Even in the full repository, the ruler only checks whether rendered text occurs anywhere in the relevant file’s global name set, not whether it belongs to the same claim ID. The resolver also states that it will preserve any source typo rather than clean it. (`src/lib/nomes.mjs:38-45`, `src/lib/nomes.mjs:60-62`, `scripts/medir-defeitos.mjs:547-557`, `scripts/medir-defeitos.mjs:947-969`, `relatorio-construtor.md:73-84`)

## Major

5. **Only two of the three header counts have denominators.** The first remains `2916 afirmações`; the builder explicitly refuses it a denominator and rewrites G5 as “2 of 2,” contrary to the requested three. I5 codifies that narrower interpretation. (`brief.md:14`, `relatorio-construtor.md:167-175`, `relatorio-construtor.md:279`, `src/views/LivroView.astro:177-189`, `tests/livro/indice.mjs:378-400`, `built/livro-razao.html:1`)

6. **The ledger search covers 149 displayed rows, not the required 2,916.** The view removes the 2,767 municipality rows before rendering, and the report acknowledges the deviation. Without JavaScript the complete rendered set of 149 remains visible, but the ledger index does not contain everything named by the brief. (`brief.md:13`, `src/views/LivroView.astro:47-59`, `relatorio-construtor.md:159-163`, `built/livro-razao.html:1`, `built/en-ledger.html:1`)

7. **`[a verificar]` still has multiple effective destinations.** Each ledger build has direct marker links to the marker page and marker spans nested inside row links, where clicking the marker opens the row instead. I6 expressly exempts those nested occurrences, contradicting the one-destination rule. (`brief.md:16`, `relatorio-construtor.md:217-220`, `tests/livro/indice.mjs:417-435`, `built/livro-razao.html:1`, `built/en-ledger.html:1`)

8. **«peça» is neither defined nor gone everywhere.** The report admits 16 remaining occurrences and postpones them to F1.10. I4 deliberately counts but does not fail those occurrences outside `/areas`. (`brief.md:15`, `relatorio-construtor.md:201-206`, `relatorio-construtor.md:278`, `tests/livro/indice.mjs:353-374`)

9. **Document titles are not consistently links.** On the supplied line page, the title in the attribution sentence is plain text, while its later copy in the facts panel is linked. The HTML gate validates a title’s destination only when an anchor already exists; it never requires the anchor. (`src/views/LinhaView.astro:504-520`, `src/views/LinhaView.astro:671-687`, `src/views/LinhaView.astro:1248-1269`, `scripts/gate-html.mjs:5416-5433`, `built/linha.html:1`)

10. **The studies account disagrees with itself.** G9 reports 20 missing-date markers across two editions, meaning ten studies per edition, and the supplied Portuguese build has ten; elsewhere the report says nine. `onde-esta-a-agua` also displays two marker occurrences, one for description and one for date, while I9 counts only markers inside the publication-date field. (`relatorio-construtor.md:251-263`, `relatorio-construtor.md:283`, `relatorio-construtor.md:349-352`, `src/views/EstudosView.astro:109-116`, `src/views/EstudosView.astro:155-165`, `tests/livro/indice.mjs:499-513`, `built/estudos.html:1`)

11. **The voice inventory is incomplete.** It declares only the two complete count strings and two empty-result strings. The new PT/EN search labels and the four changed area-count suffixes are visible strings but absent, despite the revision log claiming exactly four additions. (`design/especime-v3/INVENTARIO-FRASES.md:838-841`, `design/especime-v3/critica/REVISOES-DO-INVENTARIO.md:58`, `src/i18n/strings.mjs:984-985`, `src/i18n/strings.mjs:1291-1292`, `src/i18n/strings.mjs:2235-2236`, `src/i18n/strings.mjs:2420-2421`)

12. **The planted defects are demonstrations, not proof.** By inspection, the four exact mutations should make I1, I2, I3 and I6 red. But the plant runner merely prints observed results and never asserts them or exits nonzero on an unexpected pass; no run output is supplied. Browser checks for filtering and overflow are also omitted from `npm run verify`, because `check:indice` runs without `--navegador`. (`design/especime-v3/medicoes/nomes-plantas.py:84-100`, `tests/livro/indice.mjs:83`, `tests/livro/indice.mjs:715-716`, `package.json:37-40`, `relatorio-construtor.md:329-338`)

13. **The rulers can pass semantic equivalents of all four planted defects.** I1 accepts a wrong human name taken from another row; I2 accepts `12/08/2026`; I3 accepts a non-empty action pointing to a missing page; I3b can pass an ID-only filter because it tests only “divida” and nonsense; I6 accepts the second destination when the marker is nested in a seal. I10 also inventories only marked IDs and reason names, then compares key presence rather than rendered numbers or occurrence counts; I8 similarly misses an unmarked district aggregate. (`scripts/medir-defeitos.mjs:556-557`, `tests/livro/indice.mjs:98-108`, `tests/livro/indice.mjs:310-320`, `tests/livro/indice.mjs:579-600`, `tests/livro/indice.mjs:424-435`, `tests/livro/indice.mjs:479-492`, `tests/livro/indice.mjs:517-550`)

## Minor

No separate minor findings.

## What is fine

- Apart from the report discrepancy above, the supplied copies match the after-side patch hunks.
- The six supplied built samples contain no visible ISO full date; visible full dates use `dd.mm.aaaa`, and reference years remain years. (`built/area.html:1`, `built/linha.html:1`, `built/distrito.html:1`, `built/estudos.html:1`, `built/livro-razao.html:1`, `built/en-ledger.html:1`)
- The actual search controls are real GET forms with existing self-destinations; all 149 rows start visible, and the implementation’s search text includes name, ID and source. (`src/views/LivroView.astro:78-81`, `src/views/LivroView.astro:232-249`, `public/js/livro.js:52-61`, `src/styles/linha.css:263-271`)
- The supplied district page states that it is an index and introduces no aggregate; the target header denominators use declared proof values, and study counts resolve to ledger rows rather than page arithmetic. (`src/views/DistritoView.astro:91-106`, `src/views/LivroView.astro:177-189`, `src/views/EstudosView.astro:82-93`)
- The source URL contains `/` break opportunities, and the facts-panel copy of the document title is linked. (`src/components/CampoDaLinha.astro:49`, `src/components/CampoDaLinha.astro:109-116`, `built/linha.html:1`)

**13 distinct findings.**