# Brief I · construir a direção S nas páginas reais (bloco seguinte, Opus, ramo `identidade-v2`)

*Escrito a 2026-08-16 à noite pelo lugar de direção (Claude Fable 5) para ser lançado na sessão seguinte, depois de o limite semanal do plano repor (segunda, 11:00). Lê-se com `design/DECISAO.md` (a decisão) e `design/CRITICA-codex.md` (a crítica). Os protótipos que servem de especificação estão em `design/direcao-a/`, `-b/`, `-c/` (ramo `design-explorations`); a direção S é a síntese descrita na decisão, não um dos três.*

## Quem e onde
Um agente Opus (ou dois em sequência: 1 componentes e folha de estilos; 2 páginas e portão), no repositório do sítio `~/Instruments/OEstadoDoPais`, ramo `identidade-v2` criado de `main`. Sem push até a pré-visualização; nunca `main` sem a palavra da direção. Sem travessões no que se escreve; AO90; cada conferência nova provada num estrago plantado; estado rendido, nunca escrito; nenhum número inventado (as páginas continuam a vir do livro-razão).

## Ler primeiro
`design/DECISAO.md`, `design/CRITICA-codex.md`, `IDENTIDADE.md` (v1), `BRIEF-confianca.md` §6.8, os racionais e `tokens.css` das três direções, `src/styles/tokens.css` e `site.css`, os componentes `Claim.astro`, `Provenance.astro`, `Frase.astro`, `Masthead.astro`, `SiteFooter.astro`, as vistas `LinhaView`, `HomeView`, `MunicipioView`, `MetodoView`, `AgendaView`, `CorrecoesView`, `SobreView`, `EstudosView`, `EstudoView`, `MunicipiosView`, `LivroView`, `MarcadorView`, e `scripts/gate-html.mjs` (as conferências que a `proveniencia` e o `data-prova` fazem hoje).

## Entregas, por ordem
1. **`IDENTIDADE.md` v2**, escrita a partir da secção «O que muda na constituição» de `design/DECISAO.md`: as regras que ficam, as que mudam (§5 o selo escreve «fonte»; §6 nunca uma caixa de exemplo; §7 o quadro de estados da agenda; a cadência do corpo), o registo de que a v1 fica no git. Curta como a v1. O lugar de direção lê o diff antes de o resto avançar (parar e reportar depois deste passo se o agente for um só; se forem dois, o primeiro termina aqui).
2. **`tokens.css` v2**: neutros quentes de C testados contra os de hoje, em claro e escuro, AA garantido em todas as combinações usadas (medir com um script, não a olho); os dois acentos inalterados; a escala de tipo com corpo maior; sem tipos de rede.
3. **O selo** (`Provenance.astro`, `Claim.astro`): quadrado (cheio ou tracejado) mais «fonte» sublinhado, a unidade inteira é a porta com alvo suficiente; a etiqueta do estudo no foco/`title` e na forma longa (páginas de linha), não no texto do chip; no cabeçalho, glifo mais «fonte». O portão: a conferência da `proveniencia` (E, §1.42) passa a comparar a etiqueta no atributo declarado (`data-selo-etiqueta` ou o que existir) e o texto visível «fonte»; provar com estrago.
4. **Cabeçalho e rodapé** (`Masthead.astro`, `SiteFooter.astro`): duas leituras rotuladas e ligadas (reconferência, agenda), sem caixa; a navegação como está.
5. **Primeira página** (`HomeView`, `figuras.mjs` se preciso): grelha 4/2/1 do painel com a economia de A; onde há limiar, a linha mono «limiar 9 % · acima» / «abaixo» com o limiar no motivo `limiar-do-quadro` (o texto «acima/abaixo» é prosa da casa gerada de uma comparação simples entre dois números já existentes; sem distâncias novas); os instrumentos com a legenda de portas como hoje.
6. **Página de linha como recibo** (`LinhaView`, `CampoDaLinha`, `HistoricoDaLinha`): a ordem de A (valor com selo próprio, atribuição numa frase, prova, campo devolvido ou excerto, pedido ou endereço, verificações em tabela de duas linhas com o marcador quando falta a releitura, correções), a coluna do aparelho de 300 px com a proveniência em campos, o estado, o acesso aos dados, esta linha noutro sítio, a porta das correções; o lugar do recorte só existe quando `document.crop` existir (bloco T): até lá, nada de caixa; o «Abrir o documento na página N» como hoje. Sem «como se lê».
7. **Município** (`MunicipioView`): mosaico 4/2/1, linha do tempo como banda, aparelho de A.
8. **Método** (`MetodoView`, `InstrumentoMecanismo`): disposição A com o mecanismo a toda a largura; a prova em linha mono com portas.
9. **Agenda** (`AgendaView`): quadro de quatro estados em colunas com contagens (`data-prova`), itens curtos, calendário num eixo de tempo (SVG desenhado no servidor; as janelas a amarelo, os dias como marcas; legenda de portas), lista datada abaixo. O portão que compara `data-agenda` continua a valer; o eixo é escala de instrumento (`data-nonledger="escala-de-instrumento"` nas marcas).
10. **As restantes páginas** (`/estudos`, `/estudos/<slug>`, `/livro-razao`, `/municipios`, `/sobre`, `/correcoes`, `/a-verificar`, 404): recebem os tokens e o selo novo; nada mais muda nelas neste bloco.
11. **Móvel**: uma coisa por linha; instrumentos rolam na sua caixa; testar 390 px em headless Chrome (janela mínima de 500 px no macOS: usar iframes ou `--force-device-scale-factor`).
12. **Portão**: as três regras novas de `DECISAO.md` (etiqueta do selo declarada; `data-prova` com âncora na própria página; `data-exemplo` recusado), cada uma provada; `medir-defeitos.mjs` antes e depois; `npm run build`, `typecheck`, `ortografia --verificar`, `check-cruzamento --with-origin` verdes.
13. **Registo**: `DECISIONS.md` §1.43 «A identidade v2: a direção S construída» com `Afecta:` (metodo se o texto de prova mudar; nenhum se só o desenho mudar) e as linhas `Texto:`; `IDENTIDADE.md` v2 é a regra; `PLANO-fases.md` (fase 2 feita; bloco T a seguir); o gerador `scripts/design-bundle.mjs` corrido e o sistema de desenho no Claude Design atualizado com DesignSync (o lugar de direção faz o push).

## Aceitação
Construção verde; leitura Codex canariada das cinco páginas (plantar: um valor sem selo, um travessão, um segundo marcador, uma caixa de exemplo, um limiar sem motivo); pré-visualização no Vercel (ramo `identidade-v2`); a palavra da direção; fusão; `verify:deploy`; registos.

Custo estimado: 400k a 600k em Opus, mais a revisão.
