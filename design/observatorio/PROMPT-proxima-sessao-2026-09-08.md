# Prompt para a sessão seguinte · O Estado do País · depois de 08.09.2026

*Escrito pelo lugar de direção (Claude Fable 5.1) a 08.09.2026, para a sessão que abrir a seguir. A ordem de leitura de sempre: `design/especime-v3/PENDENTES-DO-DIRETOR.md`, `VISAO.md`, `design/observatorio/POLITICA-DA-AUTONOMIA.md` (com a emenda de 04.09), as últimas secções de `DECISIONS.md` (§1.98 a §1.102), `design/observatorio/PLANO-fiabilidade-2026-09-02.md` com as caixas riscadas, e este prompt. O prompt de 07.09 (`PROMPT-proxima-sessao-2026-09-07.md`) está cumprido e fica como registo. Sem travessões na prosa.*

## 0 · O que mudou nas regras e nos hábitos

A delegação de 04.09 (§1.98) continua: as decisões tomam-se no lugar de direção pelas recomendações escritas; ficam com o diretor a hora do advogado e o nome da segunda pessoa. A conversa com o diretor é em inglês; o conteúdo do projeto é em português. As três mudanças de processo de 07.09 estão aplicadas e são o hábito: as aterragens em lote (um ramo de integração a partir de `main`, os ramos prontos e os documentos do dia fundidos nele, um portão, um avanço rápido de `main`; a 08.09 foi `integracao-2026-09-08`), o pacote das leituras montado por `scripts/leituras/pacote.sh` (repositório, base, cabeça, pacote, brief, relatório, e as páginas construídas pedidas; o `antes/` e o `contexto/` à mão; as plantas com `plantar.py` depois), e o relatório de cada bloco a começar pela tabela das medidas. Quatro coisas aprendidas a 08.09, escritas para não se repetirem: **o motor não tem `CLAUDE.md`** (as regras do motor estão no `CLAUDE.md` do sítio; não mandar um construtor ler um ficheiro que não existe); **o portão numa worktree nova do motor fica vermelho** em `export_site_rows_test` até se copiarem as três caches de recortes da árvore principal (a nota de 01.09 no `.gitignore`); **um comando com `git push` e um `-f` de outro programa na mesma linha é bloqueado pela guarda do force-push** (despachar fluxos com `--raw-field`, em comando separado); e **os despachos de fluxo só existem para fluxos que estejam no ramo por omissão** (um fluxo novo entra em `master` antes de se poder despachar, mesmo noutro ramo). O plano do Codex foi melhorado pelo diretor na madrugada de 08.09: cinco leituras seguidas no dia (11, 26, 35, 26 e 28 minutos; 1,31 M símbolos) sem uma paragem; o `.eventos.log` continua a ser o sítio onde uma paragem se lê.

## 1 · O primeiro gesto

1. `git fetch origin` nos dois repositórios e `git worktree list`. `npm run verify:deploy` para confirmar que o lançamento do fecho de 08.09 (o lote de fecho, com a §1.102) chegou ao ar; se não chegou, `vercel ls 2>&1` diz porquê.
2. **A noite de 09.09 foi a primeira agendada com a retoma** (F2.1c em `master` desde `483c9ea5`): `gh run list --repo oestadodopais/motor --workflow corredor.yml --limit 3`, e o artefacto `corredor-<id>-resumo` da corrida agendada diz a noite pelo nome (se o INE atendeu, «não houve retoma»; se não, a retoma e o seu resultado). A contagem da semana está na linha F2.1 do plano: 1 de 7 a 08.09; cada noite verde conta mais uma; uma noite vermelha lê-se no resumo e no relatório antes de decidir. As noites seguintes lêem-se da mesma maneira; o fluxo `ensaio-diario.yml` despacha até 15.09 e apaga-se quando a semana fechar (sete verdes seguidas), com as sete corridas escritas na linha F2.1.
3. **O F1.10** («uma coisa, um lugar», ramo `lugar-2026-09-04`): lê o «Estado» no cabeçalho de `design/especime-v3/medicoes/lugar-construtor.md`. A segunda sessão de construção arrancou às 11:25 UTC de 08.09 com as duas decisões da §1.102 (a manchete do país com duas medidas de cabeça, a dívida e o desemprego; o campo `limiarFixadoPor`) e a ordem do que faltava (o 8.4, o 8.11 e o §7.3, o resto do §9, as páginas, o caminho, a busca única, «fonte» e as datas, o fecho). Se acabou: a leitura a frio do Codex (o pacote por `pacote.sh` com a base `d9066379`, cinco plantas de três classes, e o `built/` com as páginas de cada família que o bloco toca) **e a releitura do leitor de primeira vez** (a régua L7: as mesmas treze páginas de `critica/2026-09-04-codex-leitor-de-primeira-vez.md`, a contar as repetições e as ressalvas que ficaram), a segunda passagem em Opus, e a aterragem em lote com os documentos do dia. Se parou a meio: uma terceira sessão de construção a partir do relatório, com o mesmo prompt de continuação (as regras e a ordem estão no relatório e no brief, §0 a §9). Não se funde um F1.10 pela metade: o bloco é um só.
4. **O F1.1e, as sub-regiões no mapa** (o diretor, 08.09 à tarde, com o F1.1d no ar: o Alentejo como um bloco; a resposta dele: as sub-regiões NUTS III): o brief está escrito (`BRIEF-F1.1e-as-sub-regioes-no-mapa.md`) e o bloco pode construir-se **em paralelo com o que resta do F1.10**, numa worktree própria a partir de `main`, porque toca nos ficheiros do mapa que o F1.10 não toca; um construtor Opus, a medição cega do Sonnet nos alvos, a leitura a frio do Codex com cinco plantas, a segunda passagem, a aterragem (se o F1.10 aterrar primeiro, o F1.1e funde o `main` e troca o mapa da página do concelho para o nível da sub-região, como o 8.17 emendado diz).
5. **Depois do F1.10**: o bloco F1.11 (o descarregamento para pessoas, a linha do plano), com o brief a escrever pelo lugar de direção a partir da linha do plano e do R1 da ronda de leitores.
6. **As decisões e os blocos pequenos que ficaram escritos e não feitos**: a emenda do sítio que mostra a proporção das linhas lidas (`linhasSemResposta` e `anfitrioesCalados` do bloco `CONFERENCIA`, que só chegam ao sítio com o primeiro `real`; na segunda passagem do F1.10, ou num bloco pequeno); o vigia de rede do painel semanal (I115); o achado do portátil táctil do F1.1d (o toque com o cursor pousado mostra o concelho debaixo do cursor; por decidir, medido); a leitura cruzada do inventário para o bloco `lugar` quando o F1.10 fechar (as oito antigas estão lidas); os cinco acontecimentos datados sem linhas do calendário (F2.6, pelo H5).
7. **O INE**: a I116 está medida e a resposta da casa está no ar; o que resta é do diretor (a mensagem ao INE, cujo rascunho está no Gmail dele e no cofre; um runner próprio). Se ele disser que enviou, a linha dos pendentes ganha a data e a resposta do INE, quando vier, entra na I116.

## 2 · O que está no ar (na data desta nota; conferir com `git log -1` e `npm run verify:deploy`)

`main` do sítio: o F1.1d (o mapa a dois níveis com o nome ao lado, lançado às 09:40 UTC de 08.09) e os documentos do dia, mais o lote de fecho com a §1.102. `master` do motor (`483c9ea5`): o conserto de `calendario.comparar`, a segunda passagem do fluxo do ensaio, o F2.1b (a fonte calada como estado), a sonda de alcance, e o F2.1c (a retoma noutro runner, com o trabalho `resumo`).

## 3 · O que ficou a meio, e como se retoma

| bloco | ramo | estado | como se retoma |
|---|---|---|---|
| F1.10 uma coisa, um lugar | `lugar-2026-09-04` (sítio) | a primeira sessão de construção fechada em `7c9519c9` (corrida 34217864954 verde); a segunda a correr ao fechar esta nota, com o estado a ser escrito no relatório | o gesto 3 da §1 |

## 4 · O que só o diretor pode fazer

A lista está em `PENDENTES-DO-DIRETOR.md`, com o que cada coisa destrava. Os itens que travam blocos ou que o dia abriu: a mensagem ao INE (I116; o rascunho no Gmail dele); a linha do plist para a DGAL (F0.11); as chaves e `CORREDOR_ARMADO` (F2.2); a ronda de leitores (F1.3); como ler o INE para o segundo domínio (F1.5); os ramos locais fundidos por apagar; o 2FA; a segunda pessoa; a hora do advogado.

## 5 · Onde estão as coisas

O sítio: `~/Instruments/OEstadoDoPais` (`main`); o motor: `~/Instruments/ResearchHub` (`master`); as worktrees do sítio em `.claude/worktrees/` (a 08.09 ao fechar: `integracao-2026-09-08` e `lugar-2026-09-04`) e as do motor em `~/Instruments/ResearchHub-worktrees/` (nenhuma ao fechar); a nota do cofre: `~/Obsidian/Experiments/O Estado do País.md`; o rascunho da mensagem ao INE: `~/Obsidian/Handoffs/O Estado do País · rascunho da mensagem ao INE 2026-09-08.md`; as leituras a frio e as plantas: `design/especime-v3/critica/`; as ferramentas das leituras: `scripts/leituras/`.
