# Prompt para a sessão seguinte · O Estado do País · depois de 16.09.2026

*Escrito pelo lugar de direção (Claude Fable 5.1) a 16.09.2026 à noite, com os dois blocos do dia no ar, para a sessão que abrir a seguir. A ordem de leitura de sempre: `design/especime-v3/PENDENTES-DO-DIRETOR.md`, `VISAO.md`, `design/observatorio/POLITICA-DA-AUTONOMIA.md`, as últimas emendas da §1.108 de `DECISIONS.md` (as de 16.09 à tarde e à noite), `design/especime-v3/ISSUES.md` (I116 com a resposta do INE, I120 e I121 novas). A lista de progresso do diretor está no Desktop dele («O Estado do País - progress checklist.md») e diz cada bloco com os símbolos da ferramenta. Sem travessões na prosa.*

## 0 · O que mudou nas regras e nos hábitos

As regras de 15.09 continuam (as capturas nas cinco larguras antes de aterrar; a lista do «olhar para» à frente; nada que o diretor tenha de ler fica em Markdown, vai para o Drive dele como Google Doc; nenhum nome dele em página nenhuma; «casa» não é palavra para o sítio se descrever; o sítio explica-se uma vez, no Sobre e no Método). **De 16.09:** a norma de como se escreve, se mostra e se organiza (`design/observatorio/NORMA-como-se-escreve-se-mostra-e-se-organiza.md`) vale para tudo o que se constrói, e o portão das palavras proibidas em `check:voz` (com os atributos visíveis, os `details` abertos, sem rota saltada) é a sua forma mecânica; a regra dos nomes das medidas emendou-se no brief do P3 (§2b: um nome tem as palavras que a medida pede, o menos possível, diz o que mede, os partidos e as siglas correntes como a imprensa os escreve, sem jargão da fonte); as três datas de cada medida vivem no recibo (a regra 3 da carta dos conteúdos); um estudo publica-se com o método fixado antes dos dados no brief, as fontes fechadas a um registo com sha256, cada valor com o excerto da página, e a leitura a frio do Codex com plantas em cada metade (a do motor e a do sítio), e o pacote leva o texto extraído inteiro das fontes (a primeira leitura do E1 sofreu por só levar as folhas do livro-razão); as datas e as páginas são referências e não medições; o que é funcionamento do projeto não entra numa página de conteúdo. **O diretor decidiu a 16.09 à noite que não se escreve a ninguém por agora**: as duas perguntas escritas a terceiros da decisão 3 de 16.09 de manhã são uma opção para mais tarde, não uma tarefa. Os símbolos de cada bloco contam-se pela ferramenta (o total cumulativo do agente) e os de cada leitura pela linha «tokens used» do `.eventos.log`.

## 1 · O primeiro gesto

1. `git fetch origin` nos dois repositórios e `git worktree list`. **O que está no ar** (conferir com `git log -1` e `npm run verify:deploy`): `main` em `90e55e66`, dois lançamentos de 16.09 à noite na palavra do diretor («land», 21:37 UTC): o P3 (`c032cbfe`, no ar e conferido às 21:54 UTC) e o E1 (`90e55e66`, o lançamento `eq6k2xuq1`). O motor em `master` `b99e225` (o estudo `content/14 Évora 2027`, com as duas leituras a frio ao lado). Os ramos locais e remotos fundidos foram apagados a 16.09 à noite; ficam `main`, e no motor `master`. Se o `e1-2026-09-16` ainda existir (local, remoto, ou a worktree `.claude/worktrees/e1-2026-09-16`), apaga-se: está fundido.
2. **O lote de registos de 16.09 à noite** (as quatro emendas da §1.108, os dois briefs do E1 em `design/observatorio/evora/`, a leitura do E1 do sítio em `critica/`, a I116, a I119, a I120, a I121, a linha do INE nos PENDENTES, este prompt em `design/observatorio/`) aterrou depois do E1; se não aterrou, o guião está em `lote-registos.py` no scratchpad desta sessão e os textos ao lado (`s1108-emenda-*.md`), e faz-se antes de qualquer bloco novo.
3. **O bloco seguinte é o E2** (quinze anos de contas de Évora com os cinco mandatos marcados; `SINTESE-o-plano-de-evora-2026-09-15.md` §3): a primeira prova da arquitetura do SVG servido (um SVG por estado gerado no `build`, a escala escrita nas pontas, a fonte e a data dentro da imagem, a tabela por baixo), as 70 linhas que já existem mais a série trimestral do PMP da DGAL, sem atribuir a série a um mandato. O brief escreve-se com o padrão do E1 (a pergunta, as fontes fechadas, as unidades, a condição de matar, o que não se faz) e a metade do motor antes da do sítio. Depois E3 (as reuniões de câmara; o OCR dos editais é sofrível e cada valor confere-se contra a imagem), E4 (as dezassete medidas contra os atos, sem esperar pelo advogado desde que tudo se confira nas fontes oficiais; a cópia de 15.09 do programa é a âncora), E5 (a exportação semanal do IMPIC, nunca o Portal BASE), E6.
4. **Em paralelo, quando houver um lugar:** o bloco da estrutura (P5: uma página por concelho, as regiões a levar aos concelhos, o menu de cinco; retira também a explicação das duas portas na página de estudo, a descrição repetida, e o par da faixa que sobe a catraca L1, I121; e resolve a tensão das duas convenções de título em `studies.mjs`); a fatia `css-alcance` (I118, primeira parte) com a I120 (o ponto final do rótulo de IA sozinho na linha a 390 px); a faixa da primeira página em capturas para o diretor; as definições em forma de pergunta; os 71 nomes do livro-razão ainda por título de documento (motor); os XS do motor (o `generate_claims` não sobrescreve linhas; o vigia dos limiares; a precisão; o `CLAUDE.md` do motor; o chão dos provadores do corredor; o F2.1d).

## 2 · O que só o diretor pode fazer

A palavra para aterrar cada bloco, em capturas. O bloco das chaves e da corrida da noite, quando chegar a vez: explicar-lhe em palavras simples o que são as chaves de lançamento e a corrida da noite, e a decisão sobre as leituras do INE a partir de um endereço fixo (o portátil dele pelo `launchd`, como o painel semanal, ou um runner próprio), porque a resposta do INE de 16.09 confirmou que os endereços partilhados dos runners são bloqueados a montante do INE sem via de desbloqueio (I116). A mensagem da ronda dos leitores. A leitura diária do sítio, com capturas de uma linha. Nenhum correio a terceiros por agora.

## 3 · Onde estão as coisas

O plano de Évora e os seus levantamentos: `design/observatorio/evora/` (a síntese, as fontes com os registos de descarga por sha256, as formas, os dados, as referências, os dois briefs do E1). As cópias das fontes: as pastas do Drive do diretor («O Estado do País» e as duas de Évora), fora de qualquer repositório; no motor, `content/14 Évora 2027/Technical Source/raw/` reconstrói-se do manifesto e das cópias. A norma e o plano das palavras: `design/observatorio/`. As leituras a frio: `design/especime-v3/critica/` (o sítio) e `Technical Source/leituras-a-frio/` (o estudo). Os guiões da leitura: `scripts/leituras/` (o `pacote.sh`, o `plantar.py`, o `ler.sh` com o modelo fixado). O cofre do diretor: `~/Obsidian/Experiments/O Estado do País.md`.
